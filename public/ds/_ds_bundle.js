/* @ds-bundle: {"format":3,"namespace":"SPUENAPAprendizagemDesignSystem_f0eeed","components":[{"name":"Callout","sourcePath":"components/content/Callout.jsx"},{"name":"Conclusion","sourcePath":"components/content/Conclusion.jsx"},{"name":"DataTable","sourcePath":"components/content/DataTable.jsx"},{"name":"SPU_MARKS","sourcePath":"components/content/Editable.jsx"},{"name":"Editable","sourcePath":"components/content/Editable.jsx"},{"name":"MarkToolbar","sourcePath":"components/content/Editable.jsx"},{"name":"ExampleCard","sourcePath":"components/content/ExampleCard.jsx"},{"name":"FeatureGrid","sourcePath":"components/content/FeatureGrid.jsx"},{"name":"LicenseBadge","sourcePath":"components/content/LicenseBadge.jsx"},{"name":"MarkerList","sourcePath":"components/content/MarkerList.jsx"},{"name":"Masthead","sourcePath":"components/content/Masthead.jsx"},{"name":"MediaEmbed","sourcePath":"components/content/MediaEmbed.jsx"},{"name":"PageFooter","sourcePath":"components/content/PageFooter.jsx"},{"name":"PageToc","sourcePath":"components/content/PageToc.jsx"},{"name":"Panel","sourcePath":"components/content/Panel.jsx"},{"name":"PullQuote","sourcePath":"components/content/PullQuote.jsx"},{"name":"ReferenceList","sourcePath":"components/content/ReferenceList.jsx"},{"name":"ReflectionStop","sourcePath":"components/content/ReflectionStop.jsx"},{"name":"ResourceList","sourcePath":"components/content/ResourceList.jsx"},{"name":"RichText","sourcePath":"components/content/RichText.jsx"},{"name":"RichTextLinkNotes","sourcePath":"components/content/RichText.jsx"},{"name":"StatBlock","sourcePath":"components/content/StatBlock.jsx"},{"name":"Badge","sourcePath":"components/core/Badge.jsx"},{"name":"BlockRegistry","sourcePath":"components/core/BlockRegistry.jsx"},{"name":"BlockView","sourcePath":"components/core/BlockView.jsx"},{"name":"BlockDocument","sourcePath":"components/core/BlockView.jsx"},{"name":"BuilderExport","sourcePath":"components/core/BuilderExport.jsx"},{"name":"BuilderManifest","sourcePath":"components/core/BuilderManifest.jsx"},{"name":"Button","sourcePath":"components/core/Button.jsx"},{"name":"ICONS","sourcePath":"components/core/Icon.jsx"},{"name":"Icon","sourcePath":"components/core/Icon.jsx"},{"name":"ICON_NAMES","sourcePath":"components/core/Icon.jsx"},{"name":"IconButton","sourcePath":"components/core/IconButton.jsx"},{"name":"IconGallery","sourcePath":"components/core/IconGallery.jsx"},{"name":"Kicker","sourcePath":"components/core/Kicker.jsx"},{"name":"Tag","sourcePath":"components/core/Tag.jsx"},{"name":"TONES","sourcePath":"components/core/dsutil.js"},{"name":"Accordion","sourcePath":"components/interactive/Accordion.jsx"},{"name":"Carousel","sourcePath":"components/interactive/Carousel.jsx"},{"name":"CompareAB","sourcePath":"components/interactive/CompareAB.jsx"},{"name":"Flipcard","sourcePath":"components/interactive/Flipcard.jsx"},{"name":"GlossaryTerm","sourcePath":"components/interactive/GlossaryTerm.jsx"},{"name":"GlossaryFootnotes","sourcePath":"components/interactive/GlossaryTerm.jsx"},{"name":"Quiz","sourcePath":"components/interactive/Quiz.jsx"},{"name":"Timeline","sourcePath":"components/interactive/Timeline.jsx"},{"name":"Columns","sourcePath":"components/layout/Columns.jsx"},{"name":"Section","sourcePath":"components/layout/Section.jsx"},{"name":"Stack","sourcePath":"components/layout/Stack.jsx"},{"name":"BleedImage","sourcePath":"components/media/BleedImage.jsx"},{"name":"Figure","sourcePath":"components/media/Figure.jsx"},{"name":"FullBleed","sourcePath":"components/media/FullBleed.jsx"},{"name":"Hero","sourcePath":"components/media/Hero.jsx"},{"name":"ImageReveal","sourcePath":"components/media/ImageReveal.jsx"},{"name":"MapFigure","sourcePath":"components/media/MapFigure.jsx"},{"name":"TextWithImage","sourcePath":"components/media/TextWithImage.jsx"}],"sourceHashes":{"assets/image-slot.js":"10eda37fc4b6","components/content/Callout.jsx":"bd58dd54d81d","components/content/Conclusion.jsx":"5557ec112356","components/content/DataTable.jsx":"20c19a77bb43","components/content/Editable.jsx":"948f4ad224ac","components/content/ExampleCard.jsx":"b0c4401e810e","components/content/FeatureGrid.jsx":"73483b6aa40f","components/content/LicenseBadge.jsx":"d7bb5223e8b9","components/content/MarkerList.jsx":"48df5ca9db36","components/content/Masthead.jsx":"2a725bb93d83","components/content/MediaEmbed.jsx":"7c088cb9fcf6","components/content/PageFooter.jsx":"659480858749","components/content/PageToc.jsx":"f0d414a77e1c","components/content/Panel.jsx":"da016587de7f","components/content/PullQuote.jsx":"97a06ec30f66","components/content/ReferenceList.jsx":"647d974852a2","components/content/ReflectionStop.jsx":"a9a91b578040","components/content/ResourceList.jsx":"581496fe0d7f","components/content/RichText.jsx":"259fa96a3ce3","components/content/StatBlock.jsx":"39d8c99d4ada","components/core/Badge.jsx":"b86936c39ee7","components/core/BlockRegistry.jsx":"b93deeaace82","components/core/BlockView.jsx":"7f8d8f4075b5","components/core/BuilderExport.jsx":"a45c5b641dbc","components/core/BuilderManifest.jsx":"523b56ed9db6","components/core/Button.jsx":"21da7fca4d84","components/core/Icon.jsx":"6dc631bd7624","components/core/IconButton.jsx":"b970b9113c67","components/core/IconGallery.jsx":"dfd6a321e4c6","components/core/Kicker.jsx":"9659157f9138","components/core/Tag.jsx":"acc015185ba8","components/core/dsutil.js":"fc02ab4b5f88","components/interactive/Accordion.jsx":"75af244b9487","components/interactive/Carousel.jsx":"c7ee0d537698","components/interactive/CompareAB.jsx":"8e0b859fa2aa","components/interactive/Flipcard.jsx":"bb5f18b5ea23","components/interactive/GlossaryTerm.jsx":"afbbad1d3950","components/interactive/Quiz.jsx":"6276b4b3ce26","components/interactive/Timeline.jsx":"9293f664e504","components/layout/Columns.jsx":"81e84a9fd3f8","components/layout/Section.jsx":"1e289ae20f66","components/layout/Stack.jsx":"82de9c89f0ff","components/media/BleedImage.jsx":"32c55fed767a","components/media/Figure.jsx":"bc724ebf2386","components/media/FullBleed.jsx":"47735f8e8b78","components/media/Hero.jsx":"08f65cb71a96","components/media/ImageReveal.jsx":"a7b9d6c60b1f","components/media/MapFigure.jsx":"ab65cfefa66c","components/media/TextWithImage.jsx":"f719c8b8bf73","export/src/page.jsx":"edbd2371497e","m122/page.jsx":"d19ee4a927e1","m122web/js/app.js":"bcc3d7cdd08e"},"inlinedExternals":[],"unexposedExports":[{"name":"applyColor","sourcePath":"components/content/Editable.jsx"},{"name":"applyEmphasis","sourcePath":"components/content/Editable.jsx"},{"name":"applyHighlight","sourcePath":"components/content/Editable.jsx"},{"name":"applyLink","sourcePath":"components/content/Editable.jsx"},{"name":"applyTerm","sourcePath":"components/content/Editable.jsx"},{"name":"clearMarks","sourcePath":"components/content/Editable.jsx"},{"name":"cx","sourcePath":"components/core/dsutil.js"},{"name":"injectCss","sourcePath":"components/core/dsutil.js"},{"name":"isPrint","sourcePath":"components/core/dsutil.js"},{"name":"itemExtras","sourcePath":"components/interactive/Accordion.jsx"},{"name":"renderRich","sourcePath":"components/content/RichText.jsx"}]} */

(() => {

const __ds_ns = (window.SPUENAPAprendizagemDesignSystem_f0eeed = window.SPUENAPAprendizagemDesignSystem_f0eeed || {});

const __ds_scope = {};

(__ds_ns.__errors = __ds_ns.__errors || []);

// assets/image-slot.js
try { (() => {
// @ds-adherence-ignore -- omelette starter scaffold (raw elements/hex/px by design)
/* BEGIN USAGE */
/**
 * <image-slot> — user-fillable image placeholder.
 *
 * Drop this into a deck, mockup, or page wherever you want the user to
 * supply an image. You control the slot's shape and size; the user fills it
 * by dragging an image file onto it (or clicking to browse). The dropped
 * image persists across reloads via a .image-slots.state.json sidecar —
 * same read-via-fetch / write-via-window.omelette pattern as
 * design_canvas.jsx, so the filled slot shows on share links, downloaded
 * zips, and PPTX export. Outside the omelette runtime the slot is read-only.
 *
 * The host bridge only allows sidecar writes at the project root, so the
 * HTML that uses this component is assumed to live at the project root too
 * (same constraint as design_canvas.jsx).
 *
 * Attributes:
 *   id           Persistence key. REQUIRED for the drop to survive reload —
 *                every slot on the page needs a distinct id.
 *   shape        'rect' | 'rounded' | 'circle' | 'pill'   (default 'rounded')
 *                'circle' applies 50% border-radius; on a non-square slot
 *                that's an ellipse — set equal width and height for a true
 *                circle.
 *   radius       Corner radius in px for 'rounded'.       (default 12)
 *   mask         Any CSS clip-path value. Overrides `shape` — use this for
 *                hexagons, blobs, arbitrary polygons.
 *   fit          object-fit: cover | contain | fill.       (default 'cover')
 *                With cover (the default) double-clicking the filled slot
 *                enters a reframe mode: the whole image spills past the mask
 *                (translucent outside, opaque inside), drag to reposition,
 *                corner-drag to scale. The crop persists alongside the image
 *                in the sidecar. contain/fill stay static.
 *   position     object-position for fit=contain|fill.     (default '50% 50%')
 *   placeholder  Empty-state caption.                      (default 'Drop an image')
 *   src          Optional initial/fallback image URL. A user drop overrides
 *                it; clearing the drop reveals src again.
 *
 * Size and layout come from ordinary CSS on the element — width/height
 * inline or from a parent grid — so it composes with any layout.
 *
 * Usage:
 *   <image-slot id="hero"   style="width:800px;height:450px" shape="rounded" radius="20"
 *               placeholder="Drop a hero image"></image-slot>
 *   <image-slot id="avatar" style="width:120px;height:120px" shape="circle"></image-slot>
 *   <image-slot id="kite"   style="width:300px;height:300px"
 *               mask="polygon(50% 0, 100% 50%, 50% 100%, 0 50%)"></image-slot>
 */
/* END USAGE */

(() => {
  const STATE_FILE = '.image-slots.state.json';
  // Raster formats only. SVG is excluded (can carry script; createImageBitmap
  // on SVG blobs is inconsistent). GIF is excluded because the canvas
  // re-encode keeps only the first frame, so an animated GIF would silently
  // go still — better to reject than surprise.
  const ACCEPT = ['image/png', 'image/jpeg', 'image/webp', 'image/avif'];

  // ── Shared sidecar store ────────────────────────────────────────────────
  // One fetch + immediate write-on-change for every <image-slot> on the
  // page. Reads via fetch() so viewing works anywhere the HTML and sidecar
  // are served together; writes go through window.omelette.writeFile, which
  // the host allowlists to *.state.json basenames only.
  const subs = new Set();
  let slots = {};
  let storeRevision = 0;
  // ids explicitly cleared before the sidecar fetch resolved — otherwise
  // the merge below can't tell "never set" from "just deleted" and would
  // resurrect the sidecar's stale value.
  const tombstones = new Set();
  let loaded = false;
  let loadP = null;
  function load() {
    if (loadP) return loadP;
    const revision = storeRevision;
    loadP = fetch(STATE_FILE).then(r => r.ok ? r.json() : null).then(j => {
      if (revision !== storeRevision) return;
      // Merge: sidecar loses to any in-memory change that raced ahead of
      // the fetch (drop or clear) so neither is clobbered by hydration.
      if (j && typeof j === 'object') {
        const merged = Object.assign({}, j, slots);
        // A framing-only write that raced ahead of hydration must not
        // drop a user image that's only on disk — inherit u from the
        // sidecar for any in-memory entry that lacks one.
        for (const k in slots) {
          if (merged[k] && !merged[k].u && j[k]) {
            merged[k].u = typeof j[k] === 'string' ? j[k] : j[k].u;
          }
        }
        for (const id of tombstones) delete merged[id];
        slots = merged;
      }
      tombstones.clear();
    }).catch(() => {}).then(() => {
      loaded = true;
      subs.forEach(fn => fn());
    });
    return loadP;
  }

  // Serialize writes so two near-simultaneous drops on different slots
  // can't reorder at the backend and leave the sidecar with only the
  // first. A save requested mid-flight just marks dirty and re-fires on
  // completion with the then-current slots.
  let saving = false;
  let saveDirty = false;
  function save() {
    if (saving) {
      saveDirty = true;
      return;
    }
    const w = window.omelette && window.omelette.writeFile;
    if (!w) return;
    saving = true;
    Promise.resolve(w(STATE_FILE, JSON.stringify(slots))).catch(() => {}).then(() => {
      saving = false;
      if (saveDirty) {
        saveDirty = false;
        save();
      }
    });
  }
  const S_MAX = 5;
  const clampS = s => Math.max(1, Math.min(S_MAX, s));

  // Normalize a stored slot value. Pre-reframe sidecars stored a bare
  // data-URL string; newer ones store {u, s, x, y}. Either shape is valid.
  function getSlot(id) {
    const v = slots[id];
    if (!v) return null;
    return typeof v === 'string' ? {
      u: v,
      s: 1,
      x: 0,
      y: 0
    } : v;
  }
  function setSlot(id, val) {
    if (!id) return;
    if (val) {
      slots[id] = val;
      tombstones.delete(id);
    } else {
      delete slots[id];
      if (!loaded) tombstones.add(id);
    }
    subs.forEach(fn => fn());
    // A drop is rare + high-value — write immediately so nav-away can't lose
    // it. Gate on the initial read so we don't overwrite a sidecar we haven't
    // merged yet; the merge in load() keeps this change once the read lands.
    if (loaded) save();else load().then(save);
  }

  // ── Original image persistence ──────────────────────────────────────────
  // Keep the uploaded bytes intact. Compact WebP variants are generated only
  // when the author chooses a compact export, so the editor and high-quality
  // packages never depend on an already-downscaled copy.
  function toDataUrl(file) {
    return new Promise((resolve, reject) => {
      const reader = new FileReader();
      reader.onload = () => resolve(String(reader.result || ''));
      reader.onerror = () => reject(reader.error || new Error('Could not read image.'));
      reader.readAsDataURL(file);
    });
  }

  // ── Custom element ──────────────────────────────────────────────────────
  const stylesheet = ':host{display:inline-block;position:relative;vertical-align:top;' + '  font:13px/1.3 system-ui,-apple-system,sans-serif;color:rgba(0,0,0,.55);width:240px;height:160px}' + ':host([data-auto-height][data-filled]){height:auto;min-height:0!important}' + '.frame{position:absolute;inset:0;overflow:hidden;background:rgba(0,0,0,.04)}' +
  // .frame img (clipped) and .spill (unclipped ghost + handles) share the
  // same left/top/width/height in frame-%, computed by _applyView(), so the
  // inside-mask crop and the outside-mask spill stay pixel-aligned.
  '.frame img{position:absolute;max-width:none;transform:translate(-50%,-50%);' + '  -webkit-user-drag:none;user-select:none;touch-action:none}' +
  // Reframe mode (double-click): the full image spills past the mask. The
  // spill layer is sized to the IMAGE bounds so its corners are where the
  // resize handles belong. The ghost <img> inside is translucent; the real
  // clipped <img> underneath shows the opaque in-mask crop.
  '.spill{position:absolute;transform:translate(-50%,-50%);display:none;z-index:1;' + '  cursor:grab;touch-action:none}' + ':host([data-panning]) .spill{cursor:grabbing}' + '.spill .ghost{position:absolute;inset:0;width:100%;height:100%;opacity:.35;' + '  pointer-events:none;-webkit-user-drag:none;user-select:none;' + '  box-shadow:0 0 0 1px rgba(0,0,0,.2),0 12px 32px rgba(0,0,0,.2)}' + '.spill .handle{position:absolute;width:12px;height:12px;border-radius:50%;' + '  background:#fff;box-shadow:0 0 0 1.5px #c96442,0 1px 3px rgba(0,0,0,.3);' + '  transform:translate(-50%,-50%)}' + '.spill .handle[data-c=nw]{left:0;top:0;cursor:nwse-resize}' + '.spill .handle[data-c=ne]{left:100%;top:0;cursor:nesw-resize}' + '.spill .handle[data-c=sw]{left:0;top:100%;cursor:nesw-resize}' + '.spill .handle[data-c=se]{left:100%;top:100%;cursor:nwse-resize}' + ':host([data-reframe]){z-index:10}' + ':host([data-reframe]) .spill{display:block}' + ':host([data-reframe]) .frame{box-shadow:0 0 0 2px #c96442}' + '.empty{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;' + '  justify-content:center;gap:6px;text-align:center;padding:12px;box-sizing:border-box;' + '  cursor:pointer;user-select:none}' + '.empty svg{opacity:.45}' + '.empty .cap{max-width:90%;font-weight:500;letter-spacing:.01em}' + '.empty .sub{font-size:11px}' + '.empty .sub u{text-underline-offset:2px;text-decoration-color:rgba(0,0,0,.25)}' + '.empty:hover .sub u{color:rgba(0,0,0,.75);text-decoration-color:currentColor}' + ':host([data-over]) .frame{outline:2px solid #c96442;outline-offset:-2px;' + '  background:rgba(201,100,66,.10)}' + '.ring{position:absolute;inset:0;pointer-events:none;border:1.5px dashed rgba(0,0,0,.25);' + '  transition:border-color .12s}' + ':host([data-over]) .ring{border-color:#c96442}' + ':host([data-filled]) .ring{display:none}' +
  // Controls sit BELOW the mask (top:100%), absolutely positioned so the
  // author-declared slot height is unaffected. The gap is padding, not a
  // top offset, so the hover target stays contiguous with the frame.
  '.ctl{position:absolute;top:100%;left:50%;transform:translateX(-50%);padding-top:8px;' + '  display:flex;gap:6px;opacity:0;pointer-events:none;transition:opacity .12s;z-index:2;' + '  white-space:nowrap}' + ':host([data-filled][data-editable]:hover) .ctl,:host([data-reframe]) .ctl' + '  {opacity:1;pointer-events:auto}' + '.ctl button{appearance:none;border:0;border-radius:6px;padding:5px 10px;cursor:pointer;' + '  background:rgba(0,0,0,.65);color:#fff;font:11px/1 system-ui,-apple-system,sans-serif;' + '  backdrop-filter:blur(6px)}' + '.ctl button:hover{background:rgba(0,0,0,.8)}' + '.err{position:absolute;left:8px;bottom:8px;right:8px;color:#b3261e;font-size:11px;' + '  background:rgba(255,255,255,.85);padding:4px 6px;border-radius:5px;pointer-events:none}';
  const icon = '<svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" ' + 'stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">' + '<rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/>' + '<path d="m21 15-5-5L5 21"/></svg>';
  class ImageSlot extends HTMLElement {
    static get observedAttributes() {
      return ['shape', 'radius', 'mask', 'fit', 'position', 'placeholder', 'src', 'id'];
    }
    constructor() {
      super();
      const root = this.attachShadow({
        mode: 'open'
      });
      // .spill and .ctl sit OUTSIDE .frame so overflow:hidden + border-radius
      // on the frame (circle, pill, rounded) can't clip them.
      root.innerHTML = '<style>' + stylesheet + '</style>' + '<div class="frame" part="frame">' + '  <img part="image" alt="" draggable="false" style="display:none">' + '  <div class="empty" part="empty">' + icon + '    <div class="cap"></div>' + '    <div class="sub">or <u>browse files</u></div></div>' + '  <div class="ring" part="ring"></div>' + '</div>' + '<div class="spill">' + '  <img class="ghost" alt="" draggable="false">' + '  <div class="handle" data-c="nw"></div><div class="handle" data-c="ne"></div>' + '  <div class="handle" data-c="sw"></div><div class="handle" data-c="se"></div>' + '</div>' + '<div class="ctl"><button data-act="replace" title="Replace image">Replace</button>' + '  <button data-act="clear" title="Remove image">Remove</button></div>' + '<input type="file" accept="' + ACCEPT.join(',') + '" hidden>';
      this._frame = root.querySelector('.frame');
      this._ring = root.querySelector('.ring');
      this._img = root.querySelector('.frame img');
      this._empty = root.querySelector('.empty');
      this._cap = root.querySelector('.cap');
      this._sub = root.querySelector('.sub');
      this._spill = root.querySelector('.spill');
      this._ghost = root.querySelector('.ghost');
      this._err = null;
      this._input = root.querySelector('input');
      this._depth = 0;
      this._gen = 0;
      this._view = {
        s: 1,
        x: 0,
        y: 0
      };
      this._subFn = () => this._render();
      // Shadow-DOM listeners live with the shadow DOM — bound once here so
      // disconnect/reconnect (e.g. React remount) doesn't stack handlers.
      this._empty.addEventListener('click', () => this._input.click());
      root.addEventListener('click', e => {
        const act = e.target && e.target.getAttribute && e.target.getAttribute('data-act');
        if (act === 'replace') {
          this._exitReframe(true);
          this._input.click();
        }
        if (act === 'clear') {
          this._exitReframe(false);
          this._gen++;
          this._local = null;
          if (this.id) setSlot(this.id, null);else this._render();
        }
      });
      this._input.addEventListener('change', () => {
        const f = this._input.files && this._input.files[0];
        if (f) this._ingest(f);
        this._input.value = '';
      });
      // naturalWidth/Height aren't known until load — re-apply so the cover
      // baseline is computed from real dimensions, not the 100%×100% fallback.
      this._img.addEventListener('load', () => {
        this._syncAutoHeight();
        this._applyView();
      });
      // Gated on editable + fit=cover so share links and contain/fill slots
      // stay static.
      this.addEventListener('dblclick', e => {
        if (!this.hasAttribute('data-editable') || !this._reframes()) return;
        e.preventDefault();
        if (this.hasAttribute('data-reframe')) this._exitReframe(true);else this._enterReframe();
      });
      // Pan + resize both originate on the spill layer. A handle pointerdown
      // drives an aspect-locked resize anchored at the opposite corner; any
      // other pointerdown on the spill pans. Offsets are frame-% so a
      // reframed slot survives responsive resize / PPTX export.
      this._spill.addEventListener('pointerdown', e => {
        if (e.button !== 0 || !this.hasAttribute('data-reframe')) return;
        e.preventDefault();
        e.stopPropagation();
        this._spill.setPointerCapture(e.pointerId);
        const rect = this.getBoundingClientRect();
        const fw = rect.width || 1,
          fh = rect.height || 1;
        const corner = e.target.getAttribute && e.target.getAttribute('data-c');
        let move;
        if (corner) {
          // Resize about the OPPOSITE corner. Viewport-px throughout (rect
          // fw/fh, not clientWidth) so the math survives a transform:scale()
          // ancestor — deck_stage renders slides scaled-to-fit.
          const iw = this._img.naturalWidth || 1,
            ih = this._img.naturalHeight || 1;
          const base = Math.max(fw / iw, fh / ih);
          const sx = corner.includes('e') ? 1 : -1;
          const sy = corner.includes('s') ? 1 : -1;
          const s0 = this._view.s;
          const w0 = iw * base * s0,
            h0 = ih * base * s0;
          const cx0 = (50 + this._view.x) / 100 * fw;
          const cy0 = (50 + this._view.y) / 100 * fh;
          const ox = cx0 - sx * w0 / 2,
            oy = cy0 - sy * h0 / 2;
          const diag0 = Math.hypot(w0, h0);
          const ux = sx * w0 / diag0,
            uy = sy * h0 / diag0;
          move = ev => {
            const proj = (ev.clientX - rect.left - ox) * ux + (ev.clientY - rect.top - oy) * uy;
            const s = clampS(s0 * proj / diag0);
            const d = diag0 * s / s0;
            this._view.s = s;
            this._view.x = (ox + ux * d / 2) / fw * 100 - 50;
            this._view.y = (oy + uy * d / 2) / fh * 100 - 50;
            this._clampView();
            this._applyView();
          };
        } else {
          this.setAttribute('data-panning', '');
          const start = {
            px: e.clientX,
            py: e.clientY,
            x: this._view.x,
            y: this._view.y
          };
          move = ev => {
            this._view.x = start.x + (ev.clientX - start.px) / fw * 100;
            this._view.y = start.y + (ev.clientY - start.py) / fh * 100;
            this._clampView();
            this._applyView();
          };
        }
        const up = () => {
          try {
            this._spill.releasePointerCapture(e.pointerId);
          } catch {}
          this._spill.removeEventListener('pointermove', move);
          this._spill.removeEventListener('pointerup', up);
          this._spill.removeEventListener('pointercancel', up);
          this.removeAttribute('data-panning');
          this._dragUp = null;
        };
        // Stashed so _exitReframe (Escape / outside-click mid-drag) can
        // tear the capture + listeners down synchronously.
        this._dragUp = up;
        this._spill.addEventListener('pointermove', move);
        this._spill.addEventListener('pointerup', up);
        this._spill.addEventListener('pointercancel', up);
      });
      // Wheel zoom stays available inside reframe mode as a trackpad nicety —
      // zooms toward the cursor (offset' = cursor·(1-k) + offset·k).
      this.addEventListener('wheel', e => {
        if (!this.hasAttribute('data-reframe')) return;
        e.preventDefault();
        const r = this.getBoundingClientRect();
        const cx = (e.clientX - r.left) / r.width * 100 - 50;
        const cy = (e.clientY - r.top) / r.height * 100 - 50;
        const prev = this._view.s;
        const next = clampS(prev * Math.pow(1.0015, -e.deltaY));
        if (next === prev) return;
        const k = next / prev;
        this._view.s = next;
        this._view.x = cx * (1 - k) + this._view.x * k;
        this._view.y = cy * (1 - k) + this._view.y * k;
        this._clampView();
        this._applyView();
      }, {
        passive: false
      });
    }
    connectedCallback() {
      // Warn once per page — an id-less slot works for the session but
      // cannot persist, and two id-less slots would share nothing.
      if (!this.id && !ImageSlot._warned) {
        ImageSlot._warned = true;
        console.warn('<image-slot> without an id will not persist its dropped image.');
      }
      this.addEventListener('dragenter', this);
      this.addEventListener('dragover', this);
      this.addEventListener('dragleave', this);
      this.addEventListener('drop', this);
      subs.add(this._subFn);
      // width%/height% in _applyView encode the frame aspect at call time —
      // a host resize (responsive grid, pane divider) would stretch the
      // image until the next _render. Re-render on size change: _render()
      // re-seeds _view from stored before clamp/apply, so a shrink→grow
      // cycle round-trips instead of ratcheting x/y toward the narrower
      // frame's clamp range.
      this._ro = new ResizeObserver(() => this._render());
      this._ro.observe(this);
      load();
      this._render();
    }
    disconnectedCallback() {
      subs.delete(this._subFn);
      this.removeEventListener('dragenter', this);
      this.removeEventListener('dragover', this);
      this.removeEventListener('dragleave', this);
      this.removeEventListener('drop', this);
      if (this._ro) {
        this._ro.disconnect();
        this._ro = null;
      }
      this._exitReframe(false);
    }
    _enterReframe() {
      if (this.hasAttribute('data-reframe')) return;
      this.setAttribute('data-reframe', '');
      this._applyView();
      // Close on click outside (the spill handler stopPropagation()s so
      // in-image drags don't reach this) and on Escape. Listeners are held
      // on the instance so _exitReframe / disconnectedCallback can detach
      // exactly what was attached.
      this._outside = e => {
        if (e.composedPath && e.composedPath().includes(this)) return;
        this._exitReframe(true);
      };
      this._esc = e => {
        if (e.key === 'Escape') this._exitReframe(true);
      };
      document.addEventListener('pointerdown', this._outside, true);
      document.addEventListener('keydown', this._esc, true);
    }
    _exitReframe(commit) {
      if (!this.hasAttribute('data-reframe')) return;
      if (this._dragUp) this._dragUp();
      this.removeAttribute('data-reframe');
      this.removeAttribute('data-panning');
      if (this._outside) document.removeEventListener('pointerdown', this._outside, true);
      if (this._esc) document.removeEventListener('keydown', this._esc, true);
      this._outside = this._esc = null;
      if (commit) this._commitView();
    }
    attributeChangedCallback() {
      if (this.shadowRoot) this._render();
    }

    // handleEvent — one listener object for all four drag events keeps the
    // add/remove symmetric and the depth counter correct.
    handleEvent(e) {
      if (e.type === 'dragenter' || e.type === 'dragover') {
        // Without preventDefault the browser never fires 'drop'.
        e.preventDefault();
        e.stopPropagation();
        if (e.dataTransfer) e.dataTransfer.dropEffect = 'copy';
        if (e.type === 'dragenter') this._depth++;
        this.setAttribute('data-over', '');
      } else if (e.type === 'dragleave') {
        // dragenter/leave fire for every descendant crossing — count depth
        // so hovering the icon inside the empty state doesn't flicker.
        if (--this._depth <= 0) {
          this._depth = 0;
          this.removeAttribute('data-over');
        }
      } else if (e.type === 'drop') {
        e.preventDefault();
        e.stopPropagation();
        this._depth = 0;
        this.removeAttribute('data-over');
        const f = e.dataTransfer && e.dataTransfer.files && e.dataTransfer.files[0];
        if (f) this._ingest(f);
      }
    }
    async _ingest(file) {
      this._setError(null);
      if (!file || ACCEPT.indexOf(file.type) < 0) {
        this._setError('Drop a PNG, JPEG, WebP, or AVIF image.');
        return;
      }
      // Reading can take hundreds of ms on a large image. A Clear or a newer
      // drop during that window must not be clobbered when this await resumes.
      const gen = ++this._gen;
      try {
        const url = await toDataUrl(file);
        if (gen !== this._gen) return;
        // Only exit reframe once the new image is in hand — a rejected type
        // or decode failure leaves the in-progress crop untouched.
        this._exitReframe(false);
        const val = {
          u: url,
          s: 1,
          x: 0,
          y: 0
        };
        setSlot(this.id || '', val);
        // Keep a session-local copy for id-less slots so the drop still
        // shows, even though it cannot persist.
        if (!this.id) {
          this._local = val;
          this._render();
        }
      } catch (err) {
        if (gen !== this._gen) return;
        this._setError('Could not read that image.');
        console.warn('<image-slot> ingest failed:', err);
      }
    }
    _setError(msg) {
      if (this._err) {
        this._err.remove();
        this._err = null;
      }
      if (!msg) return;
      const d = document.createElement('div');
      d.className = 'err';
      d.textContent = msg;
      this.shadowRoot.appendChild(d);
      this._err = d;
      setTimeout(() => {
        if (this._err === d) {
          d.remove();
          this._err = null;
        }
      }, 3000);
    }

    // Reframing (pan/resize) is only meaningful for fit=cover — contain/fill
    // keep the old object-fit path and double-click is a no-op.
    _reframes() {
      return this.hasAttribute('data-filled') && (this.getAttribute('fit') || 'cover') === 'cover';
    }

    // Cover-baseline geometry, shared by clamp/apply/resize. Null until the
    // img has loaded (naturalWidth is 0 before that) or when the slot has no
    // layout box — ResizeObserver fires with a 0×0 rect under display:none,
    // and clamping against a degenerate 1×1 frame would silently pull the
    // stored pan toward zero.
    _geom() {
      const iw = this._img.naturalWidth,
        ih = this._img.naturalHeight;
      const fw = this.clientWidth,
        fh = this.clientHeight;
      if (!iw || !ih || !fw || !fh) return null;
      return {
        iw,
        ih,
        fw,
        fh,
        base: Math.max(fw / iw, fh / ih)
      };
    }
    _clampView() {
      // Pan range on each axis is half the overflow past the frame edge.
      const g = this._geom();
      if (!g) return;
      const mx = Math.max(0, (g.iw * g.base * this._view.s / g.fw - 1) * 50);
      const my = Math.max(0, (g.ih * g.base * this._view.s / g.fh - 1) * 50);
      this._view.x = Math.max(-mx, Math.min(mx, this._view.x));
      this._view.y = Math.max(-my, Math.min(my, this._view.y));
    }
    _applyView() {
      const g = this._geom();
      const fit = this.getAttribute('fit') || 'cover';
      if (fit !== 'cover' || !g) {
        // Non-cover, or dimensions not known yet (before img load).
        this._img.style.width = '100%';
        this._img.style.height = '100%';
        this._img.style.left = '50%';
        this._img.style.top = '50%';
        this._img.style.objectFit = fit;
        this._img.style.objectPosition = this.getAttribute('position') || '50% 50%';
        return;
      }
      // Cover baseline: img fills the frame on its tighter axis at s=1, so
      // pan works immediately on the overflowing axis without zooming first.
      // Width/height and left/top are all frame-% — depends only on the
      // frame aspect ratio, so a responsive resize keeps the same crop. The
      // spill layer mirrors the same box so its corners = image corners.
      const k = g.base * this._view.s;
      const w = g.iw * k / g.fw * 100 + '%';
      const h = g.ih * k / g.fh * 100 + '%';
      const l = 50 + this._view.x + '%';
      const t = 50 + this._view.y + '%';
      this._img.style.width = w;
      this._img.style.height = h;
      this._img.style.left = l;
      this._img.style.top = t;
      this._img.style.objectFit = '';
      this._spill.style.width = w;
      this._spill.style.height = h;
      this._spill.style.left = l;
      this._spill.style.top = t;
    }
    _commitView() {
      const current = this.id ? getSlot(this.id) : this._local;
      const v = {
        ...(current || {}),
        s: this._view.s,
        x: this._view.x,
        y: this._view.y
      };
      if (this._userUrl) v.u = this._userUrl;
      // Framing-only (no u) persists too so an author-src slot remembers its
      // crop; clearing the sidecar still falls through to src=.
      if (this.id) setSlot(this.id, v);else {
        this._local = v;
      }
    }
    _syncAutoHeight() {
      if (this.hasAttribute('data-auto-height') && this._img.naturalWidth && this._img.naturalHeight) {
        this.style.aspectRatio = `${this._img.naturalWidth} / ${this._img.naturalHeight}`;
      } else {
        this.style.removeProperty('aspect-ratio');
      }
    }
    _render() {
      // Shape / mask. Presets use border-radius so the dashed ring can
      // follow the rounded outline; clip-path is only applied for an
      // explicit `mask` (the ring is hidden there since a rectangle
      // dashed border chopped by an arbitrary polygon looks broken).
      const mask = this.getAttribute('mask');
      const shape = (this.getAttribute('shape') || 'rounded').toLowerCase();
      let radius = '';
      if (shape === 'circle') radius = '50%';else if (shape === 'pill') radius = '9999px';else if (shape === 'rounded') {
        const n = parseFloat(this.getAttribute('radius'));
        radius = (Number.isFinite(n) ? n : 12) + 'px';
      }
      this._frame.style.borderRadius = mask ? '' : radius;
      this._frame.style.clipPath = mask || '';
      this._ring.style.borderRadius = mask ? '' : radius;
      this._ring.style.display = mask ? 'none' : '';

      // Controls and reframe entry gate on this so share links stay read-only.
      const editable = !!(window.omelette && window.omelette.writeFile);
      this.toggleAttribute('data-editable', editable);
      this._sub.style.display = editable ? '' : 'none';

      // Content. The sidecar is also writable by the agent's write_file
      // tool, so its value isn't guaranteed canvas-originated — only accept
      // data:image/ URLs from it. The `src` attribute is author-controlled
      // (Claude wrote it into the HTML) so it passes through unchanged.
      let stored = this.id ? getSlot(this.id) : this._local;
      if (stored && stored.u && !/^data:image\//i.test(stored.u)) stored = null;
      const srcAttr = this.getAttribute('src') || '';
      this._userUrl = stored && stored.u || null;
      const url = this._userUrl || srcAttr;
      // Don't clobber an in-flight reframe with a store-triggered re-render.
      if (!this.hasAttribute('data-reframe')) {
        this._view = {
          s: stored && Number.isFinite(stored.s) ? clampS(stored.s) : 1,
          x: stored && Number.isFinite(stored.x) ? stored.x : 0,
          y: stored && Number.isFinite(stored.y) ? stored.y : 0
        };
      }
      this._cap.textContent = this.getAttribute('placeholder') || 'Drop an image';
      // Toggle via style.display — the [hidden] attribute alone loses to
      // the display:flex / display:block rules in the stylesheet above.
      if (url) {
        if (this._img.getAttribute('src') !== url) {
          this._img.src = url;
          this._ghost.src = url;
        }
        this._img.style.display = 'block';
        this._empty.style.display = 'none';
        this.setAttribute('data-filled', '');
        this.style.display = '';
        this._syncAutoHeight();
        this._clampView();
        this._applyView();
      } else {
        this.style.removeProperty('aspect-ratio');
        this._img.style.display = 'none';
        this._img.removeAttribute('src');
        this._ghost.removeAttribute('src');
        this._empty.style.display = 'flex';
        this.removeAttribute('data-filled');
        // Export sem imagem (read-only) → some; no canvas (editable) segue visível.
        this.style.display = editable ? '' : 'none';
      }
    }
  }
  window.__SPU_IMAGE_SLOT_RUNTIME = {
    snapshot() {
      return loaded || Object.keys(slots).length ? JSON.stringify(slots) : '';
    },
    replace(content) {
      try {
        const next = typeof content === 'string' ? JSON.parse(content) : content;
        slots = next && typeof next === 'object' && !Array.isArray(next) ? next : {};
      } catch {
        slots = {};
      }
      storeRevision++;
      tombstones.clear();
      loaded = true;
      subs.forEach(fn => fn());
      save();
    },
    clear() {
      slots = {};
      storeRevision++;
      tombstones.clear();
      loaded = true;
      subs.forEach(fn => fn());
      save();
    }
  };
  if (!customElements.get('image-slot')) {
    customElements.define('image-slot', ImageSlot);
  }
})();
})(); } catch (e) { __ds_ns.__errors.push({ path: "assets/image-slot.js", error: String((e && e.message) || e) }); }

// components/core/BlockRegistry.jsx
try { (() => {
// Registry de blocos do builder — a fonte única do QUE pode ser inserido e
// DENTRO DE QUAIS LIMITES. Mapeia cada bloco a um componente real do DS, com
// props padrão, campos editáveis (texto inline) e travas. Espelha o padrão do
// LumeAuthor (registry declarativo), adaptado ao SPU ENAP DS.
//
// kind: "container" → tem children[] (Section) · "text" → campos de texto inline
//       "list" → editado no painel (arrays) · "marker" → sem conteúdo (ex.: page-break)
// component: nome no window.NS · fields: chaves de texto inline (rich salvo em HTML)
// locks: { bg } trava o fundo · cat: categoria na biblioteca

const BLOCKS = [
// ── Estrutura ──
{
  type: 'masthead',
  component: 'Masthead',
  label: 'Cabeçalho do programa',
  icon: 'graduation-cap',
  cat: 'Estrutura',
  kind: 'text',
  locks: {
    bg: true
  },
  fields: ['org', 'program'],
  rich: true,
  props: {
    org: 'SPU',
    program: 'Programa Gestão de Imóveis Públicos'
  }
}, {
  type: 'section',
  component: 'Section',
  label: 'Seção (container)',
  icon: 'layout',
  cat: 'Estrutura',
  kind: 'container',
  props: {
    width: 'content',
    surface: 'none',
    pad: 'lg',
    children: []
  }
}, {
  type: 'sectionslider',
  component: 'SectionSlider',
  label: 'Seção-slider',
  icon: 'chevrons-right',
  cat: 'Estrutura',
  kind: 'container',
  stack: false,
  props: {
    loop: true,
    children: []
  },
  propFields: [{
    key: 'loop',
    label: 'Navegação circular',
    type: 'bool'
  }]
}, {
  type: 'sectionslide',
  component: 'Section',
  label: 'Slide (seção)',
  icon: 'layout',
  cat: 'Estrutura',
  kind: 'container',
  internal: true,
  props: {
    width: 'content',
    surface: 'none',
    pad: 'lg',
    children: []
  }
}, {
  type: 'columns',
  component: 'Columns',
  label: 'Colunas',
  icon: 'layers',
  cat: 'Estrutura',
  kind: 'container',
  stack: false,
  props: {
    count: 2,
    ratio: 'equal',
    gap: 'lg',
    children: []
  },
  propFields: [{
    key: 'count',
    label: 'Nº de colunas',
    type: 'select',
    options: [{
      value: 2,
      label: '2'
    }, {
      value: 3,
      label: '3'
    }, {
      value: 4,
      label: '4'
    }]
  }, {
    key: 'ratio',
    label: 'Proporção',
    type: 'select',
    options: [{
      value: 'equal',
      label: 'Iguais'
    }, {
      value: 'first-wide',
      label: 'Primeira em destaque (2×)'
    }, {
      value: 'last-wide',
      label: 'Última em destaque (2×)'
    }]
  }, {
    key: 'gap',
    label: 'Espaçamento',
    type: 'select',
    options: [{
      value: 'sm',
      label: 'Pequeno'
    }, {
      value: 'md',
      label: 'Médio'
    }, {
      value: 'lg',
      label: 'Grande'
    }]
  }]
}, {
  type: 'hero',
  component: 'Hero',
  label: 'Abertura (Hero)',
  icon: 'book-open',
  cat: 'Estrutura',
  kind: 'text',
  locks: {
    bg: true
  },
  fields: ['kicker', 'title', 'byline'],
  props: {
    kicker: 'Nome da competência',
    kickerIcon: 'compass',
    title: 'Título da unidade',
    byline: 'Eixo X · Competência X',
    slot: ''
  }
}, {
  type: 'titulo',
  component: null,
  label: 'Título de seção',
  icon: 'heading',
  cat: 'Estrutura',
  kind: 'text',
  fields: ['text'],
  props: {
    text: 'Título da seção',
    level: 'h2'
  }
}, {
  type: 'conclusion',
  component: 'Conclusion',
  label: 'Conclusão / síntese',
  icon: 'check-circle',
  cat: 'Estrutura',
  kind: 'container',
  locks: {
    bg: true
  },
  fields: ['kicker', 'title', 'body'],
  rich: true,
  props: {
    kicker: 'Para fechar',
    kickerIcon: 'check',
    title: 'Conclusão',
    body: '<p>Retome o essencial em poucas linhas.</p>',
    children: []
  }
}, {
  type: 'pagefooter',
  component: 'PageFooter',
  label: 'Rodapé',
  icon: 'file-text',
  cat: 'Estrutura',
  kind: 'list',
  locks: {
    bg: true
  },
  rich: true,
  fields: ['code', 'context'],
  itemsKey: 'credits',
  itemFields: [{
    key: 'role',
    label: 'Papel',
    type: 'rich',
    inline: true
  }, {
    key: 'name',
    label: 'Nome',
    type: 'rich',
    inline: true
  }],
  propFields: [{
    key: 'licenseKind',
    label: 'Tipo de licença',
    type: 'select',
    options: [{
      value: 'byncsa',
      label: 'CC BY-NC-SA'
    }, {
      value: 'byncnd',
      label: 'CC BY-NC-ND'
    }]
  }],
  props: {
    code: 'Unidade · Tema',
    context: 'Este material foi desenvolvido com fins educacionais para o Programa de Desenvolvimento Profissional em Gestão de Imóveis Públicos, da Secretaria de Patrimônio da União – SPU.',
    license: true,
    licenseKind: 'byncsa',
    credits: [{
      role: 'Conteúdo',
      name: 'Autor'
    }, {
      role: 'Revisão',
      name: '-'
    }, {
      role: 'Design Educacional',
      name: 'Luís Henrique Lindner'
    }, {
      role: 'Versão',
      name: 'Julho de 2026'
    }]
  }
}, {
  type: 'referencias',
  component: 'ReferenceList',
  label: 'Referências (ABNT)',
  icon: 'book-marked',
  cat: 'Texto',
  kind: 'list',
  itemsKey: 'items',
  itemFields: [{
    key: '',
    label: 'Referência',
    type: 'rich',
    inline: true
  }],
  props: {
    title: 'Referências',
    defaultOpen: false,
    items: ['SOBRENOME, Nome. <strong>Título</strong>. Cidade: Editora, ano.']
  }
},
// ── Texto ──
{
  type: 'prose',
  component: 'RichText',
  label: 'Parágrafo',
  icon: 'type',
  cat: 'Texto',
  kind: 'text',
  rich: true,
  fields: ['html'],
  props: {
    html: '<p>Escreva aqui. Selecione um trecho para marca-texto, cor, link ou termo.</p>'
  }
}, {
  type: 'datatable',
  component: 'DataTable',
  label: 'Tabela',
  icon: 'grid',
  cat: 'Texto',
  kind: 'list',
  fields: ['caption'],
  itemsKey: 'rows',
  propFields: [{
    key: 'dense',
    label: 'Condensada',
    type: 'bool'
  }, {
    key: 'highlightFirst',
    label: 'Destacar primeira coluna',
    type: 'bool'
  }, {
    key: 'striped',
    label: 'Linhas alternadas',
    type: 'bool'
  }, {
    key: 'tone',
    label: 'Cor',
    type: 'select',
    options: [{
      value: 'default',
      label: 'Padrão'
    }, {
      value: 'petrol',
      label: 'Petróleo'
    }, {
      value: 'terra',
      label: 'Terracota'
    }, {
      value: 'ochre',
      label: 'Ocre'
    }]
  }],
  props: {
    caption: 'Tabela',
    columns: [{
      key: 'c0',
      label: 'Coluna 1'
    }, {
      key: 'c1',
      label: 'Coluna 2'
    }],
    rows: [{
      c0: 'Valor 1',
      c1: 'Valor 2'
    }, {
      c0: 'Valor 3',
      c1: 'Valor 4'
    }],
    dense: false,
    highlightFirst: false,
    striped: false,
    tone: 'default'
  }
}, {
  type: 'pullquote',
  component: 'PullQuote',
  label: 'Citação',
  icon: 'quote',
  cat: 'Texto',
  kind: 'text',
  propFields: [{
    key: 'variant',
    label: 'Estilo',
    type: 'select',
    options: [{
      value: 'eye',
      label: 'Olho (aspas grandes)'
    }, {
      value: 'block',
      label: 'Bloco'
    }]
  }],
  fields: ['children', 'cite'],
  props: {
    variant: 'eye',
    children: 'Uma frase marcante que merece destaque.',
    cite: 'Fonte, ano'
  }
}, {
  type: 'kicker',
  component: 'Kicker',
  label: 'Eyebrow (sobrelinha)',
  icon: 'heading',
  cat: 'Texto',
  kind: 'text',
  fields: ['children'],
  props: {
    icon: 'compass',
    children: 'Sobrelinha'
  }
}, {
  type: 'markerlist',
  component: 'MarkerList',
  label: 'Lista visual',
  icon: 'list',
  cat: 'Texto',
  kind: 'list',
  itemsKey: 'items',
  itemFields: [{
    key: 'title',
    label: 'Título',
    type: 'rich',
    inline: true
  }, {
    key: 'text',
    label: 'Texto',
    type: 'rich',
    inline: true
  }],
  propFields: [{
    key: 'variant',
    label: 'Estilo',
    type: 'select',
    options: [{
      value: 'ordered',
      label: 'Numerada'
    }, {
      value: 'check',
      label: 'Checks'
    }, {
      value: 'plain',
      label: 'Bullet'
    }, {
      value: 'icon',
      label: 'Ícone'
    }]
  }, {
    key: 'icon',
    label: 'Ícone do bullet',
    type: 'icon'
  }, {
    key: 'accent',
    label: 'Cor dos marcadores',
    type: 'accent'
  }],
  props: {
    variant: 'ordered',
    icon: 'check',
    accent: '',
    items: [{
      title: 'Identificar',
      text: 'Localizar o imóvel no cadastro SPU.'
    }, {
      title: 'Avaliar',
      text: 'Verificar a destinação atual do bem.'
    }]
  }
},
// ── Destaques ──
{
  type: 'callout',
  component: 'Callout',
  label: 'Destaque / atenção',
  icon: 'lightbulb',
  cat: 'Destaques',
  kind: 'text',
  rich: true,
  fields: ['title', 'heading', 'children'],
  propFields: [{
    key: 'variant',
    label: 'Estilo',
    type: 'select',
    options: [{
      value: 'header',
      label: 'Faixa no topo'
    }, {
      value: 'ear',
      label: 'Orelha de ícone'
    }]
  }, {
    key: 'tone',
    label: 'Tom',
    type: 'select',
    options: [{
      value: 'info',
      label: 'Informativo'
    }, {
      value: 'attention',
      label: 'Atenção'
    }, {
      value: 'warning',
      label: 'Alerta'
    }, {
      value: 'success',
      label: 'Sucesso'
    }, {
      value: 'note',
      label: 'Nota'
    }, {
      value: 'neutral',
      label: 'Neutro'
    }]
  }],
  props: {
    variant: 'header',
    tone: 'info',
    icon: 'lightbulb',
    title: 'Você sabia?',
    heading: '',
    children: '<p>O ponto-chave para o aluno reter.</p>'
  }
}, {
  type: 'reflexao',
  component: 'ReflectionStop',
  label: 'Parada de reflexão',
  icon: 'flower',
  cat: 'Destaques',
  kind: 'list',
  fields: ['children'],
  rich: true,
  itemsKey: 'prompts',
  itemFields: [{
    key: '',
    label: 'Pergunta',
    type: 'rich',
    inline: true
  }],
  props: {
    prompts: ['Uma pergunta aberta que conecta o tema à vida do aluno.'],
    children: 'Antes de avançar, pense nisto.'
  }
}, {
  type: 'panel',
  component: 'Panel',
  label: 'Bloco de destaque',
  icon: 'square',
  cat: 'Destaques',
  kind: 'container',
  rich: true,
  fields: ['kicker', 'title', 'body'],
  propFields: [{
    key: 'variant',
    label: 'Estilo',
    type: 'select',
    options: [{
      value: 'box',
      label: 'Caixa'
    }, {
      value: 'feature',
      label: 'Bloco (full-bleed)'
    }, {
      value: 'accent',
      label: 'Acento'
    }]
  }, {
    key: 'bg',
    label: 'Cor de fundo',
    type: 'select',
    options: [{
      value: '',
      label: 'Quente (padrão)'
    }, {
      value: 'var(--petrol-50)',
      label: 'Petróleo claro'
    }, {
      value: 'var(--terra-50)',
      label: 'Terracota claro'
    }, {
      value: 'var(--ochre-50)',
      label: 'Ocre claro'
    }, {
      value: 'var(--green-100)',
      label: 'Verde claro'
    }, {
      value: 'var(--sand-100)',
      label: 'Areia'
    }, {
      value: 'var(--bg-paper)',
      label: 'Papel'
    }]
  }],
  props: {
    variant: 'box',
    kicker: 'Destaque',
    kickerIcon: 'map-pin',
    title: 'Título do bloco',
    color: '',
    bg: '',
    body: '<p>Conteúdo do bloco.</p>',
    children: []
  }
},
// ── Mídia ──
{
  type: 'figure',
  component: 'Figure',
  label: 'Imagem com legenda',
  icon: 'image',
  cat: 'Mídia',
  kind: 'text',
  fields: ['title', 'caption', 'credit'],
  props: {
    slot: '',
    src: '',
    fit: 'contain',
    size: 'full',
    title: '',
    caption: 'Legenda da imagem.',
    credit: ''
  }
}, {
  type: 'carousel',
  component: 'Carousel',
  label: 'Carrossel de imagens',
  icon: 'image',
  cat: 'Mídia',
  kind: 'list',
  rich: true,
  itemsKey: 'slides',
  itemFields: [{
    key: 'slot',
    label: 'Imagem',
    type: 'slot'
  }, {
    key: 'title',
    label: 'Título',
    type: 'rich',
    inline: true
  }, {
    key: 'caption',
    label: 'Legenda',
    type: 'rich',
    inline: true
  }, {
    key: 'credit',
    label: 'Crédito',
    type: 'rich',
    inline: true
  }, {
    key: 'alt',
    label: 'Texto alternativo',
    type: 'text'
  }],
  propFields: [{
    key: 'size',
    label: 'Largura',
    type: 'select',
    options: [{
      value: 'sm',
      label: 'Pequena'
    }, {
      value: 'md',
      label: 'Média'
    }, {
      value: 'lg',
      label: 'Ampla'
    }, {
      value: 'full',
      label: 'Total'
    }]
  }, {
    key: 'fit',
    label: 'Ajuste da imagem',
    type: 'select',
    options: [{
      value: 'contain',
      label: 'Mostrar completa'
    }, {
      value: 'cover',
      label: 'Preencher área'
    }]
  }],
  props: {
    size: 'full',
    fit: 'contain',
    zoom: true,
    slides: [{
      slot: '',
      title: 'Título da imagem',
      caption: 'Legenda da imagem.',
      credit: '',
      alt: ''
    }, {
      slot: '',
      title: 'Título da imagem',
      caption: 'Legenda da imagem.',
      credit: '',
      alt: ''
    }]
  }
}, {
  type: 'contentslider',
  component: 'ContentSlider',
  label: 'Slider de conteúdo',
  icon: 'panels-top-left',
  cat: 'Interativos',
  kind: 'list',
  itemsKey: 'slides',
  itemFields: [{
    key: 'showImage',
    label: 'Exibir imagem',
    type: 'bool'
  }, {
    key: 'slot',
    label: 'Imagem (opcional)',
    type: 'slot',
    optional: true
  }, {
    key: 'alt',
    label: 'Texto alternativo',
    type: 'text',
    optional: true
  }, {
    key: 'caption',
    label: 'Legenda da imagem',
    type: 'rich',
    inline: true,
    optional: true
  }, {
    key: 'labelIcon',
    label: 'Ícone da etiqueta',
    type: 'icon',
    optional: true
  }, {
    key: 'label',
    label: 'Etiqueta',
    type: 'rich',
    inline: true,
    optional: true
  }, {
    key: 'tabLabel',
    label: 'Rótulo da aba',
    type: 'rich',
    inline: true
  }, {
    key: 'title',
    label: 'Título',
    type: 'rich',
    inline: true
  }, {
    key: 'subtitle',
    label: 'Subtítulo',
    type: 'rich',
    inline: true,
    optional: true
  }, {
    key: 'description',
    label: 'Descrição',
    type: 'rich',
    optional: true
  }, {
    key: 'linkHref',
    label: 'URL do link',
    type: 'text',
    optional: true
  }, {
    key: 'linkLabel',
    label: 'Texto do link',
    type: 'rich',
    inline: true,
    optional: true
  }],
  propFields: [{
    key: 'hint',
    label: 'Chamada superior',
    type: 'rich',
    inline: true
  }, {
    key: 'accent',
    label: 'Cor de acento',
    type: 'accent'
  }, {
    key: 'showTabNumbers',
    label: 'Mostrar números nas abas',
    type: 'bool'
  }],
  props: {
    hint: 'Explore os slides',
    accent: '',
    showTabNumbers: true,
    slides: [{
      showImage: true,
      slot: '',
      alt: '',
      caption: '',
      labelIcon: '',
      label: 'Etiqueta',
      tabLabel: 'Slide 1',
      title: 'Título do slide',
      subtitle: 'Subtítulo opcional',
      description: '<p>Descrição opcional do conteúdo.</p>',
      linkHref: '',
      linkLabel: ''
    }, {
      showImage: false,
      slot: '',
      alt: '',
      caption: '',
      labelIcon: '',
      label: '',
      tabLabel: 'Slide 2',
      title: 'Outro título de slide',
      subtitle: '',
      description: '',
      linkHref: '',
      linkLabel: ''
    }]
  }
}, {
  type: 'bleedimage',
  component: 'BleedImage',
  label: 'Imagem full-bleed',
  icon: 'image',
  cat: 'Mídia',
  kind: 'text',
  locks: {
    bg: true
  },
  fields: ['title', 'caption', 'credit'],
  propFields: [{
    key: 'heightPreset',
    label: 'Altura',
    type: 'select',
    options: [{
      value: 'narrow',
      label: 'Estreita'
    }, {
      value: 'medium',
      label: 'Média'
    }, {
      value: 'wide',
      label: 'Ampla'
    }, {
      value: 'full',
      label: 'Total'
    }]
  }, {
    key: 'captionMode',
    label: 'Texto',
    type: 'select',
    options: [{
      value: 'below',
      label: 'Abaixo'
    }, {
      value: 'overlay',
      label: 'Sobre a imagem'
    }]
  }, {
    key: 'overlayBg',
    label: 'Cor do box',
    type: 'select',
    options: [{
      value: 'rgba(14,46,43,.72)',
      label: 'Petróleo translúcido'
    }, {
      value: 'rgba(109,62,41,.72)',
      label: 'Terracota translúcido'
    }, {
      value: 'rgba(143,95,18,.72)',
      label: 'Ocre translúcido'
    }, {
      value: 'rgba(255,255,255,.82)',
      label: 'Claro translúcido'
    }]
  }],
  props: {
    slot: '',
    src: '',
    zoom: false,
    fit: 'cover',
    heightPreset: 'medium',
    title: '',
    caption: '',
    credit: '',
    captionMode: 'below',
    overlayBg: 'rgba(14,46,43,.72)'
  }
}, {
  type: 'mapfigure',
  component: 'MapFigure',
  label: 'Imagem com hotspots',
  icon: 'map-pin',
  cat: 'Mídia',
  kind: 'list',
  itemsKey: 'markers',
  rich: true,
  fields: ['caption', 'credit', 'label'],
  itemFields: [{
    key: 'title',
    label: 'Título',
    type: 'rich',
    inline: true
  }, {
    key: 'description',
    label: 'Descrição',
    type: 'rich',
    inline: true
  }, {
    key: 'label',
    label: 'Rótulo do pino (opcional)',
    type: 'text'
  }, {
    key: 'x',
    label: 'X (%)',
    type: 'number'
  }, {
    key: 'y',
    label: 'Y (%)',
    type: 'number'
  }],
  props: {
    slot: '',
    markers: [],
    caption: '',
    credit: ''
  }
}, {
  type: 'examplecard',
  component: 'ExampleCard',
  label: 'Card de exemplo',
  icon: 'layers',
  cat: 'Mídia',
  kind: 'text',
  rich: true,
  fields: ['label', 'title', 'children'],
  propFields: [{
    key: 'collapse',
    label: 'Comportamento',
    type: 'select',
    options: [{
      value: 'none',
      label: 'Sempre aberto'
    }, {
      value: 'open',
      label: 'Retrátil, inicialmente aberto'
    }, {
      value: 'closed',
      label: 'Retrátil, inicialmente fechado'
    }]
  }],
  props: {
    label: 'Exemplo prático',
    icon: 'map-pin',
    color: '',
    slot: '',
    collapse: 'none',
    title: 'Título do exemplo',
    children: '<p>Descrição do caso.</p>'
  }
}, {
  type: 'statblock',
  component: 'StatBlock',
  label: 'Dados / estatísticas',
  icon: 'grid',
  cat: 'Mídia',
  kind: 'list',
  itemsKey: 'stats',
  itemFields: [{
    key: 'value',
    label: 'Valor',
    type: 'rich',
    inline: true
  }, {
    key: 'unit',
    label: 'Unidade',
    type: 'rich',
    inline: true
  }, {
    key: 'label',
    label: 'Rótulo',
    type: 'rich',
    inline: true
  }, {
    key: 'description',
    label: 'Descrição',
    type: 'rich',
    inline: true
  }],
  propFields: [{
    key: 'variant',
    label: 'Estilo',
    type: 'select',
    options: [{
      value: 'rule',
      label: 'Com régua'
    }, {
      value: 'card',
      label: 'Cartão'
    }, {
      value: 'plain',
      label: 'Simples'
    }]
  }],
  props: {
    variant: 'rule',
    columns: 3,
    stats: [{
      value: '100',
      unit: '+',
      label: 'Rótulo',
      description: ''
    }]
  }
}, {
  type: 'feature',
  component: 'FeatureGrid',
  label: 'Destaques (ícones)',
  icon: 'grid',
  cat: 'Mídia',
  kind: 'list',
  itemsKey: 'items',
  propFields: [{
    key: 'accent',
    label: 'Cor do conjunto de ícones',
    type: 'accent'
  }, {
    key: 'layout',
    label: 'Posição do ícone',
    type: 'select',
    options: [{
      value: 'top',
      label: 'Acima do conteúdo'
    }, {
      value: 'side',
      label: 'À esquerda do conteúdo'
    }]
  }],
  itemFields: [{
    key: 'icon',
    label: 'Ícone',
    type: 'icon'
  }, {
    key: 'title',
    label: 'Título',
    type: 'rich',
    inline: true
  }, {
    key: 'text',
    label: 'Texto',
    type: 'rich',
    inline: true
  }],
  props: {
    columns: 3,
    card: true,
    accent: '',
    layout: 'top',
    items: [{
      icon: 'building',
      title: 'Título',
      text: 'Uma linha.'
    }]
  }
}, {
  type: 'mediaembed',
  component: 'MediaEmbed',
  label: 'Vídeo / áudio',
  icon: 'play-circle',
  cat: 'Mídia',
  kind: 'list',
  fields: ['title'],
  propFields: [{
    key: 'type',
    label: 'Tipo',
    type: 'select',
    options: [{
      value: 'video',
      label: 'Vídeo'
    }, {
      value: 'audio',
      label: 'Áudio'
    }]
  }, {
    key: 'provider',
    label: 'Fonte',
    type: 'text'
  }, {
    key: 'url',
    label: 'URL',
    type: 'text'
  }],
  props: {
    type: 'video',
    title: 'Título',
    provider: 'Externo',
    url: 'https://'
  }
}, {
  type: 'externalembed',
  component: 'ExternalEmbed',
  label: 'Conteúdo externo (iframe)',
  icon: 'code',
  cat: 'Mídia',
  kind: 'list',
  propFields: [{
    key: 'title',
    label: 'Título acessível',
    type: 'text'
  }, {
    key: 'embed',
    label: 'URL ou código iframe',
    type: 'text'
  }, {
    key: 'responsive',
    label: 'Responsivo na largura',
    type: 'bool'
  }, {
    key: 'useEmbedDimensions',
    label: 'Herdar dimensões do iframe',
    type: 'bool'
  }, {
    key: 'width',
    label: 'Largura máxima (px)',
    type: 'number',
    min: 240,
    max: 2400,
    step: 10
  }, {
    key: 'height',
    label: 'Altura (px)',
    type: 'number',
    min: 120,
    max: 2400,
    step: 10
  }],
  props: {
    title: 'Conteúdo incorporado',
    embed: '',
    responsive: true,
    useEmbedDimensions: true,
    width: 960,
    height: 540
  }
},
// ── Interativos ──
{
  type: 'flashcard',
  component: 'Flipcard',
  label: 'Flashcard',
  icon: 'layers',
  cat: 'Interativos',
  kind: 'list',
  propFields: [{
    key: 'icon',
    label: 'Ícone da frente',
    type: 'icon'
  }, {
    key: 'term',
    label: 'Título da frente',
    type: 'rich',
    inline: true
  }, {
    key: 'description',
    label: 'Descrição curta da frente',
    type: 'rich',
    inline: true
  }, {
    key: 'definition',
    label: 'Texto do verso',
    type: 'rich'
  }, {
    key: 'color',
    label: 'Cor do flashcard',
    type: 'accent'
  }],
  props: {
    term: 'Título do flashcard',
    description: 'Uma descrição curta para apresentar o conteúdo.',
    definition: '<p>Use este espaço para desenvolver o conteúdo do verso.</p>',
    icon: 'building',
    color: 'var(--petrol-600)'
  }
}, {
  type: 'accordion',
  component: 'Accordion',
  label: 'Accordion (FAQ)',
  icon: 'list',
  cat: 'Interativos',
  kind: 'list',
  itemsKey: 'items',
  itemFields: [{
    key: 'title',
    label: 'Pergunta',
    type: 'rich',
    inline: true
  }, {
    key: 'content',
    label: 'Resposta',
    type: 'rich'
  }, {
    key: 'linkHref',
    label: 'Link (opcional)',
    type: 'text',
    optional: true
  }, {
    key: 'linkLabel',
    label: 'Texto do link',
    type: 'rich',
    inline: true,
    optional: true
  }, {
    key: 'linkButton',
    label: 'Estilo do link',
    type: 'select',
    optional: true,
    options: [{
      value: '',
      label: 'Link'
    }, {
      value: 'primary',
      label: 'Botão'
    }, {
      value: 'secondary',
      label: 'Botão 2ª'
    }]
  }],
  props: {
    numbered: true,
    items: [{
      title: 'Pergunta?',
      content: '<p>Resposta.</p>'
    }]
  }
}, {
  type: 'timeline',
  component: 'Timeline',
  label: 'Linha do tempo',
  icon: 'list',
  cat: 'Interativos',
  kind: 'list',
  itemsKey: 'eras',
  itemFields: [{
    key: 'label',
    label: 'Período',
    type: 'rich',
    inline: true
  }, {
    key: 'period',
    label: 'Intervalo',
    type: 'rich',
    inline: true
  }, {
    key: 'color',
    label: 'Cor',
    type: 'accent'
  }, {
    key: 'milestones',
    label: 'Marcos',
    type: 'list',
    itemFields: [{
      key: 'date',
      label: 'Data',
      type: 'rich',
      inline: true
    }, {
      key: 'title',
      label: 'Marco',
      type: 'rich',
      inline: true
    }, {
      key: 'content',
      label: 'Descrição',
      type: 'rich'
    }, {
      key: 'linkHref',
      label: 'Link (opcional)',
      type: 'text',
      optional: true
    }, {
      key: 'linkLabel',
      label: 'Texto do link',
      type: 'rich',
      inline: true,
      optional: true
    }, {
      key: 'linkButton',
      label: 'Estilo do link',
      type: 'select',
      optional: true,
      options: [{
        value: '',
        label: 'Link'
      }, {
        value: 'primary',
        label: 'Botão'
      }, {
        value: 'secondary',
        label: 'Botão 2ª'
      }]
    }]
  }],
  props: {
    eras: [{
      label: 'Período',
      period: 'ano–ano',
      color: 'var(--petrol-600)',
      milestones: [{
        date: 'Ano',
        title: 'Marco',
        content: '<p>O que aconteceu.</p>'
      }]
    }]
  }
}, {
  type: 'compareab',
  component: 'CompareAB',
  label: 'Comparativo A/B',
  icon: 'layers',
  cat: 'Interativos',
  kind: 'list',
  propFields: [{
    key: 'a',
    label: 'Lado A',
    type: 'object',
    fields: [{
      key: 'label',
      label: 'Rótulo',
      type: 'rich',
      inline: true
    }, {
      key: 'icon',
      label: 'Ícone',
      type: 'icon'
    }, {
      key: 'title',
      label: 'Título',
      type: 'rich',
      inline: true
    }, {
      key: 'content',
      label: 'Conteúdo',
      type: 'rich'
    }]
  }, {
    key: 'b',
    label: 'Lado B',
    type: 'object',
    fields: [{
      key: 'label',
      label: 'Rótulo',
      type: 'rich',
      inline: true
    }, {
      key: 'icon',
      label: 'Ícone',
      type: 'icon'
    }, {
      key: 'title',
      label: 'Título',
      type: 'rich',
      inline: true
    }, {
      key: 'content',
      label: 'Conteúdo',
      type: 'rich'
    }]
  }],
  props: {
    a: {
      label: 'A',
      icon: 'building',
      title: 'Opção A',
      content: '<p>…</p>'
    },
    b: {
      label: 'B',
      icon: 'scale',
      title: 'Opção B',
      content: '<p>…</p>'
    }
  }
}, {
  type: 'imagereveal',
  component: 'ImageReveal',
  label: 'Antes / depois',
  icon: 'image',
  cat: 'Interativos',
  kind: 'list',
  props: {
    beforeLabel: 'Antes',
    afterLabel: 'Depois',
    beforeSlot: '',
    afterSlot: ''
  }
}, {
  type: 'quiz',
  component: 'Quiz',
  label: 'Quiz (verificação)',
  icon: 'help-circle',
  cat: 'Interativos',
  kind: 'list',
  itemsKey: 'questions',
  itemFields: [{
    key: 'question',
    label: 'Pergunta',
    type: 'rich',
    inline: true
  }, {
    key: 'options',
    label: 'Alternativas',
    type: 'list',
    itemFields: [{
      key: 'text',
      label: 'Alternativa',
      type: 'rich',
      inline: true
    }, {
      key: 'correct',
      label: 'Correta',
      type: 'bool',
      exclusive: true
    }, {
      key: 'feedback',
      label: 'Feedback',
      type: 'rich'
    }]
  }],
  props: {
    eyebrow: 'Verifique seu entendimento',
    icon: 'help-circle',
    questions: [{
      question: 'Enunciado da pergunta?',
      options: [{
        text: 'Alternativa correta',
        correct: true,
        feedback: 'Por que está correta.'
      }, {
        text: 'Alternativa incorreta',
        feedback: 'Por que não.'
      }]
    }]
  }
}];
const BLOCK_BY_TYPE = Object.fromEntries(BLOCKS.map(b => [b.type, b]));
const BLOCK_CATS = ['Estrutura', 'Texto', 'Destaques', 'Mídia', 'Interativos'];
// Tipos que NÃO podem viver dentro de uma Section (são estrutura de página).
const STRUCTURAL_TYPES = ['masthead', 'section', 'sectionslider', 'hero', 'conclusion', 'pagefooter'];
const CHILD_TYPES = BLOCKS.filter(b => !b.internal && STRUCTURAL_TYPES.indexOf(b.type) === -1).map(b => b.type);
BLOCK_BY_TYPE.sectionslider.allowedTypes = ['sectionslide'];
BLOCK_BY_TYPE.sectionslide.allowedTypes = CHILD_TYPES;
function uid(p) {
  return p + Math.random().toString(36).slice(2, 9);
}

// Cria uma instância nova de bloco (dados serializáveis).
function newBlock(type, child) {
  const def = BLOCK_BY_TYPE[type];
  if (!def) return null;
  const b = {
    id: uid(child ? 'c' : 'b'),
    type,
    props: JSON.parse(JSON.stringify(def.props || {}))
  };
  if (type === 'section') b.props.children = [newBlock('titulo', true), newBlock('prose', true)];
  if (type === 'sectionslide') b.props.children = [newBlock('titulo', true), newBlock('prose', true)];
  if (type === 'sectionslider') b.props.children = [newBlock('sectionslide', true), newBlock('sectionslide', true)];
  return b;
}
const BlockRegistry = {
  blocks: BLOCKS,
  byType: BLOCK_BY_TYPE,
  cats: BLOCK_CATS,
  structuralTypes: STRUCTURAL_TYPES,
  childTypes: CHILD_TYPES,
  newBlock
};
Object.assign(__ds_scope, { BlockRegistry });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/BlockRegistry.jsx", error: String((e && e.message) || e) }); }

// components/core/BuilderExport.jsx
try { (() => {
// ─────────────────────────────────────────────────────────────────────────────
// Modelo de projeto serializável + export standalone.
// Um "documento" é { meta, blocks[] } — 100% JSON, sem React/HTML. O builder
// guarda isso (autosave/arquivo) e o BlockDocument o renderiza. O export
// standalone embute kit (bundle+css) + dados num único HTML offline.
// ─────────────────────────────────────────────────────────────────────────────

const SCHEMA_VERSION = 1;

// —— Documento novo, vazio ou semeado ——
function newDoc(meta) {
  return {
    schema: SCHEMA_VERSION,
    meta: {
      title: 'Nova unidade',
      lang: 'pt-BR',
      builderCredit: true,
      ...(meta || {})
    },
    blocks: []
  };
}

// —— (De)serialização: JSON puro, com validação leve ——
function serialize(doc) {
  return JSON.stringify({
    schema: SCHEMA_VERSION,
    meta: doc.meta || {},
    blocks: doc.blocks || []
  });
}
function migrate(doc) {
  // Ponto único para evoluções de schema. Hoje só garante os campos.
  const d = typeof doc === 'string' ? JSON.parse(doc) : doc;
  const blocks = Array.isArray(d.blocks) ? d.blocks : [];
  const walk = list => list.forEach(b => {
    if (!b || !b.props) {
      if (b && b.children) walk(b.children);
      return;
    }
    // 7 — Callout não expõe mais `color` (competia com `tone`).
    if (b.type === 'callout') delete b.props.color;
    // 9 — MediaEmbed: tipo unificado em 'audio'.
    if (b.type === 'mediaembed' && b.props.type === 'podcast') b.props.type = 'audio';
    // 3 — Conclusion: children-string antigo vira `body`; children passa a ser array.
    if (b.type === 'conclusion' && typeof b.props.children === 'string') {
      b.props.body = b.props.body || b.props.children;
      b.props.children = [];
    }
    // 10 — Panel: conteúdo textual antigo vira `body`; children passa a ser array de blocos.
    if (b.type === 'panel' && typeof b.props.children === 'string') {
      b.props.body = b.props.body || b.props.children;
      b.props.children = [];
      if (!Array.isArray(b.children)) b.children = [];
    }
    if (b.children) walk(b.children);
  });
  walk(blocks);
  return {
    schema: SCHEMA_VERSION,
    meta: d.meta || {},
    blocks
  };
}

// Remove blocos/props desconhecidos e normaliza ids (defensivo p/ import).
function sanitize(doc) {
  const d = migrate(doc);
  let n = 0;
  const fix = (b, child) => {
    const def = __ds_scope.BlockRegistry.byType[b.type];
    if (!def) return null;
    const id = b.id || `${child ? 'c' : 'b'}${(n++).toString(36)}${Math.random().toString(36).slice(2, 6)}`;
    const out = {
      id,
      type: b.type,
      props: {
        ...(def.props || {}),
        ...(b.props || {})
      }
    };
    if (def.kind === 'container') out.children = (b.children || []).map(c => fix(c, true)).filter(Boolean);
    return out;
  };
  d.blocks = d.blocks.map(b => fix(b, false)).filter(Boolean);
  return d;
}

// —— Estatísticas rápidas (para a UI do builder) ——
function stats(doc) {
  const d = migrate(doc);
  let total = 0;
  const byType = {};
  const walk = list => list.forEach(b => {
    total++;
    byType[b.type] = (byType[b.type] || 0) + 1;
    if (b.children) walk(b.children);
  });
  walk(d.blocks);
  return {
    total,
    byType
  };
}

// —— Export standalone: um HTML offline com kit + dados embutidos ——
// Recebe os textos do kit (bundle JS + CSS do DS) para inlinar; o builder os
// fornece (lidos do projeto). `extraHead` p/ fontes; runtimes React obrigatórios.
function exportStandaloneHTML(doc, kit) {
  const k = kit || {};
  const d = migrate(doc);
  const title = d.meta && d.meta.title || 'Conteúdo';
  const lang = d.meta && d.meta.lang || 'pt-BR';
  const data = JSON.stringify(d).replace(/</g, '\\u003c');
  const css = k.stylesCss ? `<style>${k.stylesCss}</style>` : k.stylesHref ? `<link rel="stylesheet" href="${k.stylesHref}">` : '';
  const bundle = k.bundleJs ? `<script>${k.bundleJs}</script>` : k.bundleHref ? `<script src="${k.bundleHref}"></script>` : '';
  const react = k.reactJs ? `<script>${k.reactJs}</script>` : k.reactHref ? `<script src="${k.reactHref}"></script>` : '';
  const reactDom = k.reactDomJs ? `<script>${k.reactDomJs}</script>` : k.reactDomHref ? `<script src="${k.reactDomHref}"></script>` : '';
  return `<!DOCTYPE html>
<html lang="${lang}">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>${escapeHtml(title)}</title>
${react}
${reactDom}
${css}
${k.extraHead || ''}
</head>
<body>
<div id="root"></div>
<script id="spu-doc" type="application/json">${data}</script>
${bundle}
<script>
(function () {
  var doc = JSON.parse(document.getElementById('spu-doc').textContent);
  var NS = Object.keys(window).map(function (k){return window[k];}).find(function (o){return o && o.BlockDocument;}) ||
           window[Object.keys(window).filter(function(k){return /DesignSystem/.test(k);})[0]];
  if (!NS || !NS.BlockDocument) { document.getElementById('root').textContent = 'Kit do design system não carregado.'; return; }
  ReactDOM.createRoot(document.getElementById('root')).render(
    React.createElement(NS.BlockDocument, { doc: doc, mode: 'preview' }));
})();
</script>
</body>
</html>`;
}
function escapeHtml(s) {
  return String(s).replace(/[&<>"]/g, c => ({
    '&': '&amp;',
    '<': '&lt;',
    '>': '&gt;',
    '"': '&quot;'
  })[c]);
}

// Dispara download de um texto como arquivo (no contexto do builder).
function download(filename, text, mime) {
  if (typeof document === 'undefined') return;
  const blob = new Blob([text], {
    type: mime || 'text/plain;charset=utf-8'
  });
  const a = document.createElement('a');
  a.href = URL.createObjectURL(blob);
  a.download = filename;
  document.body.appendChild(a);
  a.click();
  setTimeout(() => {
    URL.revokeObjectURL(a.href);
    a.remove();
  }, 0);
}
const BuilderExport = {
  SCHEMA_VERSION,
  newDoc,
  serialize,
  migrate,
  sanitize,
  stats,
  exportStandaloneHTML,
  download,
  // Conveniências de alto nível
  saveProject(doc, filename) {
    download((filename || 'projeto') + '.spu.json', serialize(doc), 'application/json');
  },
  exportHTML(doc, kit, filename) {
    download((filename || 'conteudo') + '.html', exportStandaloneHTML(doc, kit), 'text/html');
  }
};
Object.assign(__ds_scope, { BuilderExport });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/BuilderExport.jsx", error: String((e && e.message) || e) }); }

// components/core/Icon.jsx
try { (() => {
// Curated Lucide (MIT) icon set, inlined so icons survive React re-renders
// without a CDN dependency. Substitution flagged in readme (no brand icon set
// was provided). Add icons by pasting Lucide inner SVG markup here.
const ICONS = {
  'info': '<circle cx="12" cy="12" r="10"/><path d="M12 16v-4"/><path d="M12 8h.01"/>',
  'alert-triangle': '<path d="m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3Z"/><path d="M12 9v4"/><path d="M12 17h.01"/>',
  'alert-circle': '<circle cx="12" cy="12" r="10"/><path d="M12 8v4"/><path d="M12 16h.01"/>',
  'lightbulb': '<path d="M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5"/><path d="M9 18h6"/><path d="M10 22h4"/>',
  'quote': '<path d="M16 3a2 2 0 0 0-2 2v6a2 2 0 0 0 2 2h2.5a.5.5 0 0 1 .5.5v.5a2 2 0 0 1-2 2 1 1 0 0 0 0 2 4 4 0 0 0 4-4V5a2 2 0 0 0-2-2z"/><path d="M5 3a2 2 0 0 0-2 2v6a2 2 0 0 0 2 2h2.5a.5.5 0 0 1 .5.5v.5a2 2 0 0 1-2 2 1 1 0 0 0 0 2 4 4 0 0 0 4-4V5a2 2 0 0 0-2-2z"/>',
  'map-pin': '<path d="M20 10c0 4.993-5.539 10.193-7.399 11.799a1 1 0 0 1-1.202 0C9.539 20.193 4 14.993 4 10a8 8 0 0 1 16 0"/><circle cx="12" cy="10" r="3"/>',
  'compass': '<circle cx="12" cy="12" r="10"/><polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76"/>',
  'layers': '<path d="M12.83 2.18a2 2 0 0 0-1.66 0L2.6 6.08a1 1 0 0 0 0 1.83l8.58 3.91a2 2 0 0 0 1.66 0l8.58-3.9a1 1 0 0 0 0-1.83Z"/><path d="m22 17.65-9.17 4.16a2 2 0 0 1-1.66 0L2 17.65"/><path d="m22 12.65-9.17 4.16a2 2 0 0 1-1.66 0L2 12.65"/>',
  'calendar': '<path d="M8 2v4"/><path d="M16 2v4"/><rect width="18" height="18" x="3" y="4" rx="2"/><path d="M3 10h18"/>',
  'chevron-down': '<path d="m6 9 6 6 6-6"/>',
  'chevron-right': '<path d="m9 18 6-6-6-6"/>',
  'chevron-left': '<path d="m15 18-6-6 6-6"/>',
  'arrow-right': '<path d="M5 12h14"/><path d="m12 5 7 7-7 7"/>',
  'arrow-left': '<path d="m12 19-7-7 7-7"/><path d="M19 12H5"/>',
  'plus': '<path d="M5 12h14"/><path d="M12 5v14"/>',
  'minus': '<path d="M5 12h14"/>',
  'x': '<path d="M18 6 6 18"/><path d="m6 6 12 12"/>',
  'check': '<path d="M20 6 9 17l-5-5"/>',
  'external-link': '<path d="M15 3h6v6"/><path d="M10 14 21 3"/><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/>',
  'play-circle': '<circle cx="12" cy="12" r="10"/><polygon points="10 8 16 12 10 16 10 8"/>',
  'pause': '<rect x="6" y="4" width="4" height="16" rx="1"/><rect x="14" y="4" width="4" height="16" rx="1"/>',
  'headphones': '<path d="M3 14h3a2 2 0 0 1 2 2v3a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2zm0 0a9 9 0 0 1 18 0m0 0v5a2 2 0 0 1-2 2h-1a2 2 0 0 1-2-2v-3a2 2 0 0 1 2-2z"/>',
  'book-open': '<path d="M12 7v14"/><path d="M3 18a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1h5a4 4 0 0 1 4 4 4 4 0 0 1 4-4h5a1 1 0 0 1 1 1v13a1 1 0 0 1-1 1h-6a3 3 0 0 0-3 3 3 3 0 0 0-3-3z"/>',
  'link': '<path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/>',
  'search': '<circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/>',
  'maximize': '<path d="M8 3H5a2 2 0 0 0-2 2v3"/><path d="M21 8V5a2 2 0 0 0-2-2h-3"/><path d="M3 16v3a2 2 0 0 0 2 2h3"/><path d="M16 21h3a2 2 0 0 0 2-2v-3"/>',
  'newspaper': '<path d="M15 18h-5"/><path d="M18 14h-8"/><path d="M15 22H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h12a2 2 0 0 1 2 2v6"/><rect width="4" height="4" x="10" y="6" rx="1"/><path d="M22 10v9a2 2 0 0 1-2 2"/>',
  'file-text': '<path d="M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z"/><path d="M14 2v5h6"/><path d="M16 13H8"/><path d="M16 17H8"/><path d="M10 9H8"/>',
  'building': '<rect width="16" height="20" x="4" y="2" rx="2"/><path d="M9 22v-4h6v4"/><path d="M8 6h.01"/><path d="M16 6h.01"/><path d="M12 6h.01"/><path d="M12 10h.01"/><path d="M12 14h.01"/><path d="M16 10h.01"/><path d="M16 14h.01"/><path d="M8 10h.01"/><path d="M8 14h.01"/>',
  'target': '<circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="6"/><circle cx="12" cy="12" r="2"/>',
  'sparkles': '<path d="M9.937 15.5A2 2 0 0 0 8.5 14.063l-6.135-1.582a.5.5 0 0 1 0-.962L8.5 9.936A2 2 0 0 0 9.937 8.5l1.582-6.135a.5.5 0 0 1 .963 0L14.063 8.5A2 2 0 0 0 15.5 9.937l6.135 1.581a.5.5 0 0 1 0 .964L15.5 14.063a2 2 0 0 0-1.437 1.437l-1.582 6.135a.5.5 0 0 1-.963 0z"/>',
  'repeat': '<path d="m17 2 4 4-4 4"/><path d="M3 11v-1a4 4 0 0 1 4-4h14"/><path d="m7 22-4-4 4-4"/><path d="M21 13v1a4 4 0 0 1-4 4H3"/>',
  'rotate-cw': '<path d="M21 12a9 9 0 1 1-2.64-6.36"/><path d="M21 3v6h-6"/>',
  'scale': '<path d="m16 16 3-8 3 8c-.87.65-1.92 1-3 1s-2.13-.35-3-1Z"/><path d="m2 16 3-8 3 8c-.87.65-1.92 1-3 1s-2.13-.35-3-1Z"/><path d="M7 21h10"/><path d="M12 3v18"/><path d="M3 7h2c2 0 5-1 7-2 2 1 5 2 7 2h2"/>',
  'image': '<rect width="18" height="18" x="3" y="3" rx="2" ry="2"/><circle cx="9" cy="9" r="2"/><path d="m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21"/>',
  'layout': '<rect width="18" height="18" x="3" y="3" rx="2"/><path d="M3 9h18"/><path d="M9 21V9"/>',
  'heading': '<path d="M6 12h12"/><path d="M6 20V4"/><path d="M18 20V4"/>',
  'type': '<polyline points="4 7 4 4 20 4 20 7"/><line x1="9" x2="15" y1="20" y2="20"/><line x1="12" x2="12" y1="4" y2="20"/>',
  'grid': '<rect width="7" height="7" x="3" y="3" rx="1"/><rect width="7" height="7" x="14" y="3" rx="1"/><rect width="7" height="7" x="14" y="14" rx="1"/><rect width="7" height="7" x="3" y="14" rx="1"/>',
  'list': '<line x1="8" x2="21" y1="6" y2="6"/><line x1="8" x2="21" y1="12" y2="12"/><line x1="8" x2="21" y1="18" y2="18"/><line x1="3" x2="3.01" y1="6" y2="6"/><line x1="3" x2="3.01" y1="12" y2="12"/><line x1="3" x2="3.01" y1="18" y2="18"/>',
  'square': '<rect width="18" height="18" x="3" y="3" rx="2"/>',
  'book-marked': '<path d="M10 2v8l3-3 3 3V2"/><path d="M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20"/>',
  'check-circle': '<path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><path d="m9 11 3 3L22 4"/>',
  'help-circle': '<circle cx="12" cy="12" r="10"/><path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/><path d="M12 17h.01"/>',
  'flower': '<circle cx="12" cy="12" r="3"/><path d="M12 16.5A4.5 4.5 0 1 1 7.5 12 4.5 4.5 0 1 1 12 7.5a4.5 4.5 0 1 1 4.5 4.5 4.5 4.5 0 1 1-4.5 4.5"/><path d="M12 7.5V9"/><path d="M7.5 12H9"/><path d="M16.5 12H15"/><path d="M12 16.5V15"/>',
  'users': '<path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/>',
  'user': '<path d="M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/>',
  'globe': '<circle cx="12" cy="12" r="10"/><path d="M12 2a14.5 14.5 0 0 0 0 20 14.5 14.5 0 0 0 0-20"/><path d="M2 12h20"/>',
  'file': '<path d="M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z"/><path d="M14 2v5h6"/>',
  'video': '<path d="m22 8-6 4 6 4V8Z"/><rect width="14" height="12" x="2" y="6" rx="2" ry="2"/>',
  'mic': '<path d="M12 2a3 3 0 0 0-3 3v7a3 3 0 0 0 6 0V5a3 3 0 0 0-3-3Z"/><path d="M19 10v2a7 7 0 0 1-14 0v-2"/><line x1="12" x2="12" y1="19" y2="22"/>',
  'award': '<path d="m15.477 12.89 1.515 8.526a.5.5 0 0 1-.81.47l-3.58-2.687a1 1 0 0 0-1.197 0l-3.586 2.686a.5.5 0 0 1-.81-.469l1.514-8.526"/><circle cx="12" cy="8" r="6"/>',
  'flag': '<path d="M4 15s1-1 4-1 5 2 8 2 4-1 4-1V3s-1 1-4 1-5-2-8-2-4 1-4 1z"/><line x1="4" x2="4" y1="22" y2="15"/>',
  'star': '<polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/>',
  'bookmark': '<path d="m19 21-7-4-7 4V5a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2v16z"/>',
  'clock': '<circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/>',
  'pencil': '<path d="M21.174 6.812a1 1 0 0 0-3.986-3.987L3.842 16.174a2 2 0 0 0-.5.83l-1.321 4.352a.5.5 0 0 0 .623.622l4.353-1.32a2 2 0 0 0 .83-.497z"/><path d="m15 5 4 4"/>',
  'trash': '<path d="M3 6h18"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/>',
  'download': '<path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" x2="12" y1="15" y2="3"/>',
  'upload': '<path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" x2="12" y1="3" y2="15"/>',
  'eye': '<path d="M2.062 12.348a1 1 0 0 1 0-.696 10.75 10.75 0 0 1 19.876 0 1 1 0 0 1 0 .696 10.75 10.75 0 0 1-19.876 0"/><circle cx="12" cy="12" r="3"/>',
  'heart': '<path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.3 1.5 4.05 3 5.5l7 7Z"/>',
  'mail': '<rect width="20" height="16" x="2" y="4" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/>',
  'message-circle': '<path d="M7.9 20A9 9 0 1 0 4 16.1L2 22Z"/>',
  'settings': '<path d="M12.22 2h-.44a2 2 0 0 0-2 2v.18a2 2 0 0 1-1 1.73l-.43.25a2 2 0 0 1-2 0l-.15-.08a2 2 0 0 0-2.73.73l-.22.38a2 2 0 0 0 .73 2.73l.15.1a2 2 0 0 1 1 1.72v.51a2 2 0 0 1-1 1.74l-.15.09a2 2 0 0 0-.73 2.73l.22.38a2 2 0 0 0 2.73.73l.15-.08a2 2 0 0 1 2 0l.43.25a2 2 0 0 1 1 1.73V20a2 2 0 0 0 2 2h.44a2 2 0 0 0 2-2v-.18a2 2 0 0 1 1-1.73l.43-.25a2 2 0 0 1 2 0l.15.08a2 2 0 0 0 2.73-.73l.22-.39a2 2 0 0 0-.73-2.73l-.15-.08a2 2 0 0 1-1-1.74v-.5a2 2 0 0 1 1-1.74l.15-.09a2 2 0 0 0 .73-2.73l-.22-.38a2 2 0 0 0-2.73-.73l-.15.08a2 2 0 0 1-2 0l-.43-.25a2 2 0 0 1-1-1.73V4a2 2 0 0 0-2-2Z"/><circle cx="12" cy="12" r="3"/>',
  'home': '<path d="M15 21v-8a1 1 0 0 0-1-1h-4a1 1 0 0 0-1 1v8"/><path d="M3 10a2 2 0 0 1 .709-1.528l7-5.999a2 2 0 0 1 2.582 0l7 5.999A2 2 0 0 1 21 10v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>',
  'folder': '<path d="M20 20a2 2 0 0 0 2-2V8a2 2 0 0 0-2-2h-7.9a2 2 0 0 1-1.69-.9L9.6 3.9A2 2 0 0 0 7.93 3H4a2 2 0 0 0-2 2v13a2 2 0 0 0 2 2Z"/>',
  'graduation-cap': '<path d="M21.42 10.922a1 1 0 0 0-.019-1.838L12.83 5.18a2 2 0 0 0-1.66 0L2.6 9.08a1 1 0 0 0 0 1.832l8.57 3.908a2 2 0 0 0 1.66 0z"/><path d="M22 10v6"/><path d="M6 12.5V16a6 3 0 0 0 12 0v-3.5"/>',
  'briefcase': '<path d="M16 20V4a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v16"/><rect width="20" height="14" x="2" y="6" rx="2"/>',
  'shield': '<path d="M20 13c0 5-3.5 7.5-7.66 8.95a1 1 0 0 1-.67-.01C7.5 20.5 4 18 4 13V6a1 1 0 0 1 1-1c2 0 4.5-1.2 6.24-2.72a1.17 1.17 0 0 1 1.52 0C14.51 3.81 17 5 19 5a1 1 0 0 1 1 1z"/>',
  'zap': '<path d="M4 14a1 1 0 0 1-.78-1.63l9.9-10.2a.5.5 0 0 1 .86.46l-1.92 6.02A1 1 0 0 0 13 10h7a1 1 0 0 1 .78 1.63l-9.9 10.2a.5.5 0 0 1-.86-.46l1.92-6.02A1 1 0 0 0 11 14z"/>',
  'trending-up': '<path d="M16 7h6v6"/><path d="m22 7-8.5 8.5-5-5L2 17"/>',
  'bar-chart': '<line x1="12" x2="12" y1="20" y2="10"/><line x1="18" x2="18" y1="20" y2="4"/><line x1="6" x2="6" y1="20" y2="16"/>',
  'pie-chart': '<path d="M21.21 15.89A10 10 0 1 1 8 2.83"/><path d="M22 12A10 10 0 0 0 12 2v10z"/>',
  'database': '<ellipse cx="12" cy="5" rx="9" ry="3"/><path d="M3 5V19A9 3 0 0 0 21 19V5"/><path d="M3 12A9 3 0 0 0 21 12"/>',
  'key': '<path d="m15.5 7.5 2.3 2.3a1 1 0 0 0 1.4 0l2.1-2.1a1 1 0 0 0 0-1.4L19 4"/><path d="m21 2-9.6 9.6"/><circle cx="7.5" cy="15.5" r="5.5"/>',
  'phone': '<path d="M13.832 16.568a1 1 0 0 0 1.213-.303l.355-.465A2 2 0 0 1 17 15h3a2 2 0 0 1 2 2v3a2 2 0 0 1-2 2A18 18 0 0 1 2 4a2 2 0 0 1 2-2h3a2 2 0 0 1 2 2v3a2 2 0 0 1-.8 1.6l-.468.351a1 1 0 0 0-.292 1.233 14 14 0 0 0 6.392 6.384"/>',
  'thumbs-up': '<path d="M7 10v12"/><path d="M15 5.88 14 10h5.83a2 2 0 0 1 1.92 2.56l-2.33 8A2 2 0 0 1 17.5 22H4a2 2 0 0 1-2-2v-8a2 2 0 0 1 2-2h2.76a2 2 0 0 0 1.79-1.11L12 2a3.13 3.13 0 0 1 3 3.88Z"/>',
  'gavel': '<path d="m14.5 12.5-8 8a2.119 2.119 0 1 1-3-3l8-8"/><path d="m16 16 6-6"/><path d="m8 8 6-6"/><path d="m9 7 8 8"/><path d="m21 11-8-8"/>',
  'landmark': '<line x1="3" x2="21" y1="22" y2="22"/><line x1="6" x2="6" y1="18" y2="11"/><line x1="10" x2="10" y1="18" y2="11"/><line x1="14" x2="14" y1="18" y2="11"/><line x1="18" x2="18" y1="18" y2="11"/><polygon points="12 2 20 7 4 7"/>',
  'map': '<path d="M14.106 5.553a2 2 0 0 0 1.788 0l3.659-1.83A1 1 0 0 1 21 4.619v12.764a1 1 0 0 1-.553.894l-4.553 2.277a2 2 0 0 1-1.788 0l-4.212-2.106a2 2 0 0 0-1.788 0l-3.659 1.83A1 1 0 0 1 3 19.381V6.618a1 1 0 0 1 .553-.894l4.553-2.277a2 2 0 0 1 1.788 0z"/><path d="M15 5.764v15"/><path d="M9 3.236v15"/>'
};
function Icon({
  name,
  size = 20,
  strokeWidth = 1.9,
  className,
  label,
  style
}) {
  const inner = ICONS[name];
  const px = typeof size === 'number' ? `${size}px` : size;
  return React.createElement('svg', {
    className,
    width: px,
    height: px,
    viewBox: '0 0 24 24',
    fill: 'none',
    stroke: 'currentColor',
    strokeWidth,
    strokeLinecap: 'round',
    strokeLinejoin: 'round',
    role: label ? 'img' : 'presentation',
    'aria-label': label || undefined,
    'aria-hidden': label ? undefined : 'true',
    style: {
      flex: '0 0 auto',
      display: 'inline-block',
      verticalAlign: 'middle',
      ...style
    },
    dangerouslySetInnerHTML: {
      __html: inner || ICONS.info
    }
  });
}

// Lista de nomes de ícones disponíveis — para o builder montar o seletor.
const ICON_NAMES = Object.keys(ICONS);
Object.assign(__ds_scope, { ICONS, Icon, ICON_NAMES });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Icon.jsx", error: String((e && e.message) || e) }); }

// components/core/dsutil.js
try { (() => {
// Shared helpers for SPU ENAP design-system components.
// Injects each component's CSS once into <head>, keyed by id.
function injectCss(id, css) {
  if (typeof document === 'undefined') return;
  if (document.getElementById(id)) return;
  const el = document.createElement('style');
  el.id = id;
  el.textContent = css;
  document.head.appendChild(el);
}
function cx(...parts) {
  return parts.filter(Boolean).join(' ');
}

// True when rendering the print/PDF copy (window.__SPU_PRINT set before mount).
function isPrint() {
  return typeof window !== 'undefined' && window.__SPU_PRINT === true;
}

// Resolve a status/tone name to its CSS variables.
const TONES = {
  info: {
    color: 'var(--status-info)',
    soft: 'var(--status-info-soft)',
    icon: 'info'
  },
  attention: {
    color: 'var(--status-attention)',
    soft: 'var(--status-attention-soft)',
    icon: 'lightbulb'
  },
  warning: {
    color: 'var(--status-warning)',
    soft: 'var(--status-warning-soft)',
    icon: 'alert-triangle'
  },
  success: {
    color: 'var(--status-success)',
    soft: 'var(--status-success-soft)',
    icon: 'check'
  },
  note: {
    color: 'var(--status-note)',
    soft: 'var(--status-note-soft)',
    icon: 'book-open'
  },
  neutral: {
    color: 'var(--ink-700)',
    soft: 'var(--sand-100)',
    icon: 'info'
  }
};
Object.assign(__ds_scope, { injectCss, cx, isPrint, TONES });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/dsutil.js", error: String((e && e.message) || e) }); }

// components/content/DataTable.jsx
try { (() => {
__ds_scope.injectCss('spu-table-css', `
.spu-table-wrap{overflow-x:auto;border:1px solid var(--color-border);border-radius:var(--radius-md);background:var(--color-surface)}
.spu-table{width:100%;border-collapse:collapse;font-family:var(--font-body);font-size:var(--fs-small)}
.spu-table caption{caption-side:top;text-align:left;font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:var(--ls-eyebrow);color:var(--text-muted);padding:var(--space-4) var(--space-4) var(--space-2)}
.spu-table th{font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:.05em;font-weight:600;text-align:left;color:var(--text-muted);background:var(--color-surface-warm);padding:var(--space-3) var(--space-4);border-bottom:1px solid var(--color-border)}
.spu-table td{padding:var(--space-3) var(--space-4);border-top:1px solid var(--color-border-light);color:var(--text-body);vertical-align:top}
.spu-table tbody tr:hover{background:var(--color-primary-soft)}
.spu-table--dense td,.spu-table--dense th{padding:var(--space-2) var(--space-3)}
.spu-table__firstcol td:first-child{font-weight:700;color:var(--text-strong)}
.spu-table .ta-right{text-align:right}
.spu-table .ta-center{text-align:center}
.spu-table-wrap--striped .spu-table tbody tr:nth-child(even){background:color-mix(in srgb, var(--color-surface-warm) 52%, transparent)}
.spu-table-wrap--striped .spu-table tbody tr:hover{background:var(--color-primary-soft)}
.spu-table-wrap--tone-petrol .spu-table th{background:var(--petrol-700);color:#fff}
.spu-table-wrap--tone-terra .spu-table th{background:var(--terra-700);color:#fff}
.spu-table-wrap--tone-ochre .spu-table th{background:var(--ochre-700);color:#fff}
.spu-table .spu-richtext{line-height:inherit}
`);
function DataTable({
  columns = [],
  rows = [],
  caption,
  dense = false,
  highlightFirst = false,
  striped = false,
  tone = 'default',
  className,
  style
}) {
  const alignCls = a => a === 'right' ? 'ta-right' : a === 'center' ? 'ta-center' : '';
  return React.createElement('div', {
    className: __ds_scope.cx('spu-table-wrap', striped && 'spu-table-wrap--striped', tone !== 'default' && `spu-table-wrap--tone-${tone}`, className),
    style
  }, React.createElement('table', {
    className: __ds_scope.cx('spu-table', dense && 'spu-table--dense', highlightFirst && 'spu-table__firstcol')
  },
  caption && React.createElement('caption', null, __ds_scope.renderRich(caption, { inline: true })),
  React.createElement('thead', null,
    React.createElement('tr', null, columns.map((c, i) => React.createElement('th', {
      key: i,
      className: alignCls(c.align)
    }, __ds_scope.renderRich(c.label, { inline: true }))))
  ),
  React.createElement('tbody', null, rows.map((r, ri) => React.createElement('tr', {
    key: ri
  }, columns.map((c, ci) => React.createElement('td', {
    key: ci,
    className: alignCls(c.align)
  }, __ds_scope.renderRich(r[c.key], { inline: true })))))
  )));
}
Object.assign(__ds_scope, { DataTable });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/DataTable.jsx", error: String((e && e.message) || e) }); }

// components/content/Editable.jsx
try { (() => {
// ── Marcas aprovadas (mesmo vocabulário HTML que o RichText renderiza) ──
// Round-trip garantido: o que o Editable grava, o RichText mostra — e o
// conteúdo importado/marcado por IA usa exatamente estas tags.
const SPU_MARKS = {
  highlights: [{
    id: 'ochre',
    label: 'Ocre',
    swatch: 'var(--ochre-100)'
  }, {
    id: 'terra',
    label: 'Terracota',
    swatch: 'var(--terra-100)'
  }, {
    id: 'petrol',
    label: 'Petróleo',
    swatch: 'var(--petrol-100)'
  }, {
    id: 'green',
    label: 'Verde',
    swatch: 'var(--green-100)'
  }, {
    id: 'sand',
    label: 'Areia',
    swatch: 'var(--sand-100)'
  }],
  colors: [{
    id: 'petrol',
    label: 'Petróleo',
    swatch: 'var(--petrol-700)'
  }, {
    id: 'terra',
    label: 'Terracota',
    swatch: 'var(--terra-700)'
  }, {
    id: 'ochre',
    label: 'Ocre',
    swatch: 'var(--ochre-700)'
  }, {
    id: 'green',
    label: 'Verde',
    swatch: 'var(--green-700)'
  }, {
    id: 'muted',
    label: 'Suave',
    swatch: 'var(--text-muted)'
  }, {
    id: 'light',
    label: 'Claro',
    swatch: 'var(--text-on-dark)'
  }, {
    id: 'ochre-light',
    label: 'Ocre claro',
    swatch: 'var(--ochre-300)'
  }, {
    id: 'terra-light',
    label: 'Terracota claro',
    swatch: 'var(--terra-300)'
  }, {
    id: 'petrol-light',
    label: 'Petróleo claro',
    swatch: 'var(--petrol-300)'
  }, {
    id: 'green-light',
    label: 'Verde claro',
    swatch: 'var(--green-400)'
  }],
  fonts: [
    { id: 'display', label: 'Display' },
    { id: 'body', label: 'Corpo' },
    { id: 'serif', label: 'Serifada' },
    { id: 'mono', label: 'Monoespaçada' }
  ],
  sizes: [
    { id: 'display', label: 'Display' },
    { id: 'h2', label: 'H2' },
    { id: 'h3', label: 'H3' },
    { id: 'h4', label: 'H4' },
    { id: 'h5', label: 'H5' },
    { id: 'h6', label: 'H6' },
    { id: 'body-lg', label: 'Corpo grande' },
    { id: 'body', label: 'Corpo' },
    { id: 'small', label: 'Pequeno' },
    { id: 'caption', label: 'Legenda' },
    { id: 'eyebrow', label: 'Eyebrow' }
  ]
};
__ds_scope.injectCss('spu-editable-css', `
[data-spu-editable]{outline:none;cursor:text;border-radius:var(--radius-sm)}
[data-spu-editable]:focus-visible{box-shadow:var(--focus-ring)}
[data-spu-editable][data-empty]::before{content:attr(data-placeholder);color:var(--text-faint);pointer-events:none}
.spu-marktoolbar{position:fixed;z-index:9999;display:flex;align-items:center;gap:2px;background:var(--ink-900);color:#fff;padding:5px 6px;border-radius:10px;box-shadow:0 8px 24px rgba(0,0,0,.28);transform:translateX(-50%)}
.spu-marktoolbar button{display:grid;place-items:center;min-width:26px;height:26px;padding:0 5px;background:transparent;border:0;border-radius:7px;color:#fff;cursor:pointer;font-size:14px;line-height:1}
.spu-marktoolbar button:hover{background:rgba(255,255,255,.16)}
.spu-marktoolbar__sep{width:1px;height:18px;background:rgba(255,255,255,.22);margin:0 3px}
.spu-marktoolbar__sw{width:16px;height:12px;border-radius:3px;display:block}
.spu-marktoolbar button.is-open{background:rgba(255,255,255,.2)}
.spu-mtcaret{font-size:9px;opacity:.7;margin-left:1px}
.spu-mtpop{position:fixed;z-index:10000;background:var(--ink-900);padding:8px;border-radius:10px;box-shadow:0 10px 28px rgba(0,0,0,.32);transform:translateX(-50%)}
.spu-mtpop__grid{display:grid;grid-template-columns:repeat(5,auto);gap:4px}
.spu-mtpop__sw{display:grid;place-items:center;width:30px;height:26px;border:0;border-radius:7px;background:transparent;cursor:pointer;padding:0}
.spu-mtpop__sw:hover{background:rgba(255,255,255,.16)}
.spu-mtpop__sw span{width:18px;height:14px;border-radius:3px;display:block}
.spu-mtpop__row{display:flex;align-items:center;gap:6px}
.spu-mtpop__row input[type=text]{width:190px;border:0;border-radius:7px;padding:7px 9px;font:inherit;font-size:13px;background:rgba(255,255,255,.12);color:#fff;outline:none}
.spu-mtpop__row input[type=text]::placeholder{color:rgba(255,255,255,.5)}
.spu-mtpop__btns{display:flex;gap:6px;margin-top:8px}
.spu-mtpop__btns button{flex:1;border:0;border-radius:7px;padding:7px 10px;font:inherit;font-size:12px;font-weight:600;cursor:pointer;background:rgba(255,255,255,.14);color:#fff}
.spu-mtpop__btns button.is-primary{background:var(--color-accent,#c2613a);color:#fff}
.spu-mtpop__btns button:hover{filter:brightness(1.12)}
.spu-mtpop__list{display:flex;flex-direction:column;min-width:170px;max-height:min(330px,60vh);overflow:auto}
.spu-mtpop__list button{border:0;background:transparent;color:#fff;text-align:left;padding:7px 9px;border-radius:7px;cursor:pointer;font:inherit;font-size:12px}
.spu-mtpop__list button:hover{background:rgba(255,255,255,.16)}
`);

// ── helpers de seleção ──
function closestEditable(node) {
  let n = node;
  while (n && n.nodeType === 3) n = n.parentNode;
  return n ? n.closest('[data-spu-editable]') : null;
}
function fireInput(host) {
  host && host.dispatchEvent(new Event('input', {
    bubbles: true
  }));
}
function unwrap(node) {
  const p = node.parentNode;
  if (!p) return;
  while (node.firstChild) p.insertBefore(node.firstChild, node);
  p.removeChild(node);
}

// Envolve (ou desfaz) a seleção num elemento `tag` que casa com `selector`.
function wrapSelection(tag, attrs, selector) {
  const sel = window.getSelection();
  if (!sel || sel.rangeCount === 0 || sel.isCollapsed) return;
  const range = sel.getRangeAt(0);
  const host = closestEditable(range.commonAncestorContainer);
  if (!host) return;
  const startEl = range.startContainer.nodeType === 1 ? range.startContainer : range.startContainer.parentElement;
  const active = startEl && startEl.closest(selector);
  if (active && host.contains(active)) {
    unwrap(active);
    sel.removeAllRanges();
    fireInput(host);
    return;
  }
  const el = document.createElement(tag);
  Object.entries(attrs || {}).forEach(([k, v]) => el.setAttribute(k, v));
  try {
    el.appendChild(range.extractContents());
    el.querySelectorAll(selector).forEach(n => {
      if (n !== el) unwrap(n);
    });
    range.insertNode(el);
    sel.removeAllRanges();
    const after = document.createRange();
    after.setStartAfter(el);
    after.collapse(true);
    sel.addRange(after);
  } catch (e) {/* seleção cruzando nós — ignora */}
  fireInput(host);
}

// Input flutuante para substituir window.prompt (funciona em sandbox/export/iframe,
// onde modais nativos são ignorados). Resolve com a ação escolhida ou null.
function inlinePrompt(message, def, savedRange, canRemove = false) {
  return new Promise(resolve => {
    const sel = window.getSelection();
    const r = savedRange ? savedRange.getBoundingClientRect() : sel && sel.rangeCount ? sel.getRangeAt(0).getBoundingClientRect() : {
      left: window.innerWidth / 2,
      bottom: 80
    };
    const box = document.createElement('div');
    box.className = 'spu-mtpop';
    const promptWidth = Math.min(canRemove ? 460 : 330, window.innerWidth - 16);
    box.style.position = 'fixed';
    box.style.left = Math.min(Math.max(8, r.left), window.innerWidth - promptWidth - 8) + 'px';
    box.style.top = Math.min(r.bottom + 8, window.innerHeight - 64) + 'px';
    box.style.transform = 'none';
    box.style.display = 'flex';
    box.style.flexWrap = 'wrap';
    box.style.maxWidth = 'calc(100vw - 16px)';
    box.style.gap = '6px';
    const input = document.createElement('input');
    input.type = 'text';
    input.placeholder = message;
    input.value = def || '';
    input.style.cssText = 'border:0;border-radius:7px;padding:7px 9px;font:inherit;font-size:13px;background:rgba(255,255,255,.12);color:#fff;outline:none;width:240px';
    const ok = document.createElement('button');
    ok.textContent = 'Salvar';
    ok.style.cssText = 'border:0;border-radius:7px;background:var(--color-accent,#c2613a);color:#fff;padding:7px 12px;cursor:pointer;font:inherit;font-size:12px;font-weight:600';
    box.append(input, ok);
    let remove;
    if (canRemove) {
      remove = document.createElement('button');
      remove.textContent = 'Remover termo';
      remove.style.cssText = 'border:1px solid rgba(255,255,255,.28);border-radius:7px;background:transparent;color:#fff;padding:7px 12px;cursor:pointer;font:inherit;font-size:12px;font-weight:600';
      box.appendChild(remove);
    }
    document.body.appendChild(box);
    input.focus();
    input.select();
    const done = v => {
      box.remove();
      document.removeEventListener('pointerdown', outside, true);
      resolve(v);
    };
    const outside = e => {
      if (!box.contains(e.target)) done(null);
    };
    ok.onclick = () => done({
      action: 'save',
      value: input.value
    });
    if (remove) remove.onclick = () => done({
      action: 'remove'
    });
    input.onkeydown = e => {
      if (e.key === 'Enter') {
        e.preventDefault();
        done({
          action: 'save',
          value: input.value
        });
      }
      if (e.key === 'Escape') {
        e.preventDefault();
        done(null);
      }
    };
    setTimeout(() => document.addEventListener('pointerdown', outside, true), 0);
  });
}
function applyHighlight(id) {
  wrapSelection('mark', id === 'ochre' ? {} : {
    'data-hl': id
  }, id === 'ochre' ? 'mark:not([data-hl])' : `mark[data-hl="${id}"]`);
}
function applyColor(id) {
  wrapSelection('span', {
    'data-color': id
  }, `span[data-color="${id}"]`);
}
function applyFont(id) {
  wrapSelection('span', {
    'data-font': id
  }, `span[data-font="${id}"]`);
}
function applyFontSize(id) {
  wrapSelection('span', {
    'data-fs': id
  }, `span[data-fs="${id}"]`);
}
function applyEmphasis(cmd) {
  document.execCommand(cmd, false, null);
  const sel = window.getSelection();
  fireInput(sel ? closestEditable(sel.anchorNode) : null);
}
function applyLink(url, asButton) {
  if (!url) return;
  const attrs = {
    href: url
  };
  if (asButton) attrs['data-btn'] = asButton === true ? 'primary' : asButton; // primary|secondary|ghost
  wrapSelection('a', attrs, 'a[href]');
}
function applyTerm() {
  const sel = window.getSelection();
  if (!sel || sel.rangeCount === 0 || sel.isCollapsed) return;
  const saved = sel.getRangeAt(0).cloneRange();
  const host = closestEditable(saved.commonAncestorContainer);
  if (!host) return;
  const startEl = saved.startContainer.nodeType === 1 ? saved.startContainer : saved.startContainer.parentElement;
  const endEl = saved.endContainer.nodeType === 1 ? saved.endContainer : saved.endContainer.parentElement;
  const startTerm = startEl && startEl.closest('span[data-term]');
  const endTerm = endEl && endEl.closest('span[data-term]');
  const active = startTerm && startTerm === endTerm && host.contains(startTerm) ? startTerm : null;
  const word = (active ? active.getAttribute('data-term') || active.textContent : sel.toString()).trim();
  if (!word) return;
  const currentDefinition = active ? active.getAttribute('title') || active.getAttribute('data-definition') || active.getAttribute('data-def') || '' : '';
  // Input flutuante (sem window.prompt, que é ignorado em preview/iframe sandbox).
  inlinePrompt('Definição do termo “' + word + '”', currentDefinition, saved, !!active).then(result => {
    if (!result) return;
    if (result.action === 'remove') {
      if (active && active.isConnected) {
        unwrap(active);
        fireInput(host);
      }
      return;
    }
    const def = (result.value || '').trim();
    if (!def) return;
    if (active && active.isConnected) {
      active.setAttribute('data-term', word);
      active.setAttribute('data-definition', def);
      active.removeAttribute('title');
      active.removeAttribute('data-def');
      fireInput(host);
      return;
    }
    const s = window.getSelection();
    s.removeAllRanges();
    s.addRange(saved);
    wrapSelection('span', {
      'data-term': word,
      'data-definition': def
    }, 'span[data-term]');
  });
}
function clearMarks() {
  const sel = window.getSelection();
  if (!sel || sel.rangeCount === 0 || sel.isCollapsed) return;
  const range = sel.getRangeAt(0);
  const host = closestEditable(range.commonAncestorContainer);
  if (!host) return;
  const tmp = document.createElement('div');
  tmp.appendChild(range.cloneContents());
  tmp.querySelectorAll('span:not([data-term]), mark, [data-color], [data-font], [data-fs], [data-term], b, strong, i, em, a').forEach(unwrap);
  range.deleteContents();
  range.insertNode(document.createRange().createContextualFragment(tmp.innerHTML));
  sel.removeAllRanges();
  fireInput(host);
}

// ── Região editável (contentEditable; cola texto puro) ──
function Editable({
  html,
  onChange,
  single = false,
  placeholder,
  as = 'div',
  className,
  style
}) {
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (ref.current && ref.current.innerHTML !== (html || '')) ref.current.innerHTML = html || '';
  }, [html]);
  const sync = e => {
    const el = e.currentTarget;
    if (!el.textContent.trim() && !el.querySelector('img,mark')) el.setAttribute('data-empty', '1');else el.removeAttribute('data-empty');
    onChange && onChange(el.innerHTML);
  };
  return React.createElement(as, {
    ref,
    className: __ds_scope.cx('spu-richtext', className),
    style,
    contentEditable: true,
    suppressContentEditableWarning: true,
    spellCheck: true,
    'data-spu-editable': '1',
    'data-placeholder': placeholder || '',
    'data-empty': !html ? '1' : undefined,
    onInput: sync,
    onPaste: e => {
      e.preventDefault();
      const t = (e.clipboardData || window.clipboardData).getData('text/plain');
      document.execCommand('insertText', false, t);
    },
    onKeyDown: e => {
      if (single && e.key === 'Enter' && e.shiftKey) {
        e.preventDefault();
        document.execCommand('insertLineBreak', false, null);
        ref.current && fireInput(ref.current);
      } else if (single && e.key === 'Enter') {
        e.preventDefault();
        ref.current && ref.current.blur();
      } else if (!single && e.key === 'Enter' && !e.shiftKey) {
        e.preventDefault();
        document.execCommand('defaultParagraphSeparator', false, 'p');
        document.execCommand('insertParagraph', false, null);
        ref.current && fireInput(ref.current);
      }
    },
    onMouseDown: e => e.stopPropagation()
  });
}

// ── Barra de marcas flutuante (aparece ao selecionar dentro de [data-spu-canvas]) ──
// Destaques (marca-texto) e cores ficam em dois popovers, mantendo a barra
// estreita; B/i/listas/link/termo/limpar seguem inline.
function MarkToolbar({
  colors = true,
  links = true,
  glossary = true,
  lists = true,
  linkButtons = true
}) {
  const [box, setBox] = React.useState(null);
  const [menu, setMenu] = React.useState(null); // 'hl' | 'color' | 'font' | 'size' | 'link' | null
  const savedRange = React.useRef(null);
  const [linkUrl, setLinkUrl] = React.useState('https://');
  React.useEffect(() => {
    const update = () => {
      // Foco dentro da toolbar/popover (ex.: input do link) → mantém visível.
      const ae = document.activeElement;
      if (ae && ae.closest && ae.closest('.spu-marktoolbar, .spu-mtpop')) return;
      const sel = window.getSelection();
      if (!sel || sel.rangeCount === 0 || sel.isCollapsed) {
        setBox(null);
        setMenu(null);
        return;
      }
      const host = closestEditable(sel.getRangeAt(0).commonAncestorContainer);
      if (!host || !host.closest('[data-spu-canvas]')) {
        setBox(null);
        setMenu(null);
        return;
      }
      const r = sel.getRangeAt(0).getBoundingClientRect();
      if (!r || r.width === 0 && r.height === 0) {
        setBox(null);
        setMenu(null);
        return;
      }
      setBox({
        top: Math.max(8, r.top - 46),
        left: r.left + r.width / 2,
        allowLists: host.getAttribute('data-spu-single') !== '1'
      });
    };
    document.addEventListener('selectionchange', update);
    window.addEventListener('scroll', update, true);
    return () => {
      document.removeEventListener('selectionchange', update);
      window.removeEventListener('scroll', update, true);
    };
  }, []);
  if (!box) return null;

  // Preserva a seleção ao abrir um popover (o foco no input a perderia).
  const openMenu = which => {
    const sel = window.getSelection();
    if (sel && sel.rangeCount) savedRange.current = sel.getRangeAt(0).cloneRange();
    setMenu(m => m === which ? null : which);
  };
  const restore = () => {
    const sel = window.getSelection();
    if (savedRange.current && sel) {
      sel.removeAllRanges();
      sel.addRange(savedRange.current);
    }
  };
  const run = fn => {
    restore();
    fn();
    setMenu(null);
  };
  const sep = k => React.createElement('span', {
    key: k,
    className: 'spu-marktoolbar__sep'
  });
  const btn = (key, title, content, onClick, extra) => React.createElement('button', {
    key,
    title,
    onClick,
    ...(extra || {})
  }, content);
  const caret = React.createElement('span', {
    className: 'spu-mtcaret'
  }, '▾');
  const popHL = menu === 'hl' && React.createElement('div', {
    className: 'spu-mtpop',
    style: {
      top: box.top + 38,
      left: box.left
    },
    onMouseDown: e => e.preventDefault()
  }, React.createElement('div', {
    className: 'spu-mtpop__grid'
  }, SPU_MARKS.highlights.map(m => React.createElement('button', {
    key: m.id,
    className: 'spu-mtpop__sw',
    title: m.label,
    onClick: () => run(() => applyHighlight(m.id))
  }, React.createElement('span', {
    style: {
      background: m.swatch
    }
  })))));
  const popColor = menu === 'color' && React.createElement('div', {
    className: 'spu-mtpop',
    style: {
      top: box.top + 38,
      left: box.left
    },
    onMouseDown: e => e.preventDefault()
  }, React.createElement('div', {
    className: 'spu-mtpop__grid'
  }, SPU_MARKS.colors.map(m => React.createElement('button', {
    key: m.id,
    className: 'spu-mtpop__sw',
    title: m.label,
    onClick: () => run(() => applyColor(m.id))
  }, React.createElement('span', {
    style: {
      background: m.swatch,
      borderRadius: '50%',
      width: 16,
      height: 16
    }
  })))));
  const optionMenu = (which, items, apply) => menu === which && React.createElement('div', {
    className: 'spu-mtpop',
    style: {
      top: box.top + 38,
      left: box.left
    },
    onMouseDown: e => e.preventDefault()
  }, React.createElement('div', {
    className: 'spu-mtpop__list'
  }, items.map(item => React.createElement('button', {
    key: item.id,
    onClick: () => run(() => apply(item.id))
  }, item.label))));
  const popFont = optionMenu('font', SPU_MARKS.fonts, applyFont);
  const popSize = optionMenu('size', SPU_MARKS.sizes, applyFontSize);
  const popLink = menu === 'link' && React.createElement('div', {
    className: 'spu-mtpop',
    style: {
      top: box.top + 38,
      left: box.left
    },
    onMouseDown: e => e.stopPropagation()
  }, React.createElement('div', {
    className: 'spu-mtpop__row'
  }, React.createElement('input', {
    type: 'text',
    value: linkUrl,
    placeholder: 'https://…',
    autoFocus: true,
    onChange: e => setLinkUrl(e.target.value),
    onKeyDown: e => {
      if (e.key === 'Enter') run(() => applyLink(linkUrl.trim()));
    }
  })), React.createElement('div', {
    className: 'spu-mtpop__btns'
  }, React.createElement('button', {
    onClick: () => run(() => applyLink(linkUrl.trim()))
  }, 'Link'), linkButtons && React.createElement('button', {
    className: 'is-primary',
    title: 'Renderiza como botão do DS',
    onClick: () => run(() => applyLink(linkUrl.trim(), 'primary'))
  }, 'Botão'), linkButtons && React.createElement('button', {
    title: 'Botão secundário do DS',
    onClick: () => run(() => applyLink(linkUrl.trim(), 'secondary'))
  }, 'Botão 2ª')));
  const bar = React.createElement('div', {
    className: 'spu-marktoolbar',
    style: {
      top: box.top,
      left: box.left
    },
    onMouseDown: e => e.preventDefault()
  }, [btn('b', 'Negrito', 'B', () => applyEmphasis('bold'), {
    style: {
      fontWeight: 800
    }
  }), btn('i', 'Itálico', 'i', () => applyEmphasis('italic'), {
    style: {
      fontStyle: 'italic'
    }
  }), sep('s1'), btn('hl', 'Marca-texto', ['M', caret], () => openMenu('hl'), {
    className: menu === 'hl' ? 'is-open' : undefined,
    style: {
      fontWeight: 700
    }
  }), colors && btn('color', 'Cor do texto', ['A', caret], () => openMenu('color'), {
    className: menu === 'color' ? 'is-open' : undefined,
    style: {
      fontWeight: 700
    }
  }), btn('font', 'Família tipográfica', ['F', caret], () => openMenu('font'), {
    className: menu === 'font' ? 'is-open' : undefined
  }), btn('size', 'Tamanho do texto', ['Aa', caret], () => openMenu('size'), {
    className: menu === 'size' ? 'is-open' : undefined,
    style: {
      fontSize: 11
    }
  }), lists && box.allowLists && sep('s3'), lists && box.allowLists && btn('ul', 'Lista', '•', () => applyEmphasis('insertUnorderedList')), lists && box.allowLists && btn('ol', 'Lista numerada', '1.', () => applyEmphasis('insertOrderedList')), (links || glossary) && sep('s4'), links && btn('a', 'Link', ['🔗', caret], () => openMenu('link'), {
    className: menu === 'link' ? 'is-open' : undefined
  }), glossary && btn('t', 'Termo de glossário', 'termo', applyTerm, {
    style: {
      borderBottom: '1.5px dotted #fff',
      fontSize: 12
    }
  }), sep('s5'), btn('x', 'Limpar formatação da seleção', '✕', clearMarks)]);
  return React.createElement(React.Fragment, null, bar, popHL, popColor, popFont, popSize, popLink);
}
Object.assign(__ds_scope, { SPU_MARKS, applyHighlight, applyColor, applyFont, applyFontSize, applyEmphasis, applyLink, applyTerm, clearMarks, Editable, MarkToolbar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/Editable.jsx", error: String((e && e.message) || e) }); }

// components/content/LicenseBadge.jsx
try { (() => {
__ds_scope.injectCss('spu-license-css', `
.spu-license{display:flex;gap:var(--space-4);align-items:center;flex-wrap:wrap;padding:var(--space-4) var(--space-5);border:1px solid var(--color-border);border-radius:var(--radius-md);background:var(--color-surface)}
.spu-license__icons{display:flex;gap:var(--space-2);flex:0 0 auto}
.spu-license__chip{width:36px;height:36px;border-radius:var(--radius-pill);border:1.5px solid var(--ink-800);color:var(--ink-900);font-family:var(--font-mono);font-weight:600;font-size:.66rem;letter-spacing:.02em;display:flex;align-items:center;justify-content:center}
.spu-license__text{font-size:var(--fs-small);color:var(--text-muted);line-height:1.5}
.spu-license__text b{color:var(--text-strong);font-weight:700}
.spu-license__text a{color:var(--color-link);font-weight:600}
.spu-license--dark{border:0;border-radius:0;background:transparent;padding:0;gap:var(--space-3)}
.spu-license--dark .spu-license__chip{border-color:var(--text-on-dark);color:var(--text-on-dark)}
.spu-license--dark .spu-license__text{color:var(--text-on-dark-muted)}
.spu-license--dark .spu-license__text b{color:var(--text-on-dark)}
.spu-license--dark .spu-license__text a{color:var(--text-on-dark)}
`);
const CC_PARTS = {
  BY: 'BY',
  NC: 'NC',
  SA: 'SA',
  ND: 'ND'
};
const LICENSES = {
  byncsa: {
    label: 'CC BY-NC-SA',
    parts: ['BY', 'NC', 'SA'],
    url: 'https://creativecommons.org/licenses/by-nc-sa/4.0/deed.pt_BR'
  },
  byncnd: {
    label: 'CC BY-NC-ND',
    parts: ['BY', 'NC', 'ND'],
    url: 'https://creativecommons.org/licenses/by-nc-nd/4.0/deed.pt_BR'
  }
};
function LicenseBadge({
  kind = 'byncsa',
  licenseKind,
  parts,
  version = '4.0',
  holder,
  year,
  title,
  url,
  tone,
  className,
  style
}) {
  const preset = LICENSES[licenseKind || kind] || LICENSES.byncsa;
  const resolvedParts = parts || preset.parts;
  const resolvedUrl = url || preset.url;
  const code = 'CC ' + resolvedParts.join('-');
  return React.createElement('div', {
    className: __ds_scope.cx('spu-license', tone === 'dark' && 'spu-license--dark', className),
    style
  }, React.createElement('div', {
    className: 'spu-license__icons'
  }, React.createElement('span', {
    key: 'cc',
    className: 'spu-license__chip'
  }, 'CC'), resolvedParts.map(p => React.createElement('span', {
    key: p,
    className: 'spu-license__chip'
  }, CC_PARTS[p] || p))), React.createElement('p', {
    className: 'spu-license__text'
  }, title && React.createElement('span', {
    key: 't'
  }, React.createElement('b', null, '“', title, '”'), ' — '), holder && React.createElement('span', {
    key: 'h'
  }, holder, year ? `, ${year}. ` : '. '), 'Licenciado sob ', React.createElement('b', {
    key: 'c'
  }, `${code} ${version}`), '. ', React.createElement('a', {
    key: 'l',
    href: resolvedUrl,
    target: '_blank',
    rel: 'noopener'
  }, 'Ver licença')));
}
Object.assign(__ds_scope, { LicenseBadge });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/LicenseBadge.jsx", error: String((e && e.message) || e) }); }

// components/content/MediaEmbed.jsx
try { (() => {
__ds_scope.injectCss('spu-embed-css', `
.spu-embed{border-radius:var(--radius-md);overflow:hidden;border:1px solid var(--color-border);background:var(--petrol-900);box-shadow:var(--shadow-sm)}
.spu-embed__frame{position:relative;width:100%}
.spu-embed__frame iframe{position:absolute;inset:0;width:100%;height:100%;border:0;display:block}
.spu-embed__ph{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:var(--space-3);color:var(--text-on-dark);background:radial-gradient(120% 120% at 50% 0%, var(--petrol-700), var(--petrol-900))}
.spu-embed__play{width:64px;height:64px;border-radius:var(--radius-pill);background:var(--color-accent);color:#fff;display:flex;align-items:center;justify-content:center;box-shadow:var(--shadow-md)}
.spu-embed__phlabel{font-family:var(--font-mono);font-size:var(--fs-caption);letter-spacing:.06em;text-transform:uppercase;opacity:.8}
.spu-embed__bar{display:flex;align-items:center;gap:var(--space-3);padding:var(--space-3) var(--space-4);background:var(--color-surface)}
.spu-embed__bar svg{color:var(--color-primary-strong);flex:0 0 auto}
.spu-embed__meta{min-width:0}
.spu-embed__title{font-family:var(--font-display);font-weight:700;font-size:var(--fs-small);color:var(--text-strong);margin:0;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.spu-embed__provider{font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:.06em;color:var(--text-faint)}
.spu-embed__url{display:block;font-family:var(--font-mono);font-size:var(--fs-eyebrow);color:var(--color-primary-strong);margin-top:.2em;word-break:break-all;text-decoration:none}
.spu-embed__url:hover{text-decoration:underline}
@media print{ .spu-embed{break-inside:avoid} .spu-embed__title{white-space:normal} }
`);

// Deriva como exibir a mídia a partir da url.
function resolveMedia(url) {
  if (!url) return {
    kind: 'placeholder'
  };
  const u = url.trim();
  let m;
  // O Eduplay expõe páginas públicas em /app/video/:id e /app/audio/:id,
  // enquanto o player próprio para incorporação usa /embed/ no mesmo caminho.
  // Guardamos sempre a URL pública no documento e derivamos o player em runtime,
  // evitando persistir links temporários/assinados do CDN.
  if (m = u.match(/^https?:\/\/eduplay\.rnp\.br\/app\/(video|audio)\/(?:embed\/)?([\w-]+)(?:[/?#]|$)/i)) return {
    kind: 'iframe',
    src: `https://eduplay.rnp.br/app/${m[1].toLowerCase()}/embed/${m[2]}`,
    provider: 'Eduplay',
    audio: m[1].toLowerCase() === 'audio'
  };
  if (m = u.match(/(?:youtube\.com\/watch\?v=|youtu\.be\/|youtube\.com\/embed\/)([\w-]{11})/)) return {
    kind: 'iframe',
    src: `https://www.youtube.com/embed/${m[1]}`
  };
  if (m = u.match(/vimeo\.com\/(?:video\/)?(\d+)/)) return {
    kind: 'iframe',
    src: `https://player.vimeo.com/video/${m[1]}`
  };
  if (/open\.spotify\.com\//.test(u)) {
    const path = u.split('open.spotify.com/')[1].replace(/^intl-[a-z]{2}\//i, ''); // remove locale (/intl-pt/)
    const clean = path.split('?')[0];
    const spotifyKind = clean.split('/')[0]; // track, episode, show, playlist, album...
    const compact = spotifyKind === 'track' || spotifyKind === 'episode';
    return {
      kind: 'spotify',
      src: `https://open.spotify.com/embed/${clean}`,
      height: compact ? 152 : 352
    };
  }
  if (/\.mp4($|\?)/i.test(u)) return {
    kind: 'video',
    src: u
  };
  if (/\.(mp3|ogg|wav)($|\?)/i.test(u)) return {
    kind: 'audio',
    src: u
  };
  return {
    kind: 'link',
    src: u
  }; // desconhecido → mantém placeholder + link na legenda
}
function mediaTitleText(value) {
  let html = typeof value === 'string' ? value : React.isValidElement(value) && value.props && typeof value.props.html === 'string' ? value.props.html : '';
  if (!html) return '';
  if (typeof DOMParser !== 'undefined') {
    try {
      return new DOMParser().parseFromString('<body>' + html + '</body>', 'text/html').body.textContent.trim();
    } catch (e) {}
  }
  return html.replace(/<[^>]*>/g, '').trim();
}
function MediaEmbed({
  type = 'video',
  src,
  url,
  title,
  provider,
  aspect,
  className,
  style
}) {
  const isAudio = type === 'audio' || type === 'podcast';
  const accessibleTitle = mediaTitleText(title);
  const ratio = aspect || (isAudio ? '21 / 9' : '16 / 9');
  const icon = isAudio ? 'headphones' : 'play-circle';
  // src explícito tem prioridade; senão deriva o player a partir da url.
  const media = src ? {
    kind: 'iframe',
    src
  } : resolveMedia(url);
  let frameStyle = {
    aspectRatio: ratio
  };
  let player;
  if (media.kind === 'spotify') {
    frameStyle = {
      aspectRatio: 'auto',
      height: media.height || 152,
      minHeight: media.height || 152
    };
    player = React.createElement('iframe', {
      src: media.src,
      title: accessibleTitle || 'Spotify',
      loading: 'lazy',
      allow: 'autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture'
    });
  } else if (media.kind === 'iframe') {
    if (media.audio) frameStyle = {
      aspectRatio: 'auto',
      height: 180,
      minHeight: 180
    };
    player = React.createElement('iframe', {
      src: media.src,
      title: accessibleTitle || 'mídia',
      loading: 'lazy',
      allow: 'accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share',
      allowFullScreen: true
    });
  } else if (media.kind === 'video') {
    player = React.createElement('video', {
      src: media.src,
      controls: true,
      style: {
        position: 'absolute',
        inset: 0,
        width: '100%',
        height: '100%',
        display: 'block',
        background: '#000'
      }
    });
  } else if (media.kind === 'audio') {
    frameStyle = {
      aspectRatio: 'auto',
      minHeight: 96
    };
    player = React.createElement('audio', {
      src: media.src,
      controls: true,
      style: {
        position: 'absolute',
        left: 16,
        right: 16,
        top: '50%',
        transform: 'translateY(-50%)',
        width: 'calc(100% - 32px)'
      }
    });
  } else {
    player = React.createElement('div', {
      className: 'spu-embed__ph'
    }, React.createElement('span', {
      className: 'spu-embed__play'
    }, React.createElement(__ds_scope.Icon, {
      name: icon,
      size: 30
    })), React.createElement('span', {
      className: 'spu-embed__phlabel'
    }, isAudio ? 'Áudio externo' : 'Vídeo externo'));
  }
  return React.createElement('figure', {
    className: __ds_scope.cx('spu-embed', className),
    style: {
      margin: 0,
      ...style
    }
  }, React.createElement('div', {
    className: 'spu-embed__frame',
    style: frameStyle
  }, player), (title || provider || url) && React.createElement('figcaption', {
    className: 'spu-embed__bar'
  }, React.createElement(__ds_scope.Icon, {
    name: icon,
    size: 20
  }), React.createElement('div', {
    className: 'spu-embed__meta'
  }, title && React.createElement('p', {
    className: 'spu-embed__title'
  }, title), provider && React.createElement('div', {
    className: 'spu-embed__provider'
  }, provider), url && React.createElement('a', {
    className: 'spu-embed__url',
    href: url,
    target: '_blank',
    rel: 'noopener'
  }, url))));
}
Object.assign(__ds_scope, { MediaEmbed });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/MediaEmbed.jsx", error: String((e && e.message) || e) }); }

// components/content/ExternalEmbed.jsx
try { (() => {
__ds_scope.injectCss('spu-external-embed-css', `
.spu-external-embed{width:100%;margin-inline:auto}
.spu-external-embed__frame{position:relative;width:100%;overflow:hidden;border:1px solid var(--color-border);border-radius:var(--radius-md);background:var(--color-surface);box-shadow:var(--shadow-sm)}
.spu-external-embed__frame iframe{position:absolute;inset:0;width:100%;height:100%;border:0;display:block}
.spu-external-embed__empty{display:grid;place-items:center;min-height:220px;padding:var(--space-6);color:var(--text-muted);text-align:center;background:var(--color-surface-warm)}
.spu-external-embed__print{padding:var(--space-4);border:1px solid var(--color-border);border-radius:var(--radius-md);word-break:break-all}
.spu-external-embed__print a{color:var(--color-primary-strong)}
@media print{.spu-external-embed{break-inside:avoid}}
`);
function parseExternalEmbed(value) {
  const raw = String(value || '').trim();
  if (!raw) return {};
  let src = raw;
  let width;
  let height;
  if (/^<iframe\b/i.test(raw) && typeof DOMParser !== 'undefined') {
    try {
      const doc = new DOMParser().parseFromString(raw, 'text/html');
      const iframe = doc.querySelector('iframe');
      if (iframe) {
        src = iframe.getAttribute('src') || '';
        width = Number.parseFloat(iframe.getAttribute('width') || '') || undefined;
        height = Number.parseFloat(iframe.getAttribute('height') || '') || undefined;
      }
    } catch (e) {}
  }
  if (!/^https?:\/\//i.test(src)) return {};
  return { src, width, height };
}
function externalTitleText(value) {
  if (typeof value === 'string') return value.replace(/<[^>]*>/g, '').trim();
  if (React.isValidElement(value) && value.props && typeof value.props.html === 'string') return value.props.html.replace(/<[^>]*>/g, '').trim();
  return 'Conteúdo incorporado';
}
function ExternalEmbed({
  embed,
  title = 'Conteúdo incorporado',
  responsive = true,
  useEmbedDimensions = true,
  width,
  height = 600,
  className,
  style
}) {
  const parsed = parseExternalEmbed(embed);
  const accessibleTitle = externalTitleText(title);
  const intrinsicWidth = useEmbedDimensions && parsed.width || Number(width) || undefined;
  const intrinsicHeight = useEmbedDimensions && parsed.height || Number(height) || 600;
  const ratio = intrinsicWidth && intrinsicHeight ? `${intrinsicWidth} / ${intrinsicHeight}` : undefined;
  const rootStyle = {
    maxWidth: intrinsicWidth ? `${intrinsicWidth}px` : undefined,
    ...style
  };
  if (!parsed.src) return React.createElement('div', {
    className: __ds_scope.cx('spu-external-embed', className),
    style: rootStyle
  }, React.createElement('div', { className: 'spu-external-embed__empty' }, 'Cole uma URL pública ou o código de incorporação <iframe>.'));
  if (__ds_scope.isPrint()) return React.createElement('div', {
    className: __ds_scope.cx('spu-external-embed', className),
    style: rootStyle
  }, React.createElement('div', { className: 'spu-external-embed__print' }, React.createElement('strong', null, accessibleTitle), React.createElement('br'), React.createElement('a', {
    href: parsed.src,
    target: '_blank',
    rel: 'noopener'
  }, parsed.src)));
  const frameStyle = responsive && ratio ? {
    aspectRatio: ratio
  } : {
    height: `${intrinsicHeight}px`,
    minHeight: 120
  };
  return React.createElement('div', {
    className: __ds_scope.cx('spu-external-embed', className),
    style: rootStyle
  }, React.createElement('div', {
    className: 'spu-external-embed__frame',
    style: frameStyle
  }, React.createElement('iframe', {
    src: parsed.src,
    title: accessibleTitle,
    loading: 'lazy',
    allow: 'accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share; fullscreen',
    allowFullScreen: true
  })));
}
Object.assign(__ds_scope, { ExternalEmbed });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/ExternalEmbed.jsx", error: String((e && e.message) || e) }); }

// components/layout/SectionSlider.jsx
try { (() => {
__ds_scope.injectCss('spu-section-slider-css', `
.spu-section-slider{position:relative;overflow:hidden;background:var(--color-page)}
.spu-section-slider__track{display:flex;width:100%;transition:transform var(--dur) var(--ease-out);will-change:transform}
.spu-section-slider__track>*{flex:0 0 100%;min-width:0}
.spu-section-slider__arrow{position:absolute;z-index:5;top:50%;transform:translateY(-50%);width:44px;height:44px;display:grid;place-items:center;border:1px solid var(--color-primary);border-radius:var(--radius-pill);background:var(--color-surface);color:var(--color-primary-strong);box-shadow:var(--shadow-md);cursor:pointer}
.spu-section-slider__arrow:hover{background:var(--color-primary-soft)}
.spu-section-slider__arrow--prev{left:var(--space-3)}.spu-section-slider__arrow--next{right:var(--space-3)}
.spu-section-slider__status{position:absolute;z-index:4;right:var(--space-4);bottom:var(--space-3);padding:.35em .65em;border-radius:var(--radius-pill);background:var(--color-surface);color:var(--text-muted);box-shadow:var(--shadow-sm);font-family:var(--font-mono);font-size:var(--fs-eyebrow)}
.spu-section-slider--editing{outline:1px dashed color-mix(in srgb,var(--color-primary) 40%,transparent);outline-offset:-1px}
.spu-section-slider--print{overflow:visible}.spu-section-slider--print .spu-section-slider__track{display:flex;flex-direction:column;transform:none!important;gap:var(--flow-block)}.spu-section-slider--print .spu-section-slider__track>*{flex:auto;width:100%}.spu-section-slider--print .spu-section-slider__arrow,.spu-section-slider--print .spu-section-slider__status{display:none}
@media(max-width:720px){.spu-section-slider__arrow{width:38px;height:38px}.spu-section-slider__arrow--prev{left:var(--space-2)}.spu-section-slider__arrow--next{right:var(--space-2)}}
@media print{.spu-section-slider{overflow:visible}.spu-section-slider__track{display:flex!important;flex-direction:column!important;transform:none!important;gap:var(--flow-block)}.spu-section-slider__track>*{flex:auto!important;width:100%!important}.spu-section-slider__arrow,.spu-section-slider__status{display:none!important}}
`);
function SectionSlider({ children, loop = true, className, style, __builderEditing = false }) {
  const slides = React.Children.toArray(children);
  const [current, setCurrent] = React.useState(0);
  const printing = __ds_scope.isPrint();
  const count = slides.length;
  React.useEffect(() => {
    if (current >= count) setCurrent(Math.max(0, count - 1));
  }, [current, count]);
  const go = delta => {
    if (count < 2) return;
    setCurrent(value => loop ? (value + delta + count) % count : Math.max(0, Math.min(count - 1, value + delta)));
  };
  if (!count) return React.createElement('div', {
    className: __ds_scope.cx('spu-section-slider', className),
    style
  });
  return React.createElement('div', {
    className: __ds_scope.cx('spu-section-slider', printing && 'spu-section-slider--print', __builderEditing && 'spu-section-slider--editing', className),
    style,
    tabIndex: printing ? undefined : 0,
    role: 'region',
    'aria-roledescription': 'carrossel',
    'aria-label': 'Seções em carrossel',
    onKeyDown: printing ? undefined : event => {
      if (event.key === 'ArrowLeft') { event.preventDefault(); go(-1); }
      if (event.key === 'ArrowRight') { event.preventDefault(); go(1); }
    }
  }, React.createElement('div', {
    className: 'spu-section-slider__track',
    style: { transform: printing ? undefined : `translateX(-${current * 100}%)` }
  }, slides.map((slide, index) => React.createElement('div', {
    key: React.isValidElement(slide) && slide.key || index,
    className: 'spu-section-slider__slide',
    'aria-hidden': printing ? undefined : index !== current,
    inert: !printing && !__builderEditing && index !== current ? true : undefined
  }, slide))), !printing && count > 1 && React.createElement(React.Fragment, null,
    React.createElement('button', {
      type: 'button', className: 'spu-section-slider__arrow spu-section-slider__arrow--prev',
      onClick: () => go(-1), disabled: !loop && current === 0, 'aria-label': 'Seção anterior'
    }, React.createElement(__ds_scope.Icon, { name: 'arrow-left', size: 20 })),
    React.createElement('button', {
      type: 'button', className: 'spu-section-slider__arrow spu-section-slider__arrow--next',
      onClick: () => go(1), disabled: !loop && current === count - 1, 'aria-label': 'Próxima seção'
    }, React.createElement(__ds_scope.Icon, { name: 'arrow-right', size: 20 })),
    React.createElement('span', { className: 'spu-section-slider__status', 'aria-live': 'polite' }, `${current + 1} / ${count}`)
  ));
}
Object.assign(__ds_scope, { SectionSlider });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/SectionSlider.jsx", error: String((e && e.message) || e) }); }

// components/content/PageToc.jsx
try { (() => {
__ds_scope.injectCss('spu-pagetoc-css', `
.spu-pagetoc{position:fixed;top:var(--space-5);right:var(--space-5);z-index:90;font-family:var(--font-body)}
.spu-pagetoc__toggle{display:flex;align-items:center;gap:.5em;background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-pill);padding:.5em .9em;font-size:var(--fs-caption);font-weight:600;color:var(--text-strong);cursor:pointer;box-shadow:var(--shadow-md)}
.spu-pagetoc__panel{position:absolute;top:calc(100% + 8px);right:0;width:260px;max-height:60vh;overflow:auto;background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-md);box-shadow:var(--shadow-lg);padding:var(--space-4)}
.spu-pagetoc__title{font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:.08em;color:var(--text-faint);margin:0 0 var(--space-3)}
.spu-pagetoc__panel a{display:flex;gap:.6em;align-items:baseline;color:var(--text-strong);text-decoration:none;padding:.35em 0;font-weight:600}
.spu-pagetoc__panel a:hover,.spu-pagetoc__panel a.is-active{color:var(--color-link-hover)}
.spu-pagetoc__num{font-family:var(--font-mono);font-size:var(--fs-caption);color:var(--color-primary-strong)}
@media print{.spu-pagetoc{display:none}}
`);

// Sumário recolhido: botão fixo no canto que abre painel de âncoras.
// `items` = [{ id, text, hidden? }] (gerado a partir dos H2 do documento).
// `scroll-spy` via IntersectionObserver destaca a seção visível.
function PageToc({
  items = [],
  title = 'Neste conteúdo'
}) {
  const [open, setOpen] = React.useState(false);
  const [active, setActive] = React.useState(null);
  const visible = (items || []).filter(i => !i.hidden && i.text);
  React.useEffect(() => {
    if (!visible.length) return undefined;
    const obs = new IntersectionObserver(es => {
      es.forEach(e => {
        if (e.isIntersecting) setActive(e.target.id);
      });
    }, {
      rootMargin: '-40% 0px -55% 0px'
    });
    visible.forEach(i => {
      const el = document.getElementById(i.id);
      if (el) obs.observe(el);
    });
    return () => obs.disconnect();
  }, [items]);
  if (!visible.length) return null;
  return React.createElement('div', {
    className: 'spu-pagetoc'
  }, React.createElement('button', {
    type: 'button',
    className: 'spu-pagetoc__toggle',
    onClick: () => setOpen(o => !o),
    'aria-expanded': open
  }, React.createElement(__ds_scope.Icon, {
    name: open ? 'x' : 'list',
    size: 16
  }), 'Sumário'), open && React.createElement('nav', {
    className: 'spu-pagetoc__panel'
  }, React.createElement('p', {
    className: 'spu-pagetoc__title'
  }, title), visible.map((i, n) => React.createElement('a', {
    key: i.id,
    href: '#' + i.id,
    className: active === i.id ? 'is-active' : '',
    onClick: () => setOpen(false)
  }, React.createElement('span', {
    className: 'spu-pagetoc__num'
  }, String(n + 1).padStart(2, '0')), React.createElement('span', {
    dangerouslySetInnerHTML: {
      __html: i.text
    }
  })))));
}
Object.assign(__ds_scope, { PageToc });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/PageToc.jsx", error: String((e && e.message) || e) }); }

// components/content/PullQuote.jsx
try { (() => {
__ds_scope.injectCss('spu-quote-css', `
.spu-quote{margin:var(--flow-block) 0}
.spu-quote--block{border-left:var(--border-accent) solid var(--color-accent);padding-left:var(--space-6)}
.spu-quote--eye{text-align:center;max-width:42rem;margin-inline:auto;padding:var(--space-4) 0}
.spu-quote__glyph{color:var(--color-accent);display:flex}
.spu-quote--eye .spu-quote__glyph{justify-content:center;margin-bottom:var(--space-3)}
.spu-quote__text{font-family:var(--font-serif);font-weight:500;color:var(--text-strong);line-height:var(--lh-snug);margin:0}
.spu-quote--block .spu-quote__text{font-size:var(--fs-h4)}
.spu-quote--eye .spu-quote__text{font-size:var(--fs-h3)}
.spu-quote__cite{display:block;font-family:var(--font-mono);font-size:var(--fs-caption);font-weight:500;text-transform:uppercase;letter-spacing:.07em;color:var(--text-muted);margin-top:var(--space-4)}
.spu-quote__cite b{color:var(--color-primary-strong);font-weight:600}
`);
function PullQuote({
  children,
  quote,
  cite,
  source,
  variant = 'block',
  className,
  style
}) {
  const text = quote || children;
  const cls = __ds_scope.cx('spu-quote', `spu-quote--${variant}`, className);
  return React.createElement('figure', {
    className: cls,
    style
  }, React.createElement('span', {
    key: 'g',
    className: 'spu-quote__glyph'
  }, React.createElement(__ds_scope.Icon, {
    name: 'quote',
    size: variant === 'eye' ? 34 : 26
  })), React.createElement('blockquote', {
    key: 'q',
    className: 'spu-quote__text',
    style: {
      border: 0,
      margin: 0,
      padding: 0
    }
  }, text), (cite || source) && React.createElement('figcaption', {
    key: 'c',
    className: 'spu-quote__cite'
  }, cite && React.createElement('b', {
    key: 'a'
  }, cite), cite && source ? ' · ' : '', source));
}
Object.assign(__ds_scope, { PullQuote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/PullQuote.jsx", error: String((e && e.message) || e) }); }

// components/content/ReferenceList.jsx
try { (() => {
__ds_scope.injectCss('spu-refs-css', `
.spu-refs{margin:var(--flow-block) 0;max-width:none;padding:0}
.spu-refs__details{border:1px solid var(--color-border);border-radius:var(--radius-md);background:var(--color-surface);box-shadow:var(--shadow-sm);overflow:hidden}
.spu-refs__summary{display:flex;align-items:center;justify-content:space-between;gap:var(--space-3);cursor:pointer;padding:var(--space-4) var(--space-5);font-family:var(--font-display);font-weight:700;font-size:var(--fs-h6);color:var(--text-strong)}
.spu-refs__summary::-webkit-details-marker{display:none}
.spu-refs__summary::after{content:"";width:10px;height:10px;border-right:2px solid var(--color-primary-strong);border-bottom:2px solid var(--color-primary-strong);transform:rotate(45deg);transition:transform var(--dur) var(--ease-out);flex:0 0 auto;margin-right:.3em}
.spu-refs__details[open] .spu-refs__summary::after{transform:rotate(-135deg)}
.spu-refs__list{list-style:none;margin:0;padding:0 var(--space-5) var(--space-5)}
.spu-refs__item{font-family:var(--font-body);font-size:var(--fs-small);color:var(--text-body);line-height:1.55;padding-left:1.8em;text-indent:-1.8em;margin-bottom:var(--space-3)}
.spu-refs__item:last-child{margin-bottom:0}
.spu-refs__item b,.spu-refs__item strong{font-weight:700;color:var(--text-strong)}
@media print{.spu-refs__list{display:block !important}}
`);

// Bloco de referências ABNT colapsável. Fechado por padrão; entra dentro de uma Section.
// Aceita strings ABNT (HTML permitido: <b>/<strong> no TÍTULO da obra).
function ReferenceList({
  items = [],
  title = 'Referências',
  defaultOpen = false,
  className,
  style
}) {
  return React.createElement('div', {
    className: __ds_scope.cx('spu-refs', className),
    style
  }, React.createElement('details', {
    className: 'spu-refs__details',
    open: defaultOpen
  }, React.createElement('summary', {
    className: 'spu-refs__summary'
  }, title), React.createElement('ul', {
    className: 'spu-refs__list'
  }, items.map((it, i) => React.createElement('li', {
    key: i,
    className: 'spu-refs__item',
    dangerouslySetInnerHTML: typeof it === 'string' ? {
      __html: it
    } : undefined
  }, typeof it === 'string' ? undefined : it)))));
}
Object.assign(__ds_scope, { ReferenceList });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/ReferenceList.jsx", error: String((e && e.message) || e) }); }

// components/content/ReflectionStop.jsx
try { (() => {
__ds_scope.injectCss('spu-reflect-css', `
.spu-reflect{position:relative;background:var(--color-surface-warm);background-image:var(--texture-topo);background-size:360px;border:1px solid var(--color-border);border-radius:var(--radius-lg);padding:var(--space-8);overflow:hidden}
.spu-reflect__head{display:flex;align-items:center;gap:var(--space-3);margin-bottom:var(--space-4)}
.spu-reflect__icon{width:44px;height:44px;border-radius:var(--radius-pill);background:var(--color-accent);color:#fff;display:flex;align-items:center;justify-content:center;flex:0 0 auto}
.spu-reflect__kicker{font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:var(--ls-eyebrow);color:var(--color-accent-strong);font-weight:500}
.spu-reflect__title{font-family:var(--font-display);font-weight:700;font-size:var(--fs-h4);line-height:1.1;margin:.1em 0 0;color:var(--text-strong)}
.spu-reflect__body{color:var(--text-body);max-width:var(--measure)}
.spu-reflect__prompts{list-style:none;margin:var(--space-4) 0 0;padding:0;display:flex;flex-direction:column;gap:var(--space-3)}
.spu-reflect__prompt{display:flex;gap:var(--space-3);align-items:flex-start;font-family:var(--font-serif);font-size:var(--fs-body-lg);font-style:italic;color:var(--text-strong)}
.spu-reflect__prompt svg{color:var(--color-accent);margin-top:.25em;flex:0 0 auto}
`);
function ReflectionStop({
  title = 'Para refletir',
  kicker = 'Parada de reflexão',
  children,
  prompts = [],
  className,
  style
}) {
  return React.createElement('aside', {
    className: __ds_scope.cx('spu-reflect', className),
    style
  }, React.createElement('div', {
    className: 'spu-reflect__head'
  }, React.createElement('span', {
    className: 'spu-reflect__icon'
  }, React.createElement(__ds_scope.Icon, {
    name: 'target',
    size: 24
  })), React.createElement('div', null, React.createElement('div', {
    className: 'spu-reflect__kicker'
  }, kicker), React.createElement('p', {
    className: 'spu-reflect__title'
  }, title))), children && React.createElement('div', {
    className: 'spu-reflect__body'
  }, children), prompts.length > 0 && React.createElement('ul', {
    className: 'spu-reflect__prompts'
  }, prompts.map((p, i) => React.createElement('li', {
    key: i,
    className: 'spu-reflect__prompt'
  }, React.createElement(__ds_scope.Icon, {
    name: 'arrow-right',
    size: 18
  }), React.createElement('span', null, p)))));
}
Object.assign(__ds_scope, { ReflectionStop });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/ReflectionStop.jsx", error: String((e && e.message) || e) }); }

// components/content/ResourceList.jsx
try { (() => {
const TYPE_META = {
  link: {
    icon: 'link',
    label: 'Link'
  },
  news: {
    icon: 'newspaper',
    label: 'Notícia'
  },
  video: {
    icon: 'play-circle',
    label: 'Vídeo'
  },
  podcast: {
    icon: 'headphones',
    label: 'Podcast'
  },
  doc: {
    icon: 'file-text',
    label: 'Documento'
  },
  law: {
    icon: 'scale',
    label: 'Norma'
  }
};
__ds_scope.injectCss('spu-resource-css', `
.spu-resources{display:grid;gap:var(--space-4)}
.spu-resource{display:flex;gap:var(--space-4);align-items:flex-start;padding:var(--space-4) var(--space-5);border:1px solid var(--color-border);border-radius:var(--radius-md);background:var(--color-surface);text-decoration:none;color:inherit;transition:border-color var(--dur-fast) var(--ease-out),box-shadow var(--dur-fast) var(--ease-out),transform var(--dur-fast) var(--ease-out)}
.spu-resource:hover{border-color:var(--color-primary);box-shadow:var(--shadow-sm);transform:translateY(-1px)}
.spu-resource__icon{flex:0 0 auto;width:44px;height:44px;border-radius:var(--radius);background:var(--color-primary-soft);color:var(--color-primary-strong);display:flex;align-items:center;justify-content:center}
.spu-resource__kind{font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:.08em;color:var(--color-accent-strong);font-weight:500}
.spu-resource__title{font-family:var(--font-display);font-weight:700;font-size:var(--fs-body);color:var(--text-strong);margin:.1em 0 .15em;display:flex;align-items:center;gap:.4em}
.spu-resource__title svg{color:var(--text-faint)}
.spu-resource__desc{color:var(--text-muted);font-size:var(--fs-small);margin:0}
.spu-resource__source{font-size:var(--fs-caption);color:var(--text-faint);margin-top:.25em}
`);
function ResourceList({
  items = [],
  className,
  style
}) {
  return React.createElement('div', {
    className: __ds_scope.cx('spu-resources', className),
    style
  }, items.map((it, i) => {
    const meta = TYPE_META[it.type] || TYPE_META.link;
    return React.createElement('a', {
      key: i,
      className: 'spu-resource',
      href: it.url || '#',
      target: it.url ? '_blank' : undefined,
      rel: 'noopener'
    }, React.createElement('span', {
      className: 'spu-resource__icon'
    }, React.createElement(__ds_scope.Icon, {
      name: meta.icon,
      size: 22
    })), React.createElement('div', {
      style: {
        minWidth: 0
      }
    }, React.createElement('div', {
      className: 'spu-resource__kind'
    }, it.kind || meta.label), React.createElement('div', {
      className: 'spu-resource__title'
    }, it.title, React.createElement(__ds_scope.Icon, {
      name: 'external-link',
      size: 15
    })), it.description && React.createElement('p', {
      className: 'spu-resource__desc'
    }, it.description), it.source && React.createElement('div', {
      className: 'spu-resource__source'
    }, it.source)));
  }));
}
Object.assign(__ds_scope, { ResourceList });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/ResourceList.jsx", error: String((e && e.message) || e) }); }

// components/core/Badge.jsx
try { (() => {
__ds_scope.injectCss('spu-badge-css', `
.spu-badge{display:inline-flex;align-items:center;gap:.4em;font-family:var(--font-mono);font-size:var(--fs-eyebrow);font-weight:500;letter-spacing:var(--ls-mono);text-transform:uppercase;border-radius:var(--radius-pill);padding:.34em .7em;line-height:1;white-space:nowrap}
.spu-badge--lg{font-size:var(--fs-caption);padding:.44em .85em}
`);
function Badge({
  children,
  tone = 'info',
  icon,
  size = 'md',
  className,
  style
}) {
  const t = __ds_scope.TONES[tone] || __ds_scope.TONES.info;
  const cls = __ds_scope.cx('spu-badge', size === 'lg' && 'spu-badge--lg', className);
  return React.createElement('span', {
    className: cls,
    style: {
      color: t.color,
      background: t.soft,
      ...style
    }
  }, (icon || icon === '') && React.createElement(__ds_scope.Icon, {
    key: 'i',
    name: icon || t.icon,
    size: '1.15em'
  }), React.createElement('span', {
    key: 't'
  }, children));
}
Object.assign(__ds_scope, { Badge });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Badge.jsx", error: String((e && e.message) || e) }); }

// components/core/BuilderManifest.jsx
try { (() => {
// Manifesto legível pelo builder: listas de escolha travadas nos tokens do DS.
// O painel monta os controles a partir daqui (sem hardcode, sem picker livre).
const BuilderManifest = {
  // Tons semânticos de callouts/badges.
  tones: Object.keys(__ds_scope.TONES),
  // info, attention, warning, success, note, neutral

  // Paleta de acento oferecida (cor "dentro da paleta", não picker livre).
  accents: [{
    key: 'petrol',
    label: 'Petróleo',
    value: 'var(--petrol-600)',
    strong: 'var(--petrol-700)',
    soft: 'var(--petrol-50)'
  }, {
    key: 'terra',
    label: 'Terracota',
    value: 'var(--terra-600)',
    strong: 'var(--terra-700)',
    soft: 'var(--terra-50)'
  }, {
    key: 'ochre',
    label: 'Ocre',
    value: 'var(--ochre-600)',
    strong: 'var(--ochre-700)',
    soft: 'var(--ochre-50)'
  }, {
    key: 'green',
    label: 'Verde',
    value: 'var(--green-600)',
    strong: 'var(--green-700)',
    soft: 'var(--green-100)'
  }],
  // Marks de formatação inline do RichText.
  highlights: ['ochre', 'terra', 'petrol', 'green', 'sand'],
  textColors: ['petrol', 'terra', 'ochre', 'green', 'muted', 'light', 'ochre-light', 'terra-light', 'petrol-light', 'green-light'],
  // Papéis de tipografia (variáveis de fonte do DS).
  fontRoles: [{
    key: 'display',
    label: 'Títulos',
    var: '--font-display'
  }, {
    key: 'body',
    label: 'Corpo',
    var: '--font-body'
  }, {
    key: 'serif',
    label: 'Editorial',
    var: '--font-serif'
  }, {
    key: 'mono',
    label: 'Rótulos/dados',
    var: '--font-mono'
  }],
  // Larguras de bloco (Section.width).
  widths: ['narrow', 'content', 'wide', 'full'],
  // Superfícies de bloco (Section.surface).
  surfaces: ['none', 'page', 'white', 'warm', 'dark', 'terra-dark'],
  // Escala de spacing (gaps/paddings).
  spacing: ['space-2', 'space-4', 'space-6', 'space-8', 'space-12', 'flow-block'],
  // Nomes de ícones disponíveis (mesma lista do picker IconGallery).
  icons: __ds_scope.ICON_NAMES
};
Object.assign(__ds_scope, { BuilderManifest });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/BuilderManifest.jsx", error: String((e && e.message) || e) }); }

// components/core/Button.jsx
try { (() => {
__ds_scope.injectCss('spu-btn-css', `
.spu-btn{display:inline-flex;align-items:center;justify-content:center;gap:.5em;font-family:var(--font-display);font-weight:600;letter-spacing:-0.01em;border:1px solid transparent;border-radius:var(--radius);cursor:pointer;text-decoration:none;line-height:1;white-space:nowrap;transition:background var(--dur-fast) var(--ease-out),color var(--dur-fast) var(--ease-out),border-color var(--dur-fast) var(--ease-out),transform var(--dur-fast) var(--ease-out),box-shadow var(--dur-fast) var(--ease-out)}
.spu-btn:active{transform:translateY(1px)}
.spu-btn[aria-disabled=true],.spu-btn:disabled{opacity:.45;pointer-events:none}
.spu-btn--sm{font-size:.875rem;padding:.5em .85em}
.spu-btn--md{font-size:1rem;padding:.66em 1.15em}
.spu-btn--lg{font-size:1.1rem;padding:.8em 1.5em}
.spu-btn--full{width:100%}
.spu-btn--primary{background:var(--color-primary);color:var(--color-on-primary);box-shadow:var(--shadow-sm)}
.spu-btn--primary:hover{background:var(--color-primary-hover)}
.spu-btn--accent{background:var(--color-accent);color:var(--color-on-accent);box-shadow:var(--shadow-sm)}
.spu-btn--accent:hover{background:var(--color-accent-strong)}
.spu-btn--secondary{background:var(--color-surface);color:var(--color-primary-strong);border-color:var(--color-border-strong)}
.spu-btn--secondary:hover{border-color:var(--color-primary);background:var(--color-primary-soft)}
.spu-btn--ghost{background:transparent;color:var(--color-primary-strong)}
.spu-btn--ghost:hover{background:var(--color-primary-soft)}
`);
function Button({
  children,
  variant = 'primary',
  size = 'md',
  icon,
  iconRight,
  href,
  onClick,
  type = 'button',
  disabled = false,
  fullWidth = false,
  className,
  ...rest
}) {
  const cls = __ds_scope.cx('spu-btn', `spu-btn--${variant}`, `spu-btn--${size}`, fullWidth && 'spu-btn--full', className);
  const content = [icon && React.createElement(__ds_scope.Icon, {
    key: 'l',
    name: icon,
    size: '1.05em'
  }), React.createElement('span', {
    key: 't'
  }, children), iconRight && React.createElement(__ds_scope.Icon, {
    key: 'r',
    name: iconRight,
    size: '1.05em'
  })];
  if (href && !disabled) {
    return React.createElement('a', {
      href,
      className: cls,
      ...rest
    }, content);
  }
  return React.createElement('button', {
    type,
    className: cls,
    onClick,
    disabled,
    'aria-disabled': disabled || undefined,
    ...rest
  }, content);
}
Object.assign(__ds_scope, { Button });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Button.jsx", error: String((e && e.message) || e) }); }

// components/core/IconButton.jsx
try { (() => {
__ds_scope.injectCss('spu-iconbtn-css', `
.spu-iconbtn{display:inline-flex;align-items:center;justify-content:center;border:1px solid transparent;border-radius:var(--radius);cursor:pointer;color:var(--color-primary-strong);background:transparent;transition:background var(--dur-fast) var(--ease-out),color var(--dur-fast),border-color var(--dur-fast),transform var(--dur-fast)}
.spu-iconbtn:active{transform:translateY(1px)}
.spu-iconbtn:disabled{opacity:.4;pointer-events:none}
.spu-iconbtn--sm{width:34px;height:34px}
.spu-iconbtn--md{width:42px;height:42px}
.spu-iconbtn--lg{width:50px;height:50px}
.spu-iconbtn--soft{background:var(--color-surface);border-color:var(--color-border);box-shadow:var(--shadow-sm)}
.spu-iconbtn--soft:hover{border-color:var(--color-primary);background:var(--color-primary-soft)}
.spu-iconbtn--ghost:hover{background:var(--color-primary-soft)}
.spu-iconbtn--solid{background:var(--color-primary);color:var(--color-on-primary);box-shadow:var(--shadow-sm)}
.spu-iconbtn--solid:hover{background:var(--color-primary-hover)}
`);
function IconButton({
  name,
  label,
  variant = 'soft',
  size = 'md',
  href,
  onClick,
  disabled = false,
  className,
  ...rest
}) {
  const cls = __ds_scope.cx('spu-iconbtn', `spu-iconbtn--${variant}`, `spu-iconbtn--${size}`, className);
  const iconSize = size === 'lg' ? 22 : size === 'sm' ? 17 : 20;
  const child = React.createElement(__ds_scope.Icon, {
    name,
    size: iconSize,
    label
  });
  if (href && !disabled) return React.createElement('a', {
    href,
    className: cls,
    'aria-label': label,
    ...rest
  }, child);
  return React.createElement('button', {
    type: 'button',
    className: cls,
    onClick,
    disabled,
    'aria-label': label,
    ...rest
  }, child);
}
Object.assign(__ds_scope, { IconButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/IconButton.jsx", error: String((e && e.message) || e) }); }

// components/core/IconGallery.jsx
try { (() => {
__ds_scope.injectCss('spu-icongallery-css', `
.spu-icongallery-wrap{display:flex;flex-direction:column;gap:var(--space-3)}
.spu-icongallery__search{display:flex;align-items:center;gap:8px;padding:8px 12px;border:1px solid var(--color-border);border-radius:var(--radius-md);background:var(--color-surface);color:var(--text-muted)}
.spu-icongallery__search input{flex:1;border:0;outline:none;background:none;font:inherit;font-size:var(--fs-small);color:var(--text-strong)}
.spu-icongallery__empty{font-family:var(--font-mono);font-size:var(--fs-caption);color:var(--text-faint);padding:var(--space-4) 0;text-align:center}
.spu-icongallery__status{display:flex;align-items:center;justify-content:space-between;gap:var(--space-2);font-family:var(--font-mono);font-size:10px;letter-spacing:.02em;color:var(--text-faint)}
.spu-icongallery{display:grid;grid-template-columns:repeat(auto-fill,minmax(var(--_ig,84px),1fr));gap:var(--space-2)}
.spu-icongallery__item{display:flex;flex-direction:column;align-items:center;gap:6px;padding:12px 6px;border:1px solid var(--color-border);border-radius:var(--radius-md);background:var(--color-surface);color:var(--text-strong);cursor:pointer;font:inherit;transition:border-color var(--dur-fast) var(--ease-out),background var(--dur-fast) var(--ease-out)}
.spu-icongallery__item:hover{border-color:var(--color-primary);background:var(--color-primary-soft)}
.spu-icongallery__item.is-active{border-color:var(--color-primary);background:var(--color-primary-soft);color:var(--color-primary-strong);box-shadow:var(--focus-ring)}
.spu-icongallery__name{font-family:var(--font-mono);font-size:10px;letter-spacing:.02em;color:var(--text-faint);text-align:center;word-break:break-word;line-height:1.25}
.spu-icongallery__item.is-active .spu-icongallery__name{color:var(--color-primary-strong)}
.spu-icongallery__more{align-self:center;padding:8px 14px;border:1px solid var(--color-border);border-radius:var(--radius-md);background:var(--color-surface);color:var(--text-muted);cursor:pointer;font:inherit;font-size:var(--fs-caption)}
.spu-icongallery__more:hover{border-color:var(--color-primary);color:var(--color-primary-strong);background:var(--color-primary-soft)}
`);

const ICON_QUERY_ALIASES = {
  ajuda: ['help'], alerta: ['alert', 'warning'], amor: ['heart'], arquivo: ['file'],
  audio: ['audio', 'sound', 'volume'], calendario: ['calendar'], camera: ['camera'],
  casa: ['home', 'house'], chave: ['key'], fechar: ['close', 'x'], foto: ['image', 'camera'],
  grupo: ['users', 'people'], imagem: ['image', 'picture'], informacao: ['info'],
  livro: ['book'], local: ['map', 'pin'], mapa: ['map'], mensagem: ['message', 'mail'],
  pessoa: ['user', 'person'], predio: ['building', 'landmark'], relogio: ['clock', 'time'],
  seta: ['arrow', 'chevron'], telefone: ['phone'], usuario: ['user'], video: ['video', 'play']
};

function normalizeIconSearch(value) {
  return String(value || '').normalize('NFD').replace(/[\u0300-\u036f]/g, '').toLowerCase().replace(/[-_]+/g, ' ').trim();
}

// Grade visual de todos os ícones do DS — picker pronto para o builder.
// onPick(name) é chamado ao clicar; `value` marca o ícone selecionado.
// `search` (padrão true) mostra um campo de busca que filtra por nome.
function IconGallery({
  names,
  value,
  onPick,
  size = 22,
  itemMin = 84,
  filter,
  search = true,
  className,
  style
}) {
  const [q, setQ] = React.useState('');
  const [limit, setLimit] = React.useState(72);
  const term = normalizeIconSearch(filter != null ? String(filter) : q);
  React.useEffect(() => setLimit(72), [term]);
  const source = names || __ds_scope.ICON_NAMES;
  const searchTerms = __ds_ns.ICON_SEARCH || {};
  const tokens = term.split(/\s+/).filter(Boolean);
  let matches = source;
  if (tokens.length) matches = source.filter(name => {
    const haystack = normalizeIconSearch(name + ' ' + (searchTerms[name] || []).join(' '));
    return tokens.every(token => [token, ...(ICON_QUERY_ALIASES[token] || [])].some(candidate => haystack.includes(candidate)));
  });
  if (value && matches.includes(value)) matches = [value, ...matches.filter(name => name !== value)];
  const list = matches.slice(0, limit);
  const grid = React.createElement('div', {
    className: __ds_scope.cx('spu-icongallery', className),
    style: {
      '--_ig': typeof itemMin === 'number' ? `${itemMin}px` : itemMin,
      ...style
    },
    role: onPick ? 'listbox' : undefined
  }, list.map(name => React.createElement('button', {
    key: name,
    type: 'button',
    className: __ds_scope.cx('spu-icongallery__item', value === name && 'is-active'),
    onClick: onPick ? () => onPick(name) : undefined,
    'aria-selected': onPick ? value === name : undefined,
    title: name
  }, React.createElement(__ds_scope.Icon, {
    name,
    size
  }), React.createElement('span', {
    className: 'spu-icongallery__name'
  }, name))));

  const result = React.createElement(React.Fragment, null,
    React.createElement('div', {
      className: 'spu-icongallery__status',
      role: 'status'
    }, React.createElement('span', null, matches.length + (matches.length === 1 ? ' ícone' : ' ícones')),
    matches.length > list.length ? React.createElement('span', null, 'mostrando ' + list.length) : null),
    matches.length ? grid : React.createElement('div', {
      className: 'spu-icongallery__empty'
    }, 'Nenhum ícone para “' + term + '”'),
    matches.length > list.length ? React.createElement('button', {
      type: 'button',
      className: 'spu-icongallery__more',
      onClick: () => setLimit(current => current + 72)
    }, 'Mostrar mais') : null
  );

  // Sem busca interna (filtro controlado externamente): devolve os resultados.
  if (!search || filter != null) return React.createElement('div', {
    className: 'spu-icongallery-wrap'
  }, result);
  return React.createElement('div', {
    className: 'spu-icongallery-wrap'
  }, React.createElement('div', {
    className: 'spu-icongallery__search'
  }, React.createElement(__ds_scope.Icon, {
    name: 'search',
    size: 16
  }), React.createElement('input', {
    type: 'text',
    value: q,
    placeholder: 'Buscar ícone…',
    onChange: e => setQ(e.target.value),
    'aria-label': 'Buscar ícone'
  })), result);
}
Object.assign(__ds_scope, { IconGallery });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/IconGallery.jsx", error: String((e && e.message) || e) }); }

// components/core/Kicker.jsx
try { (() => {
__ds_scope.injectCss('spu-kicker-css', `
.spu-kicker{display:inline-flex;align-items:center;gap:.55em;font-family:var(--font-mono);font-size:var(--fs-eyebrow);font-weight:500;letter-spacing:var(--ls-eyebrow);text-transform:uppercase;color:var(--color-primary-strong)}
.spu-blockstack>.spu-block-kicker:not(:last-child){margin-bottom:calc(-1 * var(--flow-block) + var(--space-2))}
.spu-kicker__rule{width:1.8em;height:2px;background:var(--color-accent);border-radius:2px}
`);
function Kicker({
  children,
  icon,
  rule = true,
  color,
  className,
  style
}) {
  const cls = __ds_scope.cx('spu-kicker', className);
  return React.createElement('span', {
    className: cls,
    style: {
      color,
      ...style
    }
  }, rule && React.createElement('span', {
    key: 'r',
    className: 'spu-kicker__rule',
    style: color ? {
      background: color
    } : undefined
  }), icon && React.createElement(__ds_scope.Icon, {
    key: 'i',
    name: icon,
    size: '1.1em'
  }), React.createElement('span', {
    key: 't'
  }, children));
}
Object.assign(__ds_scope, { Kicker });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Kicker.jsx", error: String((e && e.message) || e) }); }

// components/core/Tag.jsx
try { (() => {
__ds_scope.injectCss('spu-tag-css', `
.spu-tag{display:inline-flex;align-items:center;gap:.4em;font-family:var(--font-body);font-size:var(--fs-small);font-weight:600;color:var(--ink-700);background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-pill);padding:.4em .85em;line-height:1;cursor:default;transition:border-color var(--dur-fast) var(--ease-out),background var(--dur-fast),color var(--dur-fast)}
button.spu-tag{cursor:pointer}
button.spu-tag:hover{border-color:var(--color-primary);color:var(--color-primary-strong)}
.spu-tag--active{background:var(--color-primary);border-color:var(--color-primary);color:var(--color-on-primary)}
.spu-tag--active:hover{color:var(--color-on-primary)}
`);
function Tag({
  children,
  icon,
  active = false,
  onClick,
  className,
  ...rest
}) {
  const cls = __ds_scope.cx('spu-tag', active && 'spu-tag--active', className);
  const content = [icon && React.createElement(__ds_scope.Icon, {
    key: 'i',
    name: icon,
    size: '1em'
  }), React.createElement('span', {
    key: 't'
  }, children)];
  if (onClick) return React.createElement('button', {
    type: 'button',
    className: cls,
    onClick,
    'aria-pressed': active,
    ...rest
  }, content);
  return React.createElement('span', {
    className: cls,
    ...rest
  }, content);
}
Object.assign(__ds_scope, { Tag });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Tag.jsx", error: String((e && e.message) || e) }); }

// components/interactive/Carousel.jsx
try { (() => {
__ds_scope.injectCss('spu-carousel-css', `
.spu-carousel{position:relative}
.spu-carousel__viewport{position:relative;overflow:hidden;border-radius:var(--radius-md)}
.spu-carousel__track{display:flex;transition:transform var(--dur-slow) var(--ease-in-out)}
.spu-carousel__slide{flex:0 0 100%;min-width:0}
.spu-carousel__slide>.spu-figure{margin-block:0}
.spu-carousel__arrow{position:absolute;z-index:3;top:50%;transform:translateY(-50%);width:44px;height:44px;display:grid;place-items:center;border:1px solid rgba(255,255,255,.45);border-radius:var(--radius-pill);background:rgba(14,46,43,.78);color:#fff;box-shadow:var(--shadow-md);cursor:pointer;transition:background var(--dur-fast) var(--ease-out),transform var(--dur-fast) var(--ease-out)}
.spu-carousel__arrow:hover{background:var(--color-primary);transform:translateY(-50%) scale(1.04)}
.spu-carousel__arrow:focus-visible{outline:3px solid var(--color-focus-ring);outline-offset:2px}
.spu-carousel__arrow--prev{left:var(--space-3)}
.spu-carousel__arrow--next{right:var(--space-3)}
.spu-carousel__nav{display:flex;align-items:center;justify-content:center;margin-top:var(--space-4)}
.spu-carousel__center{display:flex;flex-direction:column;align-items:center;gap:var(--space-2)}
.spu-carousel__dots{display:flex;align-items:center;gap:var(--space-2)}
.spu-carousel__dot{width:8px;height:8px;border-radius:var(--radius-pill);background:var(--slate-300);border:0;padding:0;cursor:pointer;transition:width var(--dur) var(--ease-out),background var(--dur) var(--ease-out)}
.spu-carousel__dot--active{background:var(--color-primary);width:24px}
.spu-carousel__count{font-family:var(--font-mono);font-size:var(--fs-caption);color:var(--text-muted);text-align:center}
.spu-carousel--print .spu-carousel__viewport{overflow:visible;border-radius:0}
.spu-carousel--print .spu-carousel__track{display:block;transform:none !important}
.spu-carousel--print .spu-carousel__slide{break-inside:avoid;margin-bottom:var(--space-6)}
.spu-carousel--print .spu-carousel__nav,.spu-carousel--print .spu-carousel__arrow{display:none !important}
@media print{
  .spu-carousel__viewport{overflow:visible !important;border-radius:0}
  .spu-carousel__track{display:block !important;transform:none !important}
  .spu-carousel__slide{break-inside:avoid;margin-bottom:var(--space-6)}
  .spu-carousel__nav,.spu-carousel__arrow{display:none !important}
}
`);
function Carousel({
  children,
  slides: slideItems,
  size = 'full',
  fit = 'contain',
  zoom = true,
  className,
  style
}) {
  const slides = Array.isArray(slideItems) ? slideItems.map((slide, index) => React.createElement(__ds_scope.Figure, {
    key: slide.slot || index,
    slot: slide.slot,
    src: slide.src,
    alt: slide.alt || '',
    title: slide.title,
    caption: slide.caption,
    credit: slide.credit,
    size,
    fit,
    zoom
  })) : React.Children.toArray(children);
  const [i, setI] = React.useState(0);
  const n = slides.length;
  const printing = __ds_scope.isPrint();
  React.useEffect(() => {
    if (i >= n) setI(Math.max(0, n - 1));
  }, [i, n]);
  const go = d => setI(p => (p + d + n) % n);
  return React.createElement('div', {
    className: __ds_scope.cx('spu-carousel', printing && 'spu-carousel--print', className),
    style
  }, React.createElement('div', {
    className: 'spu-carousel__viewport'
  }, React.createElement('div', {
    className: 'spu-carousel__track',
    style: {
      transform: `translateX(-${i * 100}%)`
    }
  }, slides.map((s, k) => React.createElement('div', {
    key: k,
    className: 'spu-carousel__slide',
    'aria-hidden': printing ? undefined : k !== i
  }, s))), !printing && n > 1 && React.createElement(React.Fragment, null, React.createElement('button', {
    type: 'button',
    className: 'spu-carousel__arrow spu-carousel__arrow--prev',
    onClick: () => go(-1),
    'aria-label': 'Imagem anterior'
  }, React.createElement(__ds_scope.Icon, {
    name: 'chevron-left',
    size: 24
  })), React.createElement('button', {
    type: 'button',
    className: 'spu-carousel__arrow spu-carousel__arrow--next',
    onClick: () => go(1),
    'aria-label': 'Próxima imagem'
  }, React.createElement(__ds_scope.Icon, {
    name: 'chevron-right',
    size: 24
  })))), !printing && n > 1 && React.createElement('div', {
    className: 'spu-carousel__nav'
  }, React.createElement('div', {
    className: 'spu-carousel__center'
  }, React.createElement('div', {
    className: 'spu-carousel__dots'
  }, slides.map((_, k) => React.createElement('button', {
    key: k,
    type: 'button',
    className: __ds_scope.cx('spu-carousel__dot', k === i && 'spu-carousel__dot--active'),
    onClick: () => setI(k),
    'aria-label': `Ir para ${k + 1}`
  }))), React.createElement('div', {
    className: 'spu-carousel__count'
  }, `${i + 1} / ${n}`))));
}
Object.assign(__ds_scope, { Carousel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/interactive/Carousel.jsx", error: String((e && e.message) || e) }); }

// components/interactive/ContentSlider.jsx
try { (() => {
__ds_scope.injectCss('spu-content-slider-css', `
.spu-content-slider{--_cs:var(--color-primary);overflow:hidden;border:1px solid var(--color-border);border-radius:calc(var(--radius-md) + 4px);background:var(--color-surface);box-shadow:var(--shadow-md)}
.spu-content-slider__top{display:flex;align-items:center;justify-content:space-between;gap:var(--space-4);padding:var(--space-4) var(--space-5);border-bottom:1px solid var(--color-border);background:color-mix(in srgb,var(--_cs) 7%,var(--color-surface))}
.spu-content-slider__hint{font-family:var(--font-mono);font-size:var(--fs-caption);font-weight:600;letter-spacing:.06em;text-transform:uppercase;color:var(--_cs)}
.spu-content-slider__count{font-family:var(--font-mono);font-size:var(--fs-small);color:var(--text-faint);white-space:nowrap}.spu-content-slider__count strong{color:var(--_cs);font-size:1.12em}
.spu-content-slider__viewport{overflow:hidden}.spu-content-slider__track{display:flex;transition:transform 560ms var(--ease-out);touch-action:pan-y}
.spu-content-slider__slide{display:grid;grid-template-columns:minmax(0,1.08fr) minmax(300px,.92fr);min-width:100%;background:var(--color-surface)}
.spu-content-slider__slide--text{grid-template-columns:1fr}.spu-content-slider__slide--text .spu-content-slider__body{width:min(100%,780px);min-height:390px;margin-inline:auto}
.spu-content-slider__media{position:relative;min-height:390px;margin:0;overflow:hidden;background:var(--color-surface-warm)}
.spu-content-slider__media image-slot,.spu-content-slider__media img{display:block;width:100%;height:100%;object-fit:cover}.spu-content-slider__media image-slot{position:absolute;inset:0}
.spu-content-slider__media figcaption{position:absolute;left:var(--space-4);bottom:var(--space-4);z-index:1;max-width:calc(100% - var(--space-8));padding:.55em .8em;border-radius:var(--radius);background:rgba(18,35,31,.84);color:#fff;font-family:var(--font-mono);font-size:var(--fs-caption)}
.spu-content-slider__body{display:flex;flex-direction:column;justify-content:center;align-items:flex-start;padding:clamp(var(--space-6),5vw,var(--space-10))}
.spu-content-slider__label{display:inline-flex;padding:.35em .65em;border-radius:var(--radius-pill);background:color-mix(in srgb,var(--_cs) 12%,var(--color-surface));color:var(--_cs);font-family:var(--font-mono);font-size:var(--fs-eyebrow);font-weight:700;letter-spacing:.09em;text-transform:uppercase}
.spu-content-slider__label--icon{align-items:center;justify-content:center;width:44px;height:44px;padding:0;border-radius:var(--radius)}
.spu-content-slider__title{margin:var(--space-3) 0 0;font-family:var(--font-display);font-size:clamp(1.75rem,3.5vw,var(--fs-h2));line-height:1.04;letter-spacing:var(--ls-heading);color:var(--text-strong)}
.spu-content-slider__subtitle{margin:var(--space-3) 0 0;font-family:var(--font-display);font-size:var(--fs-h5);font-weight:600;line-height:1.25;color:var(--_cs)}
.spu-content-slider__description{margin-top:var(--space-4);color:var(--text-muted)}
.spu-content-slider__link{display:inline-flex;align-items:center;gap:var(--space-2);margin-top:var(--space-6);padding:.72em 1em;border:1px solid var(--_cs);border-radius:var(--radius);background:var(--_cs);color:#fff;text-decoration:none;font-family:var(--font-display);font-weight:700}.spu-content-slider__link:hover{filter:brightness(1.06)}
.spu-content-slider__controls{display:grid;grid-template-columns:auto minmax(0,1fr) auto;align-items:stretch;border-top:1px solid var(--color-border)}
.spu-content-slider__arrow{display:grid;place-items:center;min-width:72px;padding:var(--space-4);border:0;background:var(--color-surface);color:var(--text-strong);cursor:pointer}.spu-content-slider__arrow:hover{background:var(--color-surface-warm)}
.spu-content-slider__nav{display:grid;grid-template-columns:repeat(var(--_count),minmax(90px,1fr));overflow-x:auto;border-inline:1px solid var(--color-border)}
.spu-content-slider__tab{position:relative;display:flex;flex-direction:column;align-items:flex-start;justify-content:center;gap:.15em;min-width:0;padding:var(--space-3) var(--space-4);border:0;border-right:1px solid var(--color-border);background:var(--color-surface);color:var(--text-muted);text-align:left;cursor:pointer}.spu-content-slider__tab:last-child{border-right:0}.spu-content-slider__tab::after{content:"";position:absolute;inset:auto 0 0;height:4px;background:transparent}.spu-content-slider__tab.is-active{background:color-mix(in srgb,var(--_cs) 7%,var(--color-surface));color:var(--text-strong)}.spu-content-slider__tab.is-active::after{background:var(--_cs)}
.spu-content-slider__tab span{font-family:var(--font-mono);font-size:var(--fs-eyebrow);color:var(--_cs)}.spu-content-slider__tab b{max-width:100%;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-family:var(--font-display);font-size:var(--fs-small)}
.spu-content-slider:focus-visible{outline:3px solid var(--color-focus-ring);outline-offset:3px}
.spu-content-slider--print .spu-content-slider__track{display:block;transform:none!important}.spu-content-slider--print .spu-content-slider__slide{break-inside:avoid;margin-bottom:var(--space-6)}.spu-content-slider--print .spu-content-slider__controls{display:none}
@media(max-width:760px){.spu-content-slider__slide{grid-template-columns:1fr}.spu-content-slider__media{min-height:0;aspect-ratio:16/10}.spu-content-slider__body,.spu-content-slider__slide--text .spu-content-slider__body{min-height:0;padding:var(--space-6)}.spu-content-slider__top{padding:var(--space-3) var(--space-4)}.spu-content-slider__arrow{min-width:52px;padding:var(--space-3)}.spu-content-slider__tab{align-items:center;padding:var(--space-3) var(--space-2)}.spu-content-slider__tab b{display:none}}
@media print{.spu-content-slider__track{display:block!important;transform:none!important}.spu-content-slider__slide{break-inside:avoid;margin-bottom:var(--space-6)}.spu-content-slider__controls{display:none!important}}
`);

function ContentSliderImage({ id, alt }) {
  return React.createElement('image-slot', {
    id,
    shape: 'rect',
    fit: 'cover',
    alt: alt || '',
    placeholder: 'Arraste uma imagem',
    style: { display: 'block', width: '100%', height: '100%' }
  });
}
function ContentSlider({ slides = [], hint = 'Explore os slides', accent, showTabNumbers = true, className, style }) {
  const [current, setCurrent] = React.useState(0);
  const startX = React.useRef(null);
  const printing = __ds_scope.isPrint();
  const n = slides.length;
  React.useEffect(() => { if (current >= n) setCurrent(Math.max(0, n - 1)); }, [current, n]);
  const go = next => { if (n) setCurrent((next + n) % n); };
  const vars = { ...style, ...(accent ? { '--_cs': accent } : {}) };
  if (!n) return null;
  return React.createElement('div', {
    className: __ds_scope.cx('spu-content-slider', printing && 'spu-content-slider--print', className),
    style: vars,
    tabIndex: printing ? undefined : 0,
    role: 'region',
    'aria-roledescription': 'carrossel',
    'aria-label': 'Slider de conteúdo',
    onKeyDown: printing ? undefined : e => { if (e.key === 'ArrowLeft') { e.preventDefault(); go(current - 1); } if (e.key === 'ArrowRight') { e.preventDefault(); go(current + 1); } }
  }, React.createElement('div', { className: 'spu-content-slider__top' },
    React.createElement('span', { className: 'spu-content-slider__hint' }, __ds_scope.renderRich(hint, { inline: true })),
    React.createElement('span', { className: 'spu-content-slider__count' }, React.createElement('strong', null, String(current + 1).padStart(2, '0')), ' / ', String(n).padStart(2, '0'))
  ), React.createElement('div', { className: 'spu-content-slider__viewport' },
    React.createElement('div', {
      className: 'spu-content-slider__track',
      style: { transform: printing ? undefined : `translateX(-${current * 100}%)` },
      onPointerDown: printing ? undefined : e => { startX.current = e.clientX; },
      onPointerUp: printing ? undefined : e => { if (startX.current == null) return; const d = e.clientX - startX.current; if (Math.abs(d) > 48) go(current + (d < 0 ? 1 : -1)); startX.current = null; },
      onPointerCancel: () => { startX.current = null; }
    }, slides.map((slide, index) => {
      const hasImage = !!slide.showImage && !!slide.slot;
      const href = typeof slide.linkHref === 'string' ? slide.linkHref.trim() : slide.linkHref;
      return React.createElement('article', {
        key: slide.id || slide.slot || index,
        className: __ds_scope.cx('spu-content-slider__slide', !hasImage && 'spu-content-slider__slide--text'),
        'aria-hidden': printing ? undefined : index !== current,
        inert: !printing && index !== current ? true : undefined
      }, hasImage && React.createElement('figure', { className: 'spu-content-slider__media' },
        React.createElement(ContentSliderImage, { id: slide.slot, alt: slide.alt }),
        slide.caption && React.createElement('figcaption', null, __ds_scope.renderRich(slide.caption, { inline: true }))
      ), React.createElement('div', { className: 'spu-content-slider__body' },
        slide.labelIcon ? React.createElement('span', { className: 'spu-content-slider__label spu-content-slider__label--icon', 'aria-hidden': 'true' }, React.createElement(__ds_scope.Icon, { name: slide.labelIcon, size: 22 })) : slide.label && React.createElement('span', { className: 'spu-content-slider__label' }, __ds_scope.renderRich(slide.label, { inline: true })),
        __ds_scope.hasRichContent(slide.title) && React.createElement('h3', { className: 'spu-content-slider__title' }, __ds_scope.renderRich(slide.title, { inline: true })),
        slide.subtitle && React.createElement('p', { className: 'spu-content-slider__subtitle' }, __ds_scope.renderRich(slide.subtitle, { inline: true })),
        slide.description && React.createElement('div', { className: 'spu-content-slider__description' }, __ds_scope.renderRich(slide.description)),
        href && React.createElement('a', { className: 'spu-content-slider__link', href, target: /^https?:/i.test(href) ? '_blank' : undefined, rel: /^https?:/i.test(href) ? 'noopener' : undefined }, __ds_scope.renderRich(slide.linkLabel || 'Saiba mais', { inline: true }), React.createElement(__ds_scope.Icon, { name: 'arrow-right', size: 16 }))
      ));
    }))
  ), !printing && n > 1 && React.createElement('div', { className: 'spu-content-slider__controls' },
    React.createElement('button', { type: 'button', className: 'spu-content-slider__arrow', onClick: () => go(current - 1), 'aria-label': 'Slide anterior' }, React.createElement(__ds_scope.Icon, { name: 'arrow-left', size: 20 })),
    React.createElement('div', { className: 'spu-content-slider__nav', role: 'tablist', style: { '--_count': n } }, slides.map((slide, index) => React.createElement('button', {
      key: index,
      type: 'button',
      role: 'tab',
      className: __ds_scope.cx('spu-content-slider__tab', index === current && 'is-active'),
      'aria-selected': index === current,
      tabIndex: index === current ? 0 : -1,
      onClick: () => setCurrent(index)
    }, showTabNumbers && React.createElement('span', null, String(index + 1).padStart(2, '0')), React.createElement('b', null, __ds_scope.renderRich(slide.tabLabel || slide.title || `Slide ${index + 1}`, { inline: true }))))),
    React.createElement('button', { type: 'button', className: 'spu-content-slider__arrow', onClick: () => go(current + 1), 'aria-label': 'Próximo slide' }, React.createElement(__ds_scope.Icon, { name: 'arrow-right', size: 20 }))
  ));
}
Object.assign(__ds_scope, { ContentSlider });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/interactive/ContentSlider.jsx", error: String((e && e.message) || e) }); }

// components/interactive/GlossaryTerm.jsx
try { (() => {
__ds_scope.injectCss('spu-gloss-css', `
.spu-gloss{position:relative;display:inline-block}
.spu-gloss__trigger{font:inherit;color:var(--color-primary-strong);font-weight:600;border:0;background:none;padding:0;cursor:help;border-bottom:2px dotted var(--petrol-300);transition:border-color var(--dur-fast) var(--ease-out),color var(--dur-fast)}
.spu-gloss__trigger:hover,.spu-gloss--open .spu-gloss__trigger{color:var(--color-accent-strong);border-bottom-color:var(--color-accent)}
.spu-gloss__pop{position:fixed;z-index:10050;width:max-content;max-width:min(320px,calc(100vw - 24px));background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-md);box-shadow:var(--shadow-lg);padding:var(--space-4);text-align:left;cursor:auto;animation:spu-pop var(--dur-fast) var(--ease-out)}
.spu-gloss__pop::after{content:"";position:absolute;top:100%;left:var(--spu-gloss-arrow,18px);width:11px;height:11px;background:var(--color-surface);border-right:1px solid var(--color-border);border-bottom:1px solid var(--color-border);transform:translate(-50%,-50%) rotate(45deg)}
.spu-gloss__pop[data-placement="bottom"]::after{top:auto;bottom:100%;transform:translate(-50%,50%) rotate(225deg)}
.spu-gloss__term{font-family:var(--font-display);font-weight:700;font-size:var(--fs-small);color:var(--text-strong);margin:0 0 .25em}
.spu-gloss__def{font-size:var(--fs-small);font-weight:400;color:var(--text-body);line-height:1.5;margin:0}
.spu-gloss__def p{margin:0 0 .55em}.spu-gloss__def p:last-child{margin-bottom:0}
.spu-gloss__src{display:block;font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:.05em;color:var(--text-faint);margin-top:var(--space-2)}
@keyframes spu-pop{from{opacity:0;transform:translateY(4px)}to{opacity:1;transform:none}}
.spu-gloss__fn{font-family:var(--font-mono);font-size:.7em;font-weight:600;color:var(--color-accent-strong);vertical-align:super;line-height:0;margin-left:.1em}
.spu-gloss-notes{margin-top:var(--space-6);padding-top:var(--space-4);border-top:1px solid var(--color-border)}
.spu-gloss-notes__h{font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:.08em;color:var(--text-faint);font-weight:600;margin:0 0 var(--space-3)}
.spu-gloss-notes ol{margin:0;padding-left:1.4em;display:flex;flex-direction:column;gap:var(--space-2)}
.spu-gloss-notes li{font-size:var(--fs-caption);color:var(--text-body);line-height:1.5}
.spu-gloss-notes strong{color:var(--text-strong);font-weight:700}
.spu-gloss-notes em{font-family:var(--font-mono);font-style:normal;text-transform:uppercase;letter-spacing:.05em;font-size:var(--fs-eyebrow);color:var(--text-faint);margin-left:.4em}
`);

// Registro de notas para a versão de impressão (deduplicado por termo).
const _glossNotes = [];
function _registerNote(term, definition, source) {
  let idx = _glossNotes.findIndex(n => n.term === term);
  if (idx === -1) {
    _glossNotes.push({
      term,
      definition,
      source
    });
    idx = _glossNotes.length - 1;
  }
  return idx + 1;
}
function _safeGlossaryDefinition(value) {
  if (typeof document === 'undefined') return '';
  const box = document.createElement('div');
  box.innerHTML = String(value || '');
  const allowed = new Set(['STRONG', 'B', 'EM', 'I', 'BR', 'P', 'SPAN']);
  Array.from(box.querySelectorAll('*')).forEach(el => {
    if (el.tagName === 'SCRIPT' || el.tagName === 'STYLE') {
      el.remove();
      return;
    }
    if (!allowed.has(el.tagName)) {
      el.replaceWith(...Array.from(el.childNodes));
      return;
    }
    Array.from(el.attributes).forEach(attr => el.removeAttribute(attr.name));
  });
  return box.innerHTML;
}
function GlossaryTerm({
  children,
  term,
  definition,
  source,
  className,
  style
}) {
  const [open, setOpen] = React.useState(false);
  const ref = React.useRef(null);
  const triggerRef = React.useRef(null);

  // —— Impressão: termo vira nota de rodapé ——
  if (__ds_scope.isPrint()) {
    const n = _registerNote(term, definition, source);
    return React.createElement('span', {
      className: __ds_scope.cx('spu-gloss', className),
      style
    }, children || term, React.createElement('sup', {
      className: 'spu-gloss__fn'
    }, n));
  }
  React.useEffect(() => {
    if (!open) return;
    const onDoc = e => {
      if (ref.current && !ref.current.contains(e.target)) setOpen(false);
    };
    const onKey = e => e.key === 'Escape' && setOpen(false);
    document.addEventListener('mousedown', onDoc);
    document.addEventListener('keydown', onKey);
    return () => {
      document.removeEventListener('mousedown', onDoc);
      document.removeEventListener('keydown', onKey);
    };
  }, [open]);
  React.useLayoutEffect(() => {
    if (!open || !triggerRef.current) return;
    const pop = document.createElement('span');
    pop.className = 'spu-gloss__pop';
    pop.setAttribute('role', 'tooltip');
    const popTerm = document.createElement('p');
    popTerm.className = 'spu-gloss__term';
    popTerm.textContent = term;
    const popDef = document.createElement('div');
    popDef.className = 'spu-gloss__def';
    popDef.innerHTML = _safeGlossaryDefinition(definition);
    pop.append(popTerm, popDef);
    if (source) {
      const popSource = document.createElement('span');
      popSource.className = 'spu-gloss__src';
      popSource.textContent = source;
      pop.appendChild(popSource);
    }
    document.body.appendChild(pop);
    const place = () => {
      const r = triggerRef.current.getBoundingClientRect();
      const width = Math.min(320, window.innerWidth - 24);
      const left = Math.min(Math.max(12, r.left), window.innerWidth - width - 12);
      const below = r.top < 150 && window.innerHeight - r.bottom > r.top;
      const arrow = Math.min(Math.max(18, r.left + r.width / 2 - left), width - 18);
      pop.style.left = left + 'px';
      pop.style.width = width + 'px';
      pop.style.top = below ? r.bottom + 10 + 'px' : 'auto';
      pop.style.bottom = below ? 'auto' : window.innerHeight - r.top + 10 + 'px';
      pop.style.setProperty('--spu-gloss-arrow', arrow + 'px');
      pop.dataset.placement = below ? 'bottom' : 'top';
    };
    place();
    window.addEventListener('resize', place);
    window.addEventListener('scroll', place, true);
    return () => {
      window.removeEventListener('resize', place);
      window.removeEventListener('scroll', place, true);
      pop.remove();
    };
  }, [open, term, definition, source]);
  return React.createElement('span', {
    className: __ds_scope.cx('spu-gloss', open && 'spu-gloss--open', className),
    style,
    ref
  }, React.createElement('button', {
    ref: triggerRef,
    type: 'button',
    className: 'spu-gloss__trigger',
    onClick: () => setOpen(o => !o),
    'aria-expanded': open
  }, children || term));
}

// Lista de notas de rodapé (só na impressão). Renderize ao fim do conteúdo.
function GlossaryFootnotes({
  title = 'Notas',
  className,
  style
}) {
  if (!__ds_scope.isPrint() || _glossNotes.length === 0) return null;
  return React.createElement('section', {
    className: __ds_scope.cx('spu-gloss-notes', className),
    style
  }, React.createElement('p', {
    className: 'spu-gloss-notes__h'
  }, title), React.createElement('ol', null, _glossNotes.map((nt, i) => React.createElement('li', {
    key: i
  }, React.createElement('strong', null, nt.term), ' — ', React.createElement(__ds_scope.RichText, {
    html: nt.definition,
    as: 'div'
  }), nt.source && React.createElement('em', null, nt.source)))));
}
Object.assign(__ds_scope, { GlossaryTerm, GlossaryFootnotes });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/interactive/GlossaryTerm.jsx", error: String((e && e.message) || e) }); }

// components/content/RichText.jsx
try { (() => {
// Mapas de paleta — o builder oferece SÓ estas chaves (cor/destaque dentro dos tokens).
const HL = {
  ochre: 'var(--ochre-100)',
  terra: 'var(--terra-100)',
  petrol: 'var(--petrol-100)',
  green: 'var(--green-100)',
  sand: 'var(--sand-100)'
};
const COLOR = {
  petrol: 'var(--petrol-700)',
  terra: 'var(--terra-700)',
  ochre: 'var(--ochre-700)',
  green: 'var(--green-700)',
  ink: 'var(--ink-900)',
  muted: 'var(--text-muted)',
  // claros/vívidos (para fundo escuro)
  light: 'var(--text-on-dark)',
  'ochre-light': 'var(--ochre-300)',
  'terra-light': 'var(--terra-300)',
  'petrol-light': 'var(--petrol-300)',
  'green-light': 'var(--green-400)'
};
__ds_scope.injectCss('spu-richtext-css', `
.spu-richtext{color:inherit;line-height:var(--lh-body)}
.spu-richtext>:first-child{margin-top:0}
.spu-richtext>:last-child{margin-bottom:0}
.spu-richtext p{margin:0 0 var(--flow-text)}
.spu-richtext strong,.spu-richtext b{font-weight:var(--fw-bold);color:var(--text-strong)}
.spu-richtext em,.spu-richtext i{font-style:italic}
.spu-richtext a{color:var(--color-link);text-decoration-color:var(--color-border-strong);text-underline-offset:.16em}
.spu-richtext a:hover{color:var(--color-link-hover)}
/* Link renderizado como botão do DS (data-btn=primary|secondary|ghost) */
.spu-richtext a[data-btn]{display:inline-flex;align-items:center;gap:.5em;text-decoration:none;font-family:var(--font-display);font-weight:600;font-size:var(--fs-small);line-height:1;padding:.7em 1.2em;border-radius:var(--radius-pill);border:1.5px solid transparent;transition:filter var(--dur-fast) var(--ease-out),background var(--dur-fast) var(--ease-out)}
.spu-richtext a[data-btn]:hover{filter:brightness(1.06)}
.spu-richtext a[data-btn="primary"]{background:var(--color-primary);color:var(--text-on-dark);border-color:var(--color-primary)}
.spu-richtext a[data-btn="secondary"]{background:transparent;color:var(--color-primary-strong);border-color:var(--color-primary)}
.spu-richtext a[data-btn="ghost"]{background:var(--color-primary-soft);color:var(--color-primary-strong);border-color:transparent}
.spu-richtext ul,.spu-richtext ol{margin:0 0 var(--flow-text);padding-left:1.4em;display:flex;flex-direction:column;gap:.3em}
.spu-richtext li{padding-left:.15em}
.spu-richtext ul li::marker{color:var(--color-primary)}
.spu-richtext ol li::marker{font-family:var(--font-mono);color:var(--color-primary);font-size:.9em}
/* Listas visuais dentro de texto rico: <ol|ul data-list="marker"> */
.spu-richtext ol[data-list="marker"],
.spu-richtext ul[data-list="marker"]{list-style:none;margin:var(--flow-text) 0;padding:0;display:flex;flex-direction:column;gap:var(--space-3);counter-reset:spu-marker}
.spu-richtext ol[data-list="marker"]>li,
.spu-richtext ul[data-list="marker"]>li{position:relative;padding-left:3.8rem;min-height:2.8rem}
.spu-richtext ol[data-list="marker"]>li{counter-increment:spu-marker}
.spu-richtext ol[data-list="marker"]>li::before{content:counter(spu-marker);position:absolute;left:0;top:.1em;width:2.6rem;height:2.6rem;border-radius:999px;background:var(--color-primary);color:var(--color-on-primary);display:grid;place-items:center;font-family:var(--font-mono);font-weight:700}
.spu-richtext ul[data-list="marker"]>li::before,
.spu-richtext ul[data-list="marker"]>li::marker{content:none}
.spu-richtext ul[data-list="marker"]>li::before{content:"";position:absolute;left:.35rem;top:.45em;width:2rem;height:2rem;border-radius:999px;background:var(--color-primary)}
/* Highlight colorido (mark) — preso à paleta via data-hl */
.spu-richtext mark{background:var(--ochre-100);color:inherit;padding:.02em .22em;border-radius:4px}
.spu-richtext mark[data-hl="terra"]{background:var(--terra-100)}
.spu-richtext mark[data-hl="petrol"]{background:var(--petrol-100)}
.spu-richtext mark[data-hl="green"]{background:var(--green-100)}
.spu-richtext mark[data-hl="sand"]{background:var(--sand-100)}
/* Cor de texto presa à paleta via data-color */
.spu-richtext [data-color="terra"]{color:var(--terra-700)}
.spu-richtext [data-color="petrol"]{color:var(--petrol-700)}
.spu-richtext [data-color="ochre"]{color:var(--ochre-700)}
.spu-richtext [data-color="green"]{color:var(--green-700)}
.spu-richtext [data-color="muted"]{color:var(--text-muted)}
.spu-richtext [data-color="light"]{color:var(--text-on-dark)}
.spu-richtext [data-color="ochre-light"]{color:var(--ochre-300)}
.spu-richtext [data-color="terra-light"]{color:var(--terra-300)}
.spu-richtext [data-color="petrol-light"]{color:var(--petrol-300)}
.spu-richtext [data-color="green-light"]{color:var(--green-400)}
/* Família e tamanho aplicáveis a trechos selecionados. */
.spu-richtext [data-font="display"]{font-family:var(--font-display)}
.spu-richtext [data-font="body"]{font-family:var(--font-body)}
.spu-richtext [data-font="serif"]{font-family:var(--font-serif)}
.spu-richtext [data-font="mono"]{font-family:var(--font-mono)}
.spu-richtext [data-fs="eyebrow"]{font-family:var(--font-mono);font-size:var(--fs-eyebrow);font-weight:600;line-height:1.3;letter-spacing:var(--ls-eyebrow);text-transform:uppercase}
.spu-richtext [data-fs="caption"]{font-size:var(--fs-caption)}
.spu-richtext [data-fs="small"]{font-size:var(--fs-small)}
.spu-richtext [data-fs="body"]{font-size:var(--fs-body)}
.spu-richtext [data-fs="body-lg"]{font-size:var(--fs-body-lg)}
.spu-richtext [data-fs="h6"],.spu-richtext [data-fs="h5"],.spu-richtext [data-fs="h4"],.spu-richtext [data-fs="h3"],.spu-richtext [data-fs="h2"],.spu-richtext [data-fs="display"]{font-family:var(--font-display);font-weight:700;line-height:var(--lh-heading);letter-spacing:var(--ls-heading);text-transform:none}
.spu-richtext [data-fs="h6"]{font-size:var(--fs-h6)}
.spu-richtext [data-fs="h5"]{font-size:var(--fs-h5)}
.spu-richtext [data-fs="h4"]{font-size:var(--fs-h4)}
.spu-richtext [data-fs="h3"]{font-size:var(--fs-h3)}
.spu-richtext [data-fs="h2"]{font-size:var(--fs-h2)}
.spu-richtext [data-fs="display"]{font-size:var(--fs-display);line-height:var(--lh-tight);letter-spacing:var(--ls-display, var(--ls-heading))}
/* Termo de glossário (marcação) */
.spu-richtext [data-term]{text-decoration:underline dotted;text-underline-offset:.2em;text-decoration-color:var(--color-primary);cursor:help}
/* Variante inline (campos de uma linha: legenda, título) */
.spu-richtext--inline,.spu-richtext--inline p{display:inline;margin:0}
/* Marcador de nota de link (só no print) */
.spu-richtext__fn{font-family:var(--font-mono);font-size:.7em;font-weight:600;color:var(--color-accent-strong);vertical-align:super;line-height:0;margin-left:.1em}
/* Notas de links ao fim do documento impresso */
.spu-richlinks{margin-top:var(--space-6);padding-top:var(--space-4);border-top:1px solid var(--color-border)}
.spu-richlinks__h{font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:.08em;color:var(--text-faint);font-weight:600;margin:0 0 var(--space-3)}
.spu-richlinks ol{margin:0;padding-left:1.4em;display:flex;flex-direction:column;gap:var(--space-2)}
.spu-richlinks li{font-size:var(--fs-caption);color:var(--text-body);line-height:1.5}
.spu-richlinks__url{font-family:var(--font-mono);font-size:var(--fs-eyebrow);color:var(--text-faint);word-break:break-all}
`);

// —— Parser leve de HTML → React ——
// Em vez de injetar o HTML cru (que deixa termos de glossário apenas
// sublinhados e fora do GlossaryFootnotes no PDF), convertemos a string em
// nós React, preservando as tags permitidas e transformando
//   <span data-term="Termo" title="Definição">Termo</span>
// no componente <GlossaryTerm>, que ganha popover clicável no HTML e nota de
// rodapé no print. Tags fora da lista são "desembrulhadas" (mantém-se o texto).

const VOID_TAGS = {
  br: 'br',
  hr: 'hr',
  img: 'img'
};
const ALLOWED = {
  p: 'p',
  strong: 'strong',
  b: 'b',
  em: 'em',
  i: 'i',
  u: 'u',
  s: 's',
  a: 'a',
  ul: 'ul',
  ol: 'ol',
  li: 'li',
  mark: 'mark',
  span: 'span',
  sub: 'sub',
  sup: 'sup',
  code: 'code',
  small: 'small',
  abbr: 'abbr',
  blockquote: 'blockquote',
  br: 'br',
  hr: 'hr',
  h1: 'h1',
  h2: 'h2',
  h3: 'h3',
  h4: 'h4',
  h5: 'h5',
  h6: 'h6'
};

// Registro de links de texto rico (para notas no fim do documento impresso).
const _richLinks = [];
function _registerLink(label, href) {
  let idx = _richLinks.findIndex(l => l.href === href && l.label === label);
  if (idx === -1) {
    _richLinks.push({
      label,
      href
    });
    idx = _richLinks.length - 1;
  }
  return idx + 1;
}
function _styleToObj(str) {
  const obj = {};
  String(str).split(';').forEach(decl => {
    const i = decl.indexOf(':');
    if (i === -1) return;
    const prop = decl.slice(0, i).trim();
    const val = decl.slice(i + 1).trim();
    if (!prop) return;
    const key = prop.replace(/-([a-z])/g, (_, c) => c.toUpperCase());
    obj[key] = val;
  });
  return obj;
}
function _attrs(el, key) {
  const props = {
    key
  };
  for (const a of Array.from(el.attributes)) {
    const name = a.name;
    if (name === 'class') props.className = a.value;else if (name === 'for') props.htmlFor = a.value;else if (name === 'style') props.style = _styleToObj(a.value);else props[name] = a.value;
  }
  return props;
}
function _convert(node, key) {
  if (node.nodeType === 3) return node.nodeValue; // texto
  if (node.nodeType !== 1) return null; // comentários etc.
  const tag = node.tagName.toLowerCase();
  const kids = _children(node);

  // Termo de glossário → componente real (popover + nota de rodapé)
  const term = node.getAttribute && node.getAttribute('data-term');
  if (term) {
    return React.createElement(__ds_scope.GlossaryTerm, {
      key,
      term,
      definition: node.getAttribute('data-definition') || node.getAttribute('title') || node.getAttribute('data-def') || '',
      source: node.getAttribute('data-source') || undefined
    }, kids.length ? kids : term);
  }

  // Link: no print, registra nota numerada; mantém o link no HTML.
  if (tag === 'a') {
    const href = node.getAttribute('href') || '';
    const props = _attrs(node, key);
    if (/^https?:/i.test(href) && __ds_scope.isPrint()) {
      const label = (node.textContent || href).trim();
      const n = _registerLink(label, href);
      return React.createElement('a', props, ...kids, React.createElement('sup', {
        className: 'spu-richtext__fn'
      }, n));
    }
    return React.createElement('a', props, ...kids);
  }
  if (VOID_TAGS[tag]) return React.createElement(VOID_TAGS[tag], _attrs(node, key));
  if (ALLOWED[tag]) return React.createElement(ALLOWED[tag], _attrs(node, key), ...kids);

  // Tag não permitida: desembrulha (mantém os filhos).
  return React.createElement(React.Fragment, {
    key
  }, ...kids);
}
function _children(node) {
  return Array.from(node.childNodes).map((c, i) => _convert(c, i)).filter(c => c != null);
}
function parseRich(html) {
  if (typeof document === 'undefined' || typeof DOMParser === 'undefined') return null;
  try {
    const doc = new DOMParser().parseFromString('<body>' + html + '</body>', 'text/html');
    return _children(doc.body);
  } catch (e) {
    return null;
  }
}

// Renderiza um valor de rich-text (string HTML produzida pelo builder) com a
// tipografia e a paleta do DS. Marks suportadas: <strong> <em> <a href>
// <ul>/<ol> <mark data-hl="…"> <span data-color="…"> <span data-term="…">.
function RichText({
  html,
  children,
  as = 'div',
  className,
  style
}) {
  if (html != null) {
    const parsed = parseRich(html);
    if (parsed) {
      return React.createElement(as, {
        className: __ds_scope.cx('spu-richtext', className),
        style
      }, ...parsed);
    }
    // Fallback (sem DOM disponível): injeção crua.
    return React.createElement(as, {
      className: __ds_scope.cx('spu-richtext', className),
      style,
      dangerouslySetInnerHTML: {
        __html: html
      }
    });
  }
  return React.createElement(as, {
    className: __ds_scope.cx('spu-richtext', className),
    style
  }, children);
}

// Notas de links de textos ricos (só na impressão). Renderize ao fim do conteúdo,
// ao lado do <GlossaryFootnotes/>.
function RichTextLinkNotes({
  title = 'Links',
  className,
  style
}) {
  if (!__ds_scope.isPrint() || _richLinks.length === 0) return null;
  return React.createElement('section', {
    className: __ds_scope.cx('spu-richlinks', className),
    style
  }, React.createElement('p', {
    className: 'spu-richlinks__h'
  }, title), React.createElement('ol', null, _richLinks.map((l, i) => React.createElement('li', {
    key: i
  }, l.label, ' — ', React.createElement('span', {
    className: 'spu-richlinks__url'
  }, l.href)))));
}

// Paletas oferecidas (para o builder construir os controles de formatação).
RichText.HIGHLIGHTS = HL;
RichText.COLORS = COLOR;

// Helper de compatibilidade: um campo editável pode chegar como **string HTML**
// (vinda do builder) ou como ReactNode (uso em código/JSX). Strings passam pelo
// RichText; ReactNode é devolvido como está. `inline` para campos de uma linha.
function renderRich(value, opts) {
  const o = opts || {};
  if (!hasRichContent(value)) return null;
  if (typeof value === 'string') {
    return React.createElement(RichText, {
      html: value,
      as: o.inline ? 'span' : 'div',
      className: __ds_scope.cx(o.inline && 'spu-richtext--inline', o.className),
      style: o.style
    });
  }
  return value;
}
function hasRichContent(value) {
  if (value == null) return false;
  if (typeof value !== 'string') return true;
  const visible = value.replace(/<!--[\s\S]*?-->/g, '').replace(/<br\s*\/?>/gi, '').replace(/<[^>]*>/g, '').replace(/(?:&nbsp;|&#160;|&#x0*a0;|\u00a0)/gi, ' ').replace(/[\s\u00a0\u200b-\u200d\ufeff]/g, '');
  return visible.length > 0;
}
Object.assign(__ds_scope, { RichText, RichTextLinkNotes, renderRich, hasRichContent });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/RichText.jsx", error: String((e && e.message) || e) }); }

// components/content/Callout.jsx
try { (() => {
__ds_scope.injectCss('spu-callout-css', `
.spu-callout__title{font-family:var(--font-mono);font-size:var(--fs-eyebrow);font-weight:600;letter-spacing:.1em;text-transform:uppercase;line-height:1.3;margin:0}
.spu-callout__body{color:var(--text-body)}
.spu-callout__body>:last-child{margin-bottom:0}
.spu-callout__heading{font-family:var(--font-display);font-weight:700;font-size:var(--fs-h4);color:var(--_c);margin:0 0 var(--space-3)}

/* —— variant "header": faixa sólida no topo + corpo branco —— */
.spu-callout--header{border-radius:var(--radius-md);overflow:hidden;border:1px solid var(--color-border);box-shadow:var(--shadow-sm)}
.spu-callout--header .spu-callout__hd{background:var(--_c);color:#fff;display:flex;align-items:center;gap:.55em;padding:.72em 1.1em}
.spu-callout--header .spu-callout__hd>svg{flex:0 0 auto}
.spu-callout--header .spu-callout__title{color:#fff;flex:1;min-width:0}
.spu-callout--header .spu-callout__bd{background:var(--color-surface);padding:1em 1.15em}

/* —— variant "ear": orelha de ícone lateral + corpo branco —— */
.spu-callout--ear{display:flex;background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-md);overflow:hidden}
.spu-callout--ear .spu-callout__ear{flex:0 0 auto;width:50px;display:flex;align-items:center;justify-content:center;background:var(--_ear);color:var(--_eic)}
.spu-callout--ear.is-soft .spu-callout__ear{border-right:1px solid var(--color-border)}
.spu-callout--ear .spu-callout__bd{padding:.95em 1.15em;min-width:0}
.spu-callout--ear .spu-callout__title{color:var(--_c);margin-bottom:.3em}
`);
function Callout({
  children,
  tone = 'info',
  title,
  heading,
  icon,
  color,
  variant = 'ear',
  emphasis = 'soft',
  className,
  style
}) {
  const t = __ds_scope.TONES[tone] || __ds_scope.TONES.info;
  const c = color || t.color;
  const ic = icon || t.icon;
  const headingEl = heading && React.createElement('h4', {
    className: 'spu-callout__heading'
  }, __ds_scope.renderRich(heading, {
    inline: true
  }));
  if (variant === 'header') {
    return React.createElement('div', {
      className: __ds_scope.cx('spu-callout', 'spu-callout--header', className),
      style: {
        '--_c': c,
        ...style
      }
    }, React.createElement('div', {
      className: 'spu-callout__hd'
    }, React.createElement(__ds_scope.Icon, {
      name: ic,
      size: 18
    }), title && React.createElement('p', {
      className: 'spu-callout__title'
    }, title)), React.createElement('div', {
      className: 'spu-callout__bd'
    }, React.createElement('div', {
      className: 'spu-callout__body'
    }, headingEl, __ds_scope.renderRich(children))));
  }

  // variant "ear"
  const strong = emphasis === 'strong';
  const ear = strong ? c : color ? `color-mix(in srgb, ${color} 14%, var(--color-surface))` : t.soft;
  const eic = strong ? '#fff' : c;
  return React.createElement('div', {
    className: __ds_scope.cx('spu-callout', 'spu-callout--ear', strong ? 'is-strong' : 'is-soft', className),
    style: {
      '--_c': c,
      '--_ear': ear,
      '--_eic': eic,
      ...style
    }
  }, React.createElement('div', {
    className: 'spu-callout__ear'
  }, React.createElement(__ds_scope.Icon, {
    name: ic,
    size: 22
  })), React.createElement('div', {
    className: 'spu-callout__bd'
  }, title && React.createElement('p', {
    className: 'spu-callout__title'
  }, title), React.createElement('div', {
    className: 'spu-callout__body'
  }, headingEl, __ds_scope.renderRich(children))));
}
Object.assign(__ds_scope, { Callout });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/Callout.jsx", error: String((e && e.message) || e) }); }

// components/content/ExampleCard.jsx
try { (() => {
__ds_scope.injectCss('spu-examplecard-css', `
.spu-examplecard{background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-lg);overflow:hidden;margin:var(--flow-block) 0;box-shadow:var(--shadow-sm)}
.spu-examplecard__hd{display:flex;align-items:center;gap:.55em;padding:.72em 1.1em;background:var(--_ec, var(--color-primary));color:#fff;font-family:var(--font-mono);font-size:var(--fs-eyebrow);font-weight:600;letter-spacing:.1em;text-transform:uppercase;line-height:1.3}
.spu-examplecard__hd svg{flex:0 0 auto}
.spu-examplecard__details>.spu-examplecard__hd{cursor:pointer;list-style:none}
.spu-examplecard__details>.spu-examplecard__hd::-webkit-details-marker{display:none}
.spu-examplecard__details>.spu-examplecard__hd::after{content:"";width:9px;height:9px;border-right:2px solid currentColor;border-bottom:2px solid currentColor;transform:rotate(45deg);transition:transform var(--dur) var(--ease-out);margin-left:auto;margin-right:.25em;flex:0 0 auto}
.spu-examplecard__details[open]>.spu-examplecard__hd::after{transform:rotate(-135deg)}
.spu-examplecard__cover{display:block;width:100%}
.spu-examplecard__cover img,.spu-examplecard__cover image-slot{display:block;width:100%}
.spu-examplecard__body{padding:clamp(1.4rem,3vw,2.2rem)}
.spu-examplecard__title{margin:0 0 var(--space-3)}
.spu-examplecard__body>:last-child{margin-bottom:0}
@media print{.spu-examplecard__details:not([open])>.spu-examplecard__content{display:block!important}.spu-examplecard__details>.spu-examplecard__hd::after{display:none}}
`);
function ExampleCard({
  children,
  label = 'Exemplo prático',
  icon = 'map-pin',
  color,
  title,
  src,
  slot,
  alt = '',
  coverHeight = 300,
  fit = 'cover',
  placeholder = 'Foto de capa',
  collapse = 'none',
  className,
  style
}) {
  const printing = __ds_scope.isPrint();
  let cover = null;
  if (slot) {
    cover = React.createElement('image-slot', {
      id: slot,
      shape: 'rect',
      fit,
      placeholder,
      style: {
        width: '100%',
        height: typeof coverHeight === 'number' ? `${coverHeight}px` : coverHeight,
        display: 'block'
      }
    });
  } else if (src) {
    cover = React.createElement('img', {
      src,
      alt
    });
  }
  const headerContent = [React.createElement(__ds_scope.Icon, {
    key: 'icon',
    name: icon,
    size: 18
  }), React.createElement('span', {
    key: 'label'
  }, label)];
  const content = React.createElement('div', {
    className: 'spu-examplecard__content'
  }, cover && React.createElement('div', {
    className: 'spu-examplecard__cover'
  }, cover), React.createElement('div', {
    className: 'spu-examplecard__body'
  }, title && React.createElement('h3', {
    className: 'spu-examplecard__title'
  }, title), __ds_scope.renderRich(children)));
  const collapsible = !printing && (collapse === 'open' || collapse === 'closed');
  return React.createElement('div', {
    className: __ds_scope.cx('spu-examplecard', className),
    style: {
      ...(color ? {
        '--_ec': color
      } : null),
      ...style
    }
  }, collapsible ? React.createElement('details', {
    className: 'spu-examplecard__details',
    open: collapse === 'open'
  }, React.createElement('summary', {
    className: 'spu-examplecard__hd'
  }, headerContent), content) : [React.createElement('div', {
    key: 'header',
    className: 'spu-examplecard__hd'
  }, headerContent), React.cloneElement(content, {
    key: 'content'
  })]);
}
Object.assign(__ds_scope, { ExampleCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/ExampleCard.jsx", error: String((e && e.message) || e) }); }

// components/content/FeatureGrid.jsx
try { (() => {
__ds_scope.injectCss('spu-features-css', `
.spu-features{display:grid;grid-template-columns:repeat(var(--spu-feature-columns,3),minmax(0,1fr));gap:var(--space-6)}
@media(max-width:900px){.spu-features[data-columns="3"],.spu-features[data-columns="4"]{grid-template-columns:repeat(2,minmax(0,1fr))}}
@media(max-width:640px){.spu-features{grid-template-columns:1fr}}
.spu-feature__icon{width:48px;height:48px;border-radius:var(--radius-md);background:var(--color-primary-soft);color:var(--color-primary-strong);display:flex;align-items:center;justify-content:center;margin-bottom:var(--space-3)}
.spu-feature__title{font-family:var(--font-display);font-weight:700;font-size:var(--fs-h6);line-height:1.25;margin:0 0 .3em;color:var(--text-strong)}
.spu-feature__text{color:var(--text-muted);font-size:var(--fs-small);margin:0}
.spu-feature--card{background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-md);padding:var(--space-5);box-shadow:var(--shadow-sm)}
.spu-feature--side{display:grid;grid-template-columns:48px minmax(0,1fr);column-gap:var(--space-4);align-items:start}
.spu-feature--side .spu-feature__icon{grid-row:1 / span 2;margin-bottom:0}
.spu-feature--side .spu-feature__title{grid-column:2}
.spu-feature--side .spu-feature__text{grid-column:2}
`);
function FeatureGrid({
  items = [],
  columns,
  card = false,
  accent,
  layout = 'top',
  className,
  style
}) {
  // Respeita a quantidade escolhida em telas largas. As media queries reduzem
  // grades maiores no tablet e todas as grades para uma coluna no celular.
  const columnCount = Math.max(1, Math.min(4, Math.round(Number(columns) || 3)));
  const gridStyle = {
    '--spu-feature-columns': columnCount,
    ...style
  };
  return React.createElement('div', {
    className: __ds_scope.cx('spu-features', className),
    'data-columns': columnCount,
    style: gridStyle
  }, items.map((it, i) => React.createElement('div', {
    key: i,
    className: __ds_scope.cx('spu-feature', card && 'spu-feature--card', layout === 'side' && 'spu-feature--side')
  }, React.createElement('div', {
    className: 'spu-feature__icon',
    style: accent ? {
      background: `color-mix(in srgb, ${accent} 14%, transparent)`,
      color: accent
    } : undefined
  }, React.createElement(__ds_scope.Icon, {
    name: it.icon || 'sparkles',
    size: 24
  })), __ds_scope.hasRichContent(it.title) && React.createElement('p', {
    className: 'spu-feature__title'
  }, __ds_scope.renderRich(it.title, {
    inline: true
  })), it.text && React.createElement('p', {
    className: 'spu-feature__text'
  }, __ds_scope.renderRich(it.text, {
    inline: true
  })))));
}
Object.assign(__ds_scope, { FeatureGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/FeatureGrid.jsx", error: String((e && e.message) || e) }); }

// components/content/MarkerList.jsx
try { (() => {
__ds_scope.injectCss('spu-mlist-css', `
.spu-mlist{list-style:none;margin:var(--flow-text) 0;padding:0;display:flex;flex-direction:column;gap:var(--space-2)}
.spu-mlist__item{display:flex;gap:var(--space-3);align-items:flex-start}
.spu-mlist__num{flex:0 0 auto;width:30px;height:30px;border-radius:var(--radius-pill);background:var(--_ml-color,var(--color-primary));color:var(--color-on-primary);font-family:var(--font-mono);font-size:.85rem;font-weight:600;display:flex;align-items:center;justify-content:center;margin-top:1px}
.spu-mlist__check{flex:0 0 auto;width:26px;height:26px;border-radius:var(--radius-pill);background:var(--_ml-soft,var(--status-success-soft));color:var(--_ml-color,var(--status-success));display:flex;align-items:center;justify-content:center;margin-top:2px}
.spu-mlist__icon{flex:0 0 auto;width:30px;height:30px;border-radius:var(--radius-pill);background:var(--_ml-soft,var(--color-primary-soft));color:var(--_ml-color,var(--color-primary-strong));display:flex;align-items:center;justify-content:center;margin-top:1px}
.spu-mlist__dot{flex:0 0 auto;width:6px;height:6px;border-radius:50%;background:var(--_ml-color,var(--color-accent));margin-top:.58em}
.spu-mlist__body{color:var(--text-body);line-height:var(--lh-snug)}
.spu-mlist__title{display:block;font-weight:700;color:var(--text-strong);margin-bottom:.2rem;margin-top:.3rem}
`);
function MarkerList({
  items = [],
  variant = 'ordered',
  icon = 'check',
  accent,
  className,
  style
}) {
  const ordered = variant === 'ordered';
  const Tag = ordered ? 'ol' : 'ul';
  return React.createElement(Tag, {
    className: __ds_scope.cx('spu-mlist', className),
    style: {
      ...(accent ? {
        '--_ml-color': accent,
        '--_ml-soft': `color-mix(in srgb, ${accent} 14%, transparent)`
      } : null),
      ...style
    }
  }, items.map((it, i) => {
    const obj = typeof it === 'string' ? {
      text: it
    } : it;
    let marker;
    if (variant === 'ordered') marker = React.createElement('span', {
      className: 'spu-mlist__num'
    }, i + 1);else if (variant === 'check') marker = React.createElement('span', {
      className: 'spu-mlist__check'
    }, React.createElement(__ds_scope.Icon, {
      name: 'check',
      size: 16
    }));else if (variant === 'icon') marker = React.createElement('span', {
      className: 'spu-mlist__icon'
    }, React.createElement(__ds_scope.Icon, {
      name: icon || 'check',
      size: 16
    }));else marker = React.createElement('span', {
      className: 'spu-mlist__dot'
    });
    return React.createElement('li', {
      key: i,
      className: 'spu-mlist__item'
    }, marker, React.createElement('div', {
      className: 'spu-mlist__body'
    }, obj.title && React.createElement('strong', {
      className: 'spu-mlist__title'
    }, __ds_scope.renderRich(obj.title, {
      inline: true
    })), __ds_scope.renderRich(obj.text, {
      inline: true
    })));
  }));
}
Object.assign(__ds_scope, { MarkerList });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/MarkerList.jsx", error: String((e && e.message) || e) }); }

// components/content/Masthead.jsx
try { (() => {
const isRichHtml = value => typeof value === 'string' && /[<&]/.test(value);
function richInline(value) {
  return isRichHtml(value) ? React.createElement(__ds_scope.RichText, {
    html: value,
    as: 'span',
    className: 'spu-richtext--inline'
  }) : value;
}
__ds_scope.injectCss('spu-masthead-css', `
.spu-masthead{background:var(--petrol-800);color:var(--text-on-dark);position:relative;z-index:5}
.spu-masthead__inner{max-width:var(--container-wide);margin:0 auto;padding:clamp(.8rem,1.8vw,1.05rem) var(--gutter);display:flex;align-items:baseline;gap:clamp(.55rem,1.6vw,1.1rem);flex-wrap:wrap}
.spu-masthead__item{font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:var(--ls-eyebrow);color:var(--ochre-300);font-weight:500;margin:0}
.spu-masthead__org{white-space:nowrap}
.spu-masthead__sep{color:rgba(246,239,227,.38)}
@media (max-width:640px){.spu-masthead__sep{display:none}}
`);
function Masthead({
  org = 'SPU',
  program,
  children,
  className,
  style
}) {
  return React.createElement('header', {
    className: __ds_scope.cx('spu-masthead', className),
    style
  }, React.createElement('div', {
    className: 'spu-masthead__inner'
  }, org && React.createElement('span', {
    className: 'spu-masthead__item spu-masthead__org'
  }, richInline(org)), org && program && React.createElement('span', {
    className: 'spu-masthead__sep',
    'aria-hidden': 'true'
  }, '·'), program && React.createElement('span', {
    className: 'spu-masthead__item'
  }, richInline(program)), children));
}
Object.assign(__ds_scope, { Masthead });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/Masthead.jsx", error: String((e && e.message) || e) }); }

// components/content/PageFooter.jsx
try { (() => {
const richInline = value => __ds_scope.renderRich(value, {
  inline: true
});
__ds_scope.injectCss('spu-pagefooter-css', `
.spu-pagefooter{background:var(--petrol-800);background-image:var(--texture-topo);background-size:420px;color:var(--text-on-dark);padding:var(--space-12) var(--gutter)}
.spu-pagefooter__inner{max-width:var(--container-content);margin:0 auto;display:grid;grid-template-columns:1.4fr 1fr;gap:clamp(1.5rem,5vw,4rem);align-items:start}
.spu-pagefooter__code{font-family:var(--font-mono);font-size:.8rem;letter-spacing:.1em;text-transform:uppercase;color:var(--text-on-dark);opacity:.8;margin:0 0 var(--space-4)}
.spu-pagefooter__context{font-size:var(--fs-small);color:var(--text-on-dark-muted);max-width:46ch;margin:0;line-height:1.6}
.spu-pagefooter__credits{list-style:none;margin:0;padding:0;display:grid;gap:var(--space-2)}
.spu-pagefooter__credits li{font-size:var(--fs-small);color:var(--text-on-dark-muted)}
.spu-pagefooter__credits b{display:block;font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:.08em;color:var(--text-on-dark);font-weight:600;opacity:.7}
.spu-pagefooter__license{max-width:var(--container-content);margin:var(--space-8) auto 0}
@media (max-width:640px){.spu-pagefooter__inner{grid-template-columns:1fr}}
`);
function PageFooter({
  code,
  context,
  credits = [],
  license = true,
  licenseKind = 'byncsa',
  licenseTone = 'dark',
  children,
  className,
  style
}) {
  return React.createElement('footer', {
    className: __ds_scope.cx('spu-pagefooter', className),
    style
  }, React.createElement('div', {
    className: 'spu-pagefooter__inner'
  }, React.createElement('div', null, code && React.createElement('p', {
    className: 'spu-pagefooter__code'
  }, richInline(code)), context && React.createElement('p', {
    className: 'spu-pagefooter__context'
  }, richInline(context)), license && React.createElement(__ds_scope.LicenseBadge, {
    kind: licenseKind,
    tone: licenseTone,
    style: {
      marginTop: 'var(--space-5)'
    }
  })), credits.length > 0 && React.createElement('ul', {
    className: 'spu-pagefooter__credits'
  }, credits.map((c, i) => React.createElement('li', {
    key: i
  }, React.createElement('b', null, richInline(c.role)), richInline(c.name))))), children && React.createElement('div', {
    className: 'spu-pagefooter__license'
  }, children));
}
__ds_scope.injectCss('spu-builder-credit-css', `
.spu-builder-credit{margin:var(--space-8) 0 0;padding:var(--space-3) var(--gutter) var(--space-4);border-top:1px solid var(--color-border);color:var(--text-faint);font-family:var(--font-body);font-size:11px;line-height:1.45;text-align:center}
.spu-pagefooter+.spu-builder-credit{margin-top:0}
.spu-builder-credit a{color:inherit;text-decoration-color:var(--color-border-strong);text-underline-offset:.16em}
.spu-builder-credit a:hover{color:var(--color-primary-strong)}
@media print{.spu-builder-credit{break-inside:avoid;margin-top:6mm;padding-block:2.5mm;font-size:8pt}}
`);
function BuilderCredit({ className, style }) {
  return React.createElement('footer', {
    className: __ds_scope.cx('spu-builder-credit', className),
    style
  }, 'Criado com SPU Builder – uma plataforma personalizada de autoria de objetos HTML/SCORM desenvolvida por ', React.createElement('a', {
    href: 'http://lattes.cnpq.br/6398736649827022',
    target: '_blank',
    rel: 'noopener noreferrer'
  }, 'Luís Henrique Lindner'), ', Dr.');
}
Object.assign(__ds_scope, { PageFooter, BuilderCredit });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/PageFooter.jsx", error: String((e && e.message) || e) }); }

// components/content/Panel.jsx
try { (() => {
__ds_scope.injectCss('spu-panel-css', `
.spu-panel{background:var(--color-surface-warm);background-image:var(--texture-topo);background-size:360px;border:1px solid var(--color-border);border-radius:var(--radius-lg);padding:clamp(1.4rem,3vw,2.2rem);margin:var(--flow-block) 0}
.spu-panel__kicker{margin-bottom:var(--space-3)}
.spu-panel__title{margin:0 0 var(--space-4)}
.spu-panel__body{color:var(--text-body)}
.spu-panel__body+:where(.spu-blockstack){margin-top:var(--space-5)}
.spu-panel>:last-child{margin-bottom:0}
.spu-panel--accent{--_panel-accent:var(--_pc,var(--color-accent));background:color-mix(in srgb,var(--_panel-accent) 9%,var(--color-surface));background-image:none;border-color:color-mix(in srgb,var(--_panel-accent) 30%,var(--color-border));box-shadow:inset 0 0 0 1px color-mix(in srgb,var(--_panel-accent) 10%,transparent)}
.spu-panel--colored .spu-panel__kicker{color:var(--_pc,var(--color-accent))}
.spu-panel--colored .spu-panel__kicker .spu-kicker__rule{background:var(--_pc,var(--color-accent))}
.spu-panel--colored .spu-panel__title{color:color-mix(in srgb,var(--_pc,var(--color-accent)) 78%,var(--text-strong))}
/* Card que extravasa a coluna de leitura (mais largo, centrado na viewport) */
.spu-panel--wide{width:min(var(--container-wide), calc(100vw - 2 * var(--gutter)));margin-left:50%;transform:translateX(-50%)}
/* Faixa full-bleed — largura total (como o hero) */
.spu-panel--feature{border-radius:0;border-inline:none;border-block:1px solid var(--color-border);background-size:420px;padding:0;margin:var(--flow-block) 0;width:100vw;margin-left:calc(50% - 50vw);max-width:none}
.spu-panel--feature .spu-panel__inner{max-width:var(--container-wide);margin:0 auto;padding:clamp(2.5rem,6vw,4.5rem) var(--gutter)}
.spu-panel--feature .spu-panel__inner>:last-child{margin-bottom:0}
`);
function Panel({
  children,
  body,
  variant = 'box',
  wide = false,
  kicker,
  kickerIcon = 'map-pin',
  title,
  color,
  bg,
  className,
  style
}) {
  const bgStyle = bg ? {
    background: bg,
    backgroundImage: 'none'
  } : null;
  const head = [kicker && React.createElement(__ds_scope.Kicker, {
    key: 'k',
    className: 'spu-panel__kicker',
    icon: kickerIcon
  }, kicker), title && React.createElement('h3', {
    key: 't',
    className: 'spu-panel__title'
  }, title)];
  const inner = [...head, body && React.createElement('div', {
    className: 'spu-panel__body'
  }, __ds_scope.renderRich(body)), __ds_scope.renderRich(children)];
  if (variant === 'feature') {
    return React.createElement('div', {
      className: __ds_scope.cx('spu-panel', 'spu-panel--feature', color && 'spu-panel--colored', className),
      style: {
        ...(color ? {
          '--_pc': color
        } : null),
        ...bgStyle,
        ...style
      }
    }, React.createElement('div', {
      className: 'spu-panel__inner'
    }, inner));
  }
  return React.createElement('div', {
    className: __ds_scope.cx('spu-panel', wide && 'spu-panel--wide', variant === 'accent' && 'spu-panel--accent', (color || variant === 'accent') && 'spu-panel--colored', className),
    style: {
      ...(color ? {
        '--_pc': color
      } : null),
      ...bgStyle,
      ...style
    }
  }, inner);
}
Object.assign(__ds_scope, { Panel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/Panel.jsx", error: String((e && e.message) || e) }); }

// components/content/StatBlock.jsx
try { (() => {
const ri = v => __ds_scope.renderRich(v, {
  inline: true
});
__ds_scope.injectCss('spu-stat-css', `
.spu-stats{display:grid;gap:var(--space-6)}
.spu-stat{padding:var(--space-2) 0;min-width:0}
.spu-stat--card{background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-md);padding:var(--space-6);box-shadow:var(--shadow-sm)}
.spu-stat--rule{border-top:var(--border-accent) solid var(--_sc, var(--color-accent));padding-top:var(--space-4)}
.spu-stat__value{font-family:var(--font-display);font-weight:800;font-size:clamp(1.9rem, 4.4vw, var(--fs-h1));line-height:.95;letter-spacing:var(--ls-display);color:var(--_sc, var(--color-primary-strong));overflow-wrap:break-word}
.spu-stat__unit{font-size:.5em;font-weight:700;color:var(--_sc, var(--color-accent));margin-left:.12em}
.spu-stat__label{font-family:var(--font-display);font-weight:700;font-size:var(--fs-h6);color:var(--text-strong);margin:.4em 0 .15em}
.spu-stat__desc{color:var(--text-muted);font-size:var(--fs-small);margin:0}
`);
function StatBlock({
  stats = [],
  columns,
  variant = 'rule',
  className,
  style
}) {
  // Responsivo: `columns` define o número desejado em telas largas, mas as
  // colunas quebram automaticamente (auto-fit) quando não cabem; min() evita
  // overflow em telas muito estreitas (cai para 1 coluna).
  const minw = columns >= 4 ? 150 : columns === 3 ? 175 : columns === 2 ? 220 : 165;
  const gridStyle = {
    gridTemplateColumns: `repeat(auto-fit, minmax(min(${minw}px, 100%), 1fr))`,
    ...style
  };
  return React.createElement('div', {
    className: __ds_scope.cx('spu-stats', className),
    style: gridStyle
  }, stats.map((s, i) => React.createElement('div', {
    key: i,
    className: __ds_scope.cx('spu-stat', variant !== 'plain' && `spu-stat--${variant}`),
    style: s.color ? {
      '--_sc': s.color
    } : undefined
  }, React.createElement('div', {
    className: 'spu-stat__value'
  }, ri(s.value), s.unit && React.createElement('span', {
    className: 'spu-stat__unit'
  }, ri(s.unit))), s.label && React.createElement('div', {
    className: 'spu-stat__label'
  }, ri(s.label)), s.description && React.createElement('p', {
    className: 'spu-stat__desc'
  }, ri(s.description)))));
}
Object.assign(__ds_scope, { StatBlock });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/StatBlock.jsx", error: String((e && e.message) || e) }); }

// components/core/BlockView.jsx
try { (() => {
// Resolve o namespace compilado do DS (window.<Namespace>) em runtime, sem
// acoplar a um nome fixo. Permite ao BlockView achar qualquer componente por nome.
function dsNamespace() {
  if (typeof window === 'undefined') return {};
  const k = Object.keys(window).find(n => /^SPUENAP.*DesignSystem/.test(n));
  return k && window[k] || {};
}

// Renderizador central: lê um bloco serializado (do BlockRegistry) e o pinta
// como o componente real do DS. Um só caminho de render para editor, preview e
// export — o builder nunca diverge da saída. Suporta bloco-filho em Section.
//
//   block = { id, type, props:{…}, (children[] se container) }
//   mode  = 'preview' | 'edit'   (edit habilitará handles/Editable depois)

// `fields` é o contrato do registry para texto editável salvo como HTML.
// O antigo `def.rich` não pode ser usado como trava: vários componentes
// históricos (Figure, PullQuote, Kicker, BleedImage...) já aceitam RichText no
// canvas sem terem recebido essa flag no registry.
function richField(def, key) {
  return def && (def.fields || []).indexOf(key) !== -1;
}

// Campos rich tratados como bloco (multilinha); o resto é inline (uma linha).
const BLOCK_LEVEL = {
  children: 1,
  body: 1,
  html: 1,
  content: 1,
  lead: 1
};

// Rótulos PT-BR para placeholders de campos vazios no modo edit.
const FIELD_PLACEHOLDER = {
  title: 'Título',
  heading: 'Subtítulo',
  caption: 'Legenda',
  credit: 'Crédito (opcional)',
  byline: 'Autoria',
  kicker: 'Sobrelinha',
  cite: 'Fonte',
  text: 'Texto',
  children: 'Conteúdo',
  body: 'Corpo',
  html: 'Texto',
  term: 'Termo',
  definition: 'Definição',
  org: 'Identidade',
  program: 'Nome do programa',
  lead: 'Texto de apresentação',
  triggerLabel: 'Clique para expandir'
};
function BlockView({
  block,
  mode = 'preview',
  onEdit
}) {
  if (!block) return null;
  const def = __ds_scope.BlockRegistry.byType[block.type];
  if (!def) return React.createElement('div', {
    style: {
      padding: 12,
      color: 'var(--status-warning)',
      font: 'var(--font-mono)'
    }
  }, `Bloco desconhecido: ${block.type}`);
  const props = block.props || {};
  const spacingStyle = {
    ...(props.style || {}),
    ...(props.__pullUp ? { marginTop: 'calc(-1 * var(--flow-block))' } : null),
    ...(props.__pullDown ? { marginBottom: 'calc(-1 * var(--flow-block))' } : null)
  };
  const componentProps = {
    ...props,
    style: spacingStyle
  };
  delete componentProps.__pullUp;
  delete componentProps.__pullDown;
  const editing = mode === 'edit';
  const emit = patch => onEdit && onEdit(block, patch);

  // No modo edit, um campo de texto vira <Editable>; senão devolve o valor cru.
  // Campos inline (title, caption, byline…) são renderizados dentro de <p>/<h1>/<span>
  // pelos componentes — por isso o Editable precisa ser <span> ali (um <div> dentro
  // de <p> é HTML inválido e aborta a subárvore no React).
  const fieldNode = (key, inline) => {
    if (!editing) return props[key];
    return React.createElement(__ds_scope.Editable, {
      key,
      html: typeof props[key] === 'string' ? props[key] : '',
      single: !!inline,
      as: inline ? 'span' : 'div',
      placeholder: FIELD_PLACEHOLDER[key] || key,
      onChange: html => emit({
        [key]: html
      })
    });
  };

  // —— Título simples (sem componente) ——
  if (block.type === 'titulo') {
    if (!editing && !__ds_scope.hasRichContent(props.text)) return null;
    const tag = props.level || 'h2';
    return React.createElement(tag, {
      id: block.id,
      className: 'spu-block-title',
      style: spacingStyle
    }, editing ? React.createElement(__ds_scope.Editable, {
      html: props.text || '',
      single: true,
      as: 'span',
      placeholder: FIELD_PLACEHOLDER.title,
      onChange: h => emit({
        text: h
      })
    }) : __ds_scope.renderRich(props.text, {
      inline: true
    }));
  }

  // —— Parágrafo (RichText puro) ——
  if (block.type === 'prose') {
    if (!editing && !__ds_scope.hasRichContent(props.html)) return null;
    return editing ? React.createElement(__ds_scope.Editable, {
      html: typeof props.html === 'string' ? props.html : '',
      style: spacingStyle,
      onChange: h => emit({
        html: h
      })
    }) : React.createElement(__ds_scope.RichText, {
      html: typeof props.html === 'string' ? props.html : undefined,
      style: spacingStyle
    });
  }
  const NS = dsNamespace();
  const Comp = def.component && NS[def.component];
  if (!Comp) return null;

  // —— Container (Section / Conclusion) com filhos ——
  if (def.kind === 'container') {
    const kids = (block.children || []).map(child => React.createElement(BlockView, {
      key: child.id,
      block: child,
      mode,
      onEdit
    }));
    // Campos de texto próprios do container (ex.: Conclusion: kicker/title/body).
    const extra = {};
    (def.fields || []).forEach(k => {
      if (editing) extra[k] = fieldNode(k, !BLOCK_LEVEL[k]);else if (richField(def, k) && typeof props[k] === 'string') extra[k] = __ds_scope.renderRich(props[k], {
        inline: !BLOCK_LEVEL[k]
      });
    });
    if (def.stack === false) {
      // Columns/grade: filhos vão direto como children do componente.
      return React.createElement(Comp, {
        ...componentProps,
        ...extra,
        children: undefined
      }, kids);
    }
    const hasKids = (block.children || []).length > 0;
    return React.createElement(Comp, {
      ...componentProps,
      ...extra,
      children: undefined
    }, hasKids && React.createElement('div', {
      className: 'spu-blockstack',
      style: {
        display: 'flex',
        flexDirection: 'column',
        gap: 'var(--flow-block)'
      }
    }, kids));
  }

  // —— Demais blocos ——
  const resolved = {
    ...componentProps
  };
  if (block.type === 'kicker') {
    resolved.className = __ds_scope.cx(props.className, 'spu-block-kicker');
  }
  (def.fields || []).forEach(k => {
    if (editing) {
      resolved[k] = fieldNode(k, !BLOCK_LEVEL[k]); // todos os fields viram Editable
    } else if (richField(def, k) && typeof resolved[k] === 'string') {
      // Preview/export must resolve every field declared as rich text, not
      // only `children`. Otherwise captions, credits, bylines and similar
      // fields display their HTML markup literally outside edit mode.
      resolved[k] = __ds_scope.renderRich(resolved[k], {
        inline: !BLOCK_LEVEL[k]
      });
    }
  });
  return React.createElement(Comp, resolved);
}

// Renderiza um documento inteiro: { meta, blocks:[…] }.
function BlockDocument({
  doc,
  mode = 'preview',
  onEdit,
  showBuilderCredit = true
}) {
  const blocks = doc && doc.blocks || [];
  const toc = doc && doc.meta && doc.meta.toc;
  return React.createElement(React.Fragment, null, toc && toc.enabled !== false && React.createElement(__ds_scope.PageToc, {
    items: toc.items || [],
    title: toc.title
  }), blocks.map(b => React.createElement(BlockView, {
    key: b.id,
    block: b,
    mode,
    onEdit
  })), showBuilderCredit && (!doc || !doc.meta || doc.meta.builderCredit !== false) && React.createElement(__ds_scope.BuilderCredit));
}
Object.assign(__ds_scope, { BlockView, BlockDocument });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/BlockView.jsx", error: String((e && e.message) || e) }); }

// components/interactive/Accordion.jsx
try { (() => {
__ds_scope.injectCss('spu-acc-css', `
.spu-acc{border:1px solid var(--color-border);border-radius:var(--radius-md);overflow:hidden;background:var(--color-surface)}
.spu-acc__item + .spu-acc__item{border-top:1px solid var(--color-border)}
.spu-acc__btn{width:100%;display:flex;align-items:center;justify-content:space-between;gap:var(--space-4);padding:var(--space-4) var(--space-5);background:none;border:0;cursor:pointer;font-family:var(--font-display);font-weight:700;font-size:var(--fs-h6);color:var(--text-strong);text-align:left;transition:background var(--dur-fast) var(--ease-out)}
.spu-acc__btn:hover{background:var(--color-primary-soft)}
.spu-acc__icon{color:var(--color-primary-strong);flex:0 0 auto;transition:transform var(--dur) var(--ease-out)}
.spu-acc__item--open .spu-acc__icon{transform:rotate(180deg)}
.spu-acc__num{font-family:var(--font-mono);font-size:.85rem;color:var(--color-accent-strong);margin-right:.6em}
.spu-acc__panel{display:grid;grid-template-rows:0fr;opacity:0;transition:grid-template-rows var(--dur) var(--ease-out), opacity var(--dur-fast) var(--ease-out)}
.spu-acc__panelInner{overflow:hidden;padding:0 var(--space-5);color:var(--text-body)}
.spu-acc__panelInner>:last-child{margin-bottom:0}
.spu-acc__item--open .spu-acc__panel{grid-template-rows:1fr;opacity:1}
.spu-acc__item--open .spu-acc__panelInner{padding-bottom:var(--space-5)}
.spu-acc__media{margin-top:var(--space-4);border-radius:var(--radius-md);overflow:hidden;border:1px solid var(--color-border);background:var(--color-surface-warm)}
.spu-acc__media img,.spu-acc__media image-slot{display:block;width:100%}
.spu-acc__media figcaption{font-size:var(--fs-caption);color:var(--text-muted);padding:var(--space-2) var(--space-3)}
.spu-acc__actions{margin-top:var(--space-4);display:flex;gap:var(--space-3);flex-wrap:wrap}
@media print{
  .spu-acc{break-inside:avoid}
  .spu-acc__btn{cursor:auto}
  .spu-acc__icon{display:none !important}
  .spu-acc__panel{grid-template-rows:1fr !important;opacity:1 !important;transition:none}
  .spu-acc__panelInner{padding-bottom:var(--space-5)}
}
@media (prefers-reduced-motion: reduce){
  .spu-acc__panel{transition:none}
}
`);

function AutoImageSlot({
  id,
  alt,
  placeholder
}) {
  const ref = React.useRef(null);
  React.useEffect(() => {
    const el = ref.current;
    if (!el) return undefined;
    const sync = () => {
      const img = el.shadowRoot && el.shadowRoot.querySelector('.frame img');
      if (img && img.getAttribute('src') && img.naturalWidth) {
        const width = el.clientWidth || el.offsetWidth || 1;
        el.style.height = Math.round(width * img.naturalHeight / img.naturalWidth) + 'px';
      } else {
        el.style.height = '160px';
      }
    };
    const image = el.shadowRoot && el.shadowRoot.querySelector('.frame img');
    if (image) image.addEventListener('load', sync);
    const observer = typeof ResizeObserver !== 'undefined' ? new ResizeObserver(sync) : null;
    if (observer) observer.observe(el);
    const stateObserver = typeof MutationObserver !== 'undefined' ? new MutationObserver(sync) : null;
    if (stateObserver) stateObserver.observe(el, { attributes: true, attributeFilter: ['data-filled'] });
    sync();
    return () => {
      if (image) image.removeEventListener('load', sync);
      if (observer) observer.disconnect();
      if (stateObserver) stateObserver.disconnect();
    };
  }, [id]);
  return React.createElement('image-slot', {
    ref,
    id,
    shape: 'rect',
    fit: 'contain',
    alt: alt || '',
    placeholder: placeholder || 'Arraste uma imagem',
    style: {
      display: 'block',
      width: '100%',
      height: 160
    }
  });
}

// Extras opcionais por item: imagem (image=src ou imageSlot=id) e link
// (link={href,label,button}). button = 'primary'|'secondary' renderiza como
// botão do DS; ausente = link sublinhado.
function itemExtras(it, ns) {
  const out = [];
  if (it.imageSlot || it.image) {
    out.push(React.createElement('figure', {
      key: 'media',
      className: ns + '__media',
      style: {
        margin: 0
      }
    }, it.imageSlot ? React.createElement(AutoImageSlot, {
      id: it.imageSlot,
      alt: it.imageAlt,
      placeholder: it.imageAlt || 'Arraste uma imagem'
    }) : React.createElement('img', {
      src: it.image,
      alt: it.imageAlt || ''
    }), it.imageCaption && React.createElement('figcaption', null, it.imageCaption)));
  }
  const links = it.links || (it.link ? [it.link] : it.linkHref ? [{
    href: it.linkHref,
    label: it.linkLabel,
    button: it.linkButton
  }] : []);
  if (links.length) {
    out.push(React.createElement('div', {
      key: 'act',
      className: __ds_scope.cx(ns + '__actions', 'spu-richtext')
    }, links.filter(l => l && l.href).map((l, j) => React.createElement('a', {
      key: j,
      href: l.href,
      target: '_blank',
      rel: 'noopener',
      'data-btn': l.button ? l.button === true ? 'primary' : l.button : undefined
    }, l.label != null ? __ds_scope.renderRich(l.label, {
      inline: true
    }) : l.href))));
  }
  return out;
}
function Accordion({
  items = [],
  multiple = false,
  defaultOpen = [],
  numbered = false,
  className,
  style
}) {
  const printing = __ds_scope.isPrint();
  const [open, setOpen] = React.useState(() => new Set(defaultOpen));
  const toggle = i => setOpen(prev => {
    const next = new Set(multiple ? prev : []);
    if (prev.has(i)) next.delete(i);else next.add(i);
    return next;
  });
  return React.createElement('div', {
    className: __ds_scope.cx('spu-acc', className),
    style
  }, items.map((it, i) => {
    const isOpen = printing || open.has(i);
    return React.createElement('div', {
      key: i,
      className: __ds_scope.cx('spu-acc__item', isOpen && 'spu-acc__item--open')
    }, React.createElement('button', {
      type: 'button',
      className: 'spu-acc__btn',
      onClick: printing ? undefined : () => toggle(i),
      'aria-expanded': isOpen
    }, React.createElement('span', null, numbered && React.createElement('span', {
      className: 'spu-acc__num'
    }, String(i + 1).padStart(2, '0')), __ds_scope.renderRich(it.title, {
      inline: true
    })), !printing && React.createElement('span', {
      className: 'spu-acc__icon'
    }, React.createElement(__ds_scope.Icon, {
      name: 'chevron-down',
      size: 22
    }))), React.createElement('div', {
      className: 'spu-acc__panel',
      'aria-hidden': !isOpen
    }, React.createElement('div', {
      className: 'spu-acc__panelInner'
    }, __ds_scope.renderRich(it.content), itemExtras(it, 'spu-acc'))));
  }));
}
Object.assign(__ds_scope, { itemExtras, Accordion });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/interactive/Accordion.jsx", error: String((e && e.message) || e) }); }

// components/interactive/CompareAB.jsx
try { (() => {
__ds_scope.injectCss('spu-ab-css', `
.spu-ab{display:grid;grid-template-columns:1fr 1fr;gap:var(--space-4);position:relative}
.spu-ab__col{border:1px solid var(--color-border);border-radius:var(--radius-md);overflow:hidden;background:var(--color-surface);box-shadow:var(--shadow-sm);display:flex;flex-direction:column}
.spu-ab__hd{display:flex;align-items:center;gap:.5em;padding:.66em 1.1em;color:#fff}
.spu-ab__hd>svg{flex:0 0 auto}
.spu-ab__col--a .spu-ab__hd{background:var(--color-primary)}
.spu-ab__col--b .spu-ab__hd{background:var(--color-accent)}
.spu-ab__label{font-family:var(--font-mono);font-size:var(--fs-eyebrow);font-weight:600;letter-spacing:.1em;text-transform:uppercase;line-height:1.3;margin:0;color:#fff}
.spu-ab__bd{padding:var(--space-5) var(--space-6)}
.spu-ab__title{font-family:var(--font-display);font-weight:700;font-size:var(--fs-h5);color:var(--text-strong);margin:0 0 .35em}
.spu-ab__body{color:var(--text-body)}
.spu-ab__body>:last-child{margin-bottom:0}
.spu-ab__vs{position:absolute;left:50%;top:50%;transform:translate(-50%,-50%);width:46px;height:46px;border-radius:var(--radius-pill);background:var(--ink-900);color:#fff;font-family:var(--font-display);font-weight:800;font-size:.95rem;display:flex;align-items:center;justify-content:center;box-shadow:var(--shadow-md);z-index:2}
@media (max-width:640px){.spu-ab{grid-template-columns:1fr}.spu-ab__vs{position:static;transform:none;margin:calc(var(--space-2)*-1) auto}}
`);
function Col(side, data) {
  return React.createElement('div', {
    className: `spu-ab__col spu-ab__col--${side}`
  }, data.label && React.createElement('div', {
    className: 'spu-ab__hd'
  }, data.icon && React.createElement(__ds_scope.Icon, {
    name: data.icon,
    size: 17
  }), React.createElement('p', {
    className: 'spu-ab__label'
  }, __ds_scope.renderRich(data.label, {
    inline: true
  }))), React.createElement('div', {
    className: 'spu-ab__bd'
  }, data.title && React.createElement('p', {
    className: 'spu-ab__title'
  }, __ds_scope.renderRich(data.title, {
    inline: true
  })), React.createElement('div', {
    className: 'spu-ab__body'
  }, __ds_scope.renderRich(data.content))));
}
function CompareAB({
  a = {},
  b = {},
  vs = 'VS',
  className,
  style
}) {
  return React.createElement('div', {
    className: __ds_scope.cx('spu-ab', className),
    style
  }, Col('a', a), vs && React.createElement('span', {
    className: 'spu-ab__vs'
  }, vs), Col('b', b));
}
Object.assign(__ds_scope, { CompareAB });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/interactive/CompareAB.jsx", error: String((e && e.message) || e) }); }

// components/interactive/Flipcard.jsx
try { (() => {
__ds_scope.injectCss('spu-flip-css', `
.spu-flip{--spu-flip-color:var(--color-primary);perspective:1400px;background:none;border:0;padding:0;width:100%;font:inherit;text-align:left;cursor:pointer;display:block;isolation:isolate;border-radius:var(--radius-lg)}
.spu-flip:focus-visible{outline:3px solid var(--color-focus-ring);outline-offset:3px}
.spu-flip__inner{position:relative;transition:transform var(--dur-slow) var(--ease-in-out);transform-style:preserve-3d;-webkit-transform-style:preserve-3d}
.spu-flip--flipped .spu-flip__inner{transform:rotateY(180deg)}
.spu-flip__face{position:absolute;inset:0;backface-visibility:hidden;-webkit-backface-visibility:hidden;border-radius:var(--radius-lg);padding:var(--space-6);display:flex;flex-direction:column;border:1px solid var(--color-border);border-top:4px solid var(--spu-flip-color);box-shadow:var(--shadow-sm);overflow:hidden;transform:translateZ(1px);transition:box-shadow var(--dur) var(--ease-out)}
.spu-flip:hover .spu-flip__face{box-shadow:var(--shadow-md)}
.spu-flip__face--front{background:var(--color-surface);background:color-mix(in srgb,var(--spu-flip-color) 7%,var(--color-surface));border-color:color-mix(in srgb,var(--spu-flip-color) 38%,var(--color-border));border-top-color:var(--spu-flip-color);opacity:1}
.spu-flip__face--back{background:var(--color-primary);background:color-mix(in srgb,var(--spu-flip-color) 62%,var(--ink-900));color:var(--text-on-dark);transform:rotateY(180deg) translateZ(1px);border-color:transparent;opacity:0}
.spu-flip--flipped .spu-flip__face--front{opacity:0}
.spu-flip--flipped .spu-flip__face--back{opacity:1}
.spu-flip__hint{position:absolute;top:var(--space-4);right:var(--space-4);color:var(--text-faint)}
.spu-flip__face--back .spu-flip__hint{color:var(--text-on-dark-muted)}
.spu-flip__icon{width:48px;height:48px;border-radius:var(--radius-md);display:flex;align-items:center;justify-content:center;background:var(--color-primary-soft);background:color-mix(in srgb,var(--spu-flip-color) 14%,var(--color-surface));color:var(--spu-flip-color);margin-bottom:var(--space-4);box-shadow:inset 0 0 0 1px color-mix(in srgb,var(--spu-flip-color) 24%,transparent)}
.spu-flip__term{font-family:var(--font-display);font-weight:700;font-size:var(--fs-h5);margin:0 0 .25em;color:var(--text-strong)}
.spu-flip__description{font-size:var(--fs-small);line-height:1.55;color:var(--text-muted);margin-top:var(--space-1)}
.spu-flip__def{font-size:var(--fs-small);line-height:1.62;flex:1;min-height:0;overflow:auto;padding-right:var(--space-1)}
.spu-flip__def>:first-child{margin-top:0}.spu-flip__def>:last-child{margin-bottom:0}
.spu-flip__cue{font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:.06em;color:var(--text-faint);margin-top:auto;padding-top:var(--space-3)}
.spu-flip__face--back .spu-flip__cue{color:var(--text-on-dark-muted)}
@media print{
  .spu-flip{cursor:auto;break-inside:avoid}
  .spu-flip__inner{transform:none !important;min-height:0 !important}
  .spu-flip__face{position:static !important;transform:none !important;backface-visibility:visible !important;-webkit-backface-visibility:visible !important;box-shadow:none;opacity:1 !important}
  .spu-flip__face--front{border-radius:var(--radius-lg) var(--radius-lg) 0 0;border-bottom:0}
  .spu-flip__face--back{border-radius:0 0 var(--radius-lg) var(--radius-lg)}
  .spu-flip__def{overflow:visible}
  .spu-flip__hint,.spu-flip__cue{display:none !important}
}
@media (prefers-reduced-motion: reduce){
  .spu-flip__inner{transition:none}
  .spu-flip__face{transition:none}
}
`);
function Flipcard({
  term,
  description,
  definition,
  icon,
  hint,
  color,
  front,
  back,
  height = 280,
  className,
  style
}) {
  const [flipped, setFlipped] = React.useState(false);
  const frontFace = front || React.createElement('div', {
    style: {
      display: 'flex',
      flexDirection: 'column',
      height: '100%'
    }
  }, icon && React.createElement('span', {
    className: 'spu-flip__icon'
  }, React.createElement(__ds_scope.Icon, {
    name: icon,
    size: 24
  })), __ds_scope.hasRichContent(term) && React.createElement('h3', {
    className: 'spu-flip__term'
  }, __ds_scope.renderRich(term, {
    inline: true
  })), __ds_scope.hasRichContent(description || hint) && React.createElement('div', {
    className: 'spu-flip__description'
  }, __ds_scope.renderRich(description || hint, {
    inline: true
  })), React.createElement('span', {
    className: 'spu-flip__cue'
  }, 'Clique para virar'));
  const backFace = back || React.createElement('div', {
    style: {
      display: 'flex',
      flexDirection: 'column',
      height: '100%'
    }
  }, React.createElement('div', {
    className: 'spu-flip__def'
  }, __ds_scope.renderRich(definition)), React.createElement('span', {
    className: 'spu-flip__cue'
  }, 'Clique para voltar'));
  const toggle = event => {
    if (event && event.target && event.target.closest && event.target.closest('a,[contenteditable="true"]')) return;
    setFlipped(f => !f);
  };
  return React.createElement('div', {
    role: 'button',
    tabIndex: 0,
    className: __ds_scope.cx('spu-flip', flipped && 'spu-flip--flipped', className),
    style: {
      '--spu-flip-color': color || 'var(--color-primary)',
      ...style
    },
    onClick: toggle,
    onKeyDown: event => {
      if (event.target !== event.currentTarget || event.key !== 'Enter' && event.key !== ' ') return;
      event.preventDefault();
      toggle(event);
    },
    'aria-pressed': flipped
  }, React.createElement('div', {
    className: 'spu-flip__inner',
    style: {
      minHeight: typeof height === 'number' ? `${height}px` : height
    }
  }, React.createElement('div', {
    className: 'spu-flip__face spu-flip__face--front'
  }, React.createElement('span', {
    className: 'spu-flip__hint'
  }, React.createElement(__ds_scope.Icon, {
    name: 'repeat',
    size: 16
  })), frontFace), React.createElement('div', {
    className: 'spu-flip__face spu-flip__face--back'
  }, React.createElement('span', {
    className: 'spu-flip__hint'
  }, React.createElement(__ds_scope.Icon, {
    name: 'repeat',
    size: 16
  })), backFace)));
}
Object.assign(__ds_scope, { Flipcard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/interactive/Flipcard.jsx", error: String((e && e.message) || e) }); }

// components/interactive/Quiz.jsx
try { (() => {
__ds_scope.injectCss('spu-quiz-css', `
.spu-quiz{max-width:34rem;margin-inline:auto;border:1px solid var(--color-border);border-radius:var(--radius-lg);background:var(--color-surface);box-shadow:var(--shadow-sm);overflow:hidden}
.spu-quiz__head{display:flex;align-items:center;justify-content:space-between;gap:var(--space-3);padding:var(--space-3) var(--space-5);border-bottom:1px solid var(--color-border)}
.spu-quiz__eyebrow{display:flex;align-items:center;gap:.5em;font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:.1em;color:var(--text-faint)}
.spu-quiz__count{font-family:var(--font-mono);font-size:var(--fs-eyebrow);color:var(--text-muted);letter-spacing:.08em}
.spu-quiz__dots{display:flex;gap:6px}
.spu-quiz__dot{width:7px;height:7px;border-radius:50%;background:var(--color-border-strong);transition:background var(--dur-fast) var(--ease-out),width var(--dur-fast) var(--ease-out)}
.spu-quiz__dot--on{background:var(--color-primary);width:18px;border-radius:4px}
.spu-quiz__dot--done{background:var(--status-success)}
.spu-quiz__viewport{overflow:hidden}
.spu-quiz__track{display:flex;transition:transform var(--dur-slow) var(--ease-in-out)}
.spu-quiz__slide{flex:0 0 100%;padding:var(--space-4) var(--space-5);box-sizing:border-box}
.spu-quiz__q{font-family:var(--font-display);font-weight:700;font-size:var(--fs-h6,1.05rem);color:var(--text-strong);margin:0 0 var(--space-3);line-height:var(--lh-tight)}
.spu-quiz__q .spu-quiz__qnum{color:var(--color-primary);margin-right:.4em}
.spu-quiz__opts{display:flex;flex-direction:column;gap:var(--space-2);list-style:none;margin:0;padding:0}
.spu-quiz__opt{display:flex;align-items:center;gap:var(--space-3);width:100%;text-align:left;font:inherit;font-size:var(--fs-small);color:var(--text-body);background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-md);padding:var(--space-2) var(--space-3);cursor:pointer;transition:border-color var(--dur-fast) var(--ease-out),background var(--dur-fast) var(--ease-out)}
.spu-quiz__opt:hover:not(:disabled){border-color:var(--color-primary);background:var(--color-primary-soft)}
.spu-quiz__opt:disabled{cursor:default}
.spu-quiz__mark{flex:none;width:22px;height:22px;border-radius:50%;border:1px solid var(--color-border-strong);display:grid;place-items:center;font-family:var(--font-mono);font-size:12px;font-weight:600;color:var(--text-muted);transition:all var(--dur-fast) var(--ease-out)}
.spu-quiz__opt--correct{border-color:var(--status-success);background:var(--status-success-soft)}
.spu-quiz__opt--correct .spu-quiz__mark{border-color:var(--status-success);background:var(--status-success);color:#fff}
.spu-quiz__opt--wrong{border-color:var(--status-warning);background:var(--status-warning-soft)}
.spu-quiz__opt--wrong .spu-quiz__mark{border-color:var(--status-warning);background:var(--status-warning);color:#fff}
.spu-quiz__opt--muted{opacity:.55}
.spu-quiz__opttext{padding-top:0}
.spu-quiz__fb{display:flex;gap:var(--space-3);margin-top:var(--space-4);padding:var(--space-3) var(--space-4);border-radius:var(--radius-md);font-size:var(--fs-small);line-height:1.5;animation:spu-quiz-fade var(--dur-base) var(--ease-out)}
.spu-quiz__fb--ok{background:var(--status-success-soft);color:var(--ink-800)}
.spu-quiz__fb--no{background:var(--status-warning-soft);color:var(--ink-800)}
.spu-quiz__fb b{color:var(--text-strong)}
.spu-quiz__fbicon{flex:none;margin-top:1px}
.spu-quiz__fb--ok .spu-quiz__fbicon{color:var(--status-success)}
.spu-quiz__fb--no .spu-quiz__fbicon{color:var(--status-warning)}
@keyframes spu-quiz-fade{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}
.spu-quiz__foot{display:flex;align-items:center;justify-content:space-between;gap:var(--space-3);padding:var(--space-3) var(--space-5) var(--space-5)}
.spu-quiz__nav{display:inline-flex;align-items:center;gap:.5em;font:inherit;font-weight:600;font-size:var(--fs-small);color:var(--color-primary-strong);background:none;border:0;padding:.4em .2em;cursor:pointer;border-radius:var(--radius-sm)}
.spu-quiz__nav:disabled{opacity:.35;cursor:default}
.spu-quiz__nav:hover:not(:disabled){color:var(--color-primary)}
.spu-quiz__score{font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:.08em;color:var(--text-muted)}
@media print{
  .spu-quiz__track{transform:none !important;display:block}
  .spu-quiz__slide{break-inside:avoid;border-bottom:1px solid var(--color-border)}
  .spu-quiz__foot,.spu-quiz__dots{display:none !important}
}
`);
const LETTERS = ['A', 'B', 'C', 'D', 'E', 'F'];
function Quiz({
  questions = [],
  eyebrow = 'Verifique seu entendimento',
  icon = 'help-circle',
  className,
  style
}) {
  const [idx, setIdx] = React.useState(0);
  const [answers, setAnswers] = React.useState({}); // { qIndex: chosenIndex }
  const print = __ds_scope.isPrint();
  const total = questions.length;
  if (!total) return null;
  const choose = (qi, oi) => {
    if (answers[qi] != null) return;
    setAnswers(a => ({
      ...a,
      [qi]: oi
    }));
  };
  const answered = Object.keys(answers).length;
  const correct = questions.reduce((n, q, i) => n + (answers[i] != null && questions[i].options[answers[i]] && questions[i].options[answers[i]].correct ? 1 : 0), 0);
  const dots = React.createElement('div', {
    className: 'spu-quiz__dots'
  }, questions.map((q, i) => React.createElement('span', {
    key: i,
    className: __ds_scope.cx('spu-quiz__dot', i === idx && 'spu-quiz__dot--on', i !== idx && answers[i] != null && 'spu-quiz__dot--done')
  })));
  const renderSlide = (q, qi) => {
    const chosen = answers[qi];
    const done = chosen != null;
    return React.createElement('div', {
      className: 'spu-quiz__slide',
      key: qi,
      'aria-hidden': !print && qi !== idx
    }, React.createElement('p', {
      className: 'spu-quiz__q'
    }, React.createElement('span', {
      className: 'spu-quiz__qnum'
    }, String(qi + 1).padStart(2, '0')), __ds_scope.renderRich(q.question, {
      inline: true
    })), React.createElement('ul', {
      className: 'spu-quiz__opts'
    }, q.options.map((o, oi) => {
      const isChosen = chosen === oi;
      const reveal = done && (o.correct || isChosen);
      const cls = __ds_scope.cx('spu-quiz__opt', done && o.correct && 'spu-quiz__opt--correct', done && isChosen && !o.correct && 'spu-quiz__opt--wrong', done && !o.correct && !isChosen && 'spu-quiz__opt--muted');
      const glyph = reveal ? React.createElement(__ds_scope.Icon, {
        name: o.correct ? 'check' : 'x',
        size: 14
      }) : LETTERS[oi];
      return React.createElement('li', {
        key: oi
      }, React.createElement('button', {
        type: 'button',
        className: cls,
        disabled: done,
        onClick: () => choose(qi, oi)
      }, React.createElement('span', {
        className: 'spu-quiz__mark'
      }, glyph), React.createElement('span', {
        className: 'spu-quiz__opttext'
      }, __ds_scope.renderRich(o.text, {
        inline: true
      }))));
    })), done && (() => {
      const ok = q.options[chosen].correct;
      const fb = q.options[chosen].feedback || q.feedback || (ok ? 'Correto.' : 'Resposta incorreta.');
      return React.createElement('div', {
        className: __ds_scope.cx('spu-quiz__fb', ok ? 'spu-quiz__fb--ok' : 'spu-quiz__fb--no')
      }, React.createElement('span', {
        className: 'spu-quiz__fbicon'
      }, React.createElement(__ds_scope.Icon, {
        name: ok ? 'check-circle' : 'alert-triangle',
        size: 18
      })), React.createElement('span', null, React.createElement('b', null, ok ? 'Correto. ' : 'Quase lá. '), __ds_scope.renderRich(fb, {
        inline: true
      })));
    })());
  };
  return React.createElement('div', {
    className: __ds_scope.cx('spu-quiz', className),
    style
  }, React.createElement('div', {
    className: 'spu-quiz__head'
  }, React.createElement('span', {
    className: 'spu-quiz__eyebrow'
  }, React.createElement(__ds_scope.Icon, {
    name: icon,
    size: 15
  }), eyebrow), total > 1 && !print ? dots : React.createElement('span', {
    className: 'spu-quiz__count'
  }, total, total > 1 ? ' questões' : ' questão')), React.createElement('div', {
    className: 'spu-quiz__viewport'
  }, React.createElement('div', {
    className: 'spu-quiz__track',
    style: print ? null : {
      transform: `translateX(-${idx * 100}%)`
    }
  }, questions.map(renderSlide))), total > 1 && !print && React.createElement('div', {
    className: 'spu-quiz__foot'
  }, React.createElement('button', {
    type: 'button',
    className: 'spu-quiz__nav',
    disabled: idx === 0,
    onClick: () => setIdx(i => Math.max(0, i - 1))
  }, React.createElement(__ds_scope.Icon, {
    name: 'arrow-left',
    size: 16
  }), 'Anterior'), React.createElement('span', {
    className: 'spu-quiz__score'
  }, answered === total ? `${correct}/${total} corretas` : `${idx + 1} de ${total}`), React.createElement('button', {
    type: 'button',
    className: 'spu-quiz__nav',
    disabled: idx === total - 1,
    onClick: () => setIdx(i => Math.min(total - 1, i + 1))
  }, 'Próxima', React.createElement(__ds_scope.Icon, {
    name: 'arrow-right',
    size: 16
  }))));
}
Object.assign(__ds_scope, { Quiz });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/interactive/Quiz.jsx", error: String((e && e.message) || e) }); }

// components/interactive/Timeline.jsx
try { (() => {
__ds_scope.injectCss('spu-tl-css', `
.spu-tl{--_era:var(--color-accent)}
.spu-tl__eras{display:flex;gap:var(--space-2);flex-wrap:wrap;margin-bottom:var(--space-6)}
.spu-tl__era{display:flex;flex-direction:column;gap:.1em;font-family:var(--font-body);border:1px solid var(--color-border);background:var(--color-surface);border-radius:var(--radius);padding:.55em .9em;cursor:pointer;text-align:left;transition:border-color var(--dur-fast) var(--ease-out),background var(--dur-fast),color var(--dur-fast)}
.spu-tl__era:hover{border-color:var(--color-primary)}
.spu-tl__era--active{background:var(--_era);border-color:var(--_era);color:#fff}
.spu-tl__eralabel{font-family:var(--font-display);font-weight:700;font-size:var(--fs-small);line-height:1.1}
.spu-tl__eraperiod{font-family:var(--font-mono);font-size:var(--fs-eyebrow);letter-spacing:.04em;opacity:.7}
.spu-tl__head{display:flex;align-items:baseline;gap:var(--space-3);margin-bottom:var(--space-5);padding-bottom:var(--space-3);border-bottom:var(--border-thick) solid var(--_era)}
.spu-tl__title{font-family:var(--font-display);font-weight:800;font-size:var(--fs-h3);letter-spacing:var(--ls-heading);margin:0;color:var(--text-strong)}
.spu-tl__period{font-family:var(--font-mono);font-size:var(--fs-caption);text-transform:uppercase;letter-spacing:.06em;color:var(--_era);font-weight:600}
.spu-tl__rail{position:relative;padding-left:42px}
.spu-tl__rail::before{content:"";position:absolute;left:13px;top:8px;bottom:8px;width:2px;background:var(--color-divider)}
.spu-tl__item{position:relative;margin-bottom:var(--space-4)}
.spu-tl__node{position:absolute;left:-35px;top:3px;width:16px;height:16px;border-radius:50%;background:var(--_era);border:3px solid var(--color-bg);box-shadow:0 0 0 1px var(--color-border)}
.spu-tl__mbtn{width:100%;display:flex;align-items:flex-start;justify-content:space-between;gap:var(--space-4);background:none;border:0;cursor:pointer;text-align:left;padding:var(--space-1) 0}
.spu-tl__date{font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:.06em;color:var(--_era);font-weight:600;filter:saturate(1.1) brightness(.85)}
.spu-tl__mtitle{font-family:var(--font-display);font-weight:700;font-size:var(--fs-h6);color:var(--text-strong);margin:.1em 0 0}
.spu-tl__chev{color:var(--text-faint);flex:0 0 auto;margin-top:3px;transition:transform var(--dur) var(--ease-out)}
.spu-tl__item--open .spu-tl__chev{transform:rotate(180deg)}
.spu-tl__content{margin-top:var(--space-3);padding:var(--space-4) var(--space-5);background:color-mix(in srgb, var(--color-surface) 82%, var(--ochre-50));border-radius:var(--radius-md);color:var(--text-body);animation:spu-fade var(--dur) var(--ease-out)}
.spu-section--warm .spu-tl__content{background:var(--color-surface)}
.spu-tl__content>:last-child{margin-bottom:0}
.spu-tl__media{margin-top:var(--space-3);border-radius:var(--radius-md);overflow:hidden;border:1px solid var(--color-border);background:var(--color-bg)}
.spu-tl__media img,.spu-tl__media image-slot{display:block;width:100%}
.spu-tl__media figcaption{font-size:var(--fs-caption);color:var(--text-muted);padding:var(--space-2) var(--space-3)}
.spu-tl__actions{margin-top:var(--space-3);display:flex;gap:var(--space-3);flex-wrap:wrap}
@media print{ .spu-tl__eras{display:none !important} .spu-tl__chev{display:none !important} .spu-tl__era-block{break-inside:avoid} .spu-tl__era-block+.spu-tl__era-block{margin-top:var(--space-8)} }
`);
function EraHead(era, accent) {
  return React.createElement('div', {
    className: 'spu-tl__head'
  }, React.createElement('h3', {
    className: 'spu-tl__title'
  }, __ds_scope.renderRich(era.label, {
    inline: true
  })), era.period && React.createElement('span', {
    className: 'spu-tl__period'
  }, __ds_scope.renderRich(era.period, {
    inline: true
  })));
}
function Timeline({
  eras = [],
  className,
  style
}) {
  const [eraIdx, setEraIdx] = React.useState(0);
  const [openMs, setOpenMs] = React.useState(0);

  // —— Impressão: todas as eras e marcos expandidos ——
  if (__ds_scope.isPrint()) {
    return React.createElement('div', {
      className: __ds_scope.cx('spu-tl', className),
      style
    }, eras.map((era, ei) => React.createElement('div', {
      key: ei,
      className: 'spu-tl__era-block',
      style: {
        '--_era': era.color || 'var(--color-accent)'
      }
    }, EraHead(era), React.createElement('div', {
      className: 'spu-tl__rail'
    }, (era.milestones || []).map((m, i) => React.createElement('div', {
      key: i,
      className: 'spu-tl__item spu-tl__item--open'
    }, React.createElement('span', {
      className: 'spu-tl__node'
    }), React.createElement('div', {
      className: 'spu-tl__mbtn'
    }, React.createElement('span', null, React.createElement('span', {
      className: 'spu-tl__date'
    }, __ds_scope.renderRich(m.date, {
      inline: true
    })), React.createElement('p', {
      className: 'spu-tl__mtitle'
    }, __ds_scope.renderRich(m.title, {
      inline: true
    })))), (m.content || m.image || m.imageSlot || m.link || m.links) && React.createElement('div', {
      className: 'spu-tl__content'
    }, __ds_scope.renderRich(m.content), __ds_scope.itemExtras(m, 'spu-tl'))))))));
  }
  const era = eras[eraIdx] || {
    milestones: []
  };
  const accent = era.color || 'var(--color-accent)';
  const pickEra = i => {
    setEraIdx(i);
    setOpenMs(0);
  };
  return React.createElement('div', {
    className: __ds_scope.cx('spu-tl', className),
    style: {
      '--_era': accent,
      ...style
    }
  }, eras.length > 1 && React.createElement('div', {
    className: 'spu-tl__eras'
  }, eras.map((e, i) => React.createElement('button', {
    key: i,
    type: 'button',
    className: __ds_scope.cx('spu-tl__era', i === eraIdx && 'spu-tl__era--active'),
    style: i === eraIdx && e.color ? {
      '--_era': e.color
    } : undefined,
    onClick: () => pickEra(i)
  }, React.createElement('span', {
    className: 'spu-tl__eralabel'
  }, __ds_scope.renderRich(e.label, {
    inline: true
  })), e.period && React.createElement('span', {
    className: 'spu-tl__eraperiod'
  }, __ds_scope.renderRich(e.period, {
    inline: true
  }))))), React.createElement('div', {
    className: 'spu-tl__head'
  }, React.createElement('h3', {
    className: 'spu-tl__title'
  }, __ds_scope.renderRich(era.label, {
    inline: true
  })), era.period && React.createElement('span', {
    className: 'spu-tl__period'
  }, __ds_scope.renderRich(era.period, {
    inline: true
  }))), React.createElement('div', {
    className: 'spu-tl__rail'
  }, (era.milestones || []).map((m, i) => {
    const isOpen = openMs === i;
    const hasBody = !!(m.content || m.image || m.imageSlot || m.link || m.links);
    return React.createElement('div', {
      key: i,
      className: __ds_scope.cx('spu-tl__item', isOpen && 'spu-tl__item--open')
    }, React.createElement('span', {
      className: 'spu-tl__node'
    }), React.createElement('button', {
      type: 'button',
      className: 'spu-tl__mbtn',
      onClick: () => setOpenMs(isOpen ? -1 : i),
      'aria-expanded': isOpen
    }, React.createElement('span', null, React.createElement('span', {
      className: 'spu-tl__date'
    }, __ds_scope.renderRich(m.date, {
      inline: true
    })), React.createElement('p', {
      className: 'spu-tl__mtitle'
    }, __ds_scope.renderRich(m.title, {
      inline: true
    }))), hasBody && React.createElement('span', {
      className: 'spu-tl__chev'
    }, React.createElement(__ds_scope.Icon, {
      name: 'chevron-down',
      size: 20
    }))), isOpen && hasBody && React.createElement('div', {
      className: 'spu-tl__content'
    }, __ds_scope.renderRich(m.content), __ds_scope.itemExtras(m, 'spu-tl')));
  })));
}
Object.assign(__ds_scope, { Timeline });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/interactive/Timeline.jsx", error: String((e && e.message) || e) }); }

// components/layout/Columns.jsx
try { (() => {
__ds_scope.injectCss('spu-columns-css', `
.spu-columns{display:grid;grid-template-columns:var(--_columns-template,repeat(auto-fit,minmax(min(var(--_cmin,260px),100%),1fr)))}
.spu-columns > *{margin-top:0 !important;align-self:start}
.spu-columns > * > :first-child,
.spu-columns .spu-blockstack > :first-child{margin-top:0 !important}
@media (max-width:680px){.spu-columns{grid-template-columns:1fr}}
`);
const GAPS = {
  none: '0',
  sm: 'var(--space-4)',
  md: 'var(--space-6)',
  lg: 'clamp(1.5rem,4vw,3rem)'
};

// Dispõe blocos lado a lado em grade responsiva (quebra sozinho em telas estreitas).
function Columns({
  count = 2,
  ratio = 'equal',
  gap = 'lg',
  minItem,
  children,
  className,
  style
}) {
  const columnCount = Math.max(1, Number(count) || 1);
  const min = minItem || (columnCount >= 4 ? 200 : columnCount === 3 ? 240 : 300);
  let template;
  if (ratio === 'first-wide') {
    template = Array.from({ length: columnCount }, (_, index) => `minmax(0,${index === 0 ? 2 : 1}fr)`).join(' ');
  } else if (ratio === 'last-wide') {
    template = Array.from({ length: columnCount }, (_, index) => `minmax(0,${index === columnCount - 1 ? 2 : 1}fr)`).join(' ');
  }
  return React.createElement('div', {
    className: __ds_scope.cx('spu-columns', className),
    style: {
      gap: GAPS[gap] || gap,
      '--_cmin': typeof min === 'number' ? `${min}px` : min,
      '--_columns-template': template,
      ...style
    }
  }, children);
}
Object.assign(__ds_scope, { Columns });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Columns.jsx", error: String((e && e.message) || e) }); }

// components/layout/Section.jsx
try { (() => {
__ds_scope.injectCss('spu-section-css', `
.spu-section{width:100%;box-sizing:border-box}
/* Dentro da pilha, o gap é a única fonte de distância entre blocos. Alguns
   componentes também possuem margem externa para uso avulso; sem este reset,
   margem + gap se somam e criam vazios exagerados. */
.spu-blockstack.spu-blockstack>*:not(.spu-block-title):not(.spu-block-kicker){margin-block:0}
.spu-block-title{margin:0}
.spu-block-title .spu-richtext{line-height:inherit}
.spu-section__inner>.spu-blockstack>.spu-block-title:is(h3,h4,h5,h6):not(:last-child){margin-bottom:calc(-1 * var(--flow-block) + var(--space-3))}
.spu-section__inner{margin-inline:auto;padding-inline:var(--gutter)}
.spu-section--narrow .spu-section__inner{max-width:var(--container-prose)}
.spu-section--content .spu-section__inner{max-width:var(--container-content)}
.spu-section--wide .spu-section__inner{max-width:var(--container-wide)}
.spu-section--full .spu-section__inner{max-width:none}
.spu-section--pad-lg .spu-section__inner{padding-block:clamp(2.5rem,6vw,5rem)}
.spu-section--pad-md .spu-section__inner{padding-block:clamp(1.5rem,4vw,2.75rem)}
.spu-section--pad-none .spu-section__inner{padding-block:0}
/* superfícies */
.spu-section--warm{background:var(--color-surface-warm);background-image:var(--texture-topo);background-size:420px;border-block:1px solid var(--color-border)}
.spu-section--dark{background:var(--color-surface-inverse);background-image:var(--texture-topo);background-size:420px;color:var(--text-on-dark)}
.spu-section--terra-dark{background:var(--terra-800);background-image:var(--texture-topo);background-size:420px;color:var(--text-on-dark)}
.spu-section--page{background:var(--bg-paper)}
.spu-section--white{background:var(--white)}
/* —— Faixa escura: remapeia os tokens de texto → todo texto "solto" clareia sozinho —— */
.spu-section--dark {
  color: var(--text-on-dark);
  --text-strong: var(--text-on-dark);
  --text-body: var(--text-on-dark);
  --text-muted: var(--text-on-dark-muted);
  --color-primary-strong: var(--text-on-dark);
}
.spu-section--dark .spu-richtext a:not([data-btn]) { color: var(--ochre-300); text-decoration-color: var(--ochre-400); }
.spu-section--dark .spu-richtext a[data-btn="primary"]{background:#fff;color:var(--petrol-900);border-color:#fff}
.spu-section--dark .spu-richtext a[data-btn="secondary"]{background:rgba(255,255,255,.08);color:#fff;border-color:rgba(255,255,255,.72)}
.spu-section--dark .spu-richtext a[data-btn="ghost"]{background:rgba(255,255,255,.14);color:#fff;border-color:transparent}
/* marcadores de lista vivos sobre fundo escuro */
.spu-section--dark .spu-richtext ul li::marker,
.spu-section--dark .spu-richtext ol li::marker{color:var(--ochre-300)}
/* marca-texto sobre fundo escuro — texto claro legível, sem mudar o HTML salvo */
.spu-section--dark .spu-richtext mark{background:rgba(196,132,36,.62);color:var(--text-on-dark)}
.spu-section--dark .spu-richtext mark[data-hl="terra"]{background:rgba(194,97,58,.62)}
.spu-section--dark .spu-richtext mark[data-hl="petrol"]{background:rgba(74,153,142,.58)}
.spu-section--dark .spu-richtext mark[data-hl="green"]{background:rgba(90,150,94,.62)}
.spu-section--dark .spu-richtext mark[data-hl="sand"]{background:rgba(237,231,218,.28)}
/* —— Cartões com fundo próprio claro: restauram os tokens escuros —— */
.spu-section--dark .spu-callout,
.spu-section--dark .spu-panel,
.spu-section--dark .spu-table-wrap,
.spu-section--dark .spu-acc,
.spu-section--dark .spu-example,
.spu-section--dark .spu-examplecard,
.spu-section--dark .spu-feature--card,
.spu-section--dark .spu-map,
.spu-section--dark .spu-figure--framed,
.spu-section--dark .spu-stat--card,
.spu-section--dark .spu-compare,
.spu-section--dark .spu-ab,
.spu-section--dark .spu-tl__content,
.spu-section--dark .spu-embed__bar,
.spu-section--dark .spu-flip,
.spu-section--dark .spu-quiz {
  --text-strong: var(--ink-900);
  --text-body: var(--ink-700);
  --text-muted: var(--ink-500);
  --text-faint: var(--ink-500);
  --color-primary-strong: var(--petrol-700);
  color: var(--text-body);
}
/* botões dentro de cards claros voltam ao modo claro (não herdam o dark) */
.spu-section--dark .spu-acc .spu-richtext a[data-btn="primary"],
.spu-section--dark .spu-tl__content .spu-richtext a[data-btn="primary"]{background:var(--color-primary);border-color:var(--color-primary);color:#fff}
.spu-section--dark .spu-acc .spu-richtext a[data-btn="secondary"],
.spu-section--dark .spu-tl__content .spu-richtext a[data-btn="secondary"]{background:transparent;border-color:var(--color-primary);color:var(--color-primary-strong)}
.spu-section--dark .spu-callout .spu-richtext a:not([data-btn]), .spu-section--dark .spu-panel .spu-richtext a:not([data-btn]),
.spu-section--dark .spu-acc .spu-richtext a:not([data-btn]), .spu-section--dark .spu-example .spu-richtext a:not([data-btn]),
.spu-section--dark .spu-figure .spu-richtext a:not([data-btn]), .spu-section--dark .spu-tl__content .spu-richtext a:not([data-btn]) {
  color: var(--color-link);
}
`);

// Bloco-container de conteúdo: largura configurável (completa ou contida) +
// superfície opcional. Aceita qualquer conteúdo/bloco como children — base
// para composição em builder.
function Section({
  id,
  width = 'content',
  surface = 'none',
  pad = 'lg',
  children,
  className,
  style,
  innerStyle,
  ...rest
}) {
  return React.createElement('section', {
    id,
    'data-screen-label': id,
    className: __ds_scope.cx('spu-section', `spu-section--${width}`, `spu-section--pad-${pad}`, surface !== 'none' && `spu-section--${surface}`, surface === 'terra-dark' && 'spu-section--dark', className),
    style,
    ...rest
  }, React.createElement('div', {
    className: 'spu-section__inner',
    style: innerStyle
  }, children));
}
Object.assign(__ds_scope, { Section });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Section.jsx", error: String((e && e.message) || e) }); }

// components/layout/Stack.jsx
try { (() => {
__ds_scope.injectCss('spu-stack-css', `
.spu-stack{display:flex;flex-direction:column}
.spu-stack>*{margin-block:0}
`);
const GAPS = {
  none: '0',
  xs: 'var(--space-2)',
  sm: 'var(--space-4)',
  md: 'var(--space-6)',
  lg: 'var(--flow-block)',
  xl: 'var(--space-16)'
};

// Empilha blocos verticalmente com gap consistente.
function Stack({
  gap = 'md',
  align,
  children,
  className,
  style
}) {
  return React.createElement('div', {
    className: __ds_scope.cx('spu-stack', className),
    style: {
      gap: GAPS[gap] || gap,
      alignItems: align,
      ...style
    }
  }, children);
}
Object.assign(__ds_scope, { Stack });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Stack.jsx", error: String((e && e.message) || e) }); }

// components/media/BleedImage.jsx
try { (() => {
__ds_scope.injectCss('spu-bleedimg-css', `
.spu-bleedimg{width:100%;margin:var(--flow-block) 0;background:var(--color-surface-warm);display:block}
.spu-bleedimg--bleed{width:100vw;margin-left:calc(50% - 50vw);margin-right:calc(50% - 50vw);max-width:none}
.spu-section__inner .spu-bleedimg--bleed{width:100vw;max-width:none;margin-left:calc(50% - 50vw);margin-right:calc(50% - 50vw)}
.spu-bleedimg__frame{position:relative;overflow:hidden;cursor:zoom-in}
.spu-bleedimg--plain .spu-bleedimg__frame{cursor:default}
.spu-bleedimg image-slot,.spu-bleedimg__frame>img{display:block;width:100%;height:100%;object-fit:cover}
.spu-bleedimg--full image-slot,.spu-bleedimg--full .spu-bleedimg__frame>img{height:auto;object-fit:contain}
.spu-bleedimg__parallax{background-size:cover;background-position:center;background-attachment:fixed}
.spu-bleedimg__cap{max-width:var(--container-content);margin:var(--space-3) auto 0;padding-inline:var(--gutter);font-size:var(--fs-caption);line-height:1.55;text-align:center;color:var(--text-muted)}
.spu-bleedimg figcaption .spu-bleedimg__cap{display:block;text-align:center}
.spu-bleedimg figcaption .spu-bleedimg__title,
.spu-bleedimg figcaption .spu-bleedimg__credit{text-align:center}
.spu-bleedimg__title{display:block;font-family:var(--font-mono);font-size:var(--fs-eyebrow);font-weight:600;text-transform:uppercase;letter-spacing:.06em;color:var(--color-primary-strong);margin-bottom:.3em}
.spu-bleedimg__credit{display:block;color:var(--text-faint);margin-top:.2em}
.spu-bleedimg__overlay{position:absolute;left:50%;bottom:var(--space-5);transform:translateX(-50%);width:min(var(--container-content),calc(100% - var(--gutter)*2));padding:var(--space-4) var(--space-5);border-radius:var(--radius-md);backdrop-filter:blur(8px);box-shadow:var(--shadow-md)}
.spu-bleedimg__overlay .spu-bleedimg__cap{margin:0;padding:0;color:inherit;text-align:left}
.spu-bleed-lightbox{position:fixed;inset:0;z-index:1000;background:rgba(14,46,43,.93);display:flex;align-items:center;justify-content:center;padding:clamp(1rem,5vw,4rem);cursor:zoom-out;animation:spu-fade var(--dur) var(--ease-out)}
.spu-bleed-lightbox img{max-width:100%;max-height:100%;border-radius:var(--radius);box-shadow:var(--shadow-lg)}
@media (max-width: 768px){.spu-bleedimg__parallax{background-attachment:scroll}}
`);
const HEIGHTS = {
  narrow: '28vh',
  medium: '46vh',
  wide: '64vh',
  full: null
};
function slotImg(el) {
  const img = el && el.shadowRoot && el.shadowRoot.querySelector('.frame img');
  return img && img.getAttribute('src') ? img : null;
}
function BleedImage({
  src,
  slot,
  alt = '',
  zoom = false,
  fit = 'cover',
  bleed = true,
  height = '46vh',
  heightPreset = 'medium',
  title,
  caption,
  credit,
  captionMode = 'below',
  overlayBg = 'rgba(14,46,43,.72)',
  overlayColor = '#fff',
  placeholder = 'Arraste uma imagem',
  className,
  style,
  ...rest
}) {
  const slotRef = React.useRef(null);
  const [open, setOpen] = React.useState(false);
  const [zoomSrc, setZoomSrc] = React.useState(src || null);
  const isFull = heightPreset === 'full';
  const resolvedHeight = isFull ? null : HEIGHTS[heightPreset] || height;
  const cssHeight = typeof resolvedHeight === 'number' ? `${resolvedHeight}px` : resolvedHeight;

  // Slot: em 'full' ajusta à proporção; nos demais, altura fixa + recorte.
  React.useEffect(() => {
    if (!slot) return undefined;
    const el = slotRef.current;
    if (!el) return undefined;
    const sync = () => {
      const img = slotImg(el);
      if (img && img.getAttribute('src')) setZoomSrc(img.getAttribute('src'));
      else setZoomSrc(null);
      if (isFull && img && img.naturalWidth) {
        const w = el.clientWidth || el.offsetWidth || 1;
        el.style.height = Math.round(w * img.naturalHeight / img.naturalWidth) + 'px';
      } else {
        el.style.height = cssHeight || '46vh';
      }
    };
    const img = el.shadowRoot && el.shadowRoot.querySelector('.frame img');
    if (img) img.addEventListener('load', sync);
    const observer = typeof ResizeObserver !== 'undefined' ? new ResizeObserver(sync) : null;
    if (observer) observer.observe(el);
    const stateObserver = typeof MutationObserver !== 'undefined' ? new MutationObserver(sync) : null;
    if (stateObserver) stateObserver.observe(el, { attributes: true, attributeFilter: ['data-filled'] });
    sync();
    return () => {
      if (img) img.removeEventListener('load', sync);
      if (observer) observer.disconnect();
      if (stateObserver) stateObserver.disconnect();
    };
  }, [slot, isFull, cssHeight]);
  React.useEffect(() => {
    if (!open) return undefined;
    const onKey = e => e.key === 'Escape' && setOpen(false);
    window.addEventListener('keydown', onKey);
    return () => window.removeEventListener('keydown', onKey);
  }, [open]);
  const current = slot ? zoomSrc : src;
  const canZoom = zoom && current;
  const hasMeta = title || caption || credit;

  // Parallax visual: só com src fixo e altura fixa (não em 'full').
  const useParallax = !!src && !isFull;
  const frameStyle = {
    height: isFull ? 'auto' : cssHeight
  };
  let media;
  if (slot) {
    media = React.createElement('image-slot', {
      ref: slotRef,
      id: slot,
      shape: 'rect',
      fit,
      placeholder,
      style: {
        width: '100%',
        height: isFull ? 'auto' : cssHeight,
        display: 'block'
      }
    });
  } else if (src && useParallax) {
    media = React.createElement('div', {
      className: 'spu-bleedimg__parallax',
      role: 'img',
      'aria-label': alt,
      style: {
        width: '100%',
        height: '100%',
        backgroundImage: `url("${src}")`
      }
    });
  } else {
    media = React.createElement('img', {
      src,
      alt
    });
  }
  const capInner = React.createElement('span', {
    className: 'spu-bleedimg__cap',
    style: captionMode === 'overlay' ? {
      color: overlayColor
    } : undefined
  }, title && React.createElement('span', {
    className: 'spu-bleedimg__title',
    style: captionMode === 'overlay' ? {
      color: overlayColor
    } : undefined
  }, title), caption, credit && React.createElement('span', {
    className: 'spu-bleedimg__credit',
    style: captionMode === 'overlay' ? {
      color: overlayColor,
      opacity: .85
    } : undefined
  }, credit));
  const overlay = hasMeta && captionMode === 'overlay' && React.createElement('div', {
    className: 'spu-bleedimg__overlay',
    style: {
      background: overlayBg,
      color: overlayColor
    }
  }, capInner);
  const figureStyle = {
    marginTop: 'var(--flow-block)',
    marginBottom: 'var(--flow-block)',
    ...(bleed ? {
      width: '100vw',
      maxWidth: 'none',
      marginLeft: 'calc(50% - 50vw)',
      marginRight: 'calc(50% - 50vw)'
    } : {
      marginLeft: 0,
      marginRight: 0
    }),
    ...style
  };
  return React.createElement('figure', {
    className: __ds_scope.cx('spu-bleedimg', bleed && 'spu-bleedimg--bleed', isFull && 'spu-bleedimg--full', !canZoom && 'spu-bleedimg--plain', className),
    style: figureStyle,
    ...rest
  }, React.createElement('div', {
    className: 'spu-bleedimg__frame',
    style: frameStyle,
    onClick: canZoom ? () => setOpen(true) : undefined
  }, media, overlay), hasMeta && captionMode === 'below' && React.createElement('figcaption', null, capInner), open && React.createElement('div', {
    className: 'spu-bleed-lightbox',
    onClick: () => setOpen(false),
    role: 'dialog',
    'aria-modal': 'true'
  }, React.createElement('img', {
    src: current,
    alt,
    onClick: e => e.stopPropagation()
  })));
}
Object.assign(__ds_scope, { BleedImage });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/media/BleedImage.jsx", error: String((e && e.message) || e) }); }

// components/media/Figure.jsx
try { (() => {
__ds_scope.injectCss('spu-ph-css', `
.spu-ph{display:flex;flex-direction:column;align-items:center;justify-content:center;gap:.5rem;width:100%;height:100%;min-height:160px;background-color:var(--color-surface-warm);background-image:var(--texture-topo);background-size:300px;color:var(--slate-500)}
.spu-ph svg{opacity:.55}
.spu-ph__label{font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:.08em}
`);
__ds_scope.injectCss('spu-figure-css', `
.spu-figure{margin:var(--flow-block) 0}
.spu-figure--sm{width:min(100%,340px);margin-inline:auto}
.spu-figure--md{width:min(100%,560px);margin-inline:auto}
.spu-figure--lg{width:min(100%,820px);margin-inline:auto}
.spu-figure--full{width:100%}
.spu-figure__frame{position:relative;border-radius:var(--radius-md);overflow:hidden;border:1px solid var(--color-border);background:var(--color-surface-warm);display:block;width:100%;padding:0;cursor:zoom-in;font:inherit}
.spu-figure__frame img{width:100%;display:block}
.spu-figure__frame > image-slot{display:block;width:100% !important;max-width:100% !important;min-width:100% !important}
.spu-figure__zoom{position:absolute;top:var(--space-3);right:var(--space-3);width:36px;height:36px;border-radius:var(--radius);background:rgba(28,36,32,.62);color:#fff;display:flex;align-items:center;justify-content:center;opacity:0;transition:opacity var(--dur-fast) var(--ease-out);pointer-events:none}
.spu-figure__frame:hover .spu-figure__zoom,.spu-figure__frame:focus-visible .spu-figure__zoom{opacity:1}
.spu-figure__cap{font-size:var(--fs-caption);color:var(--text-muted);margin-top:var(--space-3);line-height:1.55}
.spu-figure--framed .spu-figure__cap{margin-top:0;padding:var(--space-3) var(--space-4);background:var(--color-surface);border:1px solid var(--color-border);border-top:none;border-radius:0 0 var(--radius-md) var(--radius-md)}
.spu-figure--framed .spu-figure__frame{border-radius:var(--radius-md) var(--radius-md) 0 0;box-shadow:var(--shadow-sm)}
.spu-figure__cap b,.spu-figure__cap strong{font:inherit;font-weight:700;text-transform:none;letter-spacing:0;color:inherit}
.spu-figure__title{display:block;font-family:var(--font-mono);font-size:var(--fs-eyebrow);font-weight:600;text-transform:uppercase;letter-spacing:.06em;color:var(--color-primary-strong);margin-bottom:.3em}
.spu-figure__credit{display:block;color:var(--text-faint);font-size:var(--fs-caption);margin-top:.25em}
.spu-lightbox{position:fixed;inset:0;z-index:1000;background:rgba(14,46,43,.93);display:flex;align-items:center;justify-content:center;padding:clamp(1rem,5vw,4rem);cursor:zoom-out;animation:spu-fade var(--dur) var(--ease-out)}
.spu-lightbox img{max-width:100%;max-height:100%;border-radius:var(--radius);box-shadow:var(--shadow-lg)}
.spu-lightbox__close{position:absolute;top:var(--space-5);right:var(--space-5);width:44px;height:44px;border-radius:var(--radius-pill);border:none;background:rgba(255,255,255,.14);color:#fff;display:flex;align-items:center;justify-content:center;cursor:pointer}
.spu-lightbox__close:hover{background:rgba(255,255,255,.26)}
@keyframes spu-fade{from{opacity:0}to{opacity:1}}
`);

// Lê a <img> dentro do shadowRoot de um <image-slot>, se já tiver src preenchido.
function slotImg(el) {
  const img = el && el.shadowRoot && el.shadowRoot.querySelector('.frame img');
  return img && img.getAttribute('src') ? img : null;
}
const SIZE_ALIAS = {
  small: 'sm',
  pequena: 'sm',
  medium: 'md',
  medio: 'md',
  media: 'md',
  large: 'lg',
  wide: 'lg',
  ampla: 'lg',
  total: 'full'
};
function Figure({
  src,
  slot,
  alt = '',
  caption,
  title,
  credit,
  label,
  size = 'md',
  fit = 'contain',
  zoom = true,
  placeholder,
  className,
  style
}) {
  const resolvedSize = SIZE_ALIAS[size] || size;
  const [open, setOpen] = React.useState(false);
  const [zoomSrc, setZoomSrc] = React.useState(src || null);
  const slotRef = React.useRef(null);

  // Slot editável: ajusta a altura à proporção da imagem solta e capta o src p/ zoom.
  React.useEffect(() => {
    if (!slot) return undefined;
    const el = slotRef.current;
    if (!el) return undefined;
    const sync = () => {
      const img = slotImg(el);
      if (img && img.naturalWidth) {
        const w = el.clientWidth || el.offsetWidth || 1;
        el.style.height = Math.round(w * img.naturalHeight / img.naturalWidth) + 'px';
        el.setAttribute('data-has-img', '1');
        setZoomSrc(img.getAttribute('src'));
      } else {
        el.style.height = '300px';
        el.removeAttribute('data-has-img');
        setZoomSrc(null);
      }
    };
    const img = el.shadowRoot && el.shadowRoot.querySelector('.frame img');
    if (img) img.addEventListener('load', sync);
    const observer = typeof ResizeObserver !== 'undefined' ? new ResizeObserver(sync) : null;
    if (observer) observer.observe(el);
    const stateObserver = typeof MutationObserver !== 'undefined' ? new MutationObserver(sync) : null;
    if (stateObserver) stateObserver.observe(el, { attributes: true, attributeFilter: ['data-filled'] });
    sync();
    return () => {
      if (img) img.removeEventListener('load', sync);
      if (observer) observer.disconnect();
      if (stateObserver) stateObserver.disconnect();
    };
  }, [slot]);
  React.useEffect(() => {
    if (!open) return undefined;
    const onKey = e => e.key === 'Escape' && setOpen(false);
    window.addEventListener('keydown', onKey);
    return () => window.removeEventListener('keydown', onKey);
  }, [open]);
  let media;
  if (slot) {
    media = React.createElement('image-slot', {
      ref: slotRef,
      id: slot,
      shape: 'rect',
      fit,
      placeholder: placeholder || 'Arraste uma imagem',
      style: {
        width: '100%',
        height: '300px',
        display: 'block'
      }
    });
  } else if (src) {
    media = React.createElement('img', {
      src,
      alt
    });
  } else {
    media = React.createElement('div', {
      className: 'spu-ph'
    }, React.createElement(__ds_scope.Icon, {
      name: 'building',
      size: 30
    }), React.createElement('span', {
      className: 'spu-ph__label'
    }, 'Imagem'));
  }
  const canZoom = zoom && (slot ? zoomSrc : src);
  const imageTitle = title || label;
  const framed = !!(slot && (imageTitle || caption || credit));
  return React.createElement('figure', {
    className: __ds_scope.cx('spu-figure', `spu-figure--${resolvedSize}`, framed && 'spu-figure--framed', className),
    style
  }, React.createElement('button', {
    type: 'button',
    className: 'spu-figure__frame',
    style: canZoom ? undefined : {
      cursor: 'default'
    },
    onClick: canZoom ? () => setOpen(true) : undefined,
    'aria-label': canZoom ? 'Ampliar imagem' : undefined
  }, media, canZoom && React.createElement('span', {
    className: 'spu-figure__zoom'
  }, React.createElement(__ds_scope.Icon, {
    name: 'maximize',
    size: 18
  }))), (caption || imageTitle || credit) && React.createElement('figcaption', {
    className: 'spu-figure__cap'
  }, imageTitle && React.createElement('span', {
    className: 'spu-figure__title'
  }, __ds_scope.renderRich(imageTitle, {
    inline: true
  })), __ds_scope.renderRich(caption, {
    inline: true
  }), credit && React.createElement('span', {
    className: 'spu-figure__credit'
  }, typeof credit === 'string' ? 'Fonte: ' + credit : credit)), open && React.createElement('div', {
    className: 'spu-lightbox',
    onClick: () => setOpen(false),
    role: 'dialog',
    'aria-modal': 'true'
  }, React.createElement('button', {
    type: 'button',
    className: 'spu-lightbox__close',
    'aria-label': 'Fechar'
  }, React.createElement(__ds_scope.Icon, {
    name: 'x',
    size: 22
  })), React.createElement('img', {
    src: slot ? zoomSrc : src,
    alt,
    onClick: e => e.stopPropagation()
  })));
}
Object.assign(__ds_scope, { Figure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/media/Figure.jsx", error: String((e && e.message) || e) }); }

// components/media/FullBleed.jsx
try { (() => {
__ds_scope.injectCss('spu-fullbleed-css', `
.spu-fullbleed{position:relative;border-radius:var(--radius-lg);overflow:hidden;display:flex;background:var(--petrol-800);background-image:var(--texture-topo);background-size:420px}
.spu-fullbleed--bleed{border-radius:0;width:100vw;margin-left:calc(50% - 50vw);max-width:none}
.spu-fullbleed__img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover}
image-slot.spu-fullbleed__img{display:block}
.spu-fullbleed__scrim{position:absolute;inset:0;pointer-events:none}
.spu-fullbleed__content{position:relative;padding:clamp(1.75rem,5vw,3.5rem);color:var(--text-on-dark);max-width:44rem;width:100%}
.spu-fullbleed--wide .spu-fullbleed__content{max-width:var(--container-wide);margin:0 auto;padding-inline:var(--gutter)}
.spu-fullbleed__kicker{font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:var(--ls-eyebrow);color:var(--ochre-300);font-weight:500;display:flex;align-items:center;gap:.5em}
.spu-fullbleed__title{font-family:var(--font-display);font-weight:800;font-size:var(--fs-h2);line-height:1.08;letter-spacing:var(--ls-display);color:#fff;margin:.3em 0 .35em;text-wrap:balance}
.spu-fullbleed__body{color:var(--text-on-dark);font-size:var(--fs-body-lg);opacity:.92}
.spu-fullbleed__body>:last-child{margin-bottom:0}
`);
const VPOS = {
  top: 'flex-start',
  center: 'center',
  bottom: 'flex-end'
};
const SCRIMS = {
  bottom: 'linear-gradient(180deg, rgba(14,46,43,.15) 0%, rgba(14,46,43,.30) 45%, rgba(14,46,43,.86) 100%)',
  full: 'linear-gradient(180deg, rgba(14,46,43,.55), rgba(14,46,43,.72))',
  left: 'linear-gradient(90deg, rgba(14,46,43,.85) 0%, rgba(14,46,43,.35) 60%, rgba(14,46,43,.1) 100%)',
  // overlay escuro e uniforme (referência tipo "Peru") — reforço no rodapé p/ legibilidade
  dark: 'linear-gradient(180deg, rgba(11,38,35,.74) 0%, rgba(11,38,35,.72) 45%, rgba(11,38,35,.90) 100%)'
};
function FullBleed({
  src,
  slot,
  alt = '',
  kicker,
  kickerIcon,
  title,
  children,
  height = 380,
  align = 'bottom',
  scrim = 'bottom',
  bleed = false,
  wide = false,
  grayscale,
  placeholder = 'Solte uma imagem de fundo',
  className,
  style
}) {
  const scrimBg = SCRIMS[scrim] || SCRIMS.bottom;
  // Foto sem cor por padrão quando há overlay escuro — o overlay verde tinge a imagem.
  const gray = grayscale != null ? grayscale : scrim === 'dark';
  let bg = null;
  const bgFill = {
    position: 'absolute',
    inset: 0,
    width: '100%',
    height: '100%'
  };
  if (gray) bgFill.filter = 'grayscale(1) contrast(1.04)';
  if (slot) {
    bg = React.createElement('image-slot', {
      key: 'slot',
      id: slot,
      className: 'spu-fullbleed__img',
      fit: 'cover',
      placeholder,
      src: src || undefined,
      style: bgFill
    });
  } else if (src) {
    bg = React.createElement('img', {
      key: 'img',
      className: 'spu-fullbleed__img',
      src,
      alt,
      style: bgFill
    });
  }
  return React.createElement('section', {
    className: __ds_scope.cx('spu-fullbleed', bleed && 'spu-fullbleed--bleed', wide && 'spu-fullbleed--wide', className),
    style: {
      minHeight: typeof height === 'number' ? `${height}px` : height,
      alignItems: VPOS[align] || 'flex-end',
      ...style
    }
  }, bg, React.createElement('div', {
    key: 'scrim',
    className: 'spu-fullbleed__scrim',
    style: {
      background: scrimBg
    }
  }), React.createElement('div', {
    key: 'content',
    className: 'spu-fullbleed__content'
  }, kicker && React.createElement('div', {
    className: 'spu-fullbleed__kicker'
  }, kickerIcon && React.createElement(__ds_scope.Icon, {
    name: kickerIcon,
    size: 16
  }), kicker), title && React.createElement('h2', {
    className: 'spu-fullbleed__title'
  }, title), children && React.createElement('div', {
    className: 'spu-fullbleed__body'
  }, children)));
}
Object.assign(__ds_scope, { FullBleed });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/media/FullBleed.jsx", error: String((e && e.message) || e) }); }

// components/content/Conclusion.jsx
try { (() => {
const isRichHtml = value => typeof value === 'string' && /[<&]/.test(value);
function richBlock(value) {
  return isRichHtml(value) ? React.createElement(__ds_scope.RichText, {
    html: value
  }) : value;
}
__ds_scope.injectCss('spu-conclusion-css', `
.spu-conclusion .spu-fullbleed__content{max-width:62rem}
.spu-conclusion .spu-fullbleed__body{max-width:none}
.spu-conclusion__body{margin-top:var(--space-3)}
.spu-conclusion__body>:last-child{margin-bottom:0}
.spu-conclusion__extra{margin-top:var(--space-6);display:flex;flex-direction:column;gap:var(--flow-block);text-align:left}
`);

// Padrão de fechamento de página: faixa full-bleed escura, centralizada,
// com kicker "Para fechar" + título e o texto de síntese/conclusão.
// `body` = texto rico padrão; `children` = blocos extras abaixo do padrão.
function Conclusion({
  id = 'conclusao',
  kicker = 'Para fechar',
  kickerIcon = 'check',
  title = 'Conclusão',
  body,
  src,
  slot,
  scrim = 'dark',
  height,
  children,
  className,
  style,
  ...rest
}) {
  return React.createElement('div', {
    id,
    className: 'op-hero-wrap',
    'data-screen-label': id,
    ...rest
  }, React.createElement(__ds_scope.FullBleed, {
    bleed: true,
    wide: true,
    align: 'center',
    scrim,
    kicker,
    kickerIcon,
    title,
    src,
    slot,
    height,
    className: __ds_scope.cx('spu-conclusion', className),
    style
  }, body && React.createElement('div', {
    className: 'spu-conclusion__body'
  }, richBlock(body)), children && React.createElement('div', {
    className: 'spu-conclusion__extra'
  }, children)));
}
Object.assign(__ds_scope, { Conclusion });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/content/Conclusion.jsx", error: String((e && e.message) || e) }); }

// components/media/Hero.jsx
try { (() => {
__ds_scope.injectCss('spu-hero-css', `
.spu-hero{position:relative;overflow:hidden;isolation:isolate;background:var(--color-surface-warm);display:flex;flex-direction:column;justify-content:center}
.spu-hero__bg{position:absolute;inset:-18% 0;z-index:0;will-change:transform}
.spu-hero__bg img,.spu-hero__bg image-slot{width:100%;height:100%;object-fit:cover;object-position:60% center;display:block}
.spu-hero__bg::after{content:"";position:absolute;inset:0;pointer-events:none;background:linear-gradient(105deg, rgba(14,46,43,.10) 0%, rgba(14,46,43,0) 46%), linear-gradient(0deg, rgba(236,228,211,.16), rgba(236,228,211,0) 30%)}
.spu-hero__inner{position:relative;z-index:1;max-width:var(--container-wide);margin:0 auto;width:100%;min-height:inherit;padding:clamp(2.5rem,7vw,5.5rem) var(--gutter) clamp(3rem,8vw,6rem);display:flex;flex-direction:column;justify-content:center}
.spu-hero__boxes{will-change:transform;max-width:47rem}
.spu-hero__title{background:var(--_scrim);border:1px solid rgba(237,231,218,.16);border-radius:var(--radius-md);padding:clamp(1.5rem,3.4vw,2.8rem) clamp(1.5rem,3.4vw,3rem);box-shadow:var(--shadow-lg)}
.spu-hero__kicker{display:inline-flex;align-items:center;gap:.5em;font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:var(--ls-eyebrow);color:var(--ochre-300);font-weight:500}
.spu-hero__kicker svg{color:var(--ochre-300)}
.spu-hero__h1{font-family:var(--font-display);font-weight:800;font-size:clamp(1.85rem,4.2vw,var(--fs-h1));line-height:1.08;letter-spacing:var(--ls-display);color:#fff;margin:.5em 0 0;text-wrap:balance}
.spu-hero__h1 .spu-richtext{line-height:inherit;color:inherit}
.spu-hero__h1 .spu-richtext strong,.spu-hero__h1 .spu-richtext b{color:inherit}
.spu-hero__byline{align-self:flex-start;width:fit-content;max-width:100%;margin-top:clamp(.9rem,1.8vw,1.4rem);background:var(--ochre-800);color:#F6EFE3;font-family:var(--font-mono);font-size:.86rem;letter-spacing:.04em;text-transform:uppercase;padding:.85em 1.4em;border-radius:var(--radius);box-shadow:var(--shadow-md)}
.spu-hero__kicker .spu-richtext,.spu-hero__byline .spu-richtext{color:inherit;line-height:inherit}
.spu-hero__kicker .spu-richtext strong,.spu-hero__kicker .spu-richtext b,.spu-hero__byline .spu-richtext strong,.spu-hero__byline .spu-richtext b{color:inherit}
@media (max-width:720px){
  .spu-hero{display:block;min-height:0 !important}
  .spu-hero__bg{position:absolute;inset:0;height:auto;min-height:0}
  .spu-hero__inner{display:block;min-height:0;padding:max(12rem,calc(40vh - 3rem)) var(--gutter) clamp(1.75rem,6vw,2.5rem)}
  .spu-hero__boxes{transform:none !important;max-width:none;position:relative;margin-top:0}
  .spu-hero__title{box-shadow:var(--shadow-md)}
}
@media (prefers-reduced-motion:reduce){.spu-hero__bg,.spu-hero__boxes{transform:none !important}}
`);
function Hero({
  src,
  slot,
  alt = '',
  kicker,
  kickerIcon,
  title,
  byline,
  parallax = true,
  height = 'clamp(320px, 56vh, 480px)',
  scrim = 'rgba(14, 46, 43, 0.85)',
  placeholder = 'Solte a imagem de fundo do hero',
  id,
  className,
  style
}) {
  const rootRef = React.useRef(null);
  const bgRef = React.useRef(null);
  const boxRef = React.useRef(null);
  React.useEffect(() => {
    if (!parallax) return undefined;
    const mq = window.matchMedia('(max-width: 720px), (prefers-reduced-motion: reduce)');
    let raf = 0;
    const update = () => {
      raf = 0;
      const root = rootRef.current;
      if (!root) return;
      if (mq.matches) {
        if (bgRef.current) bgRef.current.style.transform = '';
        if (boxRef.current) boxRef.current.style.transform = '';
        return;
      }
      const y = -root.getBoundingClientRect().top;
      if (bgRef.current) bgRef.current.style.transform = `translate3d(0, ${y * 0.22}px, 0)`;
      if (boxRef.current) boxRef.current.style.transform = `translate3d(0, ${y * -0.16}px, 0)`;
    };
    const onScroll = () => {
      if (!raf) raf = requestAnimationFrame(update);
    };
    update();
    window.addEventListener('scroll', onScroll, {
      passive: true
    });
    window.addEventListener('resize', onScroll);
    return () => {
      window.removeEventListener('scroll', onScroll);
      window.removeEventListener('resize', onScroll);
      if (raf) cancelAnimationFrame(raf);
    };
  }, [parallax]);
  let bg = null;
  if (slot) {
    bg = React.createElement('image-slot', {
      id: slot,
      shape: 'rect',
      fit: 'cover',
      placeholder,
      src: src || undefined
    });
  } else if (src) {
    bg = React.createElement('img', {
      src,
      alt
    });
  }
  const kickerNode = __ds_scope.renderRich(kicker, {
    inline: true
  });
  const titleNode = __ds_scope.renderRich(title, {
    inline: true
  });
  const bylineNode = __ds_scope.renderRich(byline, {
    inline: true
  });
  return React.createElement('div', {
    id,
    ref: rootRef,
    className: __ds_scope.cx('spu-hero', className),
    style: {
      minHeight: typeof height === 'number' ? `${height}px` : height,
      '--_scrim': scrim,
      ...style
    },
    'data-screen-label': id
  }, React.createElement('div', {
    className: 'spu-hero__bg',
    ref: bgRef
  }, bg), React.createElement('div', {
    className: 'spu-hero__inner'
  }, React.createElement('div', {
    className: 'spu-hero__boxes',
    ref: boxRef
  }, React.createElement('div', {
    className: 'spu-hero__title'
  }, kicker && React.createElement('span', {
    className: 'spu-hero__kicker'
  }, kickerIcon && React.createElement(__ds_scope.Icon, {
    name: kickerIcon,
    size: 16
  }), kickerNode), title && React.createElement('h1', {
    className: 'spu-hero__h1'
  }, titleNode)), byline && React.createElement('div', {
    className: 'spu-hero__byline'
  }, bylineNode))));
}
Object.assign(__ds_scope, { Hero });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/media/Hero.jsx", error: String((e && e.message) || e) }); }

// components/media/ImageReveal.jsx
try { (() => {
__ds_scope.injectCss('spu-reveal-css', `
.spu-reveal{position:relative;border-radius:var(--radius-md);overflow:hidden;border:1px solid var(--color-border);user-select:none;touch-action:none;background:var(--color-surface-warm)}
.spu-reveal img{display:block;width:100%;pointer-events:none}
.spu-reveal__after{position:absolute;inset:0;will-change:clip-path}
.spu-reveal__after img{position:absolute;inset:0;height:100%;object-fit:cover}
.spu-reveal__handle{position:absolute;top:0;bottom:0;width:3px;background:#fff;box-shadow:0 0 0 1px rgba(28,36,32,.25);transform:translateX(-50%);cursor:ew-resize}
.spu-reveal__grip{position:absolute;top:50%;left:50%;transform:translate(-50%,-50%);width:44px;height:44px;border-radius:var(--radius-pill);background:#fff;color:var(--color-primary-strong);display:flex;align-items:center;justify-content:center;gap:-2px;box-shadow:var(--shadow-md)}
.spu-reveal__label{position:absolute;bottom:var(--space-3);font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:.06em;color:#fff;background:rgba(14,46,43,.7);padding:.3em .7em;border-radius:var(--radius-pill);pointer-events:none}
.spu-reveal__label--a{left:var(--space-3)}
.spu-reveal__label--b{right:var(--space-3)}
.spu-reveal-print{display:flex;flex-direction:column;gap:var(--space-3);break-inside:avoid}
.spu-reveal-print figure{position:relative;margin:0;border-radius:var(--radius-md);overflow:hidden;border:1px solid var(--color-border)}
.spu-reveal-print img{display:block;width:100%}
.spu-reveal-print__tag{position:absolute;top:var(--space-3);left:var(--space-3);font-family:var(--font-mono);font-size:var(--fs-eyebrow);text-transform:uppercase;letter-spacing:.06em;color:#fff;background:rgba(14,46,43,.78);padding:.3em .7em;border-radius:var(--radius-pill)}
`);
function Ph(label) {
  return React.createElement('div', {
    className: 'spu-ph',
    style: {
      minHeight: 260
    }
  }, React.createElement(__ds_scope.Icon, {
    name: 'layers',
    size: 28
  }), React.createElement('span', {
    className: 'spu-ph__label'
  }, label));
}

// before/after = src (data-URL/URL direto). beforeSlot/afterSlot = id de
// <image-slot> (o builder envia a imagem por drag, como no Figure).
function ImageReveal({
  before,
  after,
  beforeSlot,
  afterSlot,
  beforeLabel = 'Antes',
  afterLabel = 'Depois',
  alt = '',
  start = 50,
  className,
  style
}) {
  const beforeMedia = beforeSlot ? React.createElement('image-slot', {
    id: beforeSlot,
    shape: 'rect',
    fit: 'cover',
    placeholder: beforeLabel,
    style: {
      display: 'block',
      width: '100%',
      minHeight: 260
    }
  }) : before ? React.createElement('img', {
    src: before,
    alt
  }) : Ph(beforeLabel);
  const afterMedia = afterSlot ? React.createElement('image-slot', {
    id: afterSlot,
    shape: 'rect',
    fit: 'cover',
    placeholder: afterLabel,
    style: {
      position: 'absolute',
      inset: 0,
      width: '100%',
      height: '100%',
      display: 'block'
    }
  }) : after ? React.createElement('img', {
    src: after,
    alt
  }) : Ph(afterLabel);
  const [pct, setPct] = React.useState(start);
  const ref = React.useRef(null);
  const dragging = React.useRef(false);
  const move = clientX => {
    const el = ref.current;
    if (!el) return;
    const r = el.getBoundingClientRect();
    const p = Math.max(0, Math.min(100, (clientX - r.left) / r.width * 100));
    setPct(p);
  };
  const onDown = e => {
    dragging.current = true;
    move(e.clientX);
    e.currentTarget.setPointerCapture?.(e.pointerId);
  };
  const onMove = e => {
    if (dragging.current) move(e.clientX);
  };
  const onUp = () => {
    dragging.current = false;
  };

  // —— Impressão: antes e depois empilhados ——
  if (__ds_scope.isPrint()) {
    return React.createElement('div', {
      className: __ds_scope.cx('spu-reveal-print', className),
      style
    }, React.createElement('figure', null, React.createElement('span', {
      className: 'spu-reveal-print__tag'
    }, beforeLabel), beforeSlot ? React.createElement('image-slot', {
      id: beforeSlot,
      shape: 'rect',
      fit: 'cover',
      placeholder: beforeLabel,
      style: {
        display: 'block',
        width: '100%'
      }
    }) : before ? React.createElement('img', {
      src: before,
      alt
    }) : Ph(beforeLabel)), React.createElement('figure', null, React.createElement('span', {
      className: 'spu-reveal-print__tag'
    }, afterLabel), afterSlot ? React.createElement('image-slot', {
      id: afterSlot,
      shape: 'rect',
      fit: 'cover',
      placeholder: afterLabel,
      style: {
        display: 'block',
        width: '100%'
      }
    }) : after ? React.createElement('img', {
      src: after,
      alt
    }) : Ph(afterLabel)));
  }
  return React.createElement('figure', {
    className: __ds_scope.cx('spu-reveal', className),
    style,
    ref,
    onPointerDown: onDown,
    onPointerMove: onMove,
    onPointerUp: onUp,
    onPointerLeave: onUp
  }, beforeMedia, React.createElement('div', {
    className: 'spu-reveal__after',
    style: {
      clipPath: `inset(0 ${100 - pct}% 0 0)`
    }
  }, afterMedia), React.createElement('span', {
    className: 'spu-reveal__label spu-reveal__label--a'
  }, beforeLabel), React.createElement('span', {
    className: 'spu-reveal__label spu-reveal__label--b'
  }, afterLabel), React.createElement('div', {
    className: 'spu-reveal__handle',
    style: {
      left: `${pct}%`
    }
  }, React.createElement('div', {
    className: 'spu-reveal__grip'
  }, React.createElement(__ds_scope.Icon, {
    name: 'chevron-left',
    size: 16
  }), React.createElement(__ds_scope.Icon, {
    name: 'chevron-right',
    size: 16
  }))));
}
Object.assign(__ds_scope, { ImageReveal });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/media/ImageReveal.jsx", error: String((e && e.message) || e) }); }

// components/media/MapFigure.jsx
try { (() => {
const ri = v => __ds_scope.renderRich(v, {
  inline: true
});
__ds_scope.injectCss('spu-map-css', `
.spu-map{margin:var(--flow-block) 0}
.spu-map__stage{position:relative}
.spu-map__frame{position:relative;border-radius:var(--radius-md);overflow:hidden;border:1px solid var(--color-border);background:var(--petrol-50);background-image:var(--texture-topo);background-size:340px}
.spu-map__layer{position:absolute;inset:0;pointer-events:none}
.spu-map__layer .spu-map__marker{pointer-events:auto}
.spu-map__img{width:100%;display:block}
.spu-map__phbox{min-height:300px;display:flex;align-items:center;justify-content:center}
.spu-map__marker{position:absolute;transform:translate(-50%,-50%);z-index:4}
.spu-map__pin{width:30px;height:30px;border-radius:var(--radius-pill);border:2px solid #fff;background:var(--color-accent);color:#fff;font-family:var(--font-mono);font-weight:600;font-size:.8rem;display:flex;align-items:center;justify-content:center;cursor:pointer;box-shadow:var(--shadow-md);transition:transform var(--dur-fast) var(--ease-out),background var(--dur-fast)}
.spu-map__pin:hover{transform:scale(1.12)}
.spu-map__pin--active{background:var(--color-primary);transform:scale(1.12)}
.spu-map__pop{position:absolute;z-index:3;transform:translate(-50%,calc(-100% - 18px));width:min(260px,70vw);background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-md);box-shadow:var(--shadow-lg);padding:var(--space-4);text-align:left}
.spu-map__pop::after{content:"";position:absolute;left:50%;bottom:-7px;transform:translateX(-50%) rotate(45deg);width:12px;height:12px;background:var(--color-surface);border-right:1px solid var(--color-border);border-bottom:1px solid var(--color-border)}
.spu-map__poptitle{font-family:var(--font-display);font-weight:700;font-size:var(--fs-small);color:var(--text-strong);margin:0 0 .2em;display:flex;align-items:center;gap:.4em}
.spu-map__poptitle svg{color:var(--color-accent)}
.spu-map__popdesc{font-size:var(--fs-caption);color:var(--text-muted);margin:0;line-height:1.5}
.spu-map__cap{font-size:var(--fs-caption);color:var(--text-muted);margin-top:var(--space-3);line-height:1.5}
.spu-map__cap b,.spu-map__cap strong{font:inherit;font-weight:700;text-transform:none;letter-spacing:0;color:inherit}
.spu-map__title{display:block;font-family:var(--font-mono);font-size:var(--fs-eyebrow);font-weight:600;text-transform:uppercase;letter-spacing:.06em;color:var(--color-primary-strong);margin-bottom:.3em}
.spu-map__legend{list-style:none;margin:var(--space-4) 0 0;padding:0;display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:var(--space-3)}
.spu-map__legend li{display:flex;gap:.6em;font-size:var(--fs-caption);color:var(--text-body);line-height:1.45}
.spu-map__legend b{flex:0 0 auto;width:1.5em;height:1.5em;border-radius:var(--radius-pill);background:var(--color-accent);color:#fff;font-family:var(--font-mono);font-size:.72rem;font-weight:600;display:flex;align-items:center;justify-content:center}
.spu-map__legend strong{color:var(--text-strong);font-weight:700}
`);
function MapFigure({
  src,
  slot,
  alt = '',
  markers = [],
  caption,
  credit,
  title,
  label,
  className,
  style
}) {
  const [active, setActive] = React.useState(null);
  const imageTitle = title || label;
  const media = slot ? React.createElement('image-slot', {
    id: slot,
    shape: 'rect',
    fit: 'cover',
    'data-auto-height': '',
    placeholder: 'Arraste uma imagem',
    style: {
      width: '100%',
      display: 'block',
      minHeight: 300
    }
  }) : src ? React.createElement('img', {
    className: 'spu-map__img',
    src,
    alt
  }) : React.createElement('div', {
    className: 'spu-map__phbox'
  }, React.createElement('div', {
    className: 'spu-ph',
    style: {
      minHeight: 300
    }
  }, React.createElement(__ds_scope.Icon, {
    name: 'compass',
    size: 32
  }), React.createElement('span', {
    className: 'spu-ph__label'
  }, 'Mapa / cartografia')));
  return React.createElement('figure', {
    className: __ds_scope.cx('spu-map', className),
    style
  }, React.createElement('div', {
    className: 'spu-map__stage'
  }, React.createElement('div', {
    className: 'spu-map__frame'
  }, media), React.createElement('div', {
    className: 'spu-map__layer'
  }, markers.map((m, i) => React.createElement('div', {
    key: i,
    className: 'spu-map__marker',
    style: {
      left: `${m.x}%`,
      top: `${m.y}%`
    }
  }, active === i && React.createElement('div', {
    className: 'spu-map__pop'
  }, React.createElement('p', {
    className: 'spu-map__poptitle'
  }, React.createElement(__ds_scope.Icon, {
    name: 'map-pin',
    size: 15
  }), ri(m.title)), m.description && React.createElement('p', {
    className: 'spu-map__popdesc'
  }, ri(m.description))), React.createElement('button', {
    type: 'button',
    className: __ds_scope.cx('spu-map__pin', active === i && 'spu-map__pin--active'),
    onClick: () => setActive(active === i ? null : i),
    'aria-label': m.title
  }, m.label != null ? m.label : i + 1))))), (caption || imageTitle || credit) && React.createElement('figcaption', {
    className: 'spu-map__cap'
  }, imageTitle && React.createElement('span', {
    className: 'spu-map__title'
  }, ri(imageTitle)), ri(caption), credit && React.createElement('span', {
    style: {
      color: 'var(--text-faint)',
      display: 'block',
      marginTop: '.2em'
    }
  }, 'Fonte: ', ri(credit))), __ds_scope.isPrint() && markers.length > 0 && React.createElement('ol', {
    className: 'spu-map__legend'
  }, markers.map((m, i) => React.createElement('li', {
    key: i
  }, React.createElement('b', null, m.label != null ? m.label : i + 1), React.createElement('span', null, React.createElement('strong', null, ri(m.title)), m.description && React.createElement('span', null, ' — ', ri(m.description)))))));
}
Object.assign(__ds_scope, { MapFigure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/media/MapFigure.jsx", error: String((e && e.message) || e) }); }

// components/media/TextWithImage.jsx
try { (() => {
__ds_scope.injectCss('spu-twi-css', `
.spu-twi{display:grid;gap:clamp(1.5rem,4vw,3rem);align-items:center;margin:var(--flow-block) 0}
.spu-twi__media{border-radius:var(--radius-md);overflow:hidden;border:1px solid var(--color-border);background:var(--color-surface-warm);box-shadow:var(--shadow-sm)}
.spu-twi__media img{width:100%;display:block}
.spu-twi__cap{font-size:var(--fs-caption);color:var(--text-muted);margin-top:var(--space-3);line-height:1.5}
.spu-twi__text>:last-child{margin-bottom:0}
@media (max-width:680px){.spu-twi{grid-template-columns:1fr !important}}
`);
function TextWithImage({
  children,
  src,
  alt = '',
  caption,
  side = 'right',
  imageWidth = '44%',
  className,
  style
}) {
  const cols = side === 'left' ? `minmax(0, ${imageWidth}) 1fr` : `1fr minmax(0, ${imageWidth})`;
  const media = React.createElement('div', {
    key: 'm'
  }, React.createElement('div', {
    className: 'spu-twi__media'
  }, src ? React.createElement('img', {
    src,
    alt
  }) : React.createElement('div', {
    className: 'spu-ph'
  }, React.createElement(__ds_scope.Icon, {
    name: 'building',
    size: 28
  }), React.createElement('span', {
    className: 'spu-ph__label'
  }, 'Imagem'))), caption && React.createElement('p', {
    className: 'spu-twi__cap'
  }, caption));
  const text = React.createElement('div', {
    key: 't',
    className: 'spu-twi__text'
  }, children);
  return React.createElement('div', {
    className: __ds_scope.cx('spu-twi', className),
    style: {
      gridTemplateColumns: cols,
      ...style
    }
  }, side === 'left' ? [media, text] : [text, media]);
}
Object.assign(__ds_scope, { TextWithImage });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/media/TextWithImage.jsx", error: String((e && e.message) || e) }); }

// export/src/page.jsx
try { (() => {
/* global React */
// Built lazily once the DS bundle is ready (ds-base.js loads it async).
function buildSpuApp(NS) {
  const {
    Kicker,
    Callout,
    PullQuote,
    StatBlock,
    FeatureGrid,
    MarkerList,
    Figure,
    FullBleed,
    Hero,
    BleedImage,
    TextWithImage,
    ImageReveal,
    MapFigure,
    Flipcard,
    Carousel,
    Timeline,
    GlossaryTerm,
    Accordion,
    CompareAB,
    DataTable,
    MediaEmbed,
    ReflectionStop,
    ResourceList,
    ReferenceList,
    Panel,
    ExampleCard,
    PageFooter,
    Conclusion,
    Stack,
    Columns,
    LicenseBadge,
    Button,
    Badge,
    Tag,
    Icon
  } = NS;
  const GlossaryFootnotes = NS.GlossaryFootnotes || (() => null);
  const SECTIONS = [{
    id: 'abertura',
    label: 'Abertura'
  }, {
    id: 'conceitos',
    label: 'Conceitos'
  }, {
    id: 'historia',
    label: 'Linha do tempo'
  }, {
    id: 'territorio',
    label: 'Território'
  }, {
    id: 'regimes',
    label: 'Regimes'
  }, {
    id: 'sintese',
    label: 'Síntese'
  }, {
    id: 'conclusao',
    label: 'Conclusão'
  }, {
    id: 'fontes',
    label: 'Fontes'
  }];

  // ——— Tweaks: cor + ícone dos cards/callouts ———
  const ACCENTS = {
    petrol: {
      label: 'Petróleo',
      primary: '#1F5D5A',
      strong: '#16433F',
      soft: '#EAF3F1'
    },
    terra: {
      label: 'Terracota',
      primary: '#C4502E',
      strong: '#9E3D20',
      soft: '#FBEFE8'
    },
    ochre: {
      label: 'Ocre',
      primary: '#C8922A',
      strong: '#9A6A14',
      soft: '#FBF5E6'
    }
  };
  const ICON_OPTIONS = ['target', 'book-open', 'compass', 'layers', 'building', 'scale', 'sparkles', 'lightbulb', 'map-pin'];

  // Cards/callouts editáveis individualmente.
  const CARD_DEFS = [{
    id: 'objetivos',
    kind: 'callout',
    label: 'Callout · Objetivos',
    defaultIcon: 'target'
  }, {
    id: 'imovel',
    kind: 'flip',
    label: 'Card · Imóvel da União',
    defaultIcon: 'building',
    term: 'Imóvel da União',
    definition: 'Bem público federal sob gestão da SPU, afetado ou não a um uso público específico.'
  }, {
    id: 'afetacao',
    kind: 'flip',
    label: 'Card · Afetação',
    defaultIcon: 'scale',
    term: 'Afetação',
    definition: 'Vínculo de um bem público a uma finalidade ou serviço público, o que o torna inalienável enquanto durar.'
  }, {
    id: 'marinha',
    kind: 'flip',
    label: 'Card · Terreno de marinha',
    defaultIcon: 'map-pin',
    term: 'Terreno de marinha',
    definition: 'Faixa de 33 metros medida a partir da linha do preamar médio de 1831, de domínio da União.'
  }, {
    id: 'destinacao',
    kind: 'flip',
    label: 'Card · Destinação',
    defaultIcon: 'compass',
    term: 'Destinação',
    definition: 'Ato de dar uma finalidade a um imóvel da União — cessão, concessão, entrega ou alienação.'
  }];
  const DEFAULT_CARDS = CARD_DEFS.reduce((acc, c) => {
    acc[c.id] = {
      accent: 'petrol',
      icon: c.defaultIcon
    };
    return acc;
  }, {});
  function resolveAccent(val) {
    if (val && val[0] === '#') {
      return {
        primary: val,
        strong: val,
        soft: `color-mix(in srgb, ${val} 12%, #fff)`
      };
    }
    return ACCENTS[val] || ACCENTS.petrol;
  }
  function TweaksPanel({
    visible,
    cards,
    onChange,
    onClose
  }) {
    const [selId, setSelId] = React.useState(CARD_DEFS[0].id);
    if (!visible) return null;
    const sel = cards[selId] || DEFAULT_CARDS[selId];
    const selAccent = resolveAccent(sel.accent);
    const customActive = sel.accent && sel.accent[0] === '#';
    const setSel = patch => onChange(selId, patch);
    const swatch = key => {
      const a = ACCENTS[key];
      const active = sel.accent === key;
      return /*#__PURE__*/React.createElement("button", {
        key: key,
        type: "button",
        onClick: () => setSel({
          accent: key
        }),
        title: a.label,
        style: {
          display: 'flex',
          alignItems: 'center',
          gap: 8,
          padding: '7px 10px',
          borderRadius: 10,
          border: active ? '2px solid var(--text-strong)' : '1px solid var(--color-border)',
          background: active ? 'var(--sand-100)' : '#fff',
          cursor: 'pointer',
          font: 'inherit',
          fontSize: 13,
          fontWeight: 600,
          color: 'var(--text-strong)'
        }
      }, /*#__PURE__*/React.createElement("span", {
        style: {
          width: 18,
          height: 18,
          borderRadius: 5,
          background: a.primary,
          flex: '0 0 auto'
        }
      }), a.label);
    };
    return /*#__PURE__*/React.createElement("aside", {
      style: {
        position: 'fixed',
        top: 16,
        right: 16,
        zIndex: 1000,
        width: 268,
        background: '#fff',
        border: '1px solid var(--color-border)',
        borderRadius: 14,
        boxShadow: '0 14px 44px rgba(15,40,38,.22)',
        padding: 16,
        fontFamily: 'var(--font-body, system-ui)'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        marginBottom: 14
      }
    }, /*#__PURE__*/React.createElement("strong", {
      style: {
        fontFamily: 'var(--font-display)',
        fontSize: 15,
        color: 'var(--text-strong)'
      }
    }, "Tweaks"), /*#__PURE__*/React.createElement("button", {
      type: "button",
      onClick: onClose,
      "aria-label": "Fechar",
      style: {
        marginLeft: 'auto',
        border: 0,
        background: 'none',
        cursor: 'pointer',
        color: 'var(--text-muted)',
        padding: 4,
        lineHeight: 0
      }
    }, /*#__PURE__*/React.createElement(Icon, {
      name: "x",
      size: 18
    }))), /*#__PURE__*/React.createElement("p", {
      style: {
        font: '600 11px/1.3 var(--font-mono, monospace)',
        letterSpacing: '.08em',
        textTransform: 'uppercase',
        color: 'var(--text-faint)',
        margin: '0 0 8px'
      }
    }, "Card / callout"), /*#__PURE__*/React.createElement("select", {
      value: selId,
      onChange: e => setSelId(e.target.value),
      style: {
        width: '100%',
        padding: '9px 10px',
        borderRadius: 10,
        border: '1px solid var(--color-border)',
        font: 'inherit',
        fontSize: 13,
        fontWeight: 600,
        color: 'var(--text-strong)',
        background: '#fff',
        cursor: 'pointer'
      }
    }, CARD_DEFS.map(c => /*#__PURE__*/React.createElement("option", {
      key: c.id,
      value: c.id
    }, c.label))), /*#__PURE__*/React.createElement("p", {
      style: {
        font: '600 11px/1.3 var(--font-mono, monospace)',
        letterSpacing: '.08em',
        textTransform: 'uppercase',
        color: 'var(--text-faint)',
        margin: '16px 0 8px'
      }
    }, "Cor de destaque"), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        flexDirection: 'column',
        gap: 6
      }
    }, Object.keys(ACCENTS).map(swatch)), /*#__PURE__*/React.createElement("label", {
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 8,
        marginTop: 8,
        padding: '7px 10px',
        borderRadius: 10,
        border: customActive ? '2px solid var(--text-strong)' : '1px solid var(--color-border)',
        cursor: 'pointer',
        fontSize: 13,
        fontWeight: 600,
        color: 'var(--text-strong)'
      }
    }, /*#__PURE__*/React.createElement("input", {
      type: "color",
      value: customActive ? sel.accent : '#2E7A75',
      onChange: e => setSel({
        accent: e.target.value
      }),
      style: {
        width: 18,
        height: 18,
        padding: 0,
        border: 0,
        background: 'none',
        cursor: 'pointer'
      }
    }), "Cor personalizada"), /*#__PURE__*/React.createElement("p", {
      style: {
        font: '600 11px/1.3 var(--font-mono, monospace)',
        letterSpacing: '.08em',
        textTransform: 'uppercase',
        color: 'var(--text-faint)',
        margin: '16px 0 8px'
      }
    }, "\xCDcone"), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'grid',
        gridTemplateColumns: 'repeat(5, 1fr)',
        gap: 6
      }
    }, ICON_OPTIONS.map(name => {
      const active = sel.icon === name;
      return /*#__PURE__*/React.createElement("button", {
        key: name,
        type: "button",
        onClick: () => setSel({
          icon: name
        }),
        title: name,
        style: {
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          height: 38,
          borderRadius: 9,
          border: active ? '2px solid var(--text-strong)' : '1px solid var(--color-border)',
          background: active ? selAccent.soft : '#fff',
          color: active ? selAccent.strong : 'var(--text-muted)',
          cursor: 'pointer'
        }
      }, /*#__PURE__*/React.createElement(Icon, {
        name: name,
        size: 18
      }));
    })));
  }
  function ProgressBar({
    onActive
  }) {
    const [p, setP] = React.useState(0);
    React.useEffect(() => {
      const onScroll = () => {
        const h = document.documentElement;
        const max = h.scrollHeight - h.clientHeight;
        setP(max > 0 ? h.scrollTop / max * 100 : 0);
        if (onActive) {
          let active = SECTIONS[0].id;
          for (const s of SECTIONS) {
            const el = document.getElementById(s.id);
            if (el && el.getBoundingClientRect().top <= 120) active = s.id;
          }
          onActive(active);
        }
      };
      window.addEventListener('scroll', onScroll, {
        passive: true
      });
      onScroll();
      return () => window.removeEventListener('scroll', onScroll);
    }, [onActive]);
    return React.createElement('div', {
      className: 'op-progress'
    }, React.createElement('div', {
      className: 'op-progress__bar',
      style: {
        width: p + '%'
      }
    }));
  }

  // ——— Sumário recolhido, gerado a partir dos títulos de seção (SECTIONS) ———
  function Summary({
    active
  }) {
    const [open, setOpen] = React.useState(false);
    const ref = React.useRef(null);
    React.useEffect(() => {
      if (!open) return undefined;
      const onDoc = e => {
        if (ref.current && !ref.current.contains(e.target)) setOpen(false);
      };
      const onKey = e => {
        if (e.key === 'Escape') setOpen(false);
      };
      document.addEventListener('mousedown', onDoc);
      document.addEventListener('keydown', onKey);
      return () => {
        document.removeEventListener('mousedown', onDoc);
        document.removeEventListener('keydown', onKey);
      };
    }, [open]);
    return /*#__PURE__*/React.createElement("div", {
      className: "op-summary",
      ref: ref
    }, /*#__PURE__*/React.createElement("button", {
      type: "button",
      className: "op-summary__toggle",
      "aria-expanded": open,
      "aria-label": "Sum\xE1rio",
      onClick: () => setOpen(o => !o)
    }, /*#__PURE__*/React.createElement(Icon, {
      name: open ? 'x' : 'list',
      size: 16
    }), /*#__PURE__*/React.createElement("span", null, "Sum\xE1rio")), open && /*#__PURE__*/React.createElement("nav", {
      className: "op-summary__panel"
    }, /*#__PURE__*/React.createElement("p", {
      className: "op-summary__title"
    }, "Neste conte\xFAdo"), SECTIONS.map((s, i) => /*#__PURE__*/React.createElement("a", {
      key: s.id,
      href: '#' + s.id,
      className: active === s.id ? 'is-active' : '',
      onClick: () => setOpen(false)
    }, /*#__PURE__*/React.createElement("span", {
      className: "op-summary__num"
    }, String(i + 1).padStart(2, '0')), /*#__PURE__*/React.createElement("span", null, s.label)))));
  }
  function Section({
    id,
    children,
    narrow
  }) {
    return /*#__PURE__*/React.createElement("section", {
      id: id,
      className: 'op-section' + (narrow ? ' op-section--narrow' : '')
    }, children);
  }
  function App() {
    const [cards, setCards] = React.useState(() => {
      const saved = window.__SPU_TWEAKS && window.__SPU_TWEAKS.cards || {};
      const merged = {};
      for (const c of CARD_DEFS) merged[c.id] = {
        ...DEFAULT_CARDS[c.id],
        ...(saved[c.id] || {})
      };
      return merged;
    });
    const [panelOpen, setPanelOpen] = React.useState(false);
    const [active, setActive] = React.useState('abertura');
    React.useEffect(() => {
      const onMsg = e => {
        const t = e.data && e.data.type;
        if (t === '__activate_edit_mode') setPanelOpen(true);else if (t === '__deactivate_edit_mode') setPanelOpen(false);
      };
      window.addEventListener('message', onMsg);
      window.parent.postMessage({
        type: '__edit_mode_available'
      }, '*');
      return () => window.removeEventListener('message', onMsg);
    }, []);
    const changeCard = (id, patch) => {
      setCards(prev => {
        const next = {
          ...prev,
          [id]: {
            ...prev[id],
            ...patch
          }
        };
        window.parent.postMessage({
          type: '__edit_mode_set_keys',
          edits: {
            cards: next
          }
        }, '*');
        return next;
      });
    };
    const dismissPanel = () => {
      setPanelOpen(false);
      window.parent.postMessage({
        type: '__edit_mode_dismissed'
      }, '*');
    };
    const cardAccent = id => resolveAccent((cards[id] || DEFAULT_CARDS[id]).accent);
    const cardIcon = id => (cards[id] || DEFAULT_CARDS[id]).icon;
    const accentVarsFor = id => {
      const a = cardAccent(id);
      return {
        '--color-primary': a.primary,
        '--color-primary-strong': a.strong,
        '--color-primary-soft': a.soft
      };
    };
    return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(TweaksPanel, {
      visible: panelOpen,
      cards: cards,
      onChange: changeCard,
      onClose: dismissPanel
    }), /*#__PURE__*/React.createElement(ProgressBar, {
      onActive: setActive
    }), /*#__PURE__*/React.createElement(Summary, {
      active: active
    }), /*#__PURE__*/React.createElement(Hero, {
      id: "abertura",
      slot: "hero-unidade1",
      src: window.__resources && window.__resources.hero || '../../assets/hero-praca.jpg',
      kicker: "Unidade 1 \xB7 Fundamentos",
      kickerIcon: "compass",
      title: "O patrim\xF4nio da Uni\xE3o e o territ\xF3rio brasileiro",
      byline: "SPU \xB7 ENAP Aprendizagem",
      placeholder: "Solte aqui uma imagem de territ\xF3rio (paisagem, im\xF3vel, mapa)"
    }), /*#__PURE__*/React.createElement(Section, {
      id: "intro",
      narrow: true
    }, /*#__PURE__*/React.createElement(Kicker, {
      icon: "book-open"
    }, "Antes de come\xE7ar"), /*#__PURE__*/React.createElement("h2", null, "Por que estudar o patrim\xF4nio p\xFAblico?"), /*#__PURE__*/React.createElement("p", {
      className: "op-lead"
    }, "Cada pra\xE7a, pr\xE9dio hist\xF3rico, terreno de marinha ou \xE1rea da Uni\xE3o \xE9 um ", /*#__PURE__*/React.createElement("strong", null, "bem de toda a sociedade"), ". Compreender como esse patrim\xF4nio \xE9 identificado e destinado \xE9 o primeiro passo para uma gest\xE3o territorial justa e transparente."), /*#__PURE__*/React.createElement("p", null, "Ao longo desta unidade voc\xEA vai reconhecer os principais conceitos, percorrer a linha do tempo da forma\xE7\xE3o do territ\xF3rio e comparar os regimes de destina\xE7\xE3o dos im\xF3veis da Uni\xE3o. O conte\xFAdo \xE9 ", /*#__PURE__*/React.createElement(GlossaryTerm, {
      term: "Onepage",
      definition: "Formato de p\xE1gina \xFAnica, naveg\xE1vel por rolagem, que re\xFAne todo o conte\xFAdo de uma unidade did\xE1tica.",
      source: "Design System SPU ENAP"
    }, "interativo"), " e foi pensado para leitura confort\xE1vel em qualquer tela."), /*#__PURE__*/React.createElement(Callout, {
      variant: "header",
      tone: "info",
      title: "Objetivos de aprendizagem",
      icon: cardIcon('objetivos'),
      color: cardAccent('objetivos').primary
    }, /*#__PURE__*/React.createElement(MarkerList, {
      variant: "check",
      items: ['Distinguir as categorias de bens imóveis da União;', 'Situar a formação do patrimônio público na história do território;', 'Comparar os principais regimes de destinação.']
    }))), /*#__PURE__*/React.createElement(Section, {
      id: "conceitos"
    }, /*#__PURE__*/React.createElement("div", {
      className: "op-head"
    }, /*#__PURE__*/React.createElement(Kicker, {
      icon: "layers"
    }, "Conceitos-chave"), /*#__PURE__*/React.createElement("h2", null, "Quatro ideias para come\xE7ar"), /*#__PURE__*/React.createElement("p", {
      className: "op-lead op-measure"
    }, "Vire cada cart\xE3o para revelar a defini\xE7\xE3o. Estes conceitos voltam ao longo de toda a unidade.")), /*#__PURE__*/React.createElement("div", {
      className: "op-flipgrid"
    }, CARD_DEFS.filter(c => c.kind === 'flip').map(c => /*#__PURE__*/React.createElement("div", {
      key: c.id,
      style: accentVarsFor(c.id)
    }, /*#__PURE__*/React.createElement(Flipcard, {
      icon: cardIcon(c.id),
      term: c.term,
      definition: c.definition
    })))), /*#__PURE__*/React.createElement(TextWithImage, {
      side: "right",
      imageWidth: "42%",
      caption: "Fachada de pr\xF3prio nacional sob gest\xE3o da SPU."
    }, /*#__PURE__*/React.createElement("h3", null, "O que a SPU administra"), /*#__PURE__*/React.createElement("p", null, "A ", /*#__PURE__*/React.createElement(GlossaryTerm, {
      term: "SPU",
      definition: "Secretaria do Patrim\xF4nio da Uni\xE3o, respons\xE1vel por administrar os bens im\xF3veis da Uni\xE3o em todo o pa\xEDs.",
      source: "gov.br / SPU"
    }, "Secretaria do Patrim\xF4nio da Uni\xE3o"), " cuida de um acervo diverso: terrenos de marinha e acrescidos, pr\xF3prios nacionais, \xE1reas da Uni\xE3o em faixa de fronteira e im\xF3veis funcionais."), /*#__PURE__*/React.createElement("p", null, "Mais do que um cadastro, esse acervo \xE9 um instrumento de pol\xEDtica p\xFAblica \u2014 habita\xE7\xE3o, regulariza\xE7\xE3o fundi\xE1ria, prote\xE7\xE3o ambiental e desenvolvimento territorial."), /*#__PURE__*/React.createElement("div", {
      className: "op-btnrow"
    }, /*#__PURE__*/React.createElement(Button, {
      variant: "secondary",
      icon: "external-link",
      href: "https://www.gov.br/pgfn"
    }, "Portal gov.br"))), /*#__PURE__*/React.createElement(Panel, {
      variant: "feature",
      kicker: "O acervo em n\xFAmeros",
      kickerIcon: "sparkles"
    }, /*#__PURE__*/React.createElement(StatBlock, {
      variant: "rule",
      columns: 4,
      stats: [{
        value: '6.500',
        unit: '+',
        label: 'Imóveis',
        description: 'Sob gestão direta da SPU.'
      }, {
        value: '27',
        label: 'Unidades',
        description: 'Uma por estado e no DF.'
      }, {
        value: '33',
        unit: 'm',
        label: 'Marinha',
        description: 'Faixa a partir de 1831.'
      }, {
        value: '1946',
        label: 'Marco legal',
        description: 'Decreto-Lei nº 9.760.'
      }]
    }))), /*#__PURE__*/React.createElement(Section, {
      id: "historia",
      narrow: true
    }, /*#__PURE__*/React.createElement(Kicker, {
      icon: "calendar"
    }, "Percurso hist\xF3rico"), /*#__PURE__*/React.createElement("h2", null, "Como o territ\xF3rio p\xFAblico se formou"), /*#__PURE__*/React.createElement("p", {
      className: "op-lead"
    }, "Selecione uma era e abra cada marco para conhecer o contexto. A forma\xE7\xE3o do patrim\xF4nio da Uni\xE3o acompanha a pr\xF3pria hist\xF3ria do Brasil."), /*#__PURE__*/React.createElement(Timeline, {
      eras: [{
        label: 'Colônia',
        period: '1500–1822',
        color: 'var(--ochre-600)',
        milestones: [{
          date: '1500',
          title: 'Regime de sesmarias',
          content: /*#__PURE__*/React.createElement("p", null, "A Coroa portuguesa distribui terras por doa\xE7\xE3o condicionada ao cultivo, base do regime fundi\xE1rio colonial.")
        }, {
          date: '1808',
          title: 'Vinda da Corte',
          content: /*#__PURE__*/React.createElement("p", null, "A transfer\xEAncia da Corte reorganiza a posse e o uso do territ\xF3rio, com forte presen\xE7a de bens da Coroa.")
        }]
      }, {
        label: 'Império',
        period: '1822–1889',
        color: 'var(--terra-600)',
        milestones: [{
          date: '1831',
          title: 'Linha do preamar médio',
          content: /*#__PURE__*/React.createElement("p", null, "Define-se a refer\xEAncia que, at\xE9 hoje, demarca os terrenos de marinha.")
        }, {
          date: '1850',
          title: 'Lei de Terras',
          content: /*#__PURE__*/React.createElement("p", null, "Institui a compra como forma leg\xEDtima de acesso \xE0 terra, separando o p\xFAblico do privado.")
        }]
      }, {
        label: 'República',
        period: '1889–hoje',
        color: 'var(--petrol-600)',
        milestones: [{
          date: '1946',
          title: 'Decreto-Lei nº 9.760',
          content: /*#__PURE__*/React.createElement("p", null, "Consolida o regime jur\xEDdico dos bens im\xF3veis da Uni\xE3o \u2014 ainda a principal refer\xEAncia da gest\xE3o.")
        }, {
          date: '2016',
          title: 'Modernização da gestão',
          content: /*#__PURE__*/React.createElement("p", null, "Novas leis ampliam instrumentos de destina\xE7\xE3o e regulariza\xE7\xE3o fundi\xE1ria urbana.")
        }]
      }]
    })), /*#__PURE__*/React.createElement(Section, {
      id: "territorio"
    }, /*#__PURE__*/React.createElement("div", {
      className: "op-head"
    }, /*#__PURE__*/React.createElement(Kicker, {
      icon: "map-pin"
    }, "Dimens\xE3o cartogr\xE1fica"), /*#__PURE__*/React.createElement("h2", null, "O patrim\xF4nio no mapa"), /*#__PURE__*/React.createElement("p", {
      className: "op-lead op-measure"
    }, "Clique nos marcadores para explorar exemplos de im\xF3veis da Uni\xE3o pelo pa\xEDs. A cartografia \xE9 parte essencial da gest\xE3o.")), /*#__PURE__*/React.createElement(MapFigure, {
      label: "Mapa 1",
      caption: "Exemplos de im\xF3veis e unidades da SPU.",
      credit: "Elabora\xE7\xE3o did\xE1tica SPU/ENAP, 2026.",
      markers: [{
        x: 30,
        y: 36,
        title: 'Faixa de fronteira',
        description: 'Áreas da União ao longo das fronteiras terrestres.'
      }, {
        x: 62,
        y: 30,
        title: 'Terrenos de marinha',
        description: 'Faixa costeira sob domínio federal.'
      }, {
        x: 54,
        y: 60,
        title: 'Sede da SPU',
        description: 'Coordenação nacional, em Brasília.'
      }, {
        x: 70,
        y: 74,
        title: 'Próprios nacionais',
        description: 'Edifícios públicos federais.'
      }]
    }), /*#__PURE__*/React.createElement("div", {
      className: "op-twocol"
    }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("h3", null, "Antes e depois"), /*#__PURE__*/React.createElement("p", null, "A regulariza\xE7\xE3o fundi\xE1ria transforma territ\xF3rios. Arraste para comparar uma \xE1rea antes e depois da atua\xE7\xE3o da SPU."), /*#__PURE__*/React.createElement(ImageReveal, {
      beforeLabel: "Antes",
      afterLabel: "Depois"
    })), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("h3", null, "Galeria do territ\xF3rio"), /*#__PURE__*/React.createElement("p", null, "Um carrossel de imagens e conceitos. Use as setas ou os pontos para navegar."), /*#__PURE__*/React.createElement(Carousel, null, /*#__PURE__*/React.createElement(Figure, {
      size: "full",
      zoom: false,
      caption: "Orla urbana com terrenos de marinha."
    }), /*#__PURE__*/React.createElement(Figure, {
      size: "full",
      zoom: false,
      caption: "Pr\xF3prio nacional de valor hist\xF3rico."
    }), /*#__PURE__*/React.createElement(Callout, {
      tone: "note",
      title: "As ilhas costeiras",
      icon: "book-open"
    }, "J\xE1 foram, por regra geral, bens da Uni\xE3o antes de transfer\xEAncias espec\xEDficas.")))), /*#__PURE__*/React.createElement(ExampleCard, {
      label: "Exemplo pr\xE1tico",
      icon: "map-pin",
      slot: "exemplo-orla",
      title: "Projeto Orla"
    }, /*#__PURE__*/React.createElement("p", null, "Desenvolvido em parceria entre Uni\xE3o, estados e munic\xEDpios, o Projeto Orla ordena o uso das \xE1reas costeiras brasileiras, conciliando prote\xE7\xE3o ambiental, atividades econ\xF4micas e inclus\xE3o social."), /*#__PURE__*/React.createElement("p", null, "Os terrenos de marinha administrados pela Uni\xE3o s\xE3o um dos principais instrumentos dessa pol\xEDtica.")), /*#__PURE__*/React.createElement(ReflectionStop, {
      prompts: ['Quais imóveis públicos federais existem no seu município?', 'Quem decide sobre a destinação desses bens — e com que critérios?']
    }, "Antes de avan\xE7ar para os regimes de destina\xE7\xE3o, pense no patrim\xF4nio p\xFAblico do seu pr\xF3prio territ\xF3rio.")), /*#__PURE__*/React.createElement(Section, {
      id: "regimes",
      narrow: true
    }, /*#__PURE__*/React.createElement(Kicker, {
      icon: "scale"
    }, "Regras de gest\xE3o"), /*#__PURE__*/React.createElement("h2", null, "Como os im\xF3veis s\xE3o destinados"), /*#__PURE__*/React.createElement("p", {
      className: "op-lead"
    }, "H\xE1 v\xE1rios regimes de destina\xE7\xE3o, cada um com finalidade e prazo pr\xF3prios. Compare dois dos mais comuns:"), /*#__PURE__*/React.createElement(CompareAB, {
      a: {
        label: 'Cessão',
        icon: 'building',
        title: 'Uso não oneroso',
        content: /*#__PURE__*/React.createElement("p", null, "Destinada a \xF3rg\xE3os e entidades p\xFAblicas, em geral sem contrapartida financeira, para fins de interesse p\xFAblico.")
      },
      b: {
        label: 'Concessão',
        icon: 'scale',
        title: 'Uso oneroso',
        content: /*#__PURE__*/React.createElement("p", null, "Permite a explora\xE7\xE3o econ\xF4mica do im\xF3vel por prazo determinado, mediante pagamento \xE0 Uni\xE3o.")
      }
    }), /*#__PURE__*/React.createElement("h3", {
      style: {
        marginTop: 'var(--space-10)'
      }
    }, "Quadro-resumo das modalidades"), /*#__PURE__*/React.createElement(DataTable, {
      caption: "Tabela 1 \u2014 Modalidades de destina\xE7\xE3o (simplificado)",
      highlightFirst: true,
      columns: [{
        key: 'mod',
        label: 'Modalidade'
      }, {
        key: 'uso',
        label: 'Natureza'
      }, {
        key: 'dest',
        label: 'Destinatário típico'
      }, {
        key: 'prazo',
        label: 'Prazo',
        align: 'right'
      }],
      rows: [{
        mod: 'Cessão',
        uso: 'Não onerosa',
        dest: 'Órgãos públicos',
        prazo: 'até 20 anos'
      }, {
        mod: 'Concessão',
        uso: 'Onerosa',
        dest: 'Particulares',
        prazo: 'até 35 anos'
      }, {
        mod: 'Entrega',
        uso: 'Administrativa',
        dest: 'Órgãos federais',
        prazo: 'indeterminado'
      }, {
        mod: 'Alienação',
        uso: 'Onerosa',
        dest: 'Mercado',
        prazo: 'definitiva'
      }]
    }), /*#__PURE__*/React.createElement("h3", {
      style: {
        marginTop: 'var(--space-10)'
      }
    }, "D\xFAvidas frequentes"), /*#__PURE__*/React.createElement(Accordion, {
      numbered: true,
      defaultOpen: [0],
      items: [{
        title: 'Todo bem da União pode ser vendido?',
        content: /*#__PURE__*/React.createElement("p", null, "N\xE3o. Bens afetados ao uso p\xFAblico s\xE3o inalien\xE1veis enquanto durar a afeta\xE7\xE3o. A aliena\xE7\xE3o exige desafeta\xE7\xE3o e autoriza\xE7\xE3o legal.")
      }, {
        title: 'Quem paga pela ocupação de terreno de marinha?',
        content: /*#__PURE__*/React.createElement("p", null, "Ocupantes podem estar sujeitos a taxas como o foro e a taxa de ocupa\xE7\xE3o, conforme a situa\xE7\xE3o do im\xF3vel.")
      }, {
        title: 'A regularização fundiária envolve a SPU?',
        content: /*#__PURE__*/React.createElement("p", null, "Sim, especialmente quando h\xE1 im\xF3veis da Uni\xE3o envolvidos, em articula\xE7\xE3o com estados e munic\xEDpios.")
      }]
    }), /*#__PURE__*/React.createElement(Callout, {
      variant: "ear",
      emphasis: "strong",
      tone: "warning",
      title: "Conte\xFAdo did\xE1tico simplificado",
      style: {
        marginTop: 'var(--space-8)'
      }
    }, "As informa\xE7\xF5es desta unidade t\xEAm fim ", /*#__PURE__*/React.createElement("strong", null, "educacional"), ". Para atos concretos, consulte sempre a legisla\xE7\xE3o vigente e as orienta\xE7\xF5es oficiais da SPU.")), /*#__PURE__*/React.createElement(Section, {
      id: "sintese"
    }, /*#__PURE__*/React.createElement("div", {
      className: "op-head"
    }, /*#__PURE__*/React.createElement(Kicker, {
      icon: "sparkles"
    }, "Para fechar"), /*#__PURE__*/React.createElement("h2", null, "S\xEDntese e aprofundamento")), /*#__PURE__*/React.createElement(PullQuote, {
      variant: "eye",
      cite: "Decreto-Lei n\xBA 9.760/1946"
    }, "Os bens im\xF3veis da Uni\xE3o s\xE3o inalien\xE1veis enquanto afetados ao uso p\xFAblico."), /*#__PURE__*/React.createElement(Columns, {
      count: 2
    }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("h3", null, "Assista"), /*#__PURE__*/React.createElement(MediaEmbed, {
      type: "video",
      title: "O que \xE9 o patrim\xF4nio da Uni\xE3o?",
      provider: "V\xEDdeo externo \xB7 SPU/ENAP",
      url: "https://www.gov.br/spu"
    })), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("h3", null, "Ou\xE7a"), /*#__PURE__*/React.createElement(MediaEmbed, {
      type: "podcast",
      title: "Territ\xF3rio em foco \u2014 epis\xF3dio 1",
      provider: "Podcast externo",
      url: "https://www.gov.br/spu/pt-br/podcast"
    }))), /*#__PURE__*/React.createElement("h3", {
      style: {
        marginTop: 'var(--space-10)'
      }
    }, "Para saber mais"), /*#__PURE__*/React.createElement(ResourceList, {
      items: [{
        type: 'law',
        title: 'Lei nº 13.240/2015',
        source: 'Planalto',
        url: '#',
        description: 'Dispõe sobre a administração dos imóveis da União.'
      }, {
        type: 'news',
        title: 'SPU avança na regularização de terrenos de marinha',
        source: 'gov.br',
        url: '#'
      }, {
        type: 'doc',
        title: 'Cartilha de destinação de imóveis da União',
        source: 'SPU, 2024',
        url: '#'
      }]
    })), /*#__PURE__*/React.createElement(Conclusion, {
      id: "conclusao"
    }, /*#__PURE__*/React.createElement("p", null, "O patrim\xF4nio imobili\xE1rio da Uni\xE3o \xE9, antes de tudo, um instrumento de pol\xEDtica p\xFAblica: identific\xE1-lo, organiz\xE1-lo e destin\xE1-lo bem \xE9 o que transforma um acervo de bens em desenvolvimento territorial, justi\xE7a social e prote\xE7\xE3o ambiental."), /*#__PURE__*/React.createElement("p", null, "Compreender as categorias, a forma\xE7\xE3o hist\xF3rica e os regimes de destina\xE7\xE3o \xE9 o primeiro passo para uma gest\xE3o patrimonial moderna, integrada e orientada \xE0 gera\xE7\xE3o de valor p\xFAblico.")), /*#__PURE__*/React.createElement(Section, {
      id: "fontes",
      narrow: true
    }, /*#__PURE__*/React.createElement(ReferenceList, {
      items: ['BRASIL. <b>Decreto-Lei nº 9.760, de 5 de setembro de 1946</b>. Dispõe sobre os bens imóveis da União. Rio de Janeiro, 1946.', 'BRASIL. <b>Lei nº 13.240, de 30 de dezembro de 2015</b>. Dispõe sobre a administração, a alienação e a regularização de imóveis da União. Brasília, 2015.', 'SECRETARIA DO PATRIMÔNIO DA UNIÃO. <b>Relatório de gestão 2024</b>. Brasília: SPU, 2025.']
    }), /*#__PURE__*/React.createElement(GlossaryFootnotes, {
      title: "Notas \u2014 termos do gloss\xE1rio"
    })), /*#__PURE__*/React.createElement(PageFooter, {
      code: "Unidade 1 \xB7 Patrim\xF4nio da Uni\xE3o",
      context: "Programa de Aprendizagem em Gest\xE3o de Im\xF3veis P\xFAblicos \xB7 Conte\xFAdo did\xE1tico interativo sobre identifica\xE7\xE3o, destina\xE7\xE3o e gest\xE3o do patrim\xF4nio imobili\xE1rio p\xFAblico.",
      credits: [{
        role: 'Produção',
        name: 'SPU / ENAP'
      }, {
        role: 'Revisão',
        name: '—'
      }, {
        role: 'Design',
        name: 'SPU'
      }, {
        role: 'Versão',
        name: '2026'
      }]
    }));
  }
  window.__SPU_App = App;
  return App;
}
window.__SPU_buildApp = buildSpuApp;
})(); } catch (e) { __ds_ns.__errors.push({ path: "export/src/page.jsx", error: String((e && e.message) || e) }); }

// m122/page.jsx
try { (() => {
/* global React */
// Objeto de aprendizagem autônomo — M122.
// Construído quando o bundle do design system estiver pronto.
function buildM122App(NS) {
  const {
    Kicker,
    Callout,
    PullQuote,
    StatBlock,
    MarkerList,
    FullBleed,
    ReflectionStop,
    ReferenceList,
    LicenseBadge,
    Icon,
    Hero,
    BleedImage,
    Figure,
    Panel,
    ExampleCard,
    PageFooter,
    Conclusion
  } = NS;
  const SECTIONS = [{
    id: 'abertura',
    label: 'Início'
  }, {
    id: 'infraestrutura',
    label: 'Infraestrutura'
  }, {
    id: 'ambiental',
    label: 'Ambiental'
  }, {
    id: 'social',
    label: 'Social'
  }, {
    id: 'economica',
    label: 'Econômica'
  }, {
    id: 'conexoes',
    label: 'Conexões'
  }, {
    id: 'conclusao',
    label: 'Conclusão'
  }, {
    id: 'fontes',
    label: 'Fontes'
  }];

  // ——— Progresso de leitura + seção ativa ———
  function ProgressBar({
    onActive
  }) {
    const [p, setP] = React.useState(0);
    React.useEffect(() => {
      const onScroll = () => {
        const h = document.documentElement;
        const max = h.scrollHeight - h.clientHeight;
        setP(max > 0 ? h.scrollTop / max * 100 : 0);
        // seção ativa
        let active = SECTIONS[0].id;
        for (const s of SECTIONS) {
          const el = document.getElementById(s.id);
          if (el && el.getBoundingClientRect().top <= 120) active = s.id;
        }
        onActive(active);
      };
      window.addEventListener('scroll', onScroll, {
        passive: true
      });
      onScroll();
      return () => window.removeEventListener('scroll', onScroll);
    }, [onActive]);
    return React.createElement('div', {
      className: 'op-progress'
    }, React.createElement('div', {
      className: 'op-progress__bar',
      style: {
        width: p + '%'
      }
    }));
  }

  // ——— Sumário recolhido (canto superior direito) ———
  function Summary({
    active
  }) {
    const [open, setOpen] = React.useState(false);
    const ref = React.useRef(null);
    React.useEffect(() => {
      if (!open) return undefined;
      const onDoc = e => {
        if (ref.current && !ref.current.contains(e.target)) setOpen(false);
      };
      const onKey = e => {
        if (e.key === 'Escape') setOpen(false);
      };
      document.addEventListener('mousedown', onDoc);
      document.addEventListener('keydown', onKey);
      return () => {
        document.removeEventListener('mousedown', onDoc);
        document.removeEventListener('keydown', onKey);
      };
    }, [open]);
    return /*#__PURE__*/React.createElement("div", {
      className: "op-summary",
      ref: ref
    }, /*#__PURE__*/React.createElement("button", {
      type: "button",
      className: "op-summary__toggle",
      "aria-expanded": open,
      "aria-label": "Sum\xE1rio",
      onClick: () => setOpen(o => !o)
    }, /*#__PURE__*/React.createElement(Icon, {
      name: open ? 'x' : 'list',
      size: 16
    }), /*#__PURE__*/React.createElement("span", null, "Sum\xE1rio")), open && /*#__PURE__*/React.createElement("nav", {
      className: "op-summary__panel"
    }, /*#__PURE__*/React.createElement("p", {
      className: "op-summary__title"
    }, "Neste conte\xFAdo"), SECTIONS.map((s, i) => /*#__PURE__*/React.createElement("a", {
      key: s.id,
      href: '#' + s.id,
      className: active === s.id ? 'is-active' : '',
      onClick: () => setOpen(false)
    }, /*#__PURE__*/React.createElement("span", {
      className: "op-summary__num"
    }, String(i + 1).padStart(2, '0')), /*#__PURE__*/React.createElement("span", null, s.label)))));
  }
  function Section({
    id,
    narrow,
    children
  }) {
    return /*#__PURE__*/React.createElement("section", {
      id: id,
      "data-screen-label": id,
      className: 'op-section' + (narrow ? ' op-section--narrow' : '')
    }, children);
  }
  function App() {
    const [active, setActive] = React.useState('abertura');
    const accentVars = {
      '--color-primary': 'var(--petrol-600)',
      '--color-primary-strong': 'var(--petrol-800)',
      '--color-primary-soft': 'var(--petrol-100, #EAF3F1)',
      '--color-accent': 'var(--petrol-600)'
    };
    return /*#__PURE__*/React.createElement("div", {
      style: accentVars
    }, /*#__PURE__*/React.createElement(ProgressBar, {
      onActive: setActive
    }), /*#__PURE__*/React.createElement(Summary, {
      active: active
    }), /*#__PURE__*/React.createElement(Hero, {
      id: "abertura",
      src: "assets/M122-hero.png",
      kicker: "M122",
      kickerIcon: "building",
      title: "O patrim\xF4nio imobili\xE1rio p\xFAblico: panorama, relev\xE2ncia e potencial transformador"
    }), /*#__PURE__*/React.createElement(Section, {
      id: "intro",
      narrow: true
    }, /*#__PURE__*/React.createElement("div", {
      className: "op-prose"
    }, /*#__PURE__*/React.createElement("p", {
      className: "op-lead"
    }, "Os im\xF3veis p\xFAblicos constituem uma das mais importantes infraestruturas do Estado brasileiro. Presentes em cidades, \xE1reas rurais, florestas, rios, ilhas, zonas costeiras e regi\xF5es de fronteira, esses bens sustentam a presta\xE7\xE3o de servi\xE7os p\xFAblicos, viabilizam pol\xEDticas sociais, protegem ecossistemas estrat\xE9gicos e contribuem para o desenvolvimento econ\xF4mico e territorial."), /*#__PURE__*/React.createElement("p", null, "Quando se fala em patrim\xF4nio p\xFAblico, \xE9 comum que a aten\xE7\xE3o se concentre em escolas, hospitais, pr\xE9dios administrativos e equipamentos governamentais. Entretanto, o universo dos im\xF3veis p\xFAblicos \xE9 muito mais amplo. Ele inclui \xE1reas destinadas \xE0 conserva\xE7\xE3o ambiental, terras ocupadas por comunidades tradicionais, im\xF3veis utilizados para habita\xE7\xE3o social, infraestrutura urbana, pesquisa cient\xEDfica, defesa nacional, cultura, lazer e diversas outras finalidades de interesse coletivo."), /*#__PURE__*/React.createElement("p", null, "Mais do que ativos imobili\xE1rios, esses bens representam instrumentos permanentes de atua\xE7\xE3o do Estado sobre o territ\xF3rio. Sua relev\xE2ncia n\xE3o decorre apenas do valor econ\xF4mico que possuem, mas principalmente da sua capacidade de gerar benef\xEDcios para a sociedade."), /*#__PURE__*/React.createElement("p", null, "Entre os diversos patrim\xF4nios p\xFAblicos existentes no Brasil, destaca-se o patrim\xF4nio imobili\xE1rio da Uni\xE3o. Pela sua dimens\xE3o territorial, diversidade de tipologias e distribui\xE7\xE3o em todo o pa\xEDs, ele constitui um dos maiores acervos imobili\xE1rios p\xFAblicos do mundo e oferece um excelente exemplo para compreender a import\xE2ncia estrat\xE9gica dos im\xF3veis p\xFAblicos na promo\xE7\xE3o do desenvolvimento sustent\xE1vel, da justi\xE7a social e da prote\xE7\xE3o ambiental."))), /*#__PURE__*/React.createElement("div", {
      className: "op-hero-wrap"
    }, /*#__PURE__*/React.createElement(BleedImage, {
      slot: "m122-img1",
      fit: "cover",
      placeholder: "Arraste a imagem m122-img1.png",
      "data-comment-anchor": "7b9c918706-image-slot-75-9"
    })), /*#__PURE__*/React.createElement(Section, {
      id: "infraestrutura",
      narrow: true
    }, /*#__PURE__*/React.createElement("div", {
      className: "op-prose"
    }, /*#__PURE__*/React.createElement(Kicker, {
      icon: "layers"
    }, "Uma infraestrutura de Estado"), /*#__PURE__*/React.createElement("h2", null, "Im\xF3veis p\xFAblicos como infraestrutura territorial do Estado"), /*#__PURE__*/React.createElement("p", null, "Normalmente, quando se fala em infraestrutura p\xFAblica, pensa-se em rodovias, portos, aeroportos, sistemas de energia ou saneamento. Existe, contudo, outra infraestrutura igualmente importante: o patrim\xF4nio imobili\xE1rio p\xFAblico."), /*#__PURE__*/React.createElement("p", null, "Os im\xF3veis p\xFAblicos constituem a base f\xEDsica necess\xE1ria para que o Estado implemente pol\xEDticas p\xFAblicas e exer\xE7a suas fun\xE7\xF5es constitucionais. S\xE3o eles que permitem construir escolas, hospitais, universidades, parques, unidades de conserva\xE7\xE3o, moradias populares, centros culturais, instala\xE7\xF5es de seguran\xE7a p\xFAblica e in\xFAmeros outros equipamentos indispens\xE1veis ao funcionamento da sociedade."), /*#__PURE__*/React.createElement("p", null, "Por essa raz\xE3o, os im\xF3veis p\xFAblicos devem ser compreendidos como uma verdadeira infraestrutura territorial de Estado. Eles oferecem suporte material para a execu\xE7\xE3o de pol\xEDticas p\xFAblicas, ajudam a organizar o territ\xF3rio e permitem que governos atuem na promo\xE7\xE3o do desenvolvimento econ\xF4mico, da inclus\xE3o social e da sustentabilidade ambiental."), /*#__PURE__*/React.createElement("p", null, "A experi\xEAncia da Uni\xE3o ilustra bem essa realidade. Seu patrim\xF4nio imobili\xE1rio compreende centenas de milhares de im\xF3veis distribu\xEDdos em todas as regi\xF5es do pa\xEDs, abrangendo desde terrenos urbanos at\xE9 praias, florestas p\xFAblicas, ilhas oce\xE2nicas, terras ind\xEDgenas e \xE1reas destinadas \xE0 administra\xE7\xE3o p\xFAblica.")), /*#__PURE__*/React.createElement(Callout, {
      variant: "header",
      tone: "info",
      icon: "lightbulb",
      title: "Voc\xEA sabia?",
      color: "var(--terra-600)",
      heading: "O patrim\xF4nio da Uni\xE3o est\xE1 presente em todo o Brasil",
      style: {
        margin: '40px 0px 20px'
      }
    }, /*#__PURE__*/React.createElement("p", null, "O patrim\xF4nio imobili\xE1rio da Uni\xE3o inclui:"), /*#__PURE__*/React.createElement(MarkerList, {
      variant: "plain",
      style: {
        lineHeight: 1.45
      },
      items: ['praias marítimas;', 'terrenos de marinha;', 'terrenos marginais de rios federais;', 'ilhas oceânicas;', 'florestas públicas;', 'terras indígenas;', 'cavernas;', 'áreas militares;', 'prédios públicos;', 'imóveis destinados a universidades, portos, aeroportos e infraestrutura.']
    }), /*#__PURE__*/React.createElement("p", null, "Essa diversidade demonstra que a gest\xE3o patrimonial vai muito al\xE9m da administra\xE7\xE3o de pr\xE9dios p\xFAblicos: trata-se da gest\xE3o de territ\xF3rios estrat\xE9gicos para o desenvolvimento nacional."))), /*#__PURE__*/React.createElement(Section, {
      id: "ambiental",
      narrow: true
    }, /*#__PURE__*/React.createElement("div", {
      className: "op-prose"
    }, /*#__PURE__*/React.createElement(Kicker, {
      icon: "compass"
    }, "Dimens\xE3o ambiental"), /*#__PURE__*/React.createElement("h2", null, "A dimens\xE3o ambiental"), /*#__PURE__*/React.createElement("p", null, "Os im\xF3veis p\xFAblicos possuem uma rela\xE7\xE3o direta com a prote\xE7\xE3o do meio ambiente. Grande parte deles est\xE1 localizada em \xE1reas de elevada relev\xE2ncia ecol\xF3gica, como zonas costeiras, margens de rios, florestas, \xE1reas \xFAmidas, manguezais, restingas e regi\xF5es de recarga h\xEDdrica."), /*#__PURE__*/React.createElement("p", null, "Essas \xE1reas desempenham fun\xE7\xF5es essenciais para a conserva\xE7\xE3o da biodiversidade, a manuten\xE7\xE3o dos recursos h\xEDdricos, a regula\xE7\xE3o do clima e a prote\xE7\xE3o das popula\xE7\xF5es contra eventos extremos."), /*#__PURE__*/React.createElement("p", null, "Por isso, a gest\xE3o patrimonial moderna deve incorporar preocupa\xE7\xF5es ambientais em suas decis\xF5es. A destina\xE7\xE3o, utiliza\xE7\xE3o ou ocupa\xE7\xE3o de im\xF3veis p\xFAblicos pode contribuir para preservar ecossistemas, recuperar \xE1reas degradadas, ampliar \xE1reas verdes urbanas e fortalecer a adapta\xE7\xE3o \xE0s mudan\xE7as clim\xE1ticas."), /*#__PURE__*/React.createElement("p", null, "Os efeitos das mudan\xE7as clim\xE1ticas refor\xE7am ainda mais essa import\xE2ncia. Enchentes, secas prolongadas, eros\xE3o costeira e eleva\xE7\xE3o do n\xEDvel do mar afetam diretamente diversos im\xF3veis p\xFAblicos, exigindo planejamento territorial e a\xE7\xF5es preventivas."), /*#__PURE__*/React.createElement("p", null, "Nesse contexto, os im\xF3veis p\xFAblicos deixam de ser apenas objetos de administra\xE7\xE3o patrimonial para se tornarem instrumentos ativos de prote\xE7\xE3o ambiental e resili\xEAncia clim\xE1tica.")), /*#__PURE__*/React.createElement(Panel, {
      variant: "box",
      kicker: "Patrim\xF4nio p\xFAblico e biodiversidade",
      kickerIcon: "map-pin",
      title: "Um exemplo da dimens\xE3o ambiental"
    }, /*#__PURE__*/React.createElement("p", null, "O patrim\xF4nio da Uni\xE3o est\xE1 diretamente relacionado \xE0 gest\xE3o de ecossistemas estrat\xE9gicos."), /*#__PURE__*/React.createElement(StatBlock, {
      variant: "rule",
      columns: 2,
      stats: [{
        value: '336',
        unit: '+',
        label: 'Unidades de Conservação Federais',
        description: 'Atualmente em todo o país.',
        color: 'var(--terra-600)'
      }, {
        value: '166',
        unit: 'mi ha',
        label: 'Hectares protegidos',
        description: 'Aproximadamente.',
        color: 'var(--ochre-600)'
      }]
    }), /*#__PURE__*/React.createElement("p", {
      style: {
        margin: "20px 0px 19px"
      }
    }, "Muitas dessas \xE1reas est\xE3o associadas a im\xF3veis sob dom\xEDnio da Uni\xE3o, contribuindo para:"), /*#__PURE__*/React.createElement(MarkerList, {
      variant: "check",
      items: ['conservação da biodiversidade;', 'proteção de mananciais;', 'manutenção de serviços ecossistêmicos;', 'mitigação dos efeitos das mudanças climáticas.']
    }), /*#__PURE__*/React.createElement("p", null, /*#__PURE__*/React.createElement("strong", null, "Curiosidade:"), " 166 milh\xF5es de hectares correspondem a uma \xE1rea maior que muitos pa\xEDses do mundo."))), /*#__PURE__*/React.createElement(Section, {
      id: "social",
      narrow: true
    }, /*#__PURE__*/React.createElement("div", {
      className: "op-prose"
    }, /*#__PURE__*/React.createElement(Kicker, {
      icon: "building"
    }, "Dimens\xE3o social"), /*#__PURE__*/React.createElement("h2", null, "A dimens\xE3o social"), /*#__PURE__*/React.createElement("p", null, "O patrim\xF4nio p\xFAblico tamb\xE9m desempenha papel fundamental na promo\xE7\xE3o da cidadania e na garantia de direitos."), /*#__PURE__*/React.createElement("p", null, "Milh\xF5es de brasileiros utilizam diariamente im\xF3veis p\xFAblicos para estudar, trabalhar, receber atendimento de sa\xFAde, praticar esportes, acessar servi\xE7os governamentais ou participar de atividades culturais."), /*#__PURE__*/React.createElement("p", null, "Al\xE9m disso, in\xFAmeros im\xF3veis p\xFAblicos servem de base territorial para povos ind\xEDgenas, comunidades quilombolas, pescadores artesanais, ribeirinhos e outras popula\xE7\xF5es tradicionais."), /*#__PURE__*/React.createElement("p", null, "A destina\xE7\xE3o qualificada desse acervo patrimonial tem o potencial de contribuir de maneira significativa para a redu\xE7\xE3o das desigualdades sociais, funcionando como suporte material para pol\xEDticas de provis\xE3o habitacional, regulariza\xE7\xE3o fundi\xE1ria e implementa\xE7\xE3o de programas estrat\xE9gicos em territ\xF3rios urbanos e rurais. Esses bens, quando bem utilizados, viabilizam a instala\xE7\xE3o de equipamentos comunit\xE1rios, ampliam a oferta de servi\xE7os p\xFAblicos e fortalecem redes de prote\xE7\xE3o social."), /*#__PURE__*/React.createElement("p", null, "O uso social do patrim\xF4nio imobili\xE1rio p\xFAblico tamb\xE9m permite enfrentar situa\xE7\xF5es de vulnerabilidade, prevenindo ocupa\xE7\xF5es prec\xE1rias em \xE1reas de risco, ampliando oportunidades de acesso a direitos b\xE1sicos e promovendo maior integra\xE7\xE3o entre pol\xEDticas habitacionais, educacionais, de sa\xFAde e de assist\xEAncia social. Assim, ao servir de alicerce para pol\xEDticas p\xFAblicas estruturantes, esse patrim\xF4nio p\xFAblico consolida sua fun\xE7\xE3o social e contribui para uma sociedade mais justa, inclusiva e sustent\xE1vel."))), /*#__PURE__*/React.createElement("div", {
      "data-comment-anchor": "3dd013cb62-div-297-9"
    }, /*#__PURE__*/React.createElement(Panel, {
      variant: "feature",
      kicker: "O patrim\xF4nio p\xFAblico transformando vidas",
      kickerIcon: "sparkles"
    }, /*#__PURE__*/React.createElement(StatBlock, {
      variant: "rule",
      columns: 3,
      stats: [{
        value: '1.300',
        unit: '+',
        label: 'Destinações de imóveis da União',
        description: 'Para finalidades socioambientais, entre 2023 e 2025.'
      }, {
        value: '300',
        unit: 'mil +',
        label: 'Famílias beneficiadas',
        description: 'Pelo Programa Imóvel da Gente.'
      }, {
        value: 'R$ 16',
        unit: 'tri',
        label: 'Valor patrimonial associado',
        description: 'Às destinações realizadas.'
      }]
    }), /*#__PURE__*/React.createElement("p", {
      style: {
        margin: "20px 0px 19px"
      }
    }, "Entre 2023 e 2025, o Programa Im\xF3vel da Gente formalizou mais de 1.300 destina\xE7\xF5es de im\xF3veis da Uni\xE3o para finalidades socioambientais."), /*#__PURE__*/React.createElement("p", null, "Essas a\xE7\xF5es beneficiaram mais de 300 mil fam\xEDlias por meio de:"), /*#__PURE__*/React.createElement(MarkerList, {
      variant: "check",
      items: ['habitação de interesse social;', 'regularização fundiária;', 'equipamentos públicos;', 'proteção de comunidades tradicionais.']
    }), /*#__PURE__*/React.createElement("p", null, "O valor patrimonial associado a essas destina\xE7\xF5es ultrapassa R$ 16 trilh\xF5es."), /*#__PURE__*/React.createElement(Figure, {
      slot: "m122-img3",
      size: "full",
      title: "Patrim\xF4nio P\xFAblico que transforma vidas",
      caption: "A destina\xE7\xE3o social \xE9 uma importante ferramenta para contribuir com uma sociedade mais justa, inclusiva e sustent\xE1vel.",
      credit: "Montagem autoral estilizada com IA."
    }))), /*#__PURE__*/React.createElement(Section, {
      id: "economica",
      narrow: true
    }, /*#__PURE__*/React.createElement("div", {
      className: "op-prose"
    }, /*#__PURE__*/React.createElement(Kicker, {
      icon: "scale"
    }, "Dimens\xE3o econ\xF4mica"), /*#__PURE__*/React.createElement("h2", null, "A dimens\xE3o econ\xF4mica"), /*#__PURE__*/React.createElement("p", null, "Embora frequentemente associados a custos de manuten\xE7\xE3o, os im\xF3veis p\xFAblicos tamb\xE9m possuem enorme relev\xE2ncia econ\xF4mica."), /*#__PURE__*/React.createElement("p", null, "Eles representam ativos de elevado valor patrimonial e funcionam como suporte para atividades essenciais ao desenvolvimento do pa\xEDs. Rodovias, portos, aeroportos, sistemas de energia, saneamento e telecomunica\xE7\xF5es dependem da exist\xEAncia de \xE1reas p\xFAblicas adequadamente destinadas."), /*#__PURE__*/React.createElement("p", null, "A gest\xE3o patrimonial moderna busca maximizar esse valor p\xFAblico, promovendo usos capazes de gerar benef\xEDcios econ\xF4micos, sociais e ambientais simultaneamente."), /*#__PURE__*/React.createElement("p", null, "\xC1reas p\xFAblicas s\xE3o utilizados como suporte para infraestrutura de saneamento, energia, telecomunica\xE7\xF5es, transporte e log\xEDstica, setores que respondem diretamente pela competitividade econ\xF4mica do pa\xEDs. Quando destinados de forma planejada, tornam-se catalisadores do desenvolvimento urbano e regional, estimulando atividades produtivas, atraindo investimentos privados e fomentando cadeias locais de emprego e renda. Essa contribui\xE7\xE3o n\xE3o se limita ao uso imediato, mas tamb\xE9m envolve a valoriza\xE7\xE3o indireta de \xE1reas urbanas e rurais, que passam a receber servi\xE7os p\xFAblicos, regulariza\xE7\xE3o fundi\xE1ria e equipamentos coletivos."), /*#__PURE__*/React.createElement(Callout, {
      variant: "ear",
      emphasis: "strong",
      icon: "lightbulb",
      color: "var(--terra-600)",
      style: {
        marginTop: 'var(--flow-block)'
      }
    }, "A dimens\xE3o econ\xF4mica, portanto, n\xE3o deve ser vista apenas pela \xF3tica fiscal ou patrimonial, mas pela capacidade do patrim\xF4nio imobili\xE1rio p\xFAblico de funcionar como infraestrutura de Estado a servi\xE7o do desenvolvimento sustent\xE1vel. Gerir esses ativos de forma eficiente e articulada com outras pol\xEDticas p\xFAblicas significa ampliar sua fun\xE7\xE3o multiplicadora e refor\xE7ar seu car\xE1ter estrat\xE9gico para a economia nacional.")), /*#__PURE__*/React.createElement(Figure, {
      slot: "m122-img4",
      size: "full",
      caption: "Infogr\xE1fico \u2013 a escala do patrim\xF4nio da uni\xE3o.",
      credit: "Montagem autoral estilizada com IA."
    }), /*#__PURE__*/React.createElement(Callout, {
      variant: "header",
      tone: "success",
      icon: "target",
      title: "Patrim\xF4nio p\xFAblico e desenvolvimento sustent\xE1vel",
      color: "var(--ochre-600)"
    }, /*#__PURE__*/React.createElement("p", {
      style: {
        marginTop: 0
      }
    }, "A gest\xE3o dos im\xF3veis da Uni\xE3o possui conex\xE3o direta com diversos Objetivos de Desenvolvimento Sustent\xE1vel (ODS), especialmente:"), /*#__PURE__*/React.createElement("div", {
      className: "m122-odsgrid",
      style: {
        textAlign: "center",
        margin: "0px 0px 20px"
      }
    }, /*#__PURE__*/React.createElement(MarkerList, {
      variant: "plain",
      items: ['ODS 6 – Água e saneamento;', 'ODS 7 – Energia limpa;', 'ODS 10 – Redução das desigualdades;', 'ODS 11 – Cidades sustentáveis;', 'ODS 13 – Ação climática;', 'ODS 14 – Vida na água;', 'ODS 15 – Vida terrestre;', 'ODS 16 – Paz e instituições eficazes;', 'ODS 17 – Parcerias.']
    })), /*#__PURE__*/React.createElement("p", {
      style: {
        marginBottom: 0
      }
    }, "Isso demonstra como a gest\xE3o patrimonial est\xE1 diretamente relacionada aos grandes desafios contempor\xE2neos."))), /*#__PURE__*/React.createElement("div", {
      id: "conexoes",
      className: "op-hero-wrap"
    }, /*#__PURE__*/React.createElement(FullBleed, {
      bleed: true,
      wide: true,
      scrim: "dark",
      height: 320,
      align: "center",
      kicker: "Gest\xE3o patrimonial e pol\xEDticas socioambientais",
      kickerIcon: "link",
      title: "Como se conectam?"
    })), /*#__PURE__*/React.createElement(Section, {
      narrow: true
    }, /*#__PURE__*/React.createElement("div", {
      className: "op-prose"
    }, /*#__PURE__*/React.createElement("p", {
      className: "op-lead"
    }, "A gest\xE3o dos im\xF3veis p\xFAblicos ocorre em um ambiente institucional cada vez mais influenciado pelos desafios sociais, ambientais, clim\xE1ticos e territoriais do s\xE9culo XXI. Quest\xF5es como mudan\xE7as clim\xE1ticas, d\xE9ficit habitacional, conserva\xE7\xE3o da biodiversidade, seguran\xE7a h\xEDdrica, transi\xE7\xE3o energ\xE9tica, regulariza\xE7\xE3o fundi\xE1ria e redu\xE7\xE3o das desigualdades passaram a ocupar posi\xE7\xE3o central nas agendas governamentais. Nesse contexto, os im\xF3veis p\xFAblicos deixaram de ser percebidos apenas como bens administrativos e passaram a ser reconhecidos como instrumentos estrat\xE9gicos para a implementa\xE7\xE3o de pol\xEDticas p\xFAblicas."), /*#__PURE__*/React.createElement("p", null, "Independentemente da esfera de governo (federal, distrital/estadual ou municipal), os patrim\xF4nios imobili\xE1rios p\xFAblicos possuem potencial para apoiar uma ampla variedade de programas e iniciativas voltados ao desenvolvimento sustent\xE1vel. Terras, edif\xEDcios, \xE1reas urbanas, im\xF3veis rurais, espa\xE7os costeiros e equipamentos p\xFAblicos podem ser mobilizados para promover inclus\xE3o social, prote\xE7\xE3o ambiental, adapta\xE7\xE3o clim\xE1tica e desenvolvimento econ\xF4mico. Assim, a gest\xE3o patrimonial passa a dialogar diretamente com pol\xEDticas setoriais de habita\xE7\xE3o, meio ambiente, infraestrutura, desenvolvimento urbano, assist\xEAncia social, cultura, educa\xE7\xE3o e seguran\xE7a p\xFAblica."), /*#__PURE__*/React.createElement("h3", null, "Pol\xEDticas ambientais e conserva\xE7\xE3o da biodiversidade"), /*#__PURE__*/React.createElement("p", null, "A rela\xE7\xE3o entre im\xF3veis p\xFAblicos e pol\xEDticas ambientais \xE9 uma das mais evidentes. Muitos bens p\xFAblicos localizam-se em \xE1reas ambientalmente sens\xEDveis, como florestas, margens de rios, zonas costeiras, \xE1reas \xFAmidas, manguezais e regi\xF5es de elevada biodiversidade. Essas caracter\xEDsticas fazem com que a gest\xE3o patrimonial exer\xE7a papel relevante na conserva\xE7\xE3o dos recursos naturais e na manuten\xE7\xE3o dos servi\xE7os ecossist\xEAmicos."), /*#__PURE__*/React.createElement("p", null, "Im\xF3veis p\xFAblicos podem apoiar a cria\xE7\xE3o e consolida\xE7\xE3o de unidades de conserva\xE7\xE3o, a recupera\xE7\xE3o de \xE1reas degradadas, a implanta\xE7\xE3o de corredores ecol\xF3gicos e a prote\xE7\xE3o de mananciais. Tamb\xE9m podem servir de suporte para programas de reflorestamento, restaura\xE7\xE3o ambiental e adapta\xE7\xE3o \xE0s mudan\xE7as clim\xE1ticas."), /*#__PURE__*/React.createElement("p", null, "No \xE2mbito federal, por exemplo, diversos im\xF3veis da Uni\xE3o est\xE3o associados a pol\xEDticas de prote\xE7\xE3o ambiental, especialmente em \xE1reas costeiras, margens de rios federais e florestas p\xFAblicas. Iniciativas como o Projeto Orla demonstram como a gest\xE3o patrimonial pode atuar de forma integrada \xE0 conserva\xE7\xE3o ambiental, conciliando prote\xE7\xE3o dos ecossistemas, ordenamento territorial e desenvolvimento local.")), /*#__PURE__*/React.createElement(ExampleCard, {
      label: "Exemplo pr\xE1tico",
      icon: "map-pin",
      slot: "m122-orla",
      title: "Projeto Orla",
      placeholder: "Foto de capa \u2014 Projeto Orla"
    }, /*#__PURE__*/React.createElement("p", null, "Desenvolvido em parceria entre Uni\xE3o, estados e munic\xEDpios, o Projeto Orla busca ordenar o uso das \xE1reas costeiras brasileiras, conciliando prote\xE7\xE3o ambiental, atividades econ\xF4micas e inclus\xE3o social."), /*#__PURE__*/React.createElement("p", null, "Os terrenos de marinha administrados pela Uni\xE3o constituem um dos principais instrumentos para implementa\xE7\xE3o dessa pol\xEDtica.")), /*#__PURE__*/React.createElement("div", {
      className: "op-prose"
    }, /*#__PURE__*/React.createElement("h3", null, "Planejamento territorial e desenvolvimento urbano"), /*#__PURE__*/React.createElement("p", null, "A gest\xE3o dos im\xF3veis p\xFAblicos tamb\xE9m possui forte conex\xE3o com o planejamento urbano e territorial. Cidades brasileiras enfrentam desafios complexos relacionados ao crescimento desordenado, d\xE9ficit habitacional, ocupa\xE7\xE3o de \xE1reas de risco, escassez de \xE1reas verdes e car\xEAncia de equipamentos p\xFAblicos."), /*#__PURE__*/React.createElement("p", null, "Nesse contexto, a disponibilidade de \xE1reas p\xFAblicas representa um ativo estrat\xE9gico para qualificar o desenvolvimento urbano. Im\xF3veis p\xFAblicos podem ser utilizados para implanta\xE7\xE3o de moradias, parques urbanos, escolas, unidades de sa\xFAde, equipamentos culturais, sistemas de mobilidade e projetos de requalifica\xE7\xE3o urbana."), /*#__PURE__*/React.createElement("p", null, "Al\xE9m de viabilizar investimentos p\xFAblicos, a adequada utiliza\xE7\xE3o desses im\xF3veis permite reduzir desigualdades territoriais, fortalecer centralidades urbanas e melhorar a qualidade de vida da popula\xE7\xE3o. O patrim\xF4nio p\xFAblico torna-se, assim, um elemento estruturador das cidades."), /*#__PURE__*/React.createElement("p", null, "A experi\xEAncia federal demonstra esse potencial. Programas como o Im\xF3vel da Gente t\xEAm utilizado im\xF3veis da Uni\xE3o para apoiar pol\xEDticas habitacionais, regulariza\xE7\xE3o fundi\xE1ria e implanta\xE7\xE3o de equipamentos p\xFAblicos, contribuindo para o desenvolvimento urbano e a inclus\xE3o territorial.")), /*#__PURE__*/React.createElement(Figure, {
      slot: "m122-img5",
      size: "full",
      caption: "O mesmo im\xF3vel p\xFAblico pode gerar impactos completamente diferentes dependendo de sua destina\xE7\xE3o.",
      credit: "Montagem autoral estilizada com IA."
    }), /*#__PURE__*/React.createElement("div", {
      className: "op-prose"
    }, /*#__PURE__*/React.createElement("h3", null, "Inclus\xE3o social, habita\xE7\xE3o e regulariza\xE7\xE3o fundi\xE1ria"), /*#__PURE__*/React.createElement("p", null, "A conex\xE3o entre gest\xE3o patrimonial e pol\xEDticas sociais \xE9 particularmente importante em pa\xEDses marcados por desigualdades territoriais e fundi\xE1rias."), /*#__PURE__*/React.createElement("p", null, "Muitos im\xF3veis p\xFAblicos encontram-se ocupados por popula\xE7\xF5es de baixa renda, comunidades tradicionais, povos ind\xEDgenas, quilombolas, pescadores artesanais e outros grupos socialmente vulner\xE1veis. A gest\xE3o desses territ\xF3rios influencia diretamente o acesso \xE0 moradia, \xE0 seguran\xE7a jur\xEDdica e a diversos direitos fundamentais."), /*#__PURE__*/React.createElement("p", null, "Quando articulada a programas habitacionais e de regulariza\xE7\xE3o fundi\xE1ria, a gest\xE3o patrimonial pode transformar passivos fundi\xE1rios em oportunidades de inclus\xE3o social. A destina\xE7\xE3o de im\xF3veis p\xFAblicos para habita\xE7\xE3o de interesse social, por exemplo, contribui para reduzir o d\xE9ficit habitacional e ampliar o acesso \xE0 cidade."), /*#__PURE__*/React.createElement("p", null, "Da mesma forma, a regulariza\xE7\xE3o de territ\xF3rios ocupados por comunidades tradicionais fortalece a prote\xE7\xE3o cultural, a seguran\xE7a jur\xEDdica e a preserva\xE7\xE3o ambiental associada a esses modos de vida."), /*#__PURE__*/React.createElement("p", null, "No caso da Uni\xE3o, programas de destina\xE7\xE3o imobili\xE1ria t\xEAm sido utilizados para apoiar a provis\xE3o habitacional, a regulariza\xE7\xE3o fundi\xE1ria e a prote\xE7\xE3o de comunidades tradicionais, demonstrando como a gest\xE3o patrimonial pode atuar diretamente na promo\xE7\xE3o da justi\xE7a territorial.")), /*#__PURE__*/React.createElement(Panel, {
      variant: "box"
    }, /*#__PURE__*/React.createElement(StatBlock, {
      variant: "rule",
      columns: 1,
      stats: [{
        value: '300',
        unit: 'mil +',
        label: 'Famílias beneficiadas',
        description: 'Entre 2023 e 2025, destinações realizadas pela União beneficiaram mais de 300 mil famílias por meio de ações de habitação de interesse social e regularização fundiária.'
      }]
    })), /*#__PURE__*/React.createElement("div", {
      className: "op-prose"
    }, /*#__PURE__*/React.createElement("h3", null, "Agenda clim\xE1tica, transi\xE7\xE3o ecol\xF3gica e resili\xEAncia territorial"), /*#__PURE__*/React.createElement("p", null, "As mudan\xE7as clim\xE1ticas v\xEAm ampliando a import\xE2ncia estrat\xE9gica dos im\xF3veis p\xFAblicos. Enchentes, secas prolongadas, inc\xEAndios florestais, eros\xE3o costeira e eventos extremos exigem novas formas de planejamento e gest\xE3o do territ\xF3rio."), /*#__PURE__*/React.createElement("p", null, "Nesse cen\xE1rio, os im\xF3veis p\xFAblicos podem desempenhar diferentes fun\xE7\xF5es. Alguns atuam como \xE1reas de prote\xE7\xE3o ambiental e infraestrutura verde. Outros podem servir como espa\xE7os de drenagem urbana, corredores ecol\xF3gicos ou \xE1reas destinadas ao reassentamento de popula\xE7\xF5es expostas a riscos."), /*#__PURE__*/React.createElement("p", null, "Tamb\xE9m existe crescente potencial para utiliza\xE7\xE3o de im\xF3veis p\xFAblicos em projetos de energia renov\xE1vel, infraestrutura resiliente e solu\xE7\xF5es baseadas na natureza."), /*#__PURE__*/React.createElement("p", null, "Al\xE9m das a\xE7\xF5es preventivas, os im\xF3veis p\xFAblicos podem desempenhar papel fundamental em situa\xE7\xF5es de emerg\xEAncia. Dependendo de suas caracter\xEDsticas e localiza\xE7\xE3o, podem ser utilizados como centros de apoio, abrigos tempor\xE1rios, bases log\xEDsticas ou \xE1reas destinadas \xE0 reconstru\xE7\xE3o de comunidades afetadas por desastres."), /*#__PURE__*/React.createElement("p", null, "A experi\xEAncia recente de eventos clim\xE1ticos extremos no Brasil tem demonstrado que a gest\xE3o patrimonial precisa estar cada vez mais integrada \xE0s estrat\xE9gias de adapta\xE7\xE3o e resili\xEAncia territorial."), /*#__PURE__*/React.createElement("h3", null, "Governan\xE7a, coopera\xE7\xE3o e financiamento"), /*#__PURE__*/React.createElement("p", null, "A crescente complexidade dos desafios socioambientais exige que a gest\xE3o patrimonial seja desenvolvida de forma colaborativa."), /*#__PURE__*/React.createElement("p", null, "A utiliza\xE7\xE3o estrat\xE9gica dos im\xF3veis p\xFAblicos depende da articula\xE7\xE3o entre diferentes \xF3rg\xE3os governamentais, entes federativos, institui\xE7\xF5es de pesquisa, organiza\xE7\xF5es da sociedade civil e organismos internacionais. Nenhuma institui\xE7\xE3o, isoladamente, possui capacidade para enfrentar todos os desafios relacionados ao territ\xF3rio."), /*#__PURE__*/React.createElement("p", null, "Al\xE9m disso, diversas iniciativas ambientais, clim\xE1ticas e urbanas dependem de fontes de financiamento espec\xEDficas. Os im\xF3veis p\xFAblicos podem servir como infraestrutura de suporte para projetos financiados por fundos clim\xE1ticos, organismos multilaterais e programas de coopera\xE7\xE3o internacional."), /*#__PURE__*/React.createElement("p", null, "Essa capacidade de articula\xE7\xE3o amplia o potencial de transforma\xE7\xE3o dos patrim\xF4nios p\xFAblicos e fortalece sua contribui\xE7\xE3o para o desenvolvimento sustent\xE1vel."))), /*#__PURE__*/React.createElement(Conclusion, {
      "data-comment-anchor": "1b0c376415-div-440-9"
    }, /*#__PURE__*/React.createElement("p", null, "A gest\xE3o patrimonial contempor\xE2nea n\xE3o se restringe ao controle administrativo de im\xF3veis. Trata-se de uma atividade estrat\xE9gica, diretamente conectada \xE0s principais agendas p\xFAblicas do nosso tempo."), /*#__PURE__*/React.createElement("p", null, "Sejam pertencentes \xE0 Uni\xE3o, aos estados, aos munic\xEDpios ou a outras entidades p\xFAblicas, os im\xF3veis p\xFAblicos constituem uma infraestrutura territorial capaz de apoiar pol\xEDticas ambientais, urbanas, habitacionais, clim\xE1ticas e sociais. Quando geridos de forma integrada, tornam-se instrumentos de transforma\xE7\xE3o do territ\xF3rio e de promo\xE7\xE3o do bem-estar coletivo."), /*#__PURE__*/React.createElement("p", null, "Mais do que administrar bens, a gest\xE3o patrimonial contempor\xE2nea consiste em gerar valor p\xFAblico por meio do uso sustent\xE1vel, eficiente e socialmente relevante do patrim\xF4nio imobili\xE1rio."), /*#__PURE__*/React.createElement("p", null, "As dimens\xF5es ambiental, social e econ\xF4mica dos im\xF3veis p\xFAblicos est\xE3o profundamente interligadas. Um mesmo im\xF3vel pode simultaneamente proteger ecossistemas, apoiar pol\xEDticas habitacionais, estimular o desenvolvimento econ\xF4mico e promover seguran\xE7a jur\xEDdica. Essa capacidade de gerar m\xFAltiplos benef\xEDcios torna o patrim\xF4nio p\xFAblico um dos mais importantes instrumentos dispon\xEDveis para a atua\xE7\xE3o do Estado."), /*#__PURE__*/React.createElement("p", null, "A experi\xEAncia do patrim\xF4nio da Uni\xE3o demonstra que os im\xF3veis p\xFAblicos n\xE3o devem ser vistos apenas como bens administrativos ou ativos financeiros. Eles constituem uma infraestrutura territorial estrat\xE9gica, capaz de promover inclus\xE3o social, conserva\xE7\xE3o ambiental, desenvolvimento econ\xF4mico e melhoria da qualidade de vida."), /*#__PURE__*/React.createElement("p", null, "Compreender essa dimens\xE3o \xE9 o primeiro passo para uma gest\xE3o patrimonial moderna, integrada e orientada \xE0 gera\xE7\xE3o de valor p\xFAblico.")), /*#__PURE__*/React.createElement(Section, {
      id: "fontes",
      narrow: true
    }, /*#__PURE__*/React.createElement(ReferenceList, {
      title: "Refer\xEAncias",
      items: ['Barboza Carneiro, J. C. (2023). Proposta de tema - Agenda ambiental para projetos de interesse da SPU. Secretaria do Patrimônio da União. Documento interno para discussão.', 'Barboza Carneiro, J. C. (2024). Anotações sobre a Agenda Ambiental. Secretaria do Patrimônio da União. Apresentação interna.', 'Brasil . Instituto Chico Mendes de Conservação da Biodiversidade. (2025). Mapa temático e dados geoestatísticos das Unidades de Conservação Federais. ICMBio. https://www.gov.br/icmbio/pt-br/assuntos/dados_geoespaciais/mapa-tematico-e-dados-geoestatisticos-das-unidades-de-conservacao-federais', 'Brasil. Secretaria do Patrimônio da União. (2025). Relatório 02: Benchmarking de políticas públicas internacionais exitosas de valorização e potencialização da função socioambiental de terras públicas e proposta conceitual da agenda socioambiental da SPU (Consultoria PNUD – Produto 2). PNUD.', 'Brasil. Secretaria do Patrimônio da União. (2025). Documento de entendimento da CT AMB – Temas x Produtos. Documento de trabalho interno da Câmara Técnica de Agenda Ambiental.', 'Brasil. Secretaria do Patrimônio da União. (2025). Links de documentos de referência compartilhados na Câmara Técnica da Agenda Ambiental. Documento interno.', 'Brasil. Secretaria do Patrimônio da União; Ministério da Economia; Ministério do Turismo; Ministério do Desenvolvimento Regional; Universidade Federal de Santa Catarina; Fundação Universidade do Rio Grande; Universidade Federal de Pernambuco (2022). Manual do Projeto Orla: manual para elaboração do Plano de Gestão Integrada do Projeto Orla. Brasília, DF: Disponível em: https://www.gov.br/gestao/pt-br/assuntos/patrimonio-da-uniao/destinacao-de-imoveis/gestao-de-praias/manual-do-projeto-orla_abril-de-2022.pdf. Acesso em: 28 ago. 2025.', 'Comissão Pastoral da Terra. CPT. (2024). Conflitos no campo Brasil 2023. CPT. https://cptnacional.org.br/wp-content/uploads/2025/03/conflitos-no-campo-brasil-2023.pdf']
    })), /*#__PURE__*/React.createElement(PageFooter, {
      code: "M122 \xB7 Patrim\xF4nio imobili\xE1rio p\xFAblico",
      context: "Conte\xFAdo did\xE1tico sobre o patrim\xF4nio imobili\xE1rio p\xFAblico brasileiro \u2014 panorama, relev\xE2ncia e potencial transformador na promo\xE7\xE3o do desenvolvimento sustent\xE1vel, da justi\xE7a social e da prote\xE7\xE3o ambiental.",
      credits: [{
        role: 'Produção',
        name: 'Claudson Moreira'
      }, {
        role: 'Revisão',
        name: '—'
      }, {
        role: 'Design',
        name: 'SPU'
      }, {
        role: 'Versão',
        name: '06/2026'
      }]
    }));
  }
  window.__M122_App = App;
  return App;
}
window.__M122_buildApp = buildM122App;
})(); } catch (e) { __ds_ns.__errors.push({ path: "m122/page.jsx", error: String((e && e.message) || e) }); }

// m122web/js/app.js
try { (() => {
/* M122 — interações estáticas (sem framework) */
(function () {
  'use strict';

  var SECTIONS = [{
    id: 'abertura',
    label: 'Início'
  }, {
    id: 'infraestrutura',
    label: 'Infraestrutura'
  }, {
    id: 'ambiental',
    label: 'Ambiental'
  }, {
    id: 'social',
    label: 'Social'
  }, {
    id: 'economica',
    label: 'Econômica'
  }, {
    id: 'conexoes',
    label: 'Conexões'
  }, {
    id: 'conclusao',
    label: 'Conclusão'
  }, {
    id: 'fontes',
    label: 'Fontes'
  }];
  function ready(fn) {
    if (document.readyState !== 'loading') fn();else document.addEventListener('DOMContentLoaded', fn);
  }
  ready(function () {
    var bar = document.querySelector('.op-progress__bar');
    var summary = document.querySelector('.op-summary');
    var toggle = summary && summary.querySelector('.op-summary__toggle');

    /* —— Sumário: construir painel —— */
    var panel = null,
      links = {};
    if (summary) {
      panel = document.createElement('nav');
      panel.className = 'op-summary__panel';
      panel.style.display = 'none';
      var title = document.createElement('p');
      title.className = 'op-summary__title';
      title.textContent = 'Neste conteúdo';
      panel.appendChild(title);
      SECTIONS.forEach(function (s, i) {
        var a = document.createElement('a');
        a.href = '#' + s.id;
        var num = document.createElement('span');
        num.className = 'op-summary__num';
        num.textContent = String(i + 1).padStart(2, '0');
        var lbl = document.createElement('span');
        lbl.textContent = s.label;
        a.appendChild(num);
        a.appendChild(lbl);
        a.addEventListener('click', function () {
          close();
        });
        panel.appendChild(a);
        links[s.id] = a;
      });
      summary.appendChild(panel);
      var open = false;
      function setOpen(v) {
        open = v;
        panel.style.display = v ? 'grid' : 'none';
        toggle.setAttribute('aria-expanded', v ? 'true' : 'false');
      }
      function close() {
        setOpen(false);
      }
      toggle.addEventListener('click', function (e) {
        e.stopPropagation();
        setOpen(!open);
      });
      document.addEventListener('mousedown', function (e) {
        if (open && !summary.contains(e.target)) close();
      });
      document.addEventListener('keydown', function (e) {
        if (e.key === 'Escape') close();
      });
    }

    /* —— Barra de progresso + seção ativa —— */
    function onScroll() {
      var h = document.documentElement;
      var max = h.scrollHeight - h.clientHeight;
      if (bar) bar.style.width = (max > 0 ? h.scrollTop / max * 100 : 0) + '%';
      var active = SECTIONS[0].id;
      for (var i = 0; i < SECTIONS.length; i++) {
        var el = document.getElementById(SECTIONS[i].id);
        if (el && el.getBoundingClientRect().top <= 120) active = SECTIONS[i].id;
      }
      for (var id in links) {
        if (links.hasOwnProperty(id)) links[id].classList.toggle('is-active', id === active);
      }
    }
    window.addEventListener('scroll', onScroll, {
      passive: true
    });
    onScroll();

    /* —— Parallax do hero —— */
    var hero = document.querySelector('.m122-hero');
    var heroBg = hero && hero.querySelector('.m122-hero__bg');
    var heroBoxes = hero && hero.querySelector('.m122-hero__boxes');
    if (hero && heroBg && heroBoxes) {
      var mq = window.matchMedia('(max-width: 720px), (prefers-reduced-motion: reduce)');
      var raf = 0;
      function update() {
        raf = 0;
        if (mq.matches) {
          heroBg.style.transform = '';
          heroBoxes.style.transform = '';
          return;
        }
        var y = -hero.getBoundingClientRect().top;
        heroBg.style.transform = 'translate3d(0,' + y * 0.22 + 'px,0)';
        heroBoxes.style.transform = 'translate3d(0,' + y * -0.16 + 'px,0)';
      }
      function req() {
        if (!raf) raf = requestAnimationFrame(update);
      }
      update();
      window.addEventListener('scroll', req, {
        passive: true
      });
      window.addEventListener('resize', req);
    }

    /* —— Lightbox (ampliar imagens) —— */
    var overlay = null;
    function openLightbox(src, alt) {
      overlay = document.createElement('div');
      overlay.className = 'm122-lightbox';
      overlay.setAttribute('role', 'dialog');
      overlay.setAttribute('aria-modal', 'true');
      var img = document.createElement('img');
      img.src = src;
      img.alt = alt || '';
      overlay.appendChild(img);
      overlay.addEventListener('click', closeLightbox);
      document.body.appendChild(overlay);
      document.addEventListener('keydown', escClose);
    }
    function closeLightbox() {
      if (overlay) {
        overlay.remove();
        overlay = null;
      }
      document.removeEventListener('keydown', escClose);
    }
    function escClose(e) {
      if (e.key === 'Escape') closeLightbox();
    }
    var zoomables = document.querySelectorAll('.m122-fig__frame img, .m122-bleedimg img, .m122-cover img');
    Array.prototype.forEach.call(zoomables, function (img) {
      img.addEventListener('click', function () {
        openLightbox(img.currentSrc || img.src, img.alt);
      });
    });
  });
})();
})(); } catch (e) { __ds_ns.__errors.push({ path: "m122web/js/app.js", error: String((e && e.message) || e) }); }

__ds_ns.Callout = __ds_scope.Callout;

__ds_ns.Conclusion = __ds_scope.Conclusion;

__ds_ns.DataTable = __ds_scope.DataTable;

__ds_ns.SPU_MARKS = __ds_scope.SPU_MARKS;

__ds_ns.Editable = __ds_scope.Editable;

__ds_ns.MarkToolbar = __ds_scope.MarkToolbar;

__ds_ns.ExampleCard = __ds_scope.ExampleCard;

__ds_ns.FeatureGrid = __ds_scope.FeatureGrid;

__ds_ns.LicenseBadge = __ds_scope.LicenseBadge;

__ds_ns.MarkerList = __ds_scope.MarkerList;

__ds_ns.Masthead = __ds_scope.Masthead;

__ds_ns.MediaEmbed = __ds_scope.MediaEmbed;

__ds_ns.ExternalEmbed = __ds_scope.ExternalEmbed;

__ds_ns.SectionSlider = __ds_scope.SectionSlider;

__ds_ns.injectCss = __ds_scope.injectCss;

__ds_ns.isPrint = __ds_scope.isPrint;

__ds_ns.renderRich = __ds_scope.renderRich;

__ds_ns.PageFooter = __ds_scope.PageFooter;

__ds_ns.BuilderCredit = __ds_scope.BuilderCredit;

__ds_ns.PageToc = __ds_scope.PageToc;

__ds_ns.Panel = __ds_scope.Panel;

__ds_ns.PullQuote = __ds_scope.PullQuote;

__ds_ns.ReferenceList = __ds_scope.ReferenceList;

__ds_ns.ReflectionStop = __ds_scope.ReflectionStop;

__ds_ns.ResourceList = __ds_scope.ResourceList;

__ds_ns.RichText = __ds_scope.RichText;

__ds_ns.RichTextLinkNotes = __ds_scope.RichTextLinkNotes;

__ds_ns.StatBlock = __ds_scope.StatBlock;

__ds_ns.Badge = __ds_scope.Badge;

__ds_ns.BlockRegistry = __ds_scope.BlockRegistry;

__ds_ns.BlockView = __ds_scope.BlockView;

__ds_ns.BlockDocument = __ds_scope.BlockDocument;

__ds_ns.BuilderExport = __ds_scope.BuilderExport;

__ds_ns.BuilderManifest = __ds_scope.BuilderManifest;

__ds_ns.Button = __ds_scope.Button;

__ds_ns.ICONS = __ds_scope.ICONS;

__ds_ns.Icon = __ds_scope.Icon;

__ds_ns.ICON_NAMES = __ds_scope.ICON_NAMES;

__ds_ns.IconButton = __ds_scope.IconButton;

__ds_ns.IconGallery = __ds_scope.IconGallery;

__ds_ns.Kicker = __ds_scope.Kicker;

__ds_ns.Tag = __ds_scope.Tag;

__ds_ns.TONES = __ds_scope.TONES;

__ds_ns.Accordion = __ds_scope.Accordion;

__ds_ns.Carousel = __ds_scope.Carousel;

__ds_ns.ContentSlider = __ds_scope.ContentSlider;

__ds_ns.CompareAB = __ds_scope.CompareAB;

__ds_ns.Flipcard = __ds_scope.Flipcard;

__ds_ns.GlossaryTerm = __ds_scope.GlossaryTerm;

__ds_ns.GlossaryFootnotes = __ds_scope.GlossaryFootnotes;

__ds_ns.Quiz = __ds_scope.Quiz;

__ds_ns.Timeline = __ds_scope.Timeline;

__ds_ns.Columns = __ds_scope.Columns;

__ds_ns.Section = __ds_scope.Section;

__ds_ns.Stack = __ds_scope.Stack;

__ds_ns.BleedImage = __ds_scope.BleedImage;

__ds_ns.Figure = __ds_scope.Figure;

__ds_ns.FullBleed = __ds_scope.FullBleed;

__ds_ns.Hero = __ds_scope.Hero;

__ds_ns.ImageReveal = __ds_scope.ImageReveal;

__ds_ns.MapFigure = __ds_scope.MapFigure;

__ds_ns.TextWithImage = __ds_scope.TextWithImage;

})();
